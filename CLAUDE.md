# jsvision

> Project configuration for Claude Code. Auto-generated by /analyze_project; edit freely —
> re-running preserves your hand-written sections.

<!-- analyze_project: compacted 2026-07-13 — 28 stacked refresh comments pruned to 1; Overview/Toolchain/Commands/Project structure tightened; hand-authored Conventions/Git/Documentation/TV-fidelity/Kitchen-sink/Special-rules preserved verbatim -->

## Overview

- **Type:** monorepo (library SDK) — published `@jsvision/core`; private-until-release `@jsvision/ui` (widgets), `@jsvision/web` (browser runtime), `@jsvision/files`, the `@jsvision/theme-designer` app, `@jsvision/examples`, and `@jsvision/docs-site`.
- **Description:** SDK for building Turbo Vision-style terminal (TUI) apps in TypeScript. `@jsvision/core` is the zero-dependency **foundation engine** behind one public entry point (`src/engine/index.ts`): capability detection, input decoding, a width-correct damage-diff renderer, a native-tty host with guaranteed restore on every exit path, a safety/essentials layer, and depth-aware color + theming. `@jsvision/ui` is the Turbo Vision-style **widget framework** on core (reactive core · layout engine · view/group spine · event loop · app shell · the full widget set). `@jsvision/web` runs any app in xterm.js with no backend. Dev-only harnesses (capability probe, the kitchen-sink showcase, docs-site live examples) and a four-tier test strategy with a go/no-go gate (`yarn gate`) round it out.

## Toolchain

- **Repo shape:** **yarn 1.x + Turborepo monorepo** (`packages/*` workspaces). Public: `@jsvision/core` (published, zero runtime deps). Private-until-release: `@jsvision/ui` (widget framework), `@jsvision/web` (browser runtime), `@jsvision/files`, `@jsvision/theme-designer` (app), `@jsvision/docs-site` (VitePress site — joins `yarn verify`'s test+typecheck; its build is isolated via the `vp:build` script name), `@jsvision/examples` (demos + probe). Public packages share one lockstep version (`yarn sync-versions`; root `package.json#version` is the source of truth).
- **Language(s):** TypeScript (ESM-only, `module`/`moduleResolution` NodeNext, `strict`); shared `tsconfig.base.json`, per-package `tsconfig.json`
- **Framework(s):** none — Node built-ins + zero runtime dependencies
- **Package manager:** **yarn 1.x** (`yarn.lock`, workspaces `packages/*`)
- **Orchestration:** **Turborepo** (`turbo.json`: `build`/`typecheck`/`test`/`test:e2e`/`check:deps`; `test` dependsOn `build`, `typecheck` dependsOn `^build`)
- **Test framework:** **vitest** (two projects: `unit` = `*.{spec,impl}.test.ts`, `e2e` = `*.e2e.test.ts` single-fork). Vite resolves NodeNext `.js`→`.ts` natively.
- **Lint/format:** root-global ESLint flat config (`typescript-eslint`) + Prettier
- **Node:** LTS **22 / 24** (`engines.node >= 22`)

## Commands

> All commands run from the monorepo root and fan out via turbo unless noted.

- **Verify (run before every commit):** `yarn verify` (= `yarn lint` then `turbo run typecheck build test check:docs`)
- **Build:** `yarn build` · **Typecheck:** `yarn typecheck` · **Test (unit):** `yarn test`
- **Test (e2e):** `yarn test:e2e` (or per package, e.g. `yarn workspace @jsvision/core test:e2e`)
- **Run the theme designer (dev):** `yarn designer` (live on a TTY; piped → headless walkthrough)
- **Run the probe harness (dev):** `yarn workspace @jsvision/examples probe` (`--auto`/`--out`/`--no-matrix`)
- **Docs website (dev):** `yarn docs:dev` · **API reference:** `yarn docs:api` (TypeDoc → gitignored `api/`) · **build:** `yarn docs:build` · **validate:** `node packages/docs-site/scripts/check-docs-build.mjs` (20-check gate)
- **Runnable demos (dev):** `yarn workspace @jsvision/examples <demo>` — `demo:kitchen` (the kitchen-sink showcase) plus per-subsystem walkthroughs `demo:{controls,containers,tree,table,tabs,feedback,date,color,surface,themes,shell,events,view,layout,reactive}` and real-TTY `demo:{controls-live,resize,playground}`
- **Acceptance gate (go/no-go):** `yarn gate` (`scripts/gate.mjs` — verify + core e2e + probe --auto; map in `docs/acceptance-gate.md`)
- **Performance bench (informational):** `yarn bench` (never gates; 16 ms ceiling asserted off-CI, skipped under `CI`/`TUI_SKIP_PERF`)
- **Version sync (lockstep):** `yarn sync-versions` · `yarn sync-versions --check`
- **Lint:** `yarn lint` · **Fix:** `yarn lint:fix`
- **Dependency policy:** `yarn check:deps` (fails on any native runtime dependency)
- **JSDoc governance:** `yarn check:docs` (`scripts/check-jsdoc.mjs`; bans CodeOps/TV-C++ refs, requires `@example` on public exports) — part of `yarn verify`
- **Plugin self-sync:** `yarn plugin:sync --fix` (re-sync drifted recipe snippets, no AI) · `--detect` (JSON drift) · no-flag also drafts catalog entries via the Anthropic API (needs `ANTHROPIC_API_KEY`); outputs are verify-gated + left unstaged
- **Clean:** `rm -rf packages/*/dist .turbo`

## Project structure

````
packages/core/       Published @jsvision/core — holds src/engine/, bench/, and the 80 non-probe tests + fixtures. Paths below (src/engine/**, test/**) are RELATIVE TO THIS PACKAGE.
packages/examples/   Private @jsvision/examples — capability-probe/ + resize-demo/ + the 15 probe-*/probe.e2e tests; depends on @jsvision/core, imports it by name.
packages/ui/             @jsvision/ui — the Turbo Vision-style widget framework (retained tree + signals) on @jsvision/core. Single public entry src/index.ts re-exports each landed subsystem (layout + view + event + app-shell use EXPLICIT named re-exports; reactive uses export *) — and re-exports seven `@jsvision/core` essentials so a hello-world app imports from one package (`resolveCapabilities`/`resolveCapabilitiesAsync`/`createKeymap`/`Attr` values + `CapabilityProfile`/`Style`/`Keymap` types): src/reactive/ (RD-01 reactive core), src/layout/ (ADR-008/RD-02 cell-native layout engine), src/view/ (RD-03 view/group spine), src/event/ (RD-04 event loop), and the RD-05 app shell src/{app,desktop,window,menu,status}/ — all see below. private until first release; remaining subsystems land per codeops/features/jsvision-ui/plans/tui-ui/01-component-map.md.
packages/ui/src/layout/    RD-02 layout engine (ADR-008) — cell-native, integer-correct flex. apportion.ts (the de-risking spike: largest-remainder apportion + 1-D solveTrack, unchanged), types.ts (node model Size2D/Rect/Padding/Size/Justify/Align/Direction/LayoutProps/LayoutBox/LayoutResult + internal prop normalizers + axis projection helpers), measure.ts (naturalSize intrinsic sizing — measure() leaf or derived-from-children), layout.ts (the recursive two-axis pass), index.ts (barrel). Public API: layout(root, viewport) → Map<LayoutBox, Rect> of parent-relative integer rects; row/col via one axis abstraction, fixed/fr/auto sizing, justify/align/gap/padding, overflow (extend past edge, fr→0), degenerate→zero rects; pure/no-mutation. Tests: packages/ui/test/layout.{sizing,align,tree,packaging}.{spec,impl}.test.ts (18 ST oracles ST-01…ST-18 ↔ AC-1…AC-18 + impl). Plan: codeops/features/jsvision-ui/plans/layout-engine/.
packages/ui/src/reactive/  RD-01 reactive core — fine-grained Solid-style signals (UI-independent). 12 files ≤ 500 lines, single barrel index.ts: types (Signal/Computed/EqualsOption public + Source/Computation/Owner/Subscribable/NodeState internal), errors (ReactiveCycleError extends @jsvision/core TuiError), warnings (NODE_ENV-gated devWarn), cleanup (LIFO drain), scheduler (tracking context + two-phase glitch-free lazy-pull propagation, batch/untrack, 1000-iteration runaway guard, multi-throw drain), owner (createRoot/onCleanup/dispose + no-owner policy), signal, computed (lazy+memoized), effect, show (Show), for (For — keyed reconcile + reactive index). Public API: signal/computed/effect/batch/untrack/onCleanup/createRoot/Show/For/ReactiveCycleError, **+ the RD-03 additive primitive runWithOwner(owner, fn) + getOwner() + opaque Owner** (explicit-parent owner-scope nesting). Tests: packages/ui/test/reactive.*.{spec,impl}.test.ts (20 ST oracles ST-01…ST-20 + impl; runWithOwner = ST-21 in reactive.ownership). Plan: codeops/features/jsvision-ui/plans/reactive-core/.
packages/ui/src/view/      RD-03 view/group spine — the retained widget tree binding RD-01 reactivity + RD-02 layout into a self-drawing, themed, independently renderable frame on @jsvision/core's ScreenBuffer. 8 files ≤ 231 lines + barrel index.ts (layout + view use EXPLICIT named re-exports in src/index.ts): geometry (Point + pure intersect/translate/contains), types (ViewState/DrawContext/ThemeRoleName=keyof Theme/RenderRootOptions), theme-style (themeRoleToStyle {fg,bg} adapter), view (abstract View — bounds/state flags/onEvent stub/bind/invalidate/invalidateLayout/onMount/onCleanup + internal ViewHost seam + mount/unmount; mount untracks its scope setup so onCleanup binds to the view's own scope), group (Group — children/background/add/remove/addDynamic over Show/For + recursive mount), draw-context (makeDrawContext — stateless clipped view-local paint, all writes via ScreenBuffer+sanitize), reflow (view tree → fresh LayoutBox tree → RD-02 layout() → parent-relative bounds; visible:false omitted), render-root (createRenderRoot/RenderRoot — buffers + ScreenBuffer.clone() snapshot, caps, theme, injectable scheduler, dirty-set + per-view compose-context cache, compose walker with clip/back-to-front/bg/draw-error isolation, full-vs-partial recompose). Public API: View/Group/DrawContext/RenderRoot/createRenderRoot/Point/ViewState/ThemeRoleName/intersect/translate/contains (+ runWithOwner/getOwner/Owner via reactive). Tests: packages/ui/test/view.{tree,drawcontext,reflow,render,scheduler,dynamic,packaging}.{spec,impl}.test.ts (ST-01…ST-20 ↔ AC-1…AC-20 + ST-21/22 primitives + impl) + examples view-demo.e2e. Plan: codeops/features/jsvision-ui/plans/view-group-spine/ (runtime decisions RT-1…RT-5 in its 00-ambiguity-register.md). The core-side primitive: ScreenBuffer.clone() added to packages/core/src/engine/render/buffer.ts (exact deep copy incl. wide-lead/continuation cells) for the partial-recompose damage diff.
packages/ui/src/event/     RD-04 event loop — the host-agnostic dispatch mechanism on the RD-03 spine (no cross-package primitive; builds on existing @jsvision/core + RD-03). 8 files ≤ 227 lines + barrel index.ts (EXPLICIT named re-exports in src/index.ts): types (EventLoop/EventLoopOptions; re-exports the CommandEvent/AppEvent/DispatchEvent contract types declared in ../view/types.ts — PA-8, no view/↔event/ cycle), event-loop (createEventLoop — builds+OWNS the RenderRoot with a coalescing schedule: in a tick the loop drives flush() itself, and OUTSIDE a tick (a timer/promise/direct call between ticks) the same seam coalesces the dirtying into one microtask-deferred paint via the shared `paint()` (flush → onFrame → emitCaret) so out-of-tick mutations still repaint — closing the missing-flush bug class #68; `EventLoopOptions.scheduleMicrotask` (default `queueMicrotask`) makes the deferral injectable, and `EventLoop.stop()` (called by run()'s finally) gates the painter at teardown; the single runTick shared by EVERY public mutator → one coalesced frame per tick + onIdle + handler-error isolation, PA-11; dispatch/resize/getFocused/focus*/emitCommand/enable*/execView/endModal; routeContext seam builder), dispatch (route(ev,ctx) — keymap consume PA-1 → built-in Tab/Shift-Tab PA-10 → mouse/wheel branch → pre/focus/post 3-phase sweeps with handled short-circuit + focus-chain bubble clamped to scopeRoot PA-12), commands (createCommandRegistry — emit/enable/isEnabled, enabled-by-default explicit-override map PA-3, enqueues CommandEvent onto the tick), focus (createFocusManager — per-group current chain, isFocusable predicate + subtree semantics AR-65, focusView/focusInto/focusNext/focusPrev wrap+descend, save/restore via persisted current; pure mutations, the loop wraps each in runTick), hit-test (hitTestRoute — 1-based→0-based normalize AR-63, top-most-first walk reverse-z + ancestor-clip + skip !visible/disabled, focus-on-click climb, view-local ev.local), modal (createModalManager — execView/endModal stack, save/restore-around-modal, nested LIFO, scopeRoot capture). Public API: createEventLoop/EventLoop (+ `stop()`)/EventLoopOptions (+ `scheduleMicrotask`)/CommandEvent/AppEvent/DispatchEvent. **DX-ergonomics additive surface:** `EventLoop.onCommand(name, handler): () => void` (register a named-command handler — many fire in registration order, each in its own try/catch, a handled command is consumed, returns an unsubscribe) is served by a loop-owned `CommandSink` swept DIRECTLY in the private `route()` gated on `!modal.isActive()` (NOT mounted into the caller's tree — runtime AR-14 — so general handlers stay dormant while a modal owns the scope); the built-in quit is one internal registration through the same sink via the new `EventLoopOptions.onQuit` seam (reads the numeric exit-code arg; the bespoke QuitCommandSink is gone). Additive RD-03 edits: view/view.ts (focusable/preProcess/postProcess + onEvent(DispatchEvent) retype), view/group.ts (internal current), view/types.ts (contract types). Tests: packages/ui/test/event.{loop,dispatch,focus,mouse,modal}.{spec,impl}.test.ts + event.packaging.spec (20 ST oracles ST-01…ST-20 ↔ AC-1…AC-20 + impl) + out-of-tick-repaint.{spec,impl}.test.ts (the out-of-tick painter, ST-1…ST-6 + edges) + examples event-demo.e2e. Plan: codeops/features/jsvision-ui/plans/event-loop/ (PA-1…PA-12, runtime notes in 00-ambiguity-register.md).
packages/ui/src/{app,desktop,window,menu,status}/  RD-05 app shell — a full Turbo Vision-style application over the RD-04 loop. EXPLICIT named re-exports in src/index.ts. app/ (createApplication — composes the loop + an owned Desktop + optional MenuBar/StatusLine + an absolute full-viewport overlay into a column; **DX-ergonomics: `ApplicationOptions.caps` is optional ('auto' default → resolved once via `resolveCapabilities().profile`, concrete profile threaded to the loop + run()); `Application.onCommand` forwards to `loop.onCommand`; the bespoke quit-command sink is gone — quit is one internal `onCommand` registration through the loop's command sink via `onQuit`; `ApplicationOptions.requireTty` (default true) gates `run()` on an interactive TTY — `run()` asserts `assertEssentials(caps, { isTTY: detectTty({ input, output }) })` before `host.start()`, throwing `EssentialsNotMetError` on a launch with no interactive terminal at all (the `/dev/tty`-backed piped case still runs), `requireTty: false` opts headless runs out**; run() in run.ts wires createHost → dispatch, loop.onFrame = host.render, restore on every exit path). desktop/ (Desktop window manager — postProcess; windows() type-guard, raise/cascade/tile/focusNext-prev/Number, beginMove/beginResize/beginResizeLeft via the loop's setCapture pointer-capture seam; handleCommand returns bool + sets ev.handled to avoid double-processing; gestures.ts clamp/applyMove/applyResize/applyResizeLeft + arrange.ts TV-exact cascade/tile (tdesktop.cpp doCascade + iSqr/mostEqualDivisors/dividerLoc/calcTileRect ported verbatim, tileError→no-op)). window/ (Window extends Group — focusable, absolute, reactive title Signal, movable/resizable/zoomable/closable, restoredRect; drawFrame chrome — border + close [×]/zoom [↑]|[↕]/number + SE (─┘) & SW (└─) resize grips + frameZoneAt hit zones incl. the functional left-grow resize-left (TV dmDragGrowLeft); a WindowManager back-reference seam breaks the window↔desktop import cycle). menu/ (overlay-hosted nested menus — builders.ts menuBar/subMenu/item/separator + tilde ~X~ parseTilde/layoutTitles; controller.ts nav state machine — level stack of MenuPopups + a full-viewport outside-click catcher, ↑↓ skip separators/disabled, Enter activate, ←→ switch/open-sub, Esc one level, hotkeys, focus save/restore, overlay.visible gated on menu state PF-10; menubar.ts MenuBar pre-process — F10/Alt-hotkey/click open; popup.ts MenuPopup presentational + onPick row-click). status/ (StatusLine post-process command row — statusLine/statusItem builders, attach(seam), press-capture feedback + emit-on-release (command of the item under the release point), tildeSegments hotkey runs, matchesChord accelerator parser, greying; commands.ts Commands constants quit/close/zoom/next/prev/cascade/tile). Phase-0 additive primitives: RD-02 LayoutProps {position:'absolute',rect}, RD-03 DrawContext.role, RD-04 EventLoop setCapture/releaseCapture/onFrame, the cross-package edits = core Theme windowInactive + statusSelected roles. Tests: packages/ui/test/app-shell.{lifecycle,desktop,window,menu,status,seams,packaging}.{spec,impl}.test.ts (22 ST oracles ST-01…ST-22 ↔ AC-1…AC-22 + impl) + examples shell-demo.e2e. Plan: codeops/features/jsvision-ui/plans/app-shell/ (PA-1…PA-19, runtime notes in 00-ambiguity-register.md). **Turbo Vision behavioral fidelity** (the jsvision-ui-RD-10 follow-up: status emit-on-release + press capture, TV-exact cascade/tile geometry, functional left-grow resize) layered over these subsystems — plan codeops/features/jsvision-ui/plans/tv-behavioral-fidelity/ (PA-1…PA-11; rewrites the desktop ST-11 oracle → ST-05/06 + the status emit oracles).
packages/ui/src/controls/  RD-06 essential controls — the Tier-1 leaf widgets + validator model, faithfully ported from Turbo Vision. EXPLICIT named re-exports in src/index.ts. text.ts (Text — static multi-line paragraph; greedy word-wrap wrapText + hard-break, `staticText` role; tstatictx.cpp). label.ts (Label — postProcess hotkey label linked to a control; parseTilde/tildeSegments reused from ../menu/, ev.focusView on click/hotkey, repaints on the linked view's focus-change signal; tlabel.cpp). button.ts (Button — `[ text ]` face + ▄/█/▀ block-glyph shadow in the `shadow` role, state→faceRole, drawTitle markers, activate→ev.emit(command); tbutton.cpp). input.ts (Input — single-line editor over a two-way Signal<string>, curPos/firstPos scroll with ◄/► arrows matching tinputli.cpp, insertPrintable gated by an optional validator's isValidInput, valid() exposed; tinputli.cpp) with input-selection.ts (shift-extension + mouse selection), input-clipboard.ts (cut/copy/paste + `insertFilled`/`applyPaste`/`mapPasteChar`), and input-editing.ts (RD-13: pure `prevWord`/`nextWord` + `computeDelete` transient-revert deletion helpers, split out to keep input.ts ≤500). cluster.ts (internal abstract Cluster base — ClusterBox 5-cell box, mark/press/box/movedTo abstract, ↑↓/Space/Alt-hotkey/click nav + moveSel; tcluster.cpp). check-group.ts (CheckGroup extends Cluster — options-object `new CheckGroup({ labels, value: Signal<boolean[]> })`, ` [ ] `/`X` box; exports `CheckGroupOptions`; tcheckboxes.cpp) + radio-group.ts (RadioGroup extends Cluster — options-object `new RadioGroup({ labels, value: Signal<number> })`, ` ( ) `/`•` box, movedTo sets value; exports `RadioGroupOptions`; tradiobut.cpp). Both option types re-exported from the package barrel (matching `MultiCheckGroupOptions`). slider.ts (Slider extends View — a horizontal/vertical value slider: `─`/`│` groove + `█` thumb, keyboard (arrows/Home/End/PageUp/PageDown) + mouse drag (pointer capture) + wheel, `onInput` live / `onChange` on commit; `sliderTrack`/`sliderThumb` roles). switch.ts (Switch extends View — an on/off toggle bound to a `Signal<boolean>` (the Slider idiom): `SwitchOptions {value,label?,onLabel?,offLabel?,disabled?}`, Space/Enter/click/Alt-hotkey toggle, a bracketed 4-cell track with a sliding `●`/`o` knob, green-on (`button`/`buttonFocused`) vs dim-off (`staticText`) vs disabled (`clusterDisabled`) — **no new core role**; `measure()` + an `accelerators()` override joining the duplicate-accelerator check). track.ts (internal pure value↔position math shared with `ScrollBar` — `clampValue`/`valueToOffset`/`offsetToValue`/`stepValue` over a `TrackSpec`; ScrollBar refactored onto it, its suite unchanged). measure.ts (shared glyphWidth/stringWidth, WIDTH_MODE='wcwidth'). validators/ ({isValidInput,isValid,error?} model — types, charset (expandCharSet range-aware + allInSet), filter (allowed-charset live reject), range (TV-faithful unsigned/signed validChars + INTEGER+min/max bounds; tvalidat.cpp), lookup (allowlist membership), index barrel). Additive primitives: RD-04 ev.emit/ev.focusView on DispatchEvent (sourced from RouteContext, enriched once in route() as ev2) + a per-view lazy focusSignal() (PF-009, poked by focus.ts focusLeaf); core: the cpGrayDialog Theme roles staticText/label/labelSelected/labelShortcut/button*/cluster*/input* in packages/core/src/engine/color/theme.ts. One runtime decision PA-15 (TV-faithful range validChars) in the plan's 00-ambiguity-register.md. Tests: packages/ui/test/controls.{foundation,text-label,button,validators,input,cluster,focus,packaging}.{spec,impl}.test.ts (ST-01…ST-16 + impl) + examples controls-demo.e2e. Plan: codeops/features/jsvision-ui/plans/essential-controls/.
packages/ui/src/{scroll,list,dialog}/  RD-11 containers, scrolling & lists — the container tier, faithfully ported from Turbo Vision (GATE-1+2 decodes in the code JSDoc). EXPLICIT named re-exports in src/index.ts. scroll/ (scroll-bar.ts = ScrollBar extends View — passive TScrollBar chrome: ▲/▼ (V) · ◄/► (H) arrows + ▒ page track + ■ proportional thumb (▓ when disabled), two-way `value` Signal, thumb-drag via the PA-16 capture seam, wheel ±3·arrowStep, setRange; tscrlbar.cpp. scroller.ts = Scroller extends Group — a focusable scrolling viewport owning content + auto ScrollBar(s), draw() offsets content by -delta in reserved edges (PA-17), keyboard ↑↓/←→/PgUp/PgDn/Home/End + wheel-on-bar (PA-18); tscrolle.cpp). list/ (virtual.ts = clampIndex/keepVisible helpers. list-rows.ts = ListRows<T> extends View — the focusable single-column virtual-scroll renderer: visible-window draw, colour-only focus (listFocused>listSelected>listNormal, no marker/divider, text at col 1), keyboard/mouse/wheel, sorted (computed) + linear typeAhead, <empty>; tlstview.cpp. list-view.ts = ListView<T> extends Group = [rows fr | bar 1] with the shared focused Signal driving the owned bar.value, exposes `rows` (the focus target); tlstview.cpp:161. list-box.ts = ListBox = ListView<string> preset; tlistbox.cpp). dialog/ (dialog.ts = Dialog extends Window — the gray-`dialog` frame role (close box, no zoom, PF-001), ModalHostAware (PA-1) resolving execView to the terminating command, the valid() child-sweep close-gate = DEF-16 (cmCancel bypass, refocus first invalid), onEvent routes frame-close/Esc→cancel (PF-002); tdialog.cpp/tgroup.cpp. buttons.ts = okButton/cancelButton/yesButton/noButton (+ pairs) emitting Commands.ok/cancel/yes/no. **DX-ergonomics: message-box.ts adds the async helpers `messageBox`→'ok'|'cancel' / `confirm`→boolean / `inputBox`→string|null over `Dialog`, taking the minimal `ModalDialogHost` {loop,desktop} seam (an Application satisfies it directly), plus the shared module-internal `runDialog` engine; the editor's infoBox/confirmBox now delegate to it and `EditorDialogHost` aliases `ModalDialogHost`**). Additive primitives: RD-04 setCapture/releaseCapture on DispatchEvent + the ModalHost/ModalHostAware seam (execView injects it, PA-1/PA-16); core: 6 decoded Theme roles scrollBar*/list*/dialog.icon + the dialog role corrected TV-faithful (PA-19). frame.ts generalized (FrameRole += 'dialog', FrameState closable/zoomable gate the icon draws). Tests: packages/ui/test/{scrollbar,scroller,listview,listbox,virtual,dialog,fidelity,containers.packaging,event.modalhost}.{spec,impl}.test.ts (ST-01…ST-16 + impl) + examples containers-demo.e2e + the 4 Containers kitchen-sink stories (kitchen-sink.smoke). Plan: codeops/features/jsvision-ui/plans/containers-scrolling-lists/ (PA-16…19 runtime in 00-ambiguity-register.md).
packages/ui/src/table/     RD-16 table — the `DataGrid<T>` multi-column table tier, a documented Turbo Vision EXTENSION on the `TListViewer` spine (GATE-1 decode + GATE-2 diff against `tlstview.cpp` in the code JSDoc). EXPLICIT named re-exports in src/index.ts. columns.ts (pure, view-free math: `Column<T>`/`ColumnWidth`/`ColumnAlign`/`SortState`/`ColumnGeometry` types + `measureAutoWidths` O(rows) `auto` pre-measure (maxWidth cap, title fallback) + `apportionColumns` O(cols) divider-reserving `solveTrack` with a bounded min/max clamp fixpoint + width-aware `alignCell` (never splits a wide glyph) + stable `{col,dir}` `sortRows` comparator (locale default, out-of-range guard)). grid-rows.ts (GridRows<T> extends View — the focusable virtual-scroll body: per-column aligned cells, `│` divider at each column's right edge in `listDivider`, focus>select>zebra>normal priority, whole-grid H-scroll (`x=starts[c]−indent`), nav/wheel/select/paging incl. Ctrl+PgUp/Dn; `tlstview.cpp`. GridHeader<T> extends View — sticky `tableHeader` row + `▲`/`▼` sort indicator with PF-103 title-clip + click-to-sort toggle). data-grid.ts (DataGrid<T> extends Group — an inner `col` container so the parent's `layout` stays free; PF-101 width-matched fr-bands `topRow[header fr\|corner 1] / body[rows fr\|vbar 1] / botRow[hbar fr\|corner 1]`; `focused`↔vbar, `indent`↔hbar, shared `display` + `autoWidths` computeds; `sortBy`). Only additive surface = 1 core `tableHeader` role (`0x3F` white-on-cyan). Tests: packages/ui/test/{datagrid,grid-columns,table-theme,table.packaging}.{spec,impl}.test.ts (ST-1…ST-24 + impl) + examples table-demo.e2e + the `data-grid` kitchen-sink story (kitchen-sink.smoke). Plan: codeops/features/jsvision-ui/plans/table/.
packages/ui/src/tabs/      RD-17 tabs — the `TabView` folder-tab container, a documented Turbo Vision EXTENSION (GATE-1 whole-tree search proved TV has NO tab/notebook class; the pieces are grounded in shipped facilities + a fresh glyph/colour decode in the code JSDoc). EXPLICIT named re-exports in src/index.ts (TabView, Tab, TabViewOptions). tab-view.ts (TabView extends Group — self-contained folder-tab container; eager pages via a keyed `For` (keyed by content identity — reorder/close reuse the live page), one visible via a reactive `state.visible` FLIP (NOT `Show` — no per-switch mount/dispose, page state survives, PF-001); read/render-time self-correcting clamp + snap-off-disabled from any writer (PF-003); Ctrl+PageUp/Down + Alt-hotkey handled in `preProcess` scoped by `isWithin(ev.getFocused(), this)` to the focus-owning view (PF-002); `select`/`next`/`prev`/`closeTab`+`neighbourAfterRemove` clamp/`onChange`; view-free helpers `clampActive`/`firstEnabled`/`nextEnabled`/`prevEnabled`/`neighbourAfterRemove`/`isWithin`; an inner `col` container keeps `TabView.layout` free for `at()`/absolute placement (DataGrid PF-101 idiom); internal `TabBody` draws the `│` sides + `└─┘` bottom in `staticText`, pages inset by padding). tab-strip.ts (TabStrip extends View — the focusable renderer: local `TAB_GLYPHS` set (frame line/corner code points + the 4 decoded tees `┬┴├┤`, retained as the GATE-1 reference), pure `stripGeometry` (visible slots + overflow auto-scroll keeping the active tab visible) + `hitStrip` (tab/close/arrow); draw() renders the tabs as green BUTTON FACES (no drop shadow — `tabActive`/`tabInactive`/`tabDisabled`) joined by a flat `─` dash gap (adopted post-spike design; no `┬` notch), `~X~` hotkey accent on every enabled tab via `tildeSegments`, `×` close cell (` label × ` with a trailing pad), `◄`/`►` overflow arrows; frame chrome in `staticText`; `←→` cycling when focused). Only additive surface = 3 core `tab*` roles (button-palette green: active `0x2F`, inactive `0x20`, disabled `0x28` green-dimmed). Tests: packages/ui/test/{tabs,tab-strip,tabs-theme,tabs.packaging}.{spec,impl}.test.ts (ST-1…ST-38 + impl) + examples tabs-demo.e2e + the `containers/tabs` kitchen-sink story (kitchen-sink.smoke). Plan: codeops/features/jsvision-ui/plans/tabs/.
packages/ui/src/feedback/  RD-18 feedback — the determinate `ProgressBar` (smooth sub-cell fill) + the indeterminate `Spinner` (caller-driven frame) + the `runSpinner` timer helper. Documented new components (GATE-1: TV has NO gauge/spinner class, AR-186). EXPLICIT named re-exports in src/index.ts (ProgressBar, Spinner, runSpinner, SPINNERS + the ProgressBarOptions/LabelPosition/SpinnerOptions/SpinnerName/RunSpinnerOptions/TimerSeam types). progress-bar.ts (ProgressBar extends View — round-first eighth-block fill `█`+`PARTIAL[1..7]` `▏▎▍▌▋▊▉` over `░`; the pure `fillEighths(v,w)` snaps the 0%/100% boundaries so the visual fill agrees with the rounded percent — the last cell completes at 100%, no lingering partial (bugfix); ASCII fallback `#`/`-` chosen at draw-time from `ctx.caps`; an optional **knockout** `NN%` caption (PA-12) — each digit's bg matches what it sits on (fill colour where filled, track bg where not), fg inverts, NO grey `staticText` box; an optional positioned `label` (string | accessor, PA-13) `left`/`right`/`top`/`top-left` reserving columns/rows beside/above the bar, with a `measure()` seam advertising 2 rows for a top label; colours `progressFill`/`progressTrack`). spinner.ts (Spinner extends View — caller-driven `frame`, frozen `SPINNERS` presets dots/line/blocks, `asciiOnly`→`line` swap, negative-safe mod, sanitized label at col 2). run-spinner.ts (`runSpinner(frame, {intervalMs?, timer})` — self-re-arming one-shot timer over an injectable `TimerSeam`, idempotent leak-free stop). Additive surface: 2 core `progress*` theme roles + the `DrawContext.caps` seam (PA-1, threaded through `composeView`/`makeDrawContext`). Tests: packages/ui/test/{progress-bar,spinner,run-spinner,feedback-theme,feedback.packaging}.{spec,impl}.test.ts (ST-1…ST-17 + impl) + examples feedback-demo.e2e + the `feedback/progress-bar` + `feedback/spinner` kitchen-sink stories (kitchen-sink.smoke). Plan: codeops/features/jsvision-ui/plans/feedback/.
packages/ui/src/date/     RD-20 date family — the `Calendar` month-grid view (a faithful `TCalendarView` decode) + the `DatePicker` dropdown + the `CalendarDate` civil-date value. EXPLICIT named re-exports in src/index.ts (Calendar, DatePicker, the 9 CalendarDate helpers + the CalendarDate/CalendarOptions/DatePickerOptions/DateFormat types; the grid/format internals + `dateFormat()` stay internal). calendar-date.ts (pure, zero-dep: `CalendarDate {year, month(1-12), day}` + `daysInMonth` Gregorian-leap-correct, `dayOfWeek` Zeller/Sunday=0 per calendar.cpp:100-121, `addMonths` day-clamped, `addDays` via a proleptic-Gregorian day-number (JDN) walk, `compare`, `toISO`, `parseISO` range-validated null-safe, `fromDate`/`toDate` interop confined to the boundary). calendar-grid.ts (pure geometry: `buildMonthGrid` leading-offset + `firstDayOfWeek` rotation + null blanks, `dayColumn` = weekNumberCol + `j*3`, `isoWeek` row-Thursday ISO-8601 PA-10). calendar.ts (Calendar extends View — the GATE-1/GATE-2 grid decode: `getColor(6)`→`0x3E` normal / `getColor(7)`→`0x21` today, days 2-digit right-justified per the density's cell math; the **header + density are RD-20 extensions** (user request 2026-07-04, NOT the TV decode): a flanking `↑↓ ⟨month⟩ ⟨year⟩ ↑↓` header (PA-17/PA-18) — LEFT ↑↓=month ±1, RIGHT ↑↓=year ±1, clamped to `[min,max]` via `clampVisibleMonth`, thin `↑/↓` matching the dropdown `↓`; a **`density`** option (PA-20-runtime, default `'comfortable'`) `'compact'`=TV-exact 20×8 / `'comfortable'`=4-wide cells+3-letter labels+a `[ Today ]` footer (~28×10) / `'spacious'`=5-wide+blank week spacers (~35×15), all geometry in `calendar-metrics.ts`; a `[ Today ]` button + `T` key jump to today (PA-20); + extensions: selectable two-way `value`, a PA-4 **filled-reverse** focus cursor (`calendarCursor` `0xF0` black-on-white, PA-19-runtime) drawn only while focused (precedence cursor>selected>today>disabled>normal), min/max clamp, disabled days, ISO week# column, firstDayOfWeek; AR-199 keymap arrows=day/week · PgUp/Dn=month · Ctrl+PgUp/Dn=year · Home/End=week ends · T=today · +/- + header-arrow/[ Today ]/day-click mouse; `select`/`today`/`goToMonth`/`onChange`). calendar-metrics.ts (pure per-density geometry: `metricsFor` + `dayFieldX`/`weekdayLabelX`/`weekRowY`/`weekdayLabels`/`headerLine`; `CalendarDensity` is publicly exported). date-format.ts (pure: `DateFormat` ISO/`DD/MM/YYYY`/`MM/DD/YYYY` → `{mask, parse, serialize}` over `picture`; range-validated). date-picker.ts (DatePicker extends Group — masked `Input` (`picture(spec.mask)`) + a trailing `▐↓▌` `DateButton` (the **shared** `drawDropdownIcon`, byte-identical to ComboBox/History, PA-17) opening a `Calendar` in the generalized anchored popup (options forwarded, PF-008); two-way value⟷text bind (ComboBox idiom, each direction reads only the other signal); Down/Alt+Down/`▐↓▌`-click open with a `host===undefined` no-op guard). Additive surface: **6 core `calendar*` theme roles** (byte-frozen by date-theme.spec) + the generalized internal `openAnchoredPopup` (PA-5 — `buildContent(commit)`/`contentSize`/`focusTarget`; History/ComboBox byte-identical, AC-13). Runtime notes PA-15 (migrated popup.spec/impl to the generalized API) + PA-16 (caller `selected()`-watch pick, preserving click-pick) + the post-ship PA-17 (unified dropdown/header arrow glyphs) + PA-18 (flanking month/year header nav, clamped) in the plan's 00-ambiguity-register.md. Tests: packages/ui/test/{calendar-date,calendar-grid,calendar,date-format,date-picker,date-theme,date-popup,date.packaging}.{spec,impl}.test.ts + examples date-demo.e2e + the `date/calendar`+`date/date-picker` kitchen-sink stories (kitchen-sink.smoke). Plan: codeops/features/jsvision-ui/plans/date-family/.
packages/ui/src/color/    RD-21 color family — the `ColorSwatch` color-grid view (a faithful `TColorSelector` decode) + the `ColorPicker` dropdown. EXPLICIT named re-exports in src/index.ts (ColorSwatch, ColorPicker + the ColorSwatchOptions/ColorPickerOptions types; the pure `color-grid.ts` helpers stay internal, mirroring calendar-grid). color-grid.ts (pure geometry/nav: `gridDims`/`insideGrid`/`hitCell` returning a discriminated `number|'overshoot'|'outside'` (PA-10 revert-vs-clamp lives in the pure layer), `cellX`/`cellRow`, wrap-around `navLeft|Right|Up|Down` transcribed verbatim from `colorsel.cpp:196-217`, `isNearBlack` luminance predicate PA-2). color-swatch.ts (ColorSwatch extends View — the GATE-1/GATE-2 decode: 3-wide `█` cells (`fg=cellColor`/`bg=black`, `moveChar(j*3,icon,c,3)`), `◘`(CP437 8) marker on `indexOf(value())` at `cellX+1`, near-black→`colorMarker` `0x70`; the cursor-vs-`value` split (internal cursor Signal = nav SoT, PA-9), wrap-around arrow keys + Enter/Space commit, mouse down/move/up with pointer capture — drag-outside reverts to the pre-drag cell (`else color=oldColor`), partial-row overshoot clamps to n-1 (PA-10); opt-in commit-on-release gated on `onChange` (the discrete commit callback; live arrow/click/drag fire `onInput`, Enter/Space/mouse-up fire `onChange`, `select` fires both); frame omitted (PA-12); `select`/`onInput`/`onChange`/`nameFor`/`measure()`; GATE-2 AFTER-diff recorded, no mismatch + PF-005 `charWidth('◘')===1`/`'█'`). color-picker.ts (ColorPicker extends Group — a color chip + a trailing `▐↓▌` `ColorButton` (shared `drawDropdownIcon`) opening a `ColorSwatch` (+ an optional hex `Input`) in the generalized anchored popup, sharing `value`; Down/Alt+Down/button-click open with a `host===undefined` no-op guard; commit-on-release/Enter close; Esc/outside cancel; the hex field `filter('#0-9a-fA-F')`-gated + `toRgb()`-parsed, a two-way value⟷text bind with an RGB-equality guard — no feedback loop, preserves named values; `ColorPickerOptions` exposes the same `onInput` (live) / `onChange` (commit) split and forwards both to the hosted swatch — the commit fires the picker's `onChange` then closes, no dead callback). Additive surface: 1 core `colorMarker` theme role (`0x70`, byte-frozen by color-theme.spec; PA-14 closed-set guards `tabs`/`feedback`/`date` extended) + 2 core re-exports (`ANSI16_ORDER`/`toRgb`; `HEX_RE` stays private) + the **PA-16 generalization of `openAnchoredPopup`'s focus-loss dismiss to popup-subtree membership** (`dropdown/popup.ts`; user-decided, supersedes AC-9's no-dropdown-edit — backward-compatible, the History/ComboBox/DatePicker suites guard it so a multi-focusable popup can Tab grid→hex without self-dismissing). Tests: packages/ui/test/{color-grid,color-swatch,color-picker,color-theme,color.packaging}.{spec,impl}.test.ts + examples color-demo.e2e + the `color/color-swatch`+`color/color-picker` kitchen-sink stories (kitchen-sink.smoke). Plan: codeops/features/jsvision-ui/plans/color-family/.
packages/ui/src/surface/    RD-19 surface family — the `Surface` offscreen cell buffer (a faithful `TDrawSurface` port) + the passive `SurfaceView` `delta`-viewport (a faithful `TSurfaceView::draw()` decode). EXPLICIT named re-exports in src/index.ts (Surface, SurfaceView + the SurfaceOptions/SurfaceViewOptions/SurfaceSource types; the pure `surface-geometry.ts` helpers stay internal, and `Point`/`Rect` are reused from view/layout, PA-13 — no duplicate export). surface-geometry.ts (pure, view-free clip/margin/clamp math: `computeClip` = `TRect(0,0,surface.size).move(-delta).intersect(viewExtent)` (`tsurface.cpp:105-107`) as `{x,y,width,height}`, empty ⇒ surface fully outside; `marginRects` = the top/bottom + left/right empty-area bands in TV fill order (`:118-132`); `clampDelta` = `[0, max(0, surface−view)]` per axis, the Should-Have scroll clamp). surface.ts (Surface **wraps** core `ScreenBuffer` (PA-10 composition — `ScreenBuffer.width`/`height` are `readonly` + it has no `resize`, so `Surface` holds one and swaps it): `resize` allocates a fresh buffer + copies the overlap (AC-1, a documented extension vs TV's whole-buffer `memset 0` `tsurface.cpp:60`, PA-2) + `grow`/`clear`; `at(x,y)` bounds-checked + **read-only** (frozen copy, never the live `get` handle) and `set(x,y)` → the sanitizing `ScreenBuffer.set` — no unsanitized control byte can be stored (PA-1/AC-14); the `getDrawContext(overrides?)` paint facade (construction-default + per-call theme/caps override, PA-4) bumps a private `version` Signal on every write (PA-5/AC-6); `from(rows)`/`snapshot()` Should-Haves). surface-view.ts (SurfaceView extends View — passive `focusable=false`, no input; the draw is the `tsurface.cpp:93-141` decode recorded cell-by-cell in the class JSDoc incl. the GATE-2 AFTER-diff (no mismatch): clip/direct-copy/margin bands/null-surface + the empty-area `windowInactive` fill (`mapColor(1)`, 0 new roles); cell-aware blit keeps each surface cell's own colours + skips wide continuations (PA-11); reactive — binds `delta`+surface+`version` so a pan/swap/content bump each coalesce to one repaint (`draw()` isn't auto-tracked); Should-Have `scrollTo`/`panBy` (clampDelta) + `onScroll` change-only). Additive surface: **0 new core theme roles**, **no `@jsvision/core` export change** (reuses `ScreenBuffer`+`windowInactive`+`sanitize`+`defaultTheme`); no `dropdown/` edit. Tests: packages/ui/test/{surface-geometry,surface,surface-view,surface.packaging}.{spec,impl}.test.ts + examples surface-demo.e2e + the `surface/surface-view` kitchen-sink story (kitchen-sink.smoke). Plan: codeops/features/jsvision-ui/plans/surface-family/.
packages/ui/src/tree/      Tree/outline (demo:tree) — the `Tree` collapsible node viewer. EXPLICIT named re-exports in src/index.ts (Tree + the TreeNode/TreeOptions/MarkerStyle types). graph.ts (pure indent-graphics: `│├└─` guides + a style-aware node marker via `MarkerStyle` — `'tv'` (`+`/`─`, default, TV-faithful) | `'brackets'` (`[+]`/`[-]`, 5-cell) | `'triangle'` (`▸`/`▾`); `createGraph`/`graphWidth`/`endWidth`). tree-rows.ts (TreeRows — the focusable virtual-scroll renderer: expand/collapse →/←/Enter, ↑↓ nav + type-ahead, wheel, mouse toggle at the resolved marker hit-zone; caches the caps-resolved effective style so `triangle` falls back to `brackets` with no Unicode, and the same effective style drives both draw + hit-zone width). tree.ts (Tree extends Group — rows + optional scrollbar; opt-in `markerStyle`, default `'tv'` so all prior output is byte-unchanged). Tests: packages/ui/test/{tree-graph,tree,tree-markers,fidelity.tree}.{spec,impl}.test.ts + the `containers/tree` kitchen-sink story.
packages/ui/src/editor/    Text editor — a multi-line `Editor` view + `Memo`/`EditWindow` chrome + editor dialogs, ported from Turbo Vision's `TEditor`. EXPLICIT named re-exports in src/index.ts (Editor/Memo/EditWindow/Indicator/EditorCommands/defaultEditorDialog + the find/replace/confirm/info dialog helpers wireEditorDialogs + the EditorOptions/MemoOptions/EditWindowOptions/EditorAction/EditorKeyBindings/LineEnding/EditorDialogHost types). buffer/ (the line buffer + undo/redo history), editor.ts with its draw/events/mouse/search/clipboard concerns split across editor-{draw,events,mouse,search,clipboard,actions,types}.ts, keymap.ts (**modern Ctrl+X/C/V/A clipboard defaults** — a sanctioned break from WordStar fidelity), format.ts, indicator.ts (line/col status target), dialogs.ts (find/replace/confirm/info over an `EditorDialogHost` seam, aliased to the dialog `ModalDialogHost`). Tests under packages/ui/test/.
packages/ui/src/dropdown/    Anchored-popup infrastructure + dropdown widgets shared by the date/color/history families. EXPLICIT named re-exports in src/index.ts. popup.ts (`openAnchoredPopup` — the generalized anchored, focus-loss-dismissed popup (dismiss generalized to popup-subtree membership so a multi-focusable popup can Tab internally) reused by DatePicker/ColorPicker/ComboBox/History byte-identically; `drawDropdownIcon` the shared `▐↓▌` glyph; `absoluteRect`; `DEFAULT_MAX_ROWS` + the AnchoredPopup/AnchoredPopupOptions/PopupHost types). history.ts + history-store.ts (History — a recall dropdown over a backing store). combo-box.ts (ComboBox — an editable select-from-list dropdown; ComboBoxOptions).
packages/ui/src/terminal/    Embedded terminal-output view. EXPLICIT named re-exports in src/index.ts (Terminal + terminalWriter + the TerminalOptions type; LineRing). terminal.ts (Terminal — a scrollback terminal-output view) + ring.ts (LineRing — a bounded scrollback ring buffer).
packages/ui/src/shared/    Cross-subsystem INTERNAL utilities (not barrel-exported). warnings.ts (`devWarn(scope, message)` — the `NODE_ENV`-gated `console.warn` used by the duplicate-accelerator check and other dev-only diagnostics; kept internal so it does not collide with the reactive core's own `devWarn`).
tsconfig.base.json  turbo.json  vitest.config.ts(per package)  — shared TS config, turbo pipeline, vitest unit+e2e projects.
docs/  scripts/  .github/  CHANGELOG.md  — monorepo-level docs (now ONLY acceptance-gate.md; the techdocs website content moved to packages/docs-site/reference/), scripts (gate.mjs, check-no-native-deps.mjs, sync-versions.mjs), CI (ci.yml build/test + docs.yml Pages deploy), changelog.
--- (the subsystem layout below is rooted at packages/core/) ---
src/engine/      Source. Single public entry point: src/engine/index.ts (re-exports public API).
src/engine/capability/   RD-02 capability detection core (profile, defaults, env, table, query, detect, index) + responses.ts (RD-06-shared query-response classifier).
src/engine/input/        RD-06 input decoder (events, keys, decoder, mouse, paste, keymap, index). `WheelEvent` also carries `shift`/`alt`/`ctrl` modifier flags — the xterm SGR modifier bits (`0x04`/`0x08`/`0x10`) OR-encoded into the button byte, decoded in `mouse.ts` `buildEvent` without disturbing wheel/button/motion classification; lets a consumer distinguish e.g. plain vs. Shift+wheel (the terminal-independent horizontal-scroll fallback used by the surface-view kitchen-sink story). Corpus fixtures `test/fixtures/input-corpus/wheel.json` cover plain (all-false) + Shift+wheel reports.
src/engine/render/       RD-04 rendering engine (types, width, buffer, ansi, glyphs, serialize, sanitize, cursor, osc, index).
src/engine/host/         RD-07 host & lifecycle (types, streams, platform, modes, host, restore, signals, index) — native tty host behind an injectable RuntimeAdapter. streams.ts also exports the additive detectTty() pre-start TTY probe (RD-08 PF-001); terminal-query.ts is the RD-03 real tty-backed createTerminalQuery (layer-2 query seam).
src/engine/safety/       RD-08 safety (sanitize, errors, redact, logger, essentials, index) — essentials gate, screen-safe logger, redaction, typed errors, and the canonical injection boundary.
src/engine/color/        RD-05 color & styling (color, palette, downsample, encode, theme, index) + the jsvision-ui-RD-22 theming tier (aliases, ramp, contrast, create-theme, roles, serialize, presets, preset-seeds) — depth-aware SGR encoding (truecolor→256→16→mono), redmean nearest-color, DOS-16 palette + theme (incl. the jsvision-ui-RD-10 statusSelected pressed-item role, the jsvision-ui-RD-16 tableHeader DataGrid-header role `0x3F` white-on-cyan, and the jsvision-ui-RD-17 `tab*` folder-tab roles — button-palette green `tabActive` `0x2F`/`tabInactive` `0x20`/`tabDisabled` `0x28` green-dimmed; the jsvision-ui-RD-18 `progress*` bar roles `progressFill` `0x1B` brightCyan-on-blue / `progressTrack` `0x13` cyan-on-blue; and the jsvision-ui-RD-20 `calendar*` roles — TV-decoded `calendarNormal` `0x3E` yellow-on-cyan (`getColor(6)`) / `calendarToday` `0x21` blue-on-green (`getColor(7)`) + the extensions `calendarSelected` `0x1F` filled-blue / `calendarCursor` `0xF0` filled black-on-white reverse (PA-19-runtime) / `calendarDisabled` `0x38` / `calendarWeekNumber` `0x30`; and the jsvision-ui-RD-21 `colorMarker` role — TV-decoded `0x70` black-on-lightGray (`colorsel.cpp:136`), the forced-contrast `◘` marker on a near-black color-swatch cell; and the theme-designer `slider*` roles — `sliderTrack` (darkGray-on-lightGray) / `sliderThumb` (blue-on-lightGray), derived in `rolesFromAliases` from border/accent, attr-driven in `monochromeTheme`); the serialize() default encoder. The additive core re-exports `ANSI16_ORDER` (palette.ts) + `toRgb` (color.ts) surfaced through color/index.ts → engine/index.ts (jsvision-ui-RD-21 PA-3; `HEX_RE` stays private); the theme-designer tier adds three more re-exports — `aliasesFromSeeds` (create-theme.ts), `rgb256` (palette.ts), and the `PRESET_SEEDS` data (preset-seeds.ts, per-preset seed consts preserving tree-shaking). **13 tree-shakeable presets** live in presets.ts: the literal `turboVisionTheme`/`monochromeTheme`, the generated `slateTheme`, the curated `nordTheme`/`draculaTheme`/`solarizedDarkTheme`/`gruvboxDarkTheme` (each overriding all 18 aliases from its published spec — authentic surfaces, not a generic ramp), and six retro-desktop themes `janusTheme`(early-90s PC)/`warpTheme`(OS/2)/`solsticeTheme`(Sun CDE)/`platinumTheme`(classic Mac)/`workbenchTheme`(Amiga)/`horizonTheme`(enterprise). `createTheme` dark-mode surfaces sit near the ramp floor (deeper backdrop; was mid-gray).
bench/           (under packages/core/) RD-10 frame-performance benchmark (frame-bench.mjs) behind `yarn bench` — exported pure median/p95/measureComposeDiff helpers + a main-guarded printing CLI; never emitted to dist.
test/            (under packages/core/, 76 files) RD-09 four-tier strategy: hex-in-JSON input corpus (fixtures/input-corpus/ + input-corpus.spec/impl + -helpers), golden-screen via @xterm/headless (golden-screen.spec/impl + -helpers), Tier-3 host-tier3.e2e, seeded fuzz (input-fuzz.spec/impl + -helpers), bytes∝damage (render-bytes-damage.spec), gate consistency (gate.spec). RD-10: perf-budget.spec/impl, treeshake.spec, a11y-golden.spec, api-stability.spec, docs-presence.spec. Monorepo: sync-versions.spec. Governance specs (gate/check-deps/docs-presence/api-stability/toolchain) reach the monorepo root via ../../.. (DEF-4 to extract). Shared helpers in non-test *-helpers.ts.
packages/examples/   (capability-probe/ = foundation-RD-03 probe harness: main/args/taxonomy/env-meta/auto-probes/manual-probes/live-readout/report/matrix; resize-demo/, keyboard-mouse-playground/, reactive-demo/ = RD-01 walkthrough, layout-demo/ = RD-02 walkthrough + ASCII render, view-demo/ = RD-03 view/group spine walkthrough + ASCII render, event-demo/ = RD-04 event-loop walkthrough — focus + command + modal, ASCII render, shell-demo/ = RD-05 app-shell walkthrough — windows + WM + menu + status, ASCII render, controls-demo/ = RD-06 essential-controls walkthrough — a Label-linked Input + live filter, CheckGroup/RadioGroup, default Button command, ASCII render, controls-live/ = RD-06 live interactive audit — a real-TTY grey Turbo Vision parameters Dialog hosting every leaf control (form.ts composition + a demo-local Dialog/CommandSink in dialog.ts; needs a terminal), kitchen-sink/ = **the live showcase** (Storybook-for-TUI, `demo:kitchen`): story.ts (the `Story` contract + `at`/`firstFocusable` helpers), stories/*.story.ts + stories/index.ts (the registry — one file per component), shell.ts (the Navigator seam — menu from the registry + grey `StoryWindow` canvas swap + nav), window.ts (StoryWindow + CommandSink), main.ts; extend it per `codeops/kitchen-sink-gate.md`, feedback-demo/ = RD-18 feedback walkthrough — a `ProgressBar` filled 0→33→66→100% (smooth) + a stepped `Spinner` + ASCII `#`/`-` + `line` fallback, ASCII render, themes-demo/ = RD-22 theming demo — a pure `designer.ts` (`currentTheme`/`cycleAccent`/`cycleMode`/`cycleDepth`/`randomizeSeed`/`exportJson`/`contrastWarnings`, headless-testable) + `main.ts` (**headless narrated walkthrough only** — the live-TTY interactive designer moved to the `@jsvision/theme-designer` app), tvision-demo/ = a live interactive Turbo Vision desktop (windows + menu + status + truecolor gradient + reactive clock) + **the flagship DX-ergonomics proof: zero-config caps, single-package imports (everything from @jsvision/ui), `app.onCommand('about', …)`, and `messageBox` for the About box**) + test/ holds the probe-*/probe.e2e tests + view-demo.e2e + event-demo.e2e + shell-demo.e2e + controls-demo.e2e + tabs-demo.e2e (RD-17 TabView walkthrough) + feedback-demo.e2e (RD-18 ProgressBar+Spinner walkthrough) + themes-demo.e2e (RD-22 demo:themes walkthrough) + kitchen-sink.smoke.spec (mounts every story headlessly, incl. the `containers/tabs` + `feedback/*` + `theming/presets` stories, and every preset) (demo child runs via tsx). web-xterm/ = the `demo:web` browser dogfood (vite dev server) — the tvision desktop running on **@jsvision/web** via `mountApp` (+ `buildBrowserCaps`), its `vite.config.ts` aliasing node:fs/node:tty → `@jsvision/web/browser-stubs`; NOT in verify's typecheck scope, proven by a production `vite build` carrying no node:fs/node:tty specifier. Imports @jsvision/core, @jsvision/ui, @jsvision/files, and @jsvision/web by name; run via the demo:* / probe scripts.
packages/theme-designer/   Private @jsvision/theme-designer app — a standalone TUI for authoring @jsvision/core themes (deps core+ui+files; `tsx start`, no build/barrel; publish deferred while ui is private). src/model/ = the pure, headless designer core: types.ts (DesignerState, EditTarget alias|role, RoleOverrides, ThemeSeeds, PresetName), model.ts (createDesignerModel — two-mode state: roleSnapshot null⇒derive via createTheme({...seeds, overrides, roleOverrides}) / non-null⇒roles via applyRoleOverrides(snapshot); first alias/seed edit transitions; setAlias/setRole/select/loadPreset/reset/setDepth/importJson/exportJson/markSaved/resolvedAliases/theme + dirty lifecycle; `colorOf(target, field?)` — an alias color, or a role's `bg`/`fg`), hex-validator.ts (#rrggbb/#rgb Validator), contrast.ts (contrastRows → WCAG AA/AAA/fail per role pair), depth.ts (depthSamples — truecolor/256/16/mono; reuses colorAtDepth), downsample.ts (pure `colorAtDepth`/`downsampleTheme` — collapse a color / a whole theme to a depth; the 16-color slot uses the Borland DOS-16 hex; `'default'`/glyphs pass through — the View-menu whole-preview depth transform), index.ts barrel. src/view/ = gallery.ts (broad deep-inert live-preview scene: buttons/input/checks/list+scrollbar/progress/spinner/slider/tabs/data-grid/tree/calendar on a `GALLERY_SIZE` canvas), preview-panel.ts (hosts the gallery in a `Scroller` + a reactive depth title), roles-panel.ts (rail: 18 aliases + 63 roles → selects the edit target), inspector-panel.ts (an "Editing:" heading + a bg/fg RadioGroup toggle + R/G/B Sliders + #rrggbb Input + DOS-16 ColorSwatch + live contrast + depth readouts), color-util.ts (channel↔hex helpers over core toRgb + `sameColor` normalized-hex compare). src/host/ = file-io.ts (the ONLY fs surface: open/save reusing @jsvision/files openFile/errorBox; a bad file surfaces an error, never mutates the model), walkthrough.ts (the piped narrated tour composing the gallery under each theme+depth into a ScreenBuffer). src/app.ts (createDesignerApp — menu/status shell + workspace row sized to the real mounted viewport, chaining the shell's onResize; commands open/save/save-as/presets/reset/depth/quit with injectable file/modal seams; one createRoot wires the inspector signals ↔ model, untrack-guarded to avoid a reactive self-dependency cycle; the theme effect downsamples to the selected preview depth; an idempotent `sameColor` commit guard so a bare selection never re-commits — was reverting a loaded preset). src/main.ts (TTY split — live app vs. piped walkthrough). test/ = model/hex-validator/app {spec,impl} + walkthrough.e2e (ST-23). Tests import core/ui/files by name (built dist). Plan: codeops/features/theme-designer/plans/theme-designer/.
packages/files/    Private @jsvision/files — the Turbo Vision file-system dialog family (FileDialog/ChDirDialog + FileList/DirList/FileInput/FileInfoPane + the `openFile`/`errorBox` openers) on @jsvision/ui. src/{dialog,editor,fs,input,list}/ + openers.ts. Private until first release; depends on core+ui, imported by name (built dist). Consumed by @jsvision/theme-designer's file open/save + @jsvision/web's virtual FileSystem (its widgets do all path work through the injected `FileSystem` seam — only fs/node-fs.ts imports node:path — so a virtual fs keeps a browser graph node:path-free).
packages/web/    @jsvision/web — the browser runtime (docs-website RD-02). Single public barrel src/index.ts EXPLICIT named re-exports: host.ts (createBrowserHost — promoted verbatim from the web-xterm spike behind a local structural `TerminalLike` (both @xterm/xterm and @xterm/headless satisfy it — no @xterm/xterm import) + an injectable timer seam over core's opaque `TimerHandle`; ENTER_MODES DECSET setup minus alt-screen; lone-ESC disambiguation; the ST-2 invariant = bytes written === serialize(buffer,null,{caps})), caps.ts (buildBrowserCaps — truecolor+UTF-8 profile via resolveCapabilities, colorDepth overridable to drive the existing downsample chain), mount.ts (mountApp — the browser mirror of run(): wires the loop's frame/caret/clipboard sinks to a caller-supplied `term`/`createTerminal` (never imports @xterm/xterm), first paint + refreshCaret, term.onResize→loop.resize, optional term.focus?.() so @xterm/headless mounts; narrow local `HostElement`), virtual-fs.ts (createBrowserFileSystem — in-memory @jsvision/files `FileSystem`: 14 methods + `sep`, files+dirs only lstat===stat, deterministic overridable mtime, seedable from a plain FileTree with absolute-path keys, hand-rolled pure-POSIX resolve/join/dirname/basename/normalize importing zero node builtins — `..` clamps at root, ENOENT-shaped errors), key-reclaim.ts (attachKeyReclaim — capture-phase document keydown that focus-gated preventDefault()s browser-hijacked chords: F-keys/Ctrl+W·N·T·S·P/Tab/Shift+Tab/Alt+<letter>/Backspace + `also` extras + `['*']` wildcard; UNRECLAIMABLE_CHORDS curated list; narrow local DOM interfaces + no-cast globalThis accessors; injectable target?/isFocused seams), clipboard.ts (setClipboard — outbound-only browser Clipboard API write, gesture-gated, the bridge type exposes only writeText so no read is possible). browser-stubs.ts = the `@jsvision/web/browser-stubs` subpath ONLY (throwing node:fs/node:tty placeholders; NOT re-exported from the barrel — a consumer bundler aliases node:fs/node:tty to it). Depends core+ui+files; @xterm/xterm optional peer + @xterm/headless devDep. Reserved-but-documented params carry the repo `_`-prefix (`_term`/`_caps`). test/ = packaging/host/caps/mount/virtual-fs/key-reclaim/clipboard/security {spec,impl} (ST-1…ST-12, 41 unit tests) + test/helpers/fake-terminal.ts (controllable terminal + deterministic fake timer). Plan: codeops/features/docs-website/plans/web-runtime/.
packages/docs-site/    Private @jsvision/docs-site — the VitePress documentation & showcase website (dev-only, NEVER shipped; VitePress + mermaid + js-yaml + @xterm/* + @jsvision/* as devDeps only, so `yarn check:deps` stays green; **participates in `yarn verify`'s test + typecheck** via `test` (a vitest `unit` project) + `typecheck` (a scoped tsconfig over `examples/**` + `src/**`) scripts — the live-example harness; only its **build** is isolated — the `vp:build` script name (not `build`) keeps turbo's build/check:deps off it). **The live-example system (RD-03):** `examples/<category>/<name>.ts` modules (`defineExample({title,blurb,build(ctx)})`, SSR/headless-safe — no `@xterm/*`/DOM, the pure `@jsvision/web` `createBrowserFileSystem` the one exception) registered in `examples/index.ts`; `src/demo-shell.ts` (`demoShell` — ONE unified shell: a registry `kind: 'component'` build (a bare `View`) is wrapped in a titled, NON-closable stage `Window` centered on the desktop (component on the window surface, not the desktop pattern); a `kind: 'app'` build (an `Application`, built via `demoApp(ctx, {windowMenu?})`) gets the shared commands wired onto it. ONE menu bar — System ▸ About / View ▸ Theme+Depth over the 13 presets / a Window menu for windowing apps — plus a hints-only status line (no Theme/Depth in the footer)); `src/play/` (`play-controller.ts` TERMINAL-DRIVEN headless lifecycle — the app is built at the terminal's real cols/rows, not a preset — + `no-keyboard.ts`/`deep-link.ts`); `.vitepress/theme/components/PlayExample.vue` (client-only Play → resizable modal xterm via `mountApp`; a ResizeObserver refits live, a CAPTURE-phase wheel `preventDefault` + a background scroll-lock stop the page scrolling/zooming under the modal); the two dialog examples host a non-closable stage `Window` with an "Open the dialog" `Button` (`demo.openDialog`) so a closed dialog reopens; pages under `components/`+`apps/` embed the module `build()`-first via the `<<< @/…#example` region (each module carries a `// #region example`/`// #endregion example` pair) with the whole module behind a `::: details Full module` block (shown code == running code) + `<PlayExample id=…>`. Tests `test/*.{spec,impl}.test.ts` = ST-1 parity + ST-B5 kind · ST-2 paint-smoke · ST-D1/D2 snippet-drift (region + full embeds) · ST-B1…B4 + ST-5/9 unified-shell (component in a non-closable Window, interior on the window surface, desktop shares the menu, hints-only status) · ST-C1 dialog-reopen · ST-A1 terminal-driven resize + ST-6/7/8/10/14 Play controller · ST-11 a11y · ST-12 no-keyboard · ST-13 file-dialog · ST-14 sanitize/escape. Plans: codeops/features/docs-website/plans/live-example-system/ (build) + …/live-example-remediation/ (the post-ship unified-shell / resize / reopen / Source-framing follow-up). `.vitepress/config.ts` (base `process.env.DOCS_BASE ?? '/jsvision/'` for prod + per-PR-preview subpaths; nav/sidebar IA, `search: {provider:'local'}`, per-page SEO og/twitter/canonical via `transformHead`, `sitemap`, and a **strict meta-CSP injected per-build via `transformHtml`** — script-src carries the SHA-256 of every inline script computed at build time, no `unsafe-inline`/`unsafe-eval`; wrapped in `withMermaid()` for ```mermaid diagrams), `.vitepress/theme/` (DefaultTheme + brand `custom.css`, light+dark, WCAG-contrast body text). index.md (home) + guide/components/apps/api/reference/ section pages. reference/ = the absorbed root-`docs/` techdocs: architecture/ (index = folded Technical Architecture landing + system-overview/api-design/security), decisions/ (9 ADRs + index), guides/ (getting-started/development). public/ (favicon.svg, robots.txt, og-placeholder.png), 404.md, redirects.md (old docs/→/reference/ map). scripts/check-docs-build.mjs = the build-output gate (ST-1…ST-14 + a mermaid guard + a LIVE-EXAMPLES guard + **6 API-\* guards**: base-prefixing, section pages, dead-link check, local search index, Shiki+copy button, WCAG contrast, SEO meta, CSP hash-coverage, static assets, migration completeness, spec-oracle integrity, workflow safety, every live-example page embedding its source + mounting the Play component, and the API-reference coverage/leakage/symbol/links/determinism/search checks). **The generated API reference (RD-06):** `yarn docs:api` (`scripts/gen-api.mjs`) runs TypeDoc + `typedoc-plugin-markdown` + `typedoc-vitepress-theme` **once per package** into gitignored `api/<pkg>/**` (clean `/api/<pkg>/<kind>/<Symbol>` URLs; core `src/engine/index.ts`, ui/files/web `src/index.ts`, badged pre-release; `web/browser-stubs` excluded), merges the 4 sidebars into `api/typedoc-sidebar.json` (config.ts reads it via a defensive fs-read so `docs:dev` on a fresh checkout still starts), injects a "Documented in →" back-link into each mapped symbol page, and package-qualifies every page's frontmatter title so cross-package re-exports (e.g. `resolveCapabilities` in core+ui) stay title-unique. `api/index.md` is the committed hand-written preface. The pure **`src/api/*.mjs`** helpers (plain ESM so both the `node` scripts and the vitest `.ts` specs share one impl, per the barrel↔gate ground-truth): `barrel-exports.mjs` (TS-compiler-API public-export extractor — the coverage/leakage ground truth), `inject-back-links.mjs`, `validate-api-map.mjs`, `api-map.mjs` (the 29-row symbol↔page map + `pageLabel`; each mapped component page carries a forward "API reference →" link in its `## Related`), `packages.mjs` (the shared 4-entry list). TypeDoc/plugins/theme + `typescript` are **docs-site devDeps only** (`check:deps` unaffected). Deployed to GitHub Pages (gh-pages branch) by .github/workflows/docs.yml. Plans: codeops/features/docs-website/plans/site-foundation/ + …/api-reference/ (the TypeDoc reference).
packages/spike-data-studio/    THROWAWAY feasibility spike (@jsvision/spike-data-studio, private) — a Paradox/MS-Access-style DB front-end on @jsvision/ui + PostgreSQL; evidence for the Data Studio decision memo. Deliberately inert (no build/test/typecheck scripts, so turbo + `yarn verify` skip it); delete after the decision.

docs/            Monorepo docs (root) — now holds ONLY acceptance-gate.md (RD-09 criteria→evidence; the load-bearing oracle for packages/core/test/gate.spec.test.ts + scripts/gate.mjs — do NOT move it). The former techdocs website set (index.md, architecture/, decisions/ ADRs, guides/, .vitepress/) was absorbed into packages/docs-site/reference/ (see the packages/docs-site/ entry); moving docs/index.md retired the techdocs auto-update hook — the docs-website now owns that content.
scripts/         (root) check-no-native-deps.mjs (dependency-policy guard, takes a package dir), gate.mjs (RD-09 aggregator behind `yarn gate`, cross-package), sync-versions.mjs (lockstep version sync behind `yarn sync-versions`).
.github/workflows/ci.yml   CI matrix: 3 OS × Node 20/22/24 (yarn install --frozen-lockfile → turbo verify; POSIX e2e per package; check:deps; sync-versions --check; audit; pack).
.github/workflows/docs.yml  Docs website delivery to GitHub Pages (separate from ci.yml): production deploy on master → gh-pages root (peaceiris) + live per-PR previews under /pr-preview/pr-N/ (rossjrw, dynamic DOCS_BASE), shared docs-gh-pages concurrency, GITHUB_TOKEN only. One-time prereq: enable Pages → Deploy from branch gh-pages/root.
packages/*/dist/  Build output (gitignored). Generated by each package's tsc.
codeops/         CodeOps nested layout (marker .codeops.yml: codeopsLayout=nested, rdIdScope=per-feature). 00-roadmap.md = portfolio roadmap; features/<slug>/ holds one feature-set's 00-roadmap.md + requirements/ + plans/<plan>/. Active feature: features/jsvision-ui/ (requirements RD-* + plans layout-engine, reactive-core, tui-ui, view-group-spine, event-loop, app-shell, tv-behavioral-fidelity, essential-controls, containers-scrolling-lists, runtime-hardening). **RD-13 runtime-hardening is DONE** (10 phases / 120 tasks — the five-agent audit backlog of 3 critical + 12 major + ~25 minor defects, spec-first per HR-NN with TV GATE decodes; additive-only surface: core `KEY_NAMES`/`PasteState` exports + `CapabilityResolution.passthrough`, ui `EventLoop.onResize`/`EventLoopOptions.quitCommand` seams + public `ScrollBar.pageStep()`, and the `JSVISION_*` env renames). Archived under codeops/_archive/: foundation/ (the completed RD-01…RD-10 feature-set, incl. its requirements/) and monorepo-restructure/ (the completed repo-monorepo conversion). Source-relative links from a plan reach the repo root via ../../../../../.
_archive/        Archived Ink/React prototype — reference/inspiration only, not built (distinct from codeops/_archive/).
terminal-matrix.json       RD-03 cross-terminal evidence (appended by the probe / gate); untracked + gitignored dev-box artifact.
````

**Test files:** under each package's `test/`. Naming: `*.spec.test.ts` (specification — immutable oracle from requirements/AC), `*.impl.test.ts` (implementation/edge cases), `*.e2e.test.ts` (end-to-end, vitest `e2e` project). core tests import source via `../src/engine/...`; examples tests import the engine via `@jsvision/core`.

<!-- analyze_project: refreshed Overview, Toolchain, Commands, Project structure for the yarn+turbo+vitest monorepo (2026-06-28) -->
<!-- analyze_project: refreshed Project structure + plan-path references for the nested codeops/ layout migration; synced .prettierignore note (2026-06-29) -->

## Conventions

- **ESM-only**, zero runtime dependencies (pure-JS only; the `check:deps` guard enforces no native deps).
- **Foundation-first layering** with a single public entry point (`src/engine/index.ts`); every later RD adds modules under `src/engine/**` and re-exports public symbols there. Target 200–500 lines per file.
- **NodeNext import specifiers** use the `.js` extension even for `.ts` sources (e.g. `from './version.js'`).
- **Tests in `test/` only** — do not colocate tests with source.
- Public/exported symbols carry JSDoc (purpose, params, returns, side effects).
- **Grounded Options & Recommendations** — follow the always-on directive in the coding standards: filter out non-viable options (no strawmen), second-guess each, ground any code-modifying option in the real code, and lead with a recommendation and its reason; match ceremony to stakes.

## Git conventions

- **Commit scope:** the area touched — `scaffold`, `package`, `toolchain`, `packaging`, `tests`, `docs`, or the engine subsystem name in later RDs.
- **Main branch:** `master` • **Remote:** `origin` → `git@github.com:blendsdk/jsvision.git`. Publish (with provenance) is still deferred to a later milestone.

## Documentation for users & AI agents (NON-NEGOTIABLE)

> Every JSDoc comment on a public or exported symbol is written for the people and agents that
> **use** this framework — never for its maintainers. The doc a consumer (or an AI agent) reads on
> hover **is** the API contract. **Decode-the-code archaeology, plan/requirement IDs, and internal
> process notes never appear in shipped code.** Canonical worklist + guard: `JSDOC-CLEANUP-PLAN.md`.

- **Audience.** Public JSDoc explains, to someone who has never seen the source, _what a symbol is,
  what it does, how to use it, and what to watch out for_. AI agents feed directly on these docs and
  their `@example`s — treat every example as executable spec: realistic, correct, copy-pasteable.

- **BANNED in all shipped code (`packages/*/src`), in JSDoc _and_ code comments:**
  - CodeOps process IDs — `RD-`, `PA-`, `AR-`, `PF-`, `HR-`, `GATE-`, `AC-`, `ST-`, `ADR-`, `DEF-`,
    `FR-`, `RT-`, `PL-`, and any `codeops/…`, `plans/…`, `requirements/…` path. These reference files
    that are being removed; a reference to a deleted file is worse than no reference.
  - Turbo Vision / C++ provenance — `t*.cpp`/`*.h` citations, `getColor(N)` palette-chain
    archaeology, "faithful to `TButton::draw`" notes. Fidelity is a **build-time** discipline (see
    the TV porting guideline below), not a shipped-code artifact; the SDK will diverge from TV over
    time and these notes will only mislead.
  - This is a **semantic rewrite, not a delete**: keep the _behavior_ an ID annotated ("enabled by
    default"), drop the _code_ ("(PA-3)").

- **REQUIRED on every public / exported symbol** (anything re-exported from a package `index.ts`):
  1. A lead sentence stating what it is / does, in plain language.
  2. The behaviors, constraints, and gotchas a caller must know (ordering rules, reactive-vs-
     imperative seams, footguns such as a missing `measure()` collapsing a view to `{0,0}`).
  3. `@param` / `@returns` for every parameter and return value.
  4. An **`@example`** with real, copy-pasteable usage. Examples are for AI consumption first — make
     them correct enough to paste and run.

- **Code comments — comment _why_, not _what_, and proactively explain anything above a junior
  developer's level.** Non-obvious algorithms, invariants, subtle ordering/lifecycle dependencies,
  and "why it is done this way" decisions get a short comment so a junior can follow the code.
  Trivial lines get nothing. No maintainer traceability — a note useful only to a maintainer belongs
  in the commit message, not the code.

- **Enforcement.** `scripts/check-jsdoc.mjs` (wired into `yarn verify` + CI) fails on any banned
  reference and on any public export missing an `@example`. Guard-first: it is the objective
  done-criterion for the cleanup and the regression gate afterward. The build is not green while it
  fails.

- **Scope.** Shipped source only (`packages/*/src`). Out of scope: this `CLAUDE.md`, `codeops/`, and
  test files (their own conventions). Example/demo code (`packages/examples/`) follows the spirit —
  it is already user-facing and is itself agent-training material.

- **Plan-flow enforcement (make_plan / exec_plan).** A component is NOT `[x]` done until its public
  JSDoc carries an `@example`, its above-junior logic is commented, and `check-jsdoc.mjs` passes for
  its files.

## Turbo Vision fidelity (porting guideline)

> When you port a component that **already exists** in Borland Turbo Vision, decode the original
> first and match its geometry, glyphs, sizing, layout, hit-zones, and colors — don't reimagine a
> shape TV already defined. This is a **build-time discipline for faithful ports only**. It is _not_
> a shipped-code contract: per the documentation directive above, **no TV/C++ provenance is recorded
> in JSDoc or code comments.** New components, and any deliberate divergence from TV, carry no
> fidelity obligation.

- **Source of truth (for the decode, at porting time):** the original Turbo Vision (magiblot/tvision)
  checked out at `/home/gevik/workdir/github/tvision` — `source/tvision/t*.cpp` (drawing/sizing),
  `source/tvision/tvtext1.cpp` (the `frameChars`/glyph tables + `cpAppColor`), `include/tvision/*.h`
  - `include/tvision/app.h`/`dialogs.h` (geometry + the `cpX` palette definitions).

- **DECODE BEFORE (when porting an existing TV component).** Open the original class (e.g.
  `TMenuBox`, `TFrame`, `TButton`, `TInputLine`, `TScrollBar`, `TWindow`, `TDialog`) and decode:
  1. Its `draw()` + sizing (`getRect`/`sizeLimits`/`getItemRect`) — the exact column math, frame/gutter
     insets, padding, fill characters, `markers`/`shadows`/`specialChars`, and hit-zones.
  2. **Every color via the full `getColor(N)` palette chain** — resolve `N` through the view's local
     palette (`cpButton`/`cpInputLine`/…) → owner palette (`cpGrayDialog`/…) → `cpAppColor` → the
     attribute byte `0xHL` (high nibble = bg, low nibble = fg). Color indirection — not glyphs — is
     where fidelity silently breaks (a `TButton` shadow shipped wrong once because `getColor(8)` was
     guessed as "darkGray/black" instead of decoded to `0x70` black-on-lightGray).
  3. Watch for **mode-gated features** the color path enables/disables — e.g. `showMarkers` (the
     `[ ]` brackets) is monochrome-only; on a color palette a `TButton` has **no** brackets.
     Convert CP437 byte glyphs to Unicode (mind East-Asian ambiguous width — prefer unambiguous-narrow
     code points).

- **DIFF AFTER.** Re-open the same `.cpp` and **diff the rendered output against the decode**, cell by
  cell: glyphs, column math, hit-zones, and every resolved color. If they disagree, the code is wrong
  — fix it against the source. **The decode is a working step, not shipped-code content** — it is not
  written into JSDoc or code comments (per the documentation directive above). If an audit trail is
  wanted, put the decode in the commit message.

- **The C++ source outranks a spec test (TV-derived components only).** A `*.spec.test.ts` can encode
  a _mis-decode_ (a button oracle once asserted phantom `[ ]` brackets). So for a TV-derived
  component, if a spec oracle disagrees with a faithful C++ decode, **the spec test is the defect**:
  fix it against the source (a deliberate, narrow exception to "spec tests are immutable", scoped to
  fidelity oracles).

- **No invention for ports.** If the original is unclear or a detail isn't covered, surface it and
  ask — never substitute your own design for theirs. This governs **drawing/geometry/color** for
  components TV actually had; behavior the original couldn't have (truecolor, reactive binding, async
  modality) freely extends TV, and entirely new components have no TV counterpart to match.

## Kitchen-sink showcase (NON-NEGOTIABLE)

> Every user-facing component and capability MUST ship with a live demo ("story") in the
> **kitchen-sink showcase** (`packages/examples/kitchen-sink/`, run `yarn workspace @jsvision/examples
demo:kitchen`) — a Storybook-for-TUI that is our live selling point. **A component is not "done"
> until its story exists and passes the headless smoke test.** As we implement, the showcase grows
> with us. Canonical gate + plan checklist: [`codeops/kitchen-sink-gate.md`](codeops/kitchen-sink-gate.md).

- **The story contract (extensibility).** Adding a component to the showcase = add ONE
  `kitchen-sink/stories/<x>.story.ts` exporting a `Story` (`{ id, category, title, blurb, rd?,
build(ctx) }`) + one line in `stories/index.ts`. `build(ctx)` returns a `Group` of
  absolutely-positioned children within `ctx.width × ctx.height`; the shell owns all chrome
  (menu / status / grey canvas / navigation). A story never touches the desktop or host.
- **UX is a feature — it is the selling point.** Each story gets a one-line blurb, the live
  component, a visible bound-state echo where relevant, and interaction hints. Keep it polished: no
  clipped text, faithful TV colors, and both keyboard + mouse working.
- **Smoke test is mandatory.** Every story must pass `test/kitchen-sink.smoke.spec.test.ts` (mounts
  headlessly, paints something, unique id, required metadata) — the mechanical "the story exists and
  renders" check, no TTY needed.
- **Dogfooding / Navigator seam.** Navigation is menu-driven today (existing TV primitives, zero
  throwaway); when RD-11 lands `ListView`/`ScrollBar` the navigator upgrades to a persistent sidebar
  built from the very component it demos. Only `kitchen-sink/shell.ts` changes; stories stay untouched.
- **Plan-flow enforcement (make_plan / exec_plan).** For every user-facing component:
  - **make_plan** MUST add a story task to `99-execution-plan.md`: `[ ] Kitchen-sink story for <X>
(+ smoke test)`.
  - **exec_plan** MUST NOT mark the component `[x]` until its story is registered and the smoke test
    passes.
- **Scope.** Every _visual_ component MUST get a story; non-visual capabilities (reactivity,
  capability detection, color downsampling) get one when it is meaningful to show.

## Special rules

- **Spec-first task ordering** (CodeOps): spec tests → red → implement → green → impl tests → verify. A `*.spec.test.ts` is an immutable oracle — if it fails after implementation, fix the code, not the test.
- `npm audit` is **not** wired into `verify` (it runs in CI and is locally runnable) so audit advisories don't block local typecheck/test loops.
- Prettier is scoped to code: `codeops/` is in `.prettierignore` (CodeOps process docs, not code).

