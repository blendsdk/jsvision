import{_ as s,o as i,c as t,a4 as a}from"./chunks/framework.BlZvSPPP.js";const k=JSON.parse('{"title":"@jsvision/docs-site","description":"","frontmatter":{},"headers":[],"relativePath":"README.md","filePath":"README.md"}'),o={name:"README.md"};function n(l,e,r,d,p,h){return i(),t("div",null,[...e[0]||(e[0]=[a(`<h1 id="jsvision-docs-site" tabindex="-1">@jsvision/docs-site <a class="header-anchor" href="#jsvision-docs-site" aria-label="Permalink to &quot;@jsvision/docs-site&quot;">​</a></h1><p>The JSVision documentation &amp; showcase website, built with <a href="https://vitepress.dev" target="_blank" rel="noreferrer">VitePress</a>. This package is <strong>private and dev-only</strong> — it is never published and never enters any shipped package&#39;s dependency graph.</p><h2 id="local-development" tabindex="-1">Local development <a class="header-anchor" href="#local-development" aria-label="Permalink to &quot;Local development&quot;">​</a></h2><p>From the monorepo root:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">yarn</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docs:dev</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     # start the dev server (hot reload)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">yarn</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docs:build</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # production build → packages/docs-site/.vitepress/dist</span></span></code></pre></div><p>Or from this package:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">yarn</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          # vitepress dev</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">yarn</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> vp:build</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     # vitepress build  (deliberately NOT named \`build\`)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">yarn</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> preview</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      # serve the built site locally</span></span></code></pre></div><blockquote><p>The build script is named <code>vp:build</code>, not <code>build</code>, so <code>turbo run build</code> (and therefore <code>yarn verify</code>) skips this package entirely — the docs site is isolated from the shipped-package pipeline. It builds only via <code>yarn docs:build</code> and the docs CI workflow.</p></blockquote><p>After a build, validate the output:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">node</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> packages/docs-site/scripts/check-docs-build.mjs</span></span></code></pre></div><h2 id="deployment" tabindex="-1">Deployment <a class="header-anchor" href="#deployment" aria-label="Permalink to &quot;Deployment&quot;">​</a></h2><p>Deployment is automated by <code>.github/workflows/docs.yml</code> and serves from the <strong><code>gh-pages</code> branch</strong>:</p><ul><li><strong>Production</strong> — every push to <code>master</code> that touches the site publishes to the branch root, live at <code>https://blendsdk.github.io/jsvision/</code>.</li><li><strong>PR previews</strong> — every pull request publishes a live preview to <code>/pr-preview/pr-&lt;N&gt;/</code> and comments the URL; the preview is removed when the PR closes.</li></ul><p>No stored secret is needed — the workflow uses the ephemeral <code>GITHUB_TOKEN</code>.</p><h3 id="one-time-setup-required-before-the-first-deploy" tabindex="-1">One-time setup (required before the first deploy) <a class="header-anchor" href="#one-time-setup-required-before-the-first-deploy" aria-label="Permalink to &quot;One-time setup (required before the first deploy)&quot;">​</a></h3><p>In the GitHub repository settings:</p><ol><li><strong>Settings → Pages → Build and deployment → Source</strong> = <strong>Deploy from a branch</strong>.</li><li>Branch = <strong><code>gh-pages</code></strong>, folder = <strong><code>/ (root)</code></strong>. Save.</li><li>Enable <strong>Enforce HTTPS</strong>.</li></ol><p>The <code>gh-pages</code> branch is created automatically by the first successful <code>master</code> deploy. Until Pages is enabled as above, the workflow still runs and pushes the branch, but the site will not be served.</p><blockquote><p>Fork PRs get a read-only <code>GITHUB_TOKEN</code>, so preview deploy/comment is skipped for forks — internal branches get previews. This is acceptable for this repository&#39;s flow.</p></blockquote>`,19)])])}const u=s(o,[["render",n]]);export{k as __pageData,u as default};
