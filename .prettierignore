dist
node_modules
_archive
coverage
.turbo

# VitePress generated output + cache (docs-site)
**/.vitepress/dist
**/.vitepress/cache

# Generated TypeDoc API reference (docs-site) — regenerated every build, gitignored.
# The hand-written api/index.md preface stays formatted; only the generated trees +
# merged sidebar are exempt.
packages/docs-site/api/*/
packages/docs-site/api/typedoc-sidebar.json

# Generated plugin API reference — emitted verbatim by scripts/gen-plugin-api.mjs from the
# @jsvision/* source types and drift-guarded byte-for-byte, so Prettier must not reformat it.
tools/claude-plugin/skills/jsvision/references/api/

# CodeOps process artifacts — authored docs, not code; Prettier would churn
# their hand-built tables. Kept out of the formatting gate.
codeops
**/CHANGELOG.md
**/RELEASE_NOTES.md
# Generated release notes + per-package changelogs — drafted by @blendsdk/lockstep
# from conventional commits and committed by the release workflow, not hand-formatted.
# The root CHANGELOG.md is hand-maintained and stays gated.
packages/*/CHANGELOG.md
RELEASE_NOTES.md
# The @example compile guard's grandfather list — machine-generated, sorted, and
# deliberately one entry per line so a removed entry reads as a single deleted
# line. Prettier would expand each value across five lines and bury the ratchet.
packages/docs-site/test/jsdoc-examples.allowlist.json
