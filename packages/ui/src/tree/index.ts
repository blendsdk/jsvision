/**
 * Public entry for the tree/outline widget — the `Tree<T>` container plus its `TreeNode<T>` and
 * `TreeOptions<T>` types. The `TreeRows` renderer and the pure graph builder are internal
 * implementation details and are not exported here.
 */
export { Tree } from './tree.js';
export type { TreeNode, TreeOptions, MarkerStyle } from './tree.js';
