=== Probe 2: CRUD / transactions / concurrency / errors ===

1. paged SELECT (offset, limit 2): 1:Ada Lovelace, 2:Alan Turing
   keyset SELECT (id > 2, limit 2): 3:Grace Hopper, 4:Edsger Dijkstra

2. INSERT/UPDATE/DELETE by PK in a transaction:
   INSERT RETURNING → id=6 xmin=766
   UPDATE WHERE id=6 → balance=42.00

3. transaction rollback (multi-row edit reverted):
   before: balance=42.00
   txn threw: simulated failure → rollback
   after rollback: balance=42.00 credit_limit=500.00 (reverted ✓)

4. optimistic concurrency (xmin), two connections:
   A reads id=6, xmin=767
   B commits a change → new xmin=769
   A optimistic update with STALE xmin → CONFLICT DETECTED ✓ (0 rows)
   A re-reads fresh xmin then retries → wrote ✓

5. composite-PK (app.order_item), key = (order_id, line_no):
   UPDATE qty 2→7 keyed on composite PK → ok ✓
   (rolled back — seed pristine)

6. no-PK table (app.tag) — row identity:
   full-row-match UPDATE is ambiguous when duplicate rows exist (dup rows now: 0).
   ctid gives a physical row id ((0,1) → vip) but it CHANGES on UPDATE/VACUUM.
   DECISION: a no-PK table is READ-ONLY in the editor (or require the user to add a PK). ✓

7. constraint errors mapped to structured messages:
   CHECK (credit_limit>=0): [check] code=23514 → "Value violates rule "customer_credit_limit_check"."
   NOT NULL (name): [not-null] code=23502 → ""name" is required."
   CHECK (order_item.qty>0): [check] code=23514 → "Value violates rule "order_item_qty_check"."
   FK (order.customer_id): [foreign-key] code=23503 → "Referenced record does not exist (order_customer_id_fkey)."
   bad numeric input: [bad-input] code=22P02 → "Invalid value: invalid input syntax for type numeric: "not-a-number"."

cleanup: deleted scratch customer id=6 (1 row) — seed pristine.

Probe 2 verdict: 🟢 parameterized CRUD, txn rollback, xmin conflict detection, composite-PK all work;
   no-PK → read-only; constraint errors map to structured, user-presentable messages.
