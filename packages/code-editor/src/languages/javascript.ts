import type { CodeEditorLanguageId } from '../index.js';

/**
 * Identifies the built-in JavaScript language adapter.
 *
 * @example
 * ```ts
 * import { javascriptLanguageId } from '@jsvision/code-editor/languages/javascript';
 * ```
 */
export const javascriptLanguageId: CodeEditorLanguageId = 'javascript';
