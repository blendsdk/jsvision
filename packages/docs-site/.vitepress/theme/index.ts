import DefaultTheme from 'vitepress/theme';
import './custom.css';

// The JSVision docs theme is the VitePress default theme with brand tokens
// layered on top via custom.css. No bespoke layout components yet — the hero
// slots and live-demo components arrive in a later milestone.
export default DefaultTheme;
