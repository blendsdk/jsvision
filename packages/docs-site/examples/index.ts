/**
 * The live-example registry: one entry per runnable example module under
 * `examples/<category>/<name>.ts`. Hand-authored — the source of truth for each
 * example's id, category, chrome mode, and source path.
 *
 * Placeholder for now (empty). The real `ExampleEntry` shape and the seed
 * examples are filled in once the example contract lands.
 */
export const EXAMPLES: readonly never[] = [];
