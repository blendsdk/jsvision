# Generated TypeDoc API reference — regenerated by `yarn docs:api` before every
# build, so it is never committed. The hand-written api/index.md preface stays tracked.
api/*/
api/typedoc-sidebar.json
