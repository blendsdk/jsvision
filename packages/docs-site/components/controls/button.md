---
title: Button
description: A push button bound to a click counter — a live, in-browser JSVision example.
---

# Button

A push button bound to a click counter, with a live count echo. Focus it with **Tab**, then press
**Space** or **Enter** (or **Alt-C**) to activate it — the count beside the button updates reactively.

<PlayExample id="controls/button" title="Button" blurb="A push button bound to a click counter, with a live count echo." />

## Source

The composition running above — the exact `build()` from the module:

<<< @/examples/controls/button.ts#example{ts}

::: details Full module (imports, JSDoc, data)

<<< @/examples/controls/button.ts

:::
