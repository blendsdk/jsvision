/**
 * `@jsvision/files` — the Turbo Vision file-system dialog family on `@jsvision/ui`.
 *
 * Public API (explicit named re-exports; filled in as each subsystem lands):
 *   • the `FileSystem` seam + `node:fs` default + pure cores (`fs/`);
 *   • the listing trio `FileList`/`FileInput`/`FileInfoPane`;
 *   • `DirList`;
 *   • the `FileDialog`/`ChDirDialog` modals;
 *   • the convenience openers `openFile`/`changeDir`.
 *
 * `.js` specifiers per NodeNext ESM resolution.
 */
export {};
