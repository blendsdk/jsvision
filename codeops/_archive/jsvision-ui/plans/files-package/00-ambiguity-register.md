# Ambiguity Register — Files package (`@jsvision/files`) plan

> **Plan**: files-package · **Implements**: jsvision-ui/RD-09
> **Gate status**: ✅ **GATE PASSED** (2026-07-05)
> **CodeOps Skills Version**: 3.3.0

This register is the Zero-Ambiguity Gate audit trail for the **plan** (PA-*). Requirement-level
decisions were resolved in the RD's own register (AR-235…AR-247) and its passed preflight
(`../../requirements/00-preflight-report-RD-09.md`, PF-001…PF-005). The plan does **not** re-litigate
those; it records only the **new** decisions that surfaced turning the RD into an execution plan, plus
the source/directive-determined choices recorded for traceability.

## Gate rule

Every plan decision with semantic weight (scope, package shape, API surface, error path, fidelity
strategy) is below with an explicit resolution. User-facing choices (PA-1/PA-2/PA-3) were decided by
the user on 2026-07-05; the rest are single-dominant or source/directive-determined and recorded for
traceability.

## Register

| # | Category | Ambiguity | Resolution | Status | Source |
|---|----------|-----------|------------|--------|--------|
| PA-1 | Scope | Which of the four RD Should-Haves land in this plan (v1)? | **All four**: convenience openers, save-mode buttons (OK/Replace/Clear), History dropdown, and the caller `filter` hook — plus all six Must-Have components + the FileSystem seam. | ✅ Resolved | **User choice** 2026-07-05 |
| PA-2 | API surface | The `FileSystem` seam interface shape (AR-235 "pinned at plan time"). | **Full synchronous surface**: `readDir(path)→DirEntry[]`, `stat`, `lstat`, `resolve`, `isAbsolute`, `join`, `dirname`, `basename`, `sep`, `homedir()`, `roots()`. Everything the six components + cross-platform paths need, fully swappable per-test. | ✅ Resolved | **User choice** 2026-07-05 |
| PA-3 | Error path | How to provide the `valid()` error box (`TMsgBox`/`messageBox` is not shipped by `@jsvision/ui` and is unslotted in the component map). | **Local error dialog inside `@jsvision/files`** — a small modal gray `Dialog` (centered `Label` + OK `Button`, via `execView`). **No new `@jsvision/ui` primitive.** A faithful shared `messageBox` stays a separate future RD. Satisfies AC-9/AC-10 ("raises a message box, keeps the dialog open"). | ✅ Resolved | **User choice** 2026-07-05 |
| PA-4 | File structure | The `src/` file split for the new package. | **Dir-per-concern** (house pattern): `src/fs/` (`types.ts` seam+`DirEntry`/`FileStat`, `node-fs.ts` default, `wildcard.ts`, `scan.ts` incl. the sort comparator, `tree.ts`, `index.ts`), `src/list/` (`file-list.ts`, `file-info-pane.ts`, `dir-list.ts`), `src/input/` (`file-input.ts`), `src/dialog/` (`file-dialog.ts`, `chdir-dialog.ts`, `error-dialog.ts`), `openers.ts`, barrel `index.ts`. Every file ≤ 500 lines (split `file-dialog.ts` geometry helper out if it grows). | ✅ Resolved | House pattern (AR-246); confirmed |
| PA-5 | Packaging | How `@jsvision/files` is excluded from the public lockstep-version sync (AR-242 "pinned at plan time"). | **Automatic** — `scripts/sync-versions.mjs:71` already skips `pkg.private === true`; setting `"private": true` in the new `package.json` excludes it with **no skip-list edit**. Verified in code + the RD preflight. | ✅ Resolved | Code-verified (`sync-versions.mjs:71`) |
| PA-6 | Fidelity / theme | The 0-or-1 additive `fileInfo` theme role (AR-247 "pinned at GATE-1"). | **Decode-at-GATE-1 branch** (the RD-21 `colorMarker` pattern): resolve `cpInfoPane`'s `getColor(1)=0x1E` through the gray-dialog → `cpAppColor` chain in Phase 1. If it resolves to an attribute **already** covered by a shipped role, reuse it (0 new roles); if **distinct**, add exactly one additive `fileInfo` role (byte-frozen, guard allowlists extended). Additive + non-breaking either way. | ✅ Resolved | Source-determined (RD-21 pattern) |
| PA-7 | Verify | The plan's Verify command. | **`yarn verify`** (= `turbo run typecheck build test`), the project standard from CLAUDE.md; `yarn check:deps` + `yarn test:e2e` at packaging. | ✅ Resolved | CLAUDE.md; confirmed |
| PA-8 | API surface | The convenience-opener shape + the modal host handle. | `openFile(opts) → Promise<string \| null>` and `changeDir(opts) → Promise<string \| null>` (a `Promise` over `execView`; `null` = cancel sentinel); `opts` carries `{ wildcard?, directory?, fs?, host }`. **`host` is an `execView`-capable app handle** — the `createApplication` result / `EventLoop` (or a `{ execView(view): Promise<unknown> }` seam mirroring the kitchen-sink `StoryContext.execView`), **not** `ModalHost` (which exposes only `endModal`/`isCommandEnabled`, `event/types.ts:21-26`, and cannot open a modal). Because `execView` does **not** mount (`event-loop.ts:193` — "caller has added `view` to the tree"), each opener runs the shipped lifecycle **add-to-desktop → `execView` → remove-in-`finally`** (`kitchen-sink/shell.ts:195-205`). Thin wrappers over the dialogs. | ✅ Resolved | Single-dominant (RD Should-Have) + preflight PF-002 |
| PA-9 | Composition | How the History dropdown attaches. | Construct `new History({ link, historyId })` (the jsvision RD-14 API — `HistoryOptions`, **no rect/coords ctor**; its popup anchors to the linked `Input`) and **position it via absolute layout** at the decoded rects — FileDialog `(31,3,34,4)`, ChDirDialog `(42,3,45,4)`; a per-dialog recent-paths id. Uses `History`'s own internal popup — **no `dropdown/` edit**. | ✅ Resolved | Single-dominant (RD Should-Have); ctor corrected per preflight PF-004 |
| PA-10 | API surface | The caller `filter` hook semantics. | An optional `filter(entry: DirEntry) => boolean` on `FileList`, **AND-ed** with the faithful wildcard (never replaces it) — a pure post-scan predicate. Off by default. | ✅ Resolved | Single-dominant (RD Should-Have) |
| PA-11 | Testing | Where the in-memory `FileSystem` lives. | A **test/story helper** (`test/helpers/memory-fs.ts` + a kitchen-sink fixture), **not** a shipped export — the package ships only the seam interface + the `node:fs` default. Keeps the story/smoke test headless + deterministic (AR-244). | ✅ Resolved | Source-determined (AR-235/244) |
| PA-12 | Showcase / demo | The kitchen-sink + demo scope. | Two stories (category **`Files`**): `files/file-dialog` + `files/chdir-dialog`, both over the in-memory fs; the leaf components (FileList/DirList/FileInput/FileInfoPane) are shown *within* them. Headless `demo:files` (ASCII frame per step). Matches AC-17 + the kitchen-sink gate. | ✅ Resolved | Source-determined (AR-244/AC-17) |
| PA-13 | Fidelity | GATE-1/GATE-2 obligation. | **Mandatory for all six TV-derived draws** (`TFileDialog`/`TFileList`/`TFileInputLine`/`TFileInfoPane`/`TDirListBox`/`TChDirDialog`): BEFORE-decode recorded in the `03-*` docs (Phase 1), AFTER-diff cell-by-cell vs the `.cpp` (Phase 7). The seam/wildcard/cross-platform/sanitize/reactive/openers extensions get spec oracles but no diff. | ✅ Resolved | NON-NEGOTIABLE directive |
| PA-14 | Fidelity / cross-package | `TFileList` is **2-column** with a scrollbar handed in by the dialog (`TSortedListBox(bounds, 2, sb)`, `tfillist.cpp:64`); jsvision's shipped `ListRows`/`ListView` renders **1 column** and **hard-owns a vertical right-edge `ScrollBar`** (`list-view.ts:75`, no injection/orientation seam). Where do the 2-column layout **and** the decoded bottom scrollbar live? | **Generalize `@jsvision/ui` `ListRows`/`ListView` toward `TListViewer` — two additive, non-breaking seams:** (1) **`numCols`** (default `1`) — faithful column-major flow + `│` divider (`getColor(5)`), decoded cell-by-cell against `tlstview.cpp:draw` (`item = j*size.y + i + topItem`, colWidth = `size.x/numCols + 1`, divider at `curCol+colWidth-1`); (2) an **injectable/orientable `ScrollBar` seam** (default unchanged: ListView creates + owns + lays out a vertical right-edge bar in `[rows fr\|bar 1]`) so `FileDialog` hands `FileList` the decoded **horizontal-rendered bottom** bar at `(3,14,34,15)` exactly as TV's dialog hands `sb` to `TFileList`. **Ownership (source-faithful):** the override is **bind-to-external-bar** — the `FileDialog` **owns + places** the bar as an *absolute sibling* at that rect (as TV's parent group owns the scrollbar), and `FileList` merely **binds** to it (shares `focused`, drives `setRange`); ListView does **not** create/lay out the bar in that mode (its own bar lives at the list's right edge, but FileList's is a Dialog-placed sibling at the bottom). **Scroll-model note (source-corrected):** the passed bar is `TListViewer`'s **vScrollBar** (`tlistbox.cpp:30` — `TListViewer(bounds, numCols, 0, sb)`) driving `topItem`/`focused` — the scroll model stays **vertical** (identical to today's ListView); `numCols` only changes the *draw layout* + the bar's *step* (`pgStep=size.y*numCols`, `arStep=size.y`, `tlstview.cpp:ctor`). **No horizontal-paging model.** This fulfils the `numCols` work RD-11 reserved for "→ RD-07 (AR-104)" (`containers-scrolling-lists/01-requirements.md:29`); the "single column only" JSDoc in `list-view.ts`/`list-rows.ts` is updated when the seams land (Phase 1). **Regression guard:** RD-11 `listview`/`listbox`/`scroller`/`fidelity`/`containers.packaging` suites stay green (both seams default to today's behaviour). | ✅ Resolved | **User choice** 2026-07-05 (bottom-bar path confirmed at preflight) |

## Runtime decisions (exec_plan)

| # | Category | Ambiguity | Resolution | Status | Source |
|---|----------|-----------|------------|--------|--------|
| PA-6 (runtime) | Fidelity / theme | GATE-1 (Phase 1) resolved `TFileInfoPane::getColor(1)` fully through the chain: `cpInfoPane[1]=0x1E` → gray-dialog `cpGrayDialog[30]=0x3D` → desktop passthrough (empty palette) → `cpAppColor[0x3D=61]=0x13`. Final = **`0x13` cyan-on-blue** (TV's blue info strip under the gray dialog). That byte is *literally* present in the shipped theme, but only via `scrollBarPage`/`scrollBarControls`/`progressTrack` — all domain-alien to a file-info pane; the gray-dialog body roles (`staticText`/`label`) are `0x70`, not `0x13`. Is `0x13` "covered" (reuse) or "distinct" (add `fileInfo`)? | **Add the single additive `fileInfo` role = `0x13` cyan-on-blue** (byte-frozen, guard allowlists extended), mirroring the RD-21 `colorMarker` precedent (added a semantic role though `0x70` pre-existed). Reading PA-6's "distinct" as *no semantically-appropriate existing role* — true here. Self-documenting + decouples the info pane from scrollbar/progress roles. Additive/non-breaking; no existing role/byte/export changed. | ✅ Resolved | **User choice** 2026-07-05 (exec_plan Phase 1, GATE-1) |

| PA-2 (runtime) | API surface / behaviour | `DirEntry.kind` is `'file'\|'dir'\|'symlink'` (03-01), but a symlink's target type isn't in `kind`, so `compareEntries` (pure over `DirEntry`) can't sort a symlink→dir with dirs. How do symlinks scan/sort, and how is AC-13's "symlink→dir enterable" met? | **Scan treats a symlink as file-like** (kind `'symlink'` is the display tag): it sorts in the file group (`compareEntries` = dir iff `kind==='dir'`, keeping it pure), the wildcard applies to it, no trailing separator. `readDir` sets target `size`/`mtime` + `broken` (guarded `stat`); a **broken/cyclic** link is shown, `broken:true`, never followed. AC-13's "symlink→dir **enterable**" is met at **open-time** (the dialog re-`stat`s the entry on Enter; a link→dir is entered, a link→file resolved) — not at scan-sort time. Simplest reading faithful to the 3-value type; `..` is never filtered. | ✅ Resolved | exec_plan Phase 2 (reading of AC-13 + 03-01 type) |

| PA-3 (runtime) | Error path / API | PA-3's `valid()` error box is a **nested modal via `execView`**, but `Dialog.valid(command)` is **synchronous** and the only injected seam is `ModalHost` (`endModal`/`isCommandEnabled`, no `execView`); `DispatchEvent` exposes `popupHost` but not `execView`. How does the sync `valid()` raise the async error modal? | **`FileDialog`/`ChDirDialog` expose a `showError(message)` seam** the sync `valid()` state machine calls (then returns `false` = stay open). The PA-3 `errorBox(host, message)` component (a thin `execView` gray `Dialog` + `Label` + `okButton`) is **wired into `showError` by whoever opens the dialog** — the openers (`errorBox(host, …)`, PA-8) and the kitchen-sink story (the app's `execView`). A standalone/test-constructed dialog gets a spy/no-op. Keeps `valid()` sync + unit-testable and preserves PA-3's `execView` nested modality in production. | ✅ Resolved | exec_plan Phase 4 (refines PA-3 given the sync `valid()` + seam surface) |

## Resolution note

All 14 items resolved; zero deferred. PA-1/PA-2/PA-3/PA-14 are explicit user choices (2026-07-05).
PA-4…PA-13 are single-dominant or source/directive-determined, recorded for traceability. The RD's two
tracked deferrals (DEF-32 async reads, DEF-33 extended glob) remain deferred and are **not** in this
plan's scope. **Gate PASSED** — plan documents may be written.

> **Surface-during-authoring note:** PA-14 was discovered while authoring `02`/`03-02` (the shipped
> `ListRows` is single-column; `TFileList` is 2-column). Per the gate's surface-during-authoring rule,
> authoring paused, the item was registered, and the user decided (generalize `ui`) before the FileList
> spec was written.
