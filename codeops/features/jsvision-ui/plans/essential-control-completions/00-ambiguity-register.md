# Ambiguity Register — essential-control-completions (RD-07)

> **✅ GATE PASSED** — every item Resolved with an explicit decision; zero deferred.
> **Plan**: essential-control-completions · **Implements**: jsvision-ui/RD-07
> **CodeOps Skills Version**: 3.1.0 · **Last Updated**: 2026-07-01

Requirement-level ambiguities were settled in the RD-07 `add_requirement` interview
(`../../requirements/00-ambiguity-register.md`, **AR-115…AR-124**) and its preflight
(`../../requirements/00-preflight-report-RD-07.md`). This register holds the **plan-level**
decisions (`PA-*`): the four genuine choices the GATE-1 TV decodes surfaced (PA-1…PA-4, user), plus
the dominant/source-determined design points recorded for traceability (PA-5…PA-13). Every plan
decision back-references a `PA-*` (or an RD `AR-*`).

| PA # | Area | Question | Options | Decision | Status |
|------|------|----------|---------|----------|--------|
| **PA-1** | Input · caret/selection **unit** | TV steps by grapheme cluster (`TText::next/prev`), but the RD-06 `Input` tracks `curPos` as a JS string index + wcwidth (not grapheme-aware). How faithful? | (a) **code-point v1** — step by JS code point, matching the current `Input`; full grapheme → new `DEF`; (b) full grapheme-cluster — retrofit `curPos`/`measure`, reopens the RD-06 base + needs a segmenter | **(a) code-point v1**; grapheme fidelity deferred (**DEF-21**) | ✅ Resolved (user) |
| **PA-2** | `picture` · mask **DoS bound** | `TPXPictureValidator` has no iteration cap (`*999999999#` ≈ 1e9 loops). How to bound (security standard)? | (a) **reject at `syntaxCheck`** — clamp `*N` to `MAX_REPEAT=1024`, enforce a global step budget (∝ mask×input len) with a hard ceiling; a statically-over-cap mask fails `syntaxCheck`→invalid (allowlist); (b) clamp silently — never reject | **(a) reject at `syntaxCheck` + bounded step budget** | ✅ Resolved (user) |
| **PA-3** | `picture` · **autoFill default** | TS `picture(mask, autoFill?)` — TV's ctor has no default. Our optional default? | (a) **ON** — `picture(mask)` auto-fills trailing literals; opt out `picture(mask,false)`; (b) OFF — explicit opt-in | **(a) default ON** | ✅ Resolved (user) |
| **PA-4** | Theme · **selection role name** | The new text-selection highlight role's color is source-determined (`cpInputLine` color-3); the name is ours (distinct from `inputSelected`=focused field). | (a) **`inputSelection`**; (b) `inputHighlight` | **(a) `inputSelection`** | ✅ Resolved (user) |
| **PA-5** | Caret · **hardware seam shape** | How does the focused view's caret reach the real terminal cursor, additively? | `View.desiredCaret(): Point \| null` (default null) polled during `compose`; `RenderRoot` translates it to an absolute cell (reusing the `absOrigin` walker, `render-root.ts:95-142`); `EventLoop` gains a sibling `onCaret?: (cell: Point \| null) => void` next to `onFrame` (event-loop.ts:53); `run()` **co-owns the output stream** and writes `cursor.to()/show()/hide()` **after** `host.render`; re-applies on `onResume`. Mirrors the RD-05 `onFrame`/`setCapture` seams. Alt (extend `onFrame`/`render` signature) rejected — not additive. | **additive sibling seam; run() writes cursor post-render** | ✅ Resolved (dominant; preflight-challenger-confirmed) |
| **PA-6** | Input · **selection color** | Which theme color is the highlight? | Source-determined: `TInputLine::draw` `getColor(3)` → `cpInputLine` color-3 slot (`tinputli.cpp:157,84`). Decode the exact `cpInputLine`→`cpGrayDialog`→`cpAppColor` byte at the **implementation GATE-1**, and confirm it is a 4th slot distinct from the RD-06 `inputNormal`/`inputSelected`/`inputArrows` (slots 19/20/21). | **decode exact byte at GATE-1; new `inputSelection` role** | ✅ Resolved (source-determined) |
| **PA-7** | Clipboard · **keys + commands** | Which chords/commands? | Per RD AR-117: **Ctrl+Insert** (copy) · **Shift+Insert** (paste) · **Shift+Delete** (cut) — SIGINT-safe DOS chords (not Ctrl+C/X/V) — plus additive `Commands.cut`/`copy`/`paste`. | **Ins/Del chords + `Commands.*`** | ✅ Resolved (AR-117) |
| **PA-8** | Clipboard · **paste semantics** | How is pasted text applied/validated? | Source-determined (`tinputli.cpp:418-446`, `470-485`): paste **replaces any selection first**, then inserts **char-by-char**, each char through `validator.isValidInput` + the `maxLength` limit (invalid chars dropped individually). Our core delivers one bracketed `PasteEvent{text}` → `Input` iterates its code points. | **replace-selection-then-char-by-char-validate** | ✅ Resolved (source-determined) |
| **PA-9** | Clipboard/selection · **edge behaviors** | Empty-selection copy/cut; double-click; plain-motion collapse. | Source-determined (`tinputli.cpp`): copy with empty selection = copies empty (effective no-op); cut with empty selection = **no-op** (`deleteSelect` guards `selStart<selEnd`, :205); **double-click = select-all** (:322); a plain (un-shifted) arrow **collapses** the selection (:459). | **faithful edge behaviors ported** | ✅ Resolved (source-determined) |
| **PA-10** | MultiCheckGroup · **API/binding** | How is the multi-state exposed? | Per RD AR-120 (idiomatic, not TV's packed `uint32`): constructor `{ items, states: string, value: Signal<number[]> }`; `selRange = states.length`; item state index `0..selRange-1`; `press` cycles `(state+1)%selRange` (`tmulchkb.cpp:88-103`). Visual stays TV-faithful (`" [ ] "` box, marker `states[state]` at `col+2`, `tcluster.cpp:87-129`). | **`Signal<number[]>` + `states` string; faithful visual** | ✅ Resolved (AR-120) |
| **PA-11** | Caret · **scope** | Does the hardware caret extend to clusters (TV `setCursor` on the selected marker)? | **Input-only in RD-07.** RD-06 `CheckGroup`/`RadioGroup` ship no caret; `MultiCheckGroup` matches them (no caret request). Extending the caret to clusters is deferred (**DEF-22**) — the seam (PA-5) is general enough to add it later without host changes (RD-07 Should-Have). | **Input requests the caret; clusters unchanged** | ✅ Resolved (dominant) |
| **PA-12** | Input · **word nav** | Word-boundary rule for Ctrl+Shift+arrows? | Source-determined (`tinputli.cpp:64-82`): **space-delimited** — `prevWord`/`nextWord` jump to the first non-space after a space; no punctuation classing. | **space-delimited word jumps** | ✅ Resolved (source-determined) |
| **PA-13** | Input · **overwrite mode** | Insert vs overwrite (`sfCursorIns`)? | Per RD AR-118: **insert-only** in RD-07; overwrite + `Ins` toggle deferred (**DEF-20**). No block/underline caret-shape distinction — the logical/hardware caret is a single cell. | **insert-only; overwrite deferred** | ✅ Resolved (AR-118) |

## Runtime decisions (exec_plan)

| PA # | Area | Question | Decision | Status |
|------|------|----------|----------|--------|
| **PA-16** (runtime) | Input · paste chord w/o clipboard read | ST-06 says "Shift+Insert maps to the paste path", but OSC-52 clipboard **read** is out-of-scope/unassigned in RD-07 (`01-requirements` Out of scope), so a key chord has no way to fetch system-clipboard content. The only real paste-content source is the terminal's bracketed `PasteEvent`. | **Bracketed paste is the path**: the real paste = the terminal's bracketed `PasteEvent`, which `Input` fully handles (replace selection + char-by-char validate + maxLength). Shift+Insert / `Commands.paste` are **consumed no-ops** (system read deferred → **DEF-25**); in a real terminal Shift+Insert triggers the terminal's own paste → `PasteEvent`. Copy/cut **write** (OSC-52) are fully supported. ST-06's "same path" is verified through the `PasteEvent` it actually flows through. Rejected: (b) app-local clipboard — asymmetric (paste app-local, copy system) + a design addition; (c) core OSC-52 read — non-additive, out of RD-07 scope. | ✅ Resolved (user, 2026-07-02) |
| **PA-15** (runtime) | Input · double-click detection | ST-02 requires double-click → select-all, but the core `MouseEvent` model (`input/events.ts:28-34`) has **no** double-click flag, click-count, or timestamp — TV's `meDoubleClick` has no substrate equivalent. Detect it locally, defer it, or extend the core decoder? | **Input-local heuristic**: a second mouse-`down` at the same cell as the immediately-preceding down (before any intervening action moves the caret elsewhere) counts as a double-click → `selectAll(true)`. No core change (RD-07 stays additive-ui as scoped); the ST-02 spec encodes double-click as two consecutive downs at the same column. Rejected: (b) defer — drops a listed behavior; (c) core decoder click-count — non-additive core change outside RD-07 scope, needs timestamps the decoder lacks. | ✅ Resolved (user, 2026-07-02) |
| **PA-14** (runtime) | Input · `inputSelected` fidelity (PF-004) | GATE-1 (P0.1) resolved the full `cpInputLine`→`cpGrayDialog`→`cpAppColor` chain and **confirmed** TV draws a focused Input identically to an unfocused one: `getColor(1)`==`getColor(2)`==`cpGrayDialog[0x13]`==`cpAppColor[0x32]`==**`0x1F`** white-on-blue (`tinputli.cpp:84,139`). The shipped RD-06 `inputSelected` (slot 20, `0x2F` white-on-green) is a mis-decode — its value is actually `getColor(3)` = the text-**selection** color (`cpGrayDialog[0x14]`==`cpAppColor[0x33]`==`0x2F`). RD-07 ships the visible caret TV uses to mark focus, so focused==unfocused becomes acceptable. Fix the mis-decode or keep the green affordance? | **Fix to TV-faithful**: `inputSelected` → `0x1F` white-on-blue (= `inputNormal`, = `getColor(2)`); the new `inputSelection` role takes the `0x2F` white-on-green selection color (`getColor(3)`). Update the RD-06 spec oracles (core slot map `inputSelected: 20`→`19`; `controls.input`/`controls.focus` focus oracles) citing `tinputli.cpp:84` per the NON-NEGOTIABLE "C++ outranks our spec for TV-derived components" exception. | ✅ Resolved (user, 2026-07-02) |

## New deferrals opened by this plan (append to `../../requirements/DEFERRED.md` on ship)

- **DEF-21** — grapheme-cluster caret/selection stepping in `Input` (full `TText::next/prev` parity). From **PA-1**; unassigned. Rationale: the current `Input` is code-unit-indexed; a dependency-free segmenter + base retrofit is out of the completions slice.
- **DEF-22** — hardware caret for clusters (TV `setCursor` on the selected marker). From **PA-11**; unassigned. The PA-5 seam already supports it without host changes.
- **DEF-25** — OSC-52 clipboard **read** (Shift+Insert / `Commands.paste` fetching the system clipboard). From **PA-16** (runtime); unassigned. RD-07 ships clipboard write + bracketed-paste insertion; a system read needs a non-additive OSC-52 read round-trip in the core decoder. Today Shift+Insert relies on the terminal's own paste → `PasteEvent`.
