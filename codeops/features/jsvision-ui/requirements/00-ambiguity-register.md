# Ambiguity Register: jsvision UI

> **Status**: ✅ RD-01 items resolved · ✅ RD-02 items resolved · ✅ RD-03 items resolved (incl. preflight AR-43…AR-46) · ✅ RD-04 items resolved (AR-47…AR-59, incl. preflight AR-60…AR-66) · ✅ RD-05 items resolved (AR-67…AR-81, incl. preflight AR-82…AR-87) · ✅ RD-10 items resolved (AR-88…AR-92) · ✅ RD-06 items resolved (AR-93…AR-102) · ✅ RD-11 items resolved (AR-103…AR-114) · ✅ RD-07 items resolved (AR-115…AR-124) · 🔄 RD-12+ set-scope resolved (AR-125…AR-129) · ✅ RD-14 (Input dropdowns) items resolved (AR-130…AR-140, incl. preflight AR-162…AR-166) · ✅ RD-15 (Tree) items resolved (AR-141…AR-150) · ✅ RD-16 (Table/DataGrid) items resolved (AR-151…AR-161); RD-17…RD-19 gates pending
> **Last Updated**: 2026-07-02
> **Scope**: The `@jsvision/ui` feature-set (reimagined Turbo Vision UI layer). This
> register grows per RD; entries below cover **RD-01 (Reactive core)**,
> **RD-02 (Layout engine)**, **RD-03 (View/Group spine)**,
> **RD-04 (Event loop + focus + modality + commands)**,
> **RD-05 (App shell — Application/Desktop/Window/MenuBar/StatusLine)**,
> **RD-10 (TV behavioral fidelity)**,
> **RD-06 (Essential controls + validators)**, and
> **RD-11 (Containers, scrolling & lists — ScrollBar/Scroller/ListView/Dialog)**.

Every decision in an RD back-references its `AR #` here. AR-01…AR-04 are explicit
user choices (offered as multiple options, user selected); AR-05…AR-11 are
single-dominant-option decisions (no second viable alternative — recorded for
traceability); AR-12 is a housekeeping decision; AR-13…AR-18 resolve the under-specified
edges surfaced by the RD-01 preflight (PF-001…PF-009) — all accepted by the user 2026-06-29.
AR-19…AR-29 cover **RD-02 (Layout engine)**: AR-19 inherits the architecture settled in
**ADR-008** (dominant); AR-20…AR-29 are explicit user choices made during the RD-02
`add_requirement` interview (2026-06-29), the subset boundary ADR-008 deferred to requirements.
AR-30…AR-42 cover **RD-03 (View/Group spine + DrawContext + theming)**, made during the
RD-03 `add_requirement` interview (2026-06-29): AR-30…AR-36 are explicit user choices
(multiple viable options, user selected the recommended one); AR-37…AR-42 are
single-dominant-option decisions recorded for traceability (geometry reuse, composition
model, DrawContext surface, concrete surface, z-order, render root). AR-43…AR-46 resolve the
under-specified edges surfaced by the **RD-03 preflight** (PF-001/PF-002/PF-003/PF-005,
`00-preflight-report.md`) — all accepted by the user 2026-06-29.
AR-47…AR-59 cover **RD-04 (Event loop + focus + modality + commands)**, made during the
RD-04 `add_requirement` interview (2026-06-29): AR-47…AR-54 are explicit user choices
(two viable options each, user selected the recommended one — the RD-04/RD-05 boundary, focus
model, input seam, mouse hit-testing, 3-phase dispatch, the command layer, modality, and the
loop↔render integration); AR-55…AR-59 are single-dominant-option decisions recorded for
traceability (central `EventLoop` object, the additive `focusable` predicate, focus traversal
keys, the `onIdle`/broadcast/timer boundary, and the headless demo vehicle).
AR-60…AR-66 resolve the under-specified edges surfaced by the **RD-04 preflight**
(PF-401…PF-407, `00-preflight-report-RD-04.md`) — all accepted by the user 2026-06-29.
AR-67…AR-81 cover **RD-05 (App shell — Application/Desktop/Window/MenuBar/StatusLine)**,
made during the RD-05 `add_requirement` interview (2026-06-30): AR-67…AR-74 are explicit
user choices (two or three viable options each, user selected the recommended one — the
window-manager ambition, menu depth, the Scroller/ScrollBar boundary, the demo vehicle,
the `run()`/termination model, the StatusLine dynamics, the active/inactive frame theming
location, and window resize); AR-75…AR-81 are single-dominant-option decisions recorded for
traceability (composition-over-inheritance, the standard command vocabulary, the tilde
hotkey convention, raise semantics, modal-window reuse of `execView`, the desktop background,
and packaging).
AR-82…AR-87 resolve the under-specified edges surfaced by the **RD-05 preflight**
(PF-001…PF-006, `00-preflight-report-RD-05.md`) — all accepted by the user 2026-06-30 (Option A).
AR-82 (pointer-capture seam) and AR-84 (`onFrame` hook) add two **additive intra-package seams** to
the RD-04 `EventLoop` (composed, not re-shaped); AR-83 corrects a stale how-it-works claim about
core's host suspend/resume; AR-85…AR-87 tighten the command vocabulary, the `run()` exit code, and
the cascade/tile edge cases.

| AR # | Area | Question | Options Considered | Decision | Status |
|------|------|----------|--------------------|----------|--------|
| AR-01 | RD-01 · Signal API | How are signals read/written? | (a) callable + methods `s()`/`s.set()`/`s.update()`; (b) tuple `[get,set]` (Solid); (c) ref object `.value` (Vue) | **(a) callable + methods** — read by calling, write via `.set`/`.update` | ✅ Resolved (user) |
| AR-02 | RD-01 · Effect timing | When do effects run after a write? | (a) synchronous + explicit `batch()`; (b) auto-batch on a microtask | **(a) synchronous, glitch-free; `batch(fn)` coalesces writes**; redraws are frame-scheduled by the view layer (RD-03) | ✅ Resolved (user) |
| AR-03 | RD-01 · Disposal | How are computations cleaned up on unmount? | (a) owner-scope tree + `onCleanup`; (b) manual `dispose()` handles only | **(a) owner-scope tree + `onCleanup`** — disposing a scope disposes everything created under it | ✅ Resolved (user) |
| AR-04 | RD-01 · `For` keying | How are list items keyed for reconciliation? | (a) key function; (b) reference identity; (c) index | **(a) key function** `key: item => item.id` — stable identity across moves | ✅ Resolved (user) |
| AR-05 | RD-01 · Equality | When does a write count as a change? | Referential `Object.is` (dominant); deep-equal (rejected: cost/ambiguous); always-notify (rejected: wasteful) | **`Object.is` by default**, skip notify if unchanged; per-signal `equals` override; `equals: false` forces always-notify | ✅ Resolved |
| AR-06 | RD-01 · Computed | Eager or lazy evaluation? | Lazy + memoized (dominant); eager (rejected: computes unused values) | **Lazy + memoized** — recompute on read only after a dependency changed | ✅ Resolved |
| AR-07 | RD-01 · Consistency | May an effect observe a partially-updated graph? | Glitch-free topological order (requirement); ad-hoc (rejected: incorrect) | **Glitch-free required** — dependents run in topological order; no inconsistent intermediate reads | ✅ Resolved |
| AR-08 | RD-01 · Untracked read | Provide a non-subscribing read? | Provide `untrack(fn)` (standard); omit (rejected: needed for effects that read-without-depending) | **Provide `untrack(fn)`** | ✅ Resolved |
| AR-09 | RD-01 · Reactive↔view seam | Where does "mark widget dirty" live? | RD-01 owns primitives only, RD-03 owns binding (dominant); RD-01 owns widget invalidation (rejected: couples reactivity to views) | **RD-01 provides `effect`/owner/`onCleanup`; the view spine (RD-03) binds a property by creating an effect that marks its widget dirty.** No VDOM. | ✅ Resolved |
| AR-10 | RD-01 · Packaging | Where does it live + dependency policy? | `packages/ui/src/reactive/`, zero deps, ESM/NodeNext, re-exported via the single `@jsvision/ui` entry (only viable per project conventions) | **As stated** — pure TS, zero runtime deps (passes `check:deps`) | ✅ Resolved |
| AR-11 | RD-01 · `Show` semantics | What does `Show` do? | `Show(cond, then, else?)` mounts one branch, disposes the inactive branch's scope, re-evaluates on `cond` change (dominant) | **As stated** | ✅ Resolved |
| AR-12 | Feature-set · Requirements folder | Reuse the existing `requirements/` set or start fresh? | Start fresh for the UI feature-set; archive the stale foundation scaffolding (dominant — foundation is done/archived); reuse (rejected: mixes two feature-sets, old `@blendsdk/tui` name) | **Fresh UI set; foundation README/register/_draft moved to `plans/_archive/foundation/requirements/`; RD numbering restarts at RD-01** | ✅ Resolved |
| AR-13 | RD-01 · Error model | Does `ReactiveCycleError` extend core's `TuiError`? | (a) extend `@jsvision/core`'s `TuiError` (SDK-wide `catch (e instanceof TuiError)` convention); (b) standalone `extends Error` (rejected: fragments the error model for marginal decoupling) | **(a) extend `TuiError`** — imported from `@jsvision/core` (declared workspace dep; `check:deps` still passes) | ✅ Resolved (preflight PF-001) |
| AR-14 | RD-01 · No-owner computation | What happens when a computation is created with no active owner? | (a) allowed but never auto-disposed + dev `console.warn` (Solid-parity); (b) throw a `TuiError` (rejected: breaks ad-hoc/test usage + top-level app setup) | **(a) allowed, never auto-disposed, dev-warned** | ✅ Resolved (preflight PF-002) |
| AR-15 | RD-01 · Exception in a run | How is a throw inside an `effect`/`computed` run handled? | (a) abort the run, fire its `onCleanup`, propagate to the `set`/`batch` caller, no rollback, queued siblings still run; (b) swallow-and-log per effect (rejected: hides bugs, un-idiomatic) | **(a) abort + cleanup + propagate, no rollback** | ✅ Resolved (preflight PF-003) |
| AR-16 | RD-01 · Nested `batch` | Flush at the outermost close or per nested `batch`? | (a) outermost-only — inner `batch` joins the outer, effects flush once when the outermost returns (Solid-parity); (b) per-batch flush (rejected: defeats coalescing, surprising) | **(a) outermost-only flush** | ✅ Resolved (preflight PF-006) |
| AR-17 | RD-01 · `For` duplicate keys | What happens when `key` yields the same value for two live items? | (a) keys unique among live items; a duplicate is a usage error — dev `console.warn` + last-writer-wins; (b) throw (rejected: a transient duplicate during an in-flight data update would crash a valid UI) | **(a) dev-warn + last-writer-wins** | ✅ Resolved (preflight PF-009) |
| AR-18 | RD-01 · Runaway limit | What is the runaway-guard iteration bound? | (a) fixed 1000 propagation iterations, not configurable in v1 (deterministic AC); (b) configurable scheduler knob (rejected: premature config) | **(a) fixed 1000, v1** | ✅ Resolved (preflight PF-004) |
| AR-19 | RD-02 · Engine architecture | Build vs buy; model; location? | Settled in **ADR-008**: build cell-native pure-TS flex engine (integer apportionment, not float-round) in `packages/ui/src/layout/` on the `apportion`/`solveTrack` spike; grid is Tier 2. Buy (Yoga/Taffy) rejected (WASM, float-round gaps, breaks zero-dep identity). | **Per ADR-008** — cell-native pure-TS, flex-first, `src/layout/` | ✅ Resolved (ADR-008) |
| AR-20 | RD-02 · Sizing model | How are main-axis box sizes expressed? | (a) `fr`/fixed/auto tokens (matches the spike's `TrackItem`; terminal-idiomatic); (b) CSS grow/shrink/basis (rejected: full-fidelity flexbox ADR-008 dropped); (c) both | **(a) fixed cells · `fr` grow-weight · `auto` (content) tokens** | ✅ Resolved (user) |
| AR-21 | RD-02 · Content sizing | Is intrinsic/`auto` (size-to-content) in v1? | (a) include via a `measure(available) → natural size` seam on a box (keeps RD-02 view-independent; RD-03 views supply `measure`); (b) defer — fixed + `fr` only (rejected: a label/button couldn't size to content) | **(a) `auto` via a `measure()` seam** | ✅ Resolved (user) |
| AR-22 | RD-02 · v1 primitives | Which layout containers ship in v1? | (a) `row` + `col` only (1-D flex; 2-D via nesting); (b) +`stack` (z-overlap); (c) +`grid` (ADR-008 Tier 2) | **(a) `row` + `col` only** — `grid` Tier 2, `stack`/overlay deferred until a real need | ✅ Resolved (user) |
| AR-23 | RD-02 · Layout input | What does the layout pass operate on? | (a) a standalone `LayoutBox = { props, children, measure? }` tree (pure function; view-independent; RD-03 produces it); (b) layout props on RD-03's `ViewNode` (rejected: couples to a not-yet-existing shape) | **(a) standalone `LayoutBox` tree** | ✅ Resolved (user) |
| AR-24 | RD-02 · Justify (main axis) | How is leftover main-axis space distributed (no `fr` present)? | (a) `start`/`center`/`end`/`space-between`; (b) full 6-value CSS set (+`space-around`/`space-evenly`); (c) `start`/`center`/`end` only | **(a) start · center · end · space-between** — around/evenly deferred | ✅ Resolved (user) |
| AR-25 | RD-02 · Align (cross axis) | Cross-axis alignment values + default? | (a) `start`/`center`/`end`/`stretch`, default **stretch**; (b) same set, default `start`; (c) `stretch` only | **(a) start/center/end/stretch, default stretch** | ✅ Resolved (user) |
| AR-26 | RD-02 · Min/max constraints | Per-box min/max size clamps in v1? | (a) defer (ADR-008 lists min/max outside the v1 subset; constrained apportionment is real effort); (b) include clamps now (rejected for v1: more algorithm + edge cases) | **(a) deferred** — documented limitation; revisit | ✅ Resolved (user) |
| AR-27 | RD-02 · Coordinate space | Are computed rects parent-relative or absolute? | (a) **parent-relative** (each rect relative to its parent content box; pass given the root rect — composes with clip/scroll/nesting); (b) absolute screen rects (rejected: recompute on ancestor move, couples to origin) | **(a) parent-relative rects** | ✅ Resolved (user) |
| AR-28 | RD-02 · Overflow | What happens when fixed/auto children exceed the container (no `fr` to absorb)? | (a) rects **extend past the edge**; renderer/clip (and later scroll) handles visibility; `fr` clamps to 0 — no shrink (shrink was dropped, AR-20); (b) clamp children into bounds (rejected: silently drops content, less predictable) | **(a) extend past edge; renderer clips** | ✅ Resolved (user) |
| AR-29 | RD-02 · Gap & padding | Are between-children `gap` and container `padding` in v1? | (a) both (`gap` already in `solveTrack`; per-side `padding` insets the content box); (b) gap only (rejected: forms/panels need padding) | **(a) both gap and padding** | ✅ Resolved (user) |
| AR-30 | RD-03 · RD-03/RD-04 boundary | What of `View` ships in RD-03 vs RD-04? | (a) define the **complete** `View`/`Group` shape — overridable `onEvent` stub + `state` flags (`visible`/`disabled`/`focused`) — but defer dispatch & focus-traversal **logic** to RD-04; (b) pure structure+draw spine, no events/state at all (rejected: forces re-opening `View` + every subclass in RD-04, and RD-03 already needs `focused`/`disabled` to draw `buttonFocused`/dimmed) | **(a) complete shape, deferred logic** — final base class, zero subclass churn when RD-04 lands | ✅ Resolved (user) |
| AR-31 | RD-03 · Reactive↔view seam (closes AR-09) | How does a widget bind a signal to redraw? | (a) each `View` owns an RD-01 owner scope (`createRoot`) + a `bind(reader, apply?)` helper that creates an `effect` under that scope running `apply` + `this.invalidate()` on change, auto-disposed on unmount; (b) per-view scope only, widgets write `effect(() => { read; this.invalidate() })` by hand (rejected: boilerplate, every widget re-implements bind-and-invalidate) | **(a) View owns a scope + `bind()` helper** — the disciplined-hybrid DX payoff, leak-free | ✅ Resolved (user) |
| AR-32 | RD-03 · Redraw pump (closes AR-02) | After `invalidate()`, who pumps the repaint? | (a) RD-03 owns a coalescing scheduler — `invalidate()` adds the view to a dirty set + schedules **one** flush (default `queueMicrotask`, **injectable**) that recomposes dirty subtrees → `ScreenBuffer`; (b) RD-03 only exposes `compose()`/`paint()`, RD-04's loop drives it (rejected: RD-03 can't render standalone, blocks the Phase-0 demo, contradicts AR-02) | **(a) coalescing scheduler, injectable** — renders standalone now, RD-04 injects its driver later | ✅ Resolved (user) |
| AR-33 | RD-03 · Layout seam | How is the view tree sized via RD-02? | (a) RD-03 owns the **reflow pass** — build a `LayoutBox` tree from the view tree (each `View` contributes `LayoutProps` + a `measure` deferring to the widget), call `layout(root, viewport)`, write parent-relative rects back to `view.bounds`; relayout (sizing) and repaint (drawing) are **distinct dirty-phases**; (b) layout invoked externally by the app (rejected: no single owner of the RD-02↔RD-03 seam, every app rewires it) | **(a) RD-03 owns the reflow pass**; layout props live on the `View` | ✅ Resolved (user) |
| AR-34 | RD-03 · Clipping / occlusion | How much clipping does the v1 spine do on overlap? | (a) **bounds-clip only** (writes clipped to own rect ∩ ancestor rects) + back-to-front paint (top overpaints); occlusion/cover-detection (TV `exposed()`) **deferred**; (b) include cover/exposed detection in v1 (rejected: real complexity for an optimization core's damage-diff already neutralizes — `serialize()` only emits changed cells) | **(a) bounds-clip + back-to-front; occlusion deferred** | ✅ Resolved (user) |
| AR-35 | RD-03 · Theming | How does theming resolve, at what granularity? | (a) **one app-level theme** (core's `Theme` shape/roles); `ctx.color(role)` resolves a named role → `Style` at draw time; widgets pick state-dependent roles themselves (`focused ? 'buttonFocused' : 'button'`); per-subtree **override deferred** behind a seam (`DrawContext` carries the active theme → future override is non-breaking); (b) app theme + per-subtree override in v1 (rejected: inheritance machinery before any widget needs it; `defaultTheme` roles already cover Phase 0) | **(a) one app theme, role-name resolution; override deferred** | ✅ Resolved (user) |
| AR-36 | RD-03 · Dynamic children + lifecycle | How do `Show`/`For` compose into the retained tree? | (a) **reuse RD-01 `Show`/`For` with `N=View`** — a `Group` takes static children + reactive producers; each view's owner scope nests under its parent's, so unmount disposes descendant scopes + runs `onCleanup`; `View` gets `onMount` + `onCleanup` hooks; (b) a bespoke RD-03 reconciler (rejected: duplicates the already-generic `Show`/`For`, two ways to express structure, breaks the "structure goes through `Show`/`For`" rule) | **(a) reuse `Show`/`For` (`N=View`); nested scopes ⇒ leak-free unmount** | ✅ Resolved (user) |
| AR-37 | RD-03 · Geometry types | New geometry or reuse RD-02's? | reuse the already-public `Rect`/`Size2D` interfaces + add `Point` + pure helpers (`intersect`/`translate`/`contains`); a `Rect` **class** with methods rejected (forks the type, breaks DRY now that RD-02's interfaces are public from `@jsvision/ui`) | **Reuse `Rect`/`Size2D` + add `Point` + pure helpers** | ✅ Resolved (dominant) |
| AR-38 | RD-03 · Composition model | One shared buffer or per-view offscreen buffers? | composite children into the single shared core `ScreenBuffer` with clip+offset (view-local coords), back-to-front; per-view offscreen buffers rejected (wasted memory; the damage-diff already makes overpaint cheap) | **Single shared `ScreenBuffer`, clip+offset, view-local coords** | ✅ Resolved (dominant) |
| AR-39 | RD-03 · `DrawContext` surface | What paint API does `draw(ctx)` get? | a **stateless**, auto-clipped, view-local facade mirroring `ScreenBuffer` essentials: `text`/`fill`/`fillRect`/`box`/`shadow` + `color(role)`; writes outside the clip silently dropped; a stateful cursor (`moveTo`+`write`) API rejected (core's buffer is stateless x,y — consistency) | **Stateless facade; out-of-clip writes dropped** | ✅ Resolved (dominant) |
| AR-40 | RD-03 · Concrete surface | What classes are concrete in RD-03? | `View` **abstract**; `Group` the one **concrete** container (composites children + fills an optional background role — a capability the spine needs so overlap doesn't leak stale cells); **no leaf widgets** — first controls (`Text`/`Button`/…) land in RD-06; acceptance uses test `View` subclasses + the Phase-0 demo | **`View` abstract, `Group` concrete; no leaf widgets in RD-03** | ✅ Resolved (dominant) |
| AR-41 | RD-03 · `visible:false` semantics | Does a hidden view keep its space? | (a) **`display:none`** — skipped in draw AND omitted from the `LayoutBox` tree, siblings reflow to fill the space (matches TV `sfVisible`); (b) `visibility:hidden` (reserve space) rejected for the default (rarer; can be added later as a separate prop) | **(a) `display:none` — excluded from layout** | ✅ Resolved (user) |
| AR-42 | RD-03 · `draw()` throws | What happens when a view's `draw()` throws mid-frame? | (a) **isolate + log** via core's screen-safe logger, skip that view's subtree, finish the frame (one crashing widget can't blank the app); (b) propagate + abort the frame (rejected: harsh for an app framework — a single bad widget tanks the whole render). Intentionally **differs** from RD-01 AR-15 (effects propagate): a render loop should degrade gracefully, the log keeps the bug visible | **(a) isolate + log, finish the frame** | ✅ Resolved (user) |
| AR-43 | RD-03 · Owner-scope nesting | How does an imperatively-added child's scope nest under its parent's? | (a) **add a small additive RD-01 primitive `runWithOwner(owner, fn)`** (exported via the reactive surface); `Group.add`/`Show`/`For` create each child scope under the parent at add-time; (b) construction-time `createRoot` only (rejected: `createRoot` nests under the *ambient* owner — `null` for an imperative `new View()` — so the child scope would not nest under the parent and the "leak-free by construction" guarantee fails for the imperative/demo path); (c) reach into reactive internals (`setOwner`) from RD-03 (rejected: depends on un-exported internals, breaks the single-public-entry convention) | **(a) `runWithOwner` additive primitive; scope created at add-time under the parent** | ✅ Resolved (preflight PF-001) |
| AR-44 | RD-03 · Render-root encoding | What does the render root need to emit a frame via core `serialize()`? | (a) **carry a required `caps: CapabilityProfile` and retain the previous `ScreenBuffer`** — core `serialize(current, previous, { caps })` requires both (caps non-optional, drives depth-aware downsampling; previous drives the damage diff); (b) omit them from the render-root model (rejected: AC-16/AC-19 could not call `serialize()` and the diff degrades to a full repaint) | **(a) required `caps` + retained previous buffer (double-buffer swap)** | ✅ Resolved (preflight PF-002) |
| AR-45 | RD-03 · Theme-role type & adapter | Where does `ThemeRoleName` come from and how does a role become a `Style`? | (a) **`ThemeRoleName = keyof Theme` (RD-03-owned)** + a `ThemeRole → Style` adapter in `color()` (map `fg`/`bg`, default `attrs`, ignore role-only extras like `hotkey`/`border`/`title`); (b) import `ThemeRoleName` from `@jsvision/core` (rejected: core exports no such type — only `ColorRole`/`Theme`/`ThemeRole`; roles are `{fg,bg,hotkey?}`, not `Style`) | **(a) `keyof Theme` + role→Style adapter** | ✅ Resolved (preflight PF-003) |
| AR-46 | RD-03 · Bind & relayout | Does a reactive `bind` trigger reflow or only repaint? | (a) **repaint by default; layout-affecting binds opt in to a reflow** (`bind(reader, apply, { relayout: true })` or `this.invalidateLayout()` in `apply`); (b) `bind` always relayouts (rejected: defeats the repaint/relayout split AR-32/AR-33 and AC-9, reflows on every draw-only change) | **(a) repaint default, relayout opt-in** | ✅ Resolved (preflight PF-005) |
| AR-47 | RD-04 · RD-04/RD-05 boundary | What does RD-04 ship — mechanism or Application? | (a) the host-agnostic **dispatch mechanism** (event loop + 3-phase dispatch + focus manager + modality + commands) wired to RD-03's `RenderRoot`; concrete `Application`/`Desktop`/`MenuBar`/`StatusLine` and `run()` → RD-05; (b) a full `Application` object now (rejected: blurs the spine/shell line, larger RD, couples dispatch to concrete app objects) | **(a) mechanism only; Application → RD-05** — mirrors the RD-03 ship-the-shape/defer-logic split (AR-30) | ✅ Resolved (user) |
| AR-48 | RD-04 · Focus model | How is focus modeled in the composed tree? | (a) **per-group `current` chain (TV-faithful)** — each `Group` tracks a `current` focused child; global focus is the root→leaf path of `current` pointers; Tab/Shift-Tab cycles within the active group; nested groups compose; a modal group owns its own focus subchain; (b) flat global focused-view + computed linear tab-order (rejected: modality/nesting + restore-focus-on-re-entry must be rebuilt on top) | **(a) per-group `current` chain** | ✅ Resolved (user) |
| AR-49 | RD-04 · Input seam | How does the loop receive input? | (a) **pure `dispatch(event)` entry, injectable source** — RD-04 is a pure event-driven machine; the real `createHost({onInput})` wiring is a thin adapter (deferred to RD-05's `run()`); makes focus/dispatch/modality unit-testable with no TTY; (b) RD-04 owns `createHost` wiring directly (rejected: couples the dispatch engine to a live TTY, hampers pure tests) — consistent with core's pure decoder + RD-03's injectable scheduler | **(a) pure `dispatch`, injectable source** | ✅ Resolved (user) |
| AR-50 | RD-04 · Mouse hit-testing | Where does mouse routing live? | (a) **in RD-04** — walk the tree top-most-first to find the view under `(x,y)`, route mouse/wheel there, focus-on-click; (b) defer to RD-05 (rejected: leaves the decoded core mouse/wheel event classes undispatched; RD-03 explicitly deferred hit-testing to "the event model; RD-04") | **(a) top-most-first hit-test in RD-04** | ✅ Resolved (user) |
| AR-51 | RD-04 · Dispatch model | 3-phase or simple bubble? | (a) **faithful 3-phase (pre/focus/post)** — pre-process groups (accelerators/hotkeys) root→down, then the focused view, then post-process groups (default/cancel buttons, menu hotkeys); a `handled` flag halts propagation; (b) simple focus→ancestor bubble (rejected: menu hotkeys + default-button semantics must be bolted on later) — component map: "keep the 3-phase model, good middleware" | **(a) faithful 3-phase** | ✅ Resolved (user) |
| AR-52 | RD-04 · Commands | Typed command layer in RD-04 or raw dispatch only? | (a) **typed command layer** — a `CommandEvent` (typed string command) dispatched through the same 3-phase machine; the loop owns a command registry with enable/disable; an app-level keymap binds key chords → command names; (b) raw key/mouse dispatch only, commands → RD-05 (rejected: `execView` would have no typed result vocabulary; component map lists commands under the Tier-0 event loop) | **(a) typed command layer (CommandEvent + enable/disable + key→command keymap)** | ✅ Resolved (user) |
| AR-53 | RD-04 · Modality | How does `execView` implement modality? | (a) **modal stack + `endModal(result)` → Promise** — `execView(view)` pushes onto a modal stack, captures dispatch to the top modal subtree, saves/restores focus around it, resolves the awaited Promise when `endModal(result)` (or a cmOK/cmCancel command) ends it with a typed result; (b) defer modality to RD-05 (rejected: `execView` is Tier-0 event-loop and the Phase-0 demo payoff) | **(a) modal stack + `endModal` → `Promise<Result>`** | ✅ Resolved (user) |
| AR-54 | RD-04 · Loop↔render integration | Does the loop drive `RenderRoot` frames? | (a) **loop injects its tick as the scheduler** — invalidations from one input batch flush as exactly one frame per loop tick (deterministic); resize → `RenderRoot.resize` + flush; an `onIdle` hook fires when the queue drains; uses the injectable-`schedule` seam RD-03 built (AR-32); (b) keep RenderRoot's default `queueMicrotask` (rejected: loses one-frame-per-input-tick determinism) | **(a) loop drives frames via the injected scheduler** | ✅ Resolved (user) |
| AR-55 | RD-04 · Central object | What object owns the mechanism, and what's deferred? | (a) **`EventLoop` via `createEventLoop(renderRoot, opts)`** — owns `dispatch(event)`, the focus manager, the command registry, the modal stack, and scheduler injection; `run()`/`Application` and the real `createHost` wiring → RD-05 (which owns `run()` per the component map); (b) fold everything into `RenderRoot` (rejected: overloads the render seam with input/focus concerns) | **(a) `EventLoop` (`createEventLoop`); `run()`/host wiring → RD-05** | ✅ Resolved (dominant) |
| AR-56 | RD-04 · Focusable predicate | How is "can this view take focus?" expressed? | (a) **additive `focusable` option on `View`** (TV's `ofSelectable`); the predicate is `visible && !disabled && focusable`; a `Group` is focusable iff it has a focusable descendant; additive to RD-03's final shape (a new option flag, not a re-shape, per AR-30); (b) infer focusability from an `onEvent` override (rejected: implicit, can't express a decorative interactive-looking view) | **(a) additive `focusable` flag + predicate** | ✅ Resolved (dominant) |
| AR-57 | RD-04 · Focus traversal | Which keys move focus, and how across nesting? | (a) **Tab / Shift-Tab** advance `current` among focusable siblings in the active group (wrap at ends); entering a child `Group` focuses that group's `current` (or its first focusable); a mouse click focuses the hit view's chain (AR-50) — TV-faithful; (b) arrow-key spatial navigation (rejected for v1: spatial nav is a later enhancement; Tab order is deterministic and matches forms) | **(a) Tab/Shift-Tab over the current-chain (wrap); click-to-focus** | ✅ Resolved (dominant) |
| AR-58 | RD-04 · Idle / broadcast / timers | What ancillary loop features ship in v1? | (a) **`onIdle` hook only** (fires when the event queue drains); a typed broadcast/message bus and a timer-queue wrapper are **deferred to RD-05** (cross-view state is already served by RD-01 signals; Node's native `setTimeout`/`setInterval` cover timers); (b) ship broadcast + timer queue now (rejected: premature — no v1 consumer, signals subsume cross-view messaging) | **(a) `onIdle` only; broadcast + timer-queue → RD-05** | ✅ Resolved (dominant) |
| AR-59 | RD-04 · Demo & acceptance vehicle | How is RD-04 demonstrated and accepted? | (a) **headless scripted-dispatch demo (`demo:events`)** — feed synthetic `InputEvent`s into `dispatch()`, show focus moving (Tab), a command firing (Enter→'ok'), and a modal `execView` resolving, with ASCII frames; deterministic + CI-able, consistent with `demo:view`; the real interactive TTY app lands with RD-05's `run()`; (b) a real-TTY interactive demo now (rejected: needs the host wiring deferred to RD-05 and isn't deterministically testable) | **(a) headless `demo:events`; interactive TTY → RD-05** | ✅ Resolved (dominant) |
| AR-60 | RD-04 · `handled` flag carrier (PF-401) | Where does the mutable `handled` propagation flag live, given core's `InputEvent` is readonly with no such field? | (a) **dispatch envelope** `DispatchEvent { event, handled, local? }` the loop wraps each event in — `onEvent(ev: DispatchEvent)` receives it, core events stay pure/readonly; (b) `onEvent` returns a handled-sentinel (viable, but discards the one-flag-any-phase ergonomics the ACs lean on); (c) add `handled` to core's `InputEvent` (rejected: pollutes the pure decoder model, cross-package change) | **(a) `DispatchEvent` envelope** (also carries view-local mouse coords) | ✅ Resolved (preflight PF-401) |
| AR-61 | RD-04 · Scheduler ownership (PF-402) | How does the loop drive frames, given RD-03's `schedule` seam is construct-time/private (no runtime setter)? | (a) **the loop builds the `RenderRoot`** (`createEventLoop(viewport, opts)`) so it controls the construct-time `schedule` seam, suppresses the root's self-flush, and drives `flush()` itself; exposes `renderRoot` + `mount(root)`; (b) loop calls `flush()` per batch without owning the scheduler (viable fallback; surfaces the batch-API question, PF-405); (c) add a mutable `setScheduler` seam to `RenderRoot` (rejected: a mutable surface RD-03 deliberately withheld) | **(a) loop builds + owns the render root** | ✅ Resolved (preflight PF-402) |
| AR-62 | RD-04 · Keymap (PF-403) | Bespoke key-chord parser or reuse core's? | (a) **reuse `@jsvision/core`'s `createKeymap`/`Keymap`** (the `'ctrl+q'` lowercase `'+'` grammar + build-time validation); `EventLoopOptions.keymap?: Keymap`, loop calls `keymap.lookup`; (b) ui-local keymap copying core's grammar (rejected: duplicates parsing core already ships — DRY) | **(a) reuse core `createKeymap`/`Keymap`** | ✅ Resolved (preflight PF-403) |
| AR-63 | RD-04 · Mouse coordinate base (PF-404) | How are core's 1-based mouse coords reconciled with 0-based view bounds? | (a) **normalize to 0-based** (`x-1`,`y-1`) at the dispatch boundary before hit-test + view-local translation; (b) leave to planning (rejected: a silent off-by-one exactly where AC-7/AC-8 test) | **(a) normalize 1-based→0-based at the boundary** | ✅ Resolved (preflight PF-404) |
| AR-64 | RD-04 · Batch / tick model (PF-405) | What is a "batch" / where is the event queue, given the singular `dispatch(event)`? | (a) **a dispatch tick** = one `dispatch(event)` + its synchronous command cascade; the queue is the cascade queue; `onIdle` fires on drain; one coalesced `flush()` per tick (AC-16/AC-18 reworded); (b) an explicit array/`begin`-`end` batch API (deferred — adopt only if the host must hand the loop an atomic multi-event chunk) | **(a) tick = one dispatch + its cascade** | ✅ Resolved (preflight PF-405) |
| AR-65 | RD-04 · Focusable default + subtree semantics (PF-406) | What is the base `View.focusable` default, and do `!visible`/`disabled` ancestors propagate? | (a) **default `false`** (decorative-by-default); eligibility = `visible && !disabled && focusable` **and no `!visible`/`disabled` ancestor** (TV subtree semantics); hit-testing skips such subtrees; (b) default `true` + ancestor propagation deferred (rejected: surprising for decorative views; leaves propagation unresolved) | **(a) default `false` + subtree semantics** | ✅ Resolved (preflight PF-406) |
| AR-66 | RD-04 · Handler-error logger seam (PF-407) | Where does the loop get the screen-safe logger AC-19 requires? | (a) **add `logger?: Logger` to `EventLoopOptions`** (default a disabled `createLogger()`), used for `onEvent` errors and forwarded to the loop-built render root for `draw()` errors — symmetric with RD-03's AR-42; (b) expose the root's private logger on the `RenderRoot` interface (rejected: new RenderRoot surface, couples loop to root internals) | **(a) injectable `EventLoopOptions.logger`** | ✅ Resolved (preflight PF-407) |
| AR-67 | RD-05 · Window-manager ambition | How interactive is the Desktop/Window manager? | (a) **full interactive WM** — focus-on-click **raise** + drag-move (title) + zoom (maximize/restore) + cascade/tile commands + window-number focus (Alt-N) + close box; (b) move+raise+close only (rejected: defers the layout commands, a half desktop); (c) static frames, focus-on-click only (rejected: barely a shell, doesn't exercise RD-04's machinery) | **(a) full interactive WM** | ✅ Resolved (user) |
| AR-68 | RD-05 · Menu subsystem depth | How deep is the MenuBar/MenuPopup subsystem? | (a) **full menus** — dropdown popups, F10/Alt-hotkey/click activation, ↑↓/Enter/←→/Esc navigation, **nested** submenus, tilde accelerators shown, enable/disable wired to the RD-04 command registry; (b) single-level popups only (rejected: no nested submenus, incomplete for Tier-0); (c) bar render + command emit, no popups (rejected: non-navigable menus) | **(a) full nested menus** | ✅ Resolved (user) |
| AR-69 | RD-05 · Scroller/ScrollBar boundary | Do the scrolling widgets ship in RD-05? | (a) **defer both `ScrollBar` + `Scroller` to RD-06** — they pair with `ListView`'s virtual scroll; a ScrollBar with no Scroller is decorative and a Scroller has nothing to show without a scrollable content widget; RD-05 stays the frame/desktop/menu/status/lifecycle shell (the RD-05 roadmap title is refined accordingly); (b) ScrollBar widget only now (rejected: decorative without a Scroller); (c) both now (rejected: adds a viewport-clipping concern on top of the WM, premature without content widgets) | **(a) defer both to RD-06** | ✅ Resolved (user) |
| AR-70 | RD-05 · Demo & acceptance vehicle | How is RD-05 demonstrated and accepted? | (a) **both** — a headless ASCII `demo:shell` (deterministic synthetic-dispatch, CI-able acceptance, like `demo:events`) **plus** a real-TTY interactive demo (manual `run()` verification, like `demo:resize`); proves both the pure shell and live host wiring; (b) headless only (rejected: never exercises live `createHost` at the demo layer); (c) real-TTY only (rejected: not deterministic for CI) | **(a) both headless + real-TTY** | ✅ Resolved (user) |
| AR-71 | RD-05 · `run()` / termination | What drives termination and what does `run()` resolve to? | (a) **`'quit'` command → `run(): Promise<number>` exit code** — wire `createHost({onInput,onResize,onSuspend,onResume})` → `dispatch`/`resize`, run until the `'quit'` command (TV cmQuit), resolve the exit code; `host.stop()` restore guaranteed on every exit/throw/signal path; (b) resolve on last-window-closed (rejected: that's app policy, not shell policy); (c) either-whichever-first (rejected: two termination paths to specify/test for no Phase-0 need) | **(a) `'quit'` command → exit code; restore on every path** | ✅ Resolved (user) |
| AR-72 | RD-05 · StatusLine dynamics | Static items or context-sensitive? | (a) **static item list + command enable/disable greying** — items emit commands on click/hotkey and grey by reflecting the RD-04 registry; defer TV's help-context ranges; (b) context-sensitive help-context ranges (rejected for v1: adds a new cross-cutting help-context concept to View for no Phase-0 demo need) | **(a) static + enable/disable; ranges deferred** | ✅ Resolved (user) |
| AR-73 | RD-05 · Active/inactive frame theming | Where does the active vs inactive window distinction live? | (a) **add an additive active/inactive window role to `@jsvision/core`'s `Theme`** — core owns theming, one source of truth, every later widget RD benefits; matches the additive-core-primitive pattern (RD-01 `runWithOwner`, RD-03 `ScreenBuffer.clone()`); (b) derive UI-side from the existing `window` role (rejected: bakes a theming decision into the UI layer instead of the theme); (c) no distinction in v1 (rejected: with raised/overlapping windows the user can't see which is active) | **(a) additive active/inactive role on core `Theme`** | ✅ Resolved (user) |
| AR-74 | RD-05 · Window resize | Free drag-resize in addition to zoom? | (a) **free drag-resize (lower-right corner) + zoom** — arbitrary resize with live content reflow, completing the full interactive WM (AR-67); TV-faithful grow; (b) zoom (maximize/restore) only (rejected: incomplete for the full-WM choice; windows could only change size by zooming) | **(a) free drag-resize + zoom** | ✅ Resolved (user) |
| AR-75 | RD-05 · Application composition | Compose the mechanism or subclass a base Program? | (a) **composition over inheritance** — `createApplication(opts)` composes the RD-04 `EventLoop`, an owned `Desktop`, optional `MenuBar`, optional `StatusLine`; no MI chain (dominant — component map §2: "composition, not inheritance"); (b) subclass a `TProgram`-style base (rejected: revives the TV multiple-inheritance chain the model explicitly dropped) | **(a) composition over inheritance** | ✅ Resolved (dominant) |
| AR-76 | RD-05 · Standard command vocabulary | Constants module or bare literals? | (a) **a `Commands` constants module** (`quit`/`close`/`zoom`/`next`/`prev`/`cascade`/`tile`/`resize`/`move`) so menus/status/keymaps bind by constant; discoverable + refactor-safe (dominant); (b) bare string literals at each call site (rejected: stringly-typed, drift-prone — coding-standard "enums/constants for discriminators, not bare string literals") | **(a) `Commands` constants module** | ✅ Resolved (dominant) |
| AR-77 | RD-05 · Hotkey convention | How is a label's hotkey marked? | (a) **tilde `~X~` marker** in the label string (TV-faithful; already in the component map builder sketch `subMenu('~F~ile', …)`) — one convention covers menu items and status items; (b) a separate explicit `hotkey` field (rejected: diverges from the established sketch and the prior art, two places to keep in sync) | **(a) tilde `~X~` marker** | ✅ Resolved (dominant) |
| AR-78 | RD-05 · Raise semantics | What does clicking a window do to z-order? | (a) **click raises the window to the top of the desktop's child (z) order and focuses it; the active window is the top-most focused window** — this is the raise RD-04 (AR-50) explicitly named as the Desktop's job; (b) focus only, no raise (rejected: RD-04 already does focus-on-click — raise is the missing piece, and overlapping windows need it) | **(a) click raises to top + focuses; active = top-most focused** | ✅ Resolved (dominant) |
| AR-79 | RD-05 · Modal windows | Reuse `execView` or build a rich Dialog now? | (a) **reuse RD-04's `execView`/`endModal` modal mechanism** for modal windows; the rich `Dialog` widget (standard buttons, validators, result mapping) is **RD-06** (dominant — the mechanism already exists, AR-53; Dialog is a control); (b) build a rich `Dialog` in RD-05 (rejected: a control, not shell; belongs with the other RD-06 controls) | **(a) reuse `execView`/`endModal`; rich `Dialog` → RD-06** | ✅ Resolved (dominant) |
| AR-80 | RD-05 · Desktop background | How is the desktop filled? | (a) **core theme `desktop` role + its `pattern` glyph** — the role already exists in `defaultTheme` (`{ pattern:'░', fg, bg }`); reuse it (dominant); (b) a bespoke background fill (rejected: reinvents an existing role — DRY) | **(a) reuse core `desktop` role + pattern** | ✅ Resolved (dominant) |
| AR-81 | RD-05 · Packaging | How is RD-05 packaged + re-exported? | (a) **pure TS, zero runtime deps, ESM/NodeNext, lives under `packages/ui/src/`, re-exported through the single `@jsvision/ui` entry with explicit named re-exports** (inherits the package convention — `check:deps`, layout convention AC-18); (b) barrel `export *` (rejected: the layout convention mandates explicit named re-exports for non-`reactive` subsystems) | **(a) pure TS, zero deps, explicit named re-exports** | ✅ Resolved (dominant) |
| AR-82 | RD-05 · Drag/resize pointer capture (PF-001) | How do drag-move / single-cell resize keep tracking the cursor when it leaves the title bar / corner, given RD-04 routes each mouse event only to the top-most hit view (no capture, no bubble — `dispatch.ts:116`, `hit-test.ts:108-113`)? | (a) **add an additive `setCapture(view)`/`releaseCapture()` seam to the loop**; while captured, all mouse move/drag/up route to the captured view until release; (b) best-effort (cursor must stay on the affordance) — rejected: a 1-cell resize corner is untrackable and a clamped title drag breaks by construction | **(a) additive pointer-capture seam (intra-package; loop composed, not re-shaped)** | ✅ Resolved (preflight PF-001) |
| AR-83 | RD-05 · Suspend/resume ownership (PF-002) | Who re-asserts terminal modes + full-repaints on SIGCONT — the app or core's host? | (a) **core's host owns it** — `signals.ts:110-122` re-asserts modes + full-repaints the last buffer, *then* fires `onResume` (`types.ts:44`); RD-05's `onResume` is notify-only (optional state resync); (b) the app re-asserts modes + repaints in `onResume` (rejected: a stale how-it-works claim — redundant, risks double enter-mode writes) | **(a) host owns mode-reassert + repaint; `onResume` notify-only** | ✅ Resolved (preflight PF-002) |
| AR-84 | RD-05 · Frame delivery to the host (PF-003) | How does every composed frame reach `host.render`, given the loop owns `RenderRoot`/drives `flush()` but exposes no frame-flushed hook (`event/types.ts:36-63`) and async `endModal`/command frames flush with no caller waiting? | (a) **add an additive `onFrame`/`onFlush` seam** (fired at the end of `runTick` after `flush()` + on `resize`); `run()` pushes `renderRoot.buffer()` → `host.render` on every frame, sync or async; (b) `run()` polls the buffer only after each synchronous `dispatch` (rejected: async `endModal`/command frames never paint) | **(a) additive `onFrame` hook (intra-package; loop composed, not re-shaped)** | ✅ Resolved (preflight PF-003) |
| AR-85 | RD-05 · `resize`/`move` command vocabulary (PF-004) | Do `Commands.resize`/`move` ship in v1, given no requirement specifies what emitting them does (move/resize are mouse-drag gestures, not commands)? | (a) **drop `resize`/`move` from the v1 `Commands` set** (keep mouse-drag only; add them when a keyboard move/resize mode is specified); (b) add a keyboard move/resize mode now (rejected: scope growth in an XL RD, no Phase-0 need); (c) keep as reserved no-ops (rejected: ships dead API) | **(a) drop `resize`/`move` from v1** | ✅ Resolved (preflight PF-004) |
| AR-86 | RD-05 · `run()` exit-code source (PF-005) | What determines the exit code `run()` resolves on `'quit'`? | (a) **`0` by default; the optional numeric `emitCommand('quit', code)` arg overrides; a throw/signal path resolves a non-zero code** (reuses the existing `emitCommand` arg channel); (b) leave to planning (rejected: AC-3 stays unverifiable) | **(a) `0` default, overridable via the `quit` arg** | ✅ Resolved (preflight PF-005) |
| AR-87 | RD-05 · Cascade/Tile edge cases (PF-006) | How do `cascade`/`tile` behave vs the resize minimum size and zoomed windows? | (a) **tile clamps each cell to the window minimum** (cells may extend past the desktop edge — RD-02 overflow AR-28); cascade/tile **un-zoom** a maximized window first; 0 windows = no-op, 1 = fill (reuses established conventions); (b) leave to planning (rejected: AC-11 under-specified) | **(a) clamp-to-minimum + un-zoom-first + degenerate-count handling** | ✅ Resolved (preflight PF-006) |
| AR-88 | RD-10 · Status press feedback + emit timing | Does the status line emit on press (current) or release (TV)? | (a) **emit on mouse-release, TV-faithful** — repaint the held item black-on-green (`cSelect`) on mouse-down, track the drag, emit the command on mouse-up **only if still over the same enabled item**, drag-off cancels (`tstatusl.cpp` `drawSelect`); reuses the additive pointer-capture loop seam (AR-82); **supersedes** today's emit-on-press behavior and updates its spec/impl tests; (b) keep emit-on-press + add only the green highlight (rejected: no release semantics, not TV-faithful) | **(a) emit on release; green held-highlight; supersedes emit-on-press** | ✅ Resolved (user) |
| AR-89 | RD-10 · Cascade geometry | Match TV's cascade exactly or keep the RD-05 preset? | (a) **TV-exact cascade** — each window keeps the desktop's bottom-right corner and staggers its top-left by **+1 col / +1 row** (equal deltas), so windows extend to the corner and shrink as they stack (`tdesktop.cpp:67-98`); **supersedes AR-87**'s +2col/+1row fixed-size preset and rewrites the ST-11 spec oracle; (b) keep the AR-87 preset (rejected by the user — fidelity directive) | **(a) TV-exact cascade; supersedes AR-87** | ✅ Resolved (user) |
| AR-90 | RD-10 · Tile topology | Match TV's tile algorithm or keep the near-square preset? | (a) **TV-exact tile** — `mostEqualDivisors` picks cols×rows, `dividerLoc` splits the desktop proportionally with **no remainder**, `leftOver` gives trailing columns an extra row; **n=2 stacks** (1 col × 2 rows) (`tdesktop.cpp:127-235`); **supersedes AR-87**'s `ceil(sqrt)` floor-grid and rewrites ST-11; (b) keep the AR-87 preset (rejected — fidelity) | **(a) TV-exact tile; supersedes AR-87** | ✅ Resolved (user) |
| AR-91 | RD-10 · Left-grow resize gesture | Wire the bottom-left grip to a functional resize? | (a) **left-grow resize (TV `dmDragGrowLeft`)** — the bottom-left grip `└─` (already drawn, commit `1caa188`) resizes by moving the **left + bottom** edges while anchoring the right edge; add a left-resize hit-zone in `frameZoneAt` + a new gesture mode in `gestures.ts`; Must-Have; (b) leave the grip decorative (rejected by the user — all four Must) | **(a) functional left-grow resize** | ✅ Resolved (user) |
| AR-92 | RD-10 · RD placement | Where does this fidelity work sit, given RD-06…RD-09 are reserved for the widget tiers? | (a) **a dedicated RD-10 "Turbo Vision behavioral fidelity" RD** after the reserved tiers; numbering does not force execution order (the roadmap note flags it can run next); no renumbering; (b) take RD-06 and shift the widget tiers up (rejected: rewrites many README/roadmap references); (c) maintenance tasks `T-NN` (rejected: multi-phase + touches spec oracles, the user asked for requirements) | **(a) dedicated RD-10** | ✅ Resolved (user) |
| AR-93 | RD-06 · Scope split | Does RD-06 hold all of Phase-1's controls, or a focused core? | (a) **split — RD-06 = the leaf controls + validators** (`Text`/`Label`/`Button`/`Input`/`CheckGroup`/`RadioGroup` + internal `Cluster` base + the validator model), and **`ScrollBar`/`Scroller`/`ListView`/`Dialog` → a sibling `RD-11`** (containers/scrolling/lists) **stubbed now, authored later** — keeps RD-06 small and demoable in a plain `Window`; RD-11's `Dialog` depends on RD-06's controls existing so authoring it after avoids churn; (b) one big RD-06 (all of Phase 1) — rejected by the user (a 40+-task monster plan, long red→green) | **(a) split; RD-11 sibling stubbed now** | ✅ Resolved (user) |
| AR-94 | RD-06 · Input depth | How much of `TInputLine` ships in RD-06's `Input`? | (a) **lean** — single-line edit, cursor, horizontal scroll-within-field, `maxLength`, two-way `value` signal, validator hook; **text selection + cut/copy/paste clipboard DEFERRED (tracked → RD-07)** — a self-contained add-on that doesn't reshape `Input`; (b) full `TInputLine` (incl. selection + app-clipboard) now — heavier, against the focused split | **(a) lean; selection+clipboard deferred (tracked)** | ✅ Resolved (user) |
| AR-95 | RD-06 · Validator set | Which `TValidator` units ship in RD-06? | (a) **core three** — `filter(chars)` (`TFilterValidator`), `range(min,max)` (`TRangeValidator`), `lookup(list)` (`TLookup`/`TStringLookupValidator`); **`picture(mask)` (`TPXPictureValidator`, the mask mini-DSL — M effort) DEFERRED (tracked → RD-07)**; (b) all four now — picture/mask DSL complexity in RD-06 | **(a) core three; picture/mask deferred (tracked)** | ✅ Resolved (user) |
| AR-96 | RD-06 · Cluster controls | Which checkbox/radio cluster controls ship? | (a) **`CheckGroup` + `RadioGroup`** over an internal `Cluster` base (`TCheckBoxes`/`TRadioButtons`); **`MultiCheckGroup` (`TMultiCheckBoxes`, multi-bit) DEFERRED (tracked → RD-07)** — the component map marks it lower-priority; the two common clusters cover ~all forms; (b) add `MultiCheckGroup` too — slightly more surface | **(a) Check+Radio; MultiCheck deferred (tracked)** | ✅ Resolved (user) |
| AR-97 | RD-06 · Control theme roles | How are faithful TV control colors modeled, given core `Theme` has `button`/`buttonFocused`/`dialog` but no `input`/`label`/`cluster` roles? | (a) **add the faithful `cpGrayDialog` roles the RD-06 controls use to core `@jsvision/core` `Theme`** — `staticText` (slot 6), `label`/`labelSelected`/`labelShortcut` (7–9), `buttonNormal`/`buttonDefault`/`buttonSelected`/`buttonDisabled`/`buttonShortcut` (10–14), `clusterNormal`/`clusterSelected`/`clusterShortcut`/`clusterDisabled` (16–18,31), `inputNormal`/`inputSelected`/`inputArrows` (19–21) — additive cross-package edits (same pattern as `windowInactive`/`statusSelected`); ScrollBar (4–5)/ListViewer (26–29) roles → RD-11, History (22–25) → RD-07; (b) minimal new roles (`input`/`label`/`cluster` only), reuse `button`/`buttonFocused` — defers the state variants; (c) reuse existing roles, no new ones — rejected: violates the TV-fidelity directive for control colors | **(a) faithful cpGrayDialog control roles on core `Theme`** | ✅ Resolved (user) |
| AR-98 | RD-06 · Demo vehicle | What proves RD-06, with no `Dialog`/`ListView` yet (those are RD-11)? | (a) **a headless `demo:controls` walkthrough** — `Label`+`Input` (with a `filter`/`range` validator rejecting bad input) + `CheckGroup` + `RadioGroup` + `Button` in a `Window`, dispatch-driven, an ASCII frame per interaction (matches `demo:view`/`demo:events`/`demo:shell`); needs no `Dialog`; (b) defer the demo to RD-11's `tvforms` clone — rejected: no immediate RD-06 visual proof | **(a) headless `demo:controls`** | ✅ Resolved (user) |
| AR-99 | RD-06 · Deferred-scope tracking | Where are RD-06's deferred items registered so they are not lost? | (a) **an explicit "Deferred (tracked)" table in RD-06** listing each item (Input selection+clipboard · `picture`/mask validator · `MultiCheckGroup`) with its **target RD (RD-07 control completions)**, plus a roadmap note so they surface there — durable in the requirements set + visible on the roadmap; (b) a dedicated `requirements/DEFERRED.md` register — rejected: a new doc convention to maintain alongside the RD Won't-Have sections | **(a) per-RD "Deferred (tracked)" table + roadmap note; PLUS a consolidated `DEFERRED.md` index** (added 2026-06-30 at the user's request — the per-RD tables stay authoritative, `DEFERRED.md` aggregates all deferrals feature-wide with stable `DEF-NN` ids so none are lost) | ✅ Resolved (user; amended 2026-06-30) |
| AR-100 | RD-06 · Binding model | How do controls expose their value? | (a) **two-way signal binding** — `Input.value: Signal<string>`, `CheckGroup`/`RadioGroup` bound to selection signals; reads/writes flow through the signal (the disciplined-hybrid model, AR-09; component map "two-way signal `value`") (dominant); (b) callback getter/setter pairs — rejected: diverges from the established reactive model | **(a) two-way signal binding** | ✅ Resolved (dominant) |
| AR-101 | RD-06 · Validator firing semantics | When does each validator fire? | (a) **TV `TValidator` semantics** — `filter` is a *transient* validator that rejects disallowed keystrokes live (per char); `range`/`lookup` are *blocking* validators checked on completion / focus-leave (`isValidInput` vs `isValid`/`valid()`); the TV-fidelity directive makes `TValidator`/`TInputLine::checkValid` the oracle (source-determined); (b) validate everything on every keystroke — rejected: not TV-faithful, fights numeric range entry | **(a) TV transient-vs-blocking firing** | ✅ Resolved (source-determined) |
| AR-102 | RD-06 · Package layout | Where do the controls live + how re-exported? | (a) **a new `packages/ui/src/controls/` subsystem** (one barrel `index.ts`; per-control files ≤ 500 lines) with **explicit named re-exports** from `src/index.ts`, validators under `controls/validators/` — inherits the established per-subsystem layout + the layout-convention re-export rule (dominant); (b) scatter controls across existing subsystem dirs — rejected: breaks the one-subsystem-dir pattern | **(a) `src/controls/` subsystem, explicit named re-exports** | ✅ Resolved (dominant) |
| AR-103 | RD-11 · ListView columns | How many columns does the base `ListView` support, given TV `TListViewer` has `numCols`? | (a) **single-column base now** — one vertical virtual-scroll list; the multi-column grid → the RD-07 `Table`/`DataGrid` (component map reserves "a real table" for RD-07); keeps RD-11 focused and covers the navigator + all common list needs; (b) port TV's full `numCols` multi-column layout in RD-11 — rejected: larger scope, overlaps the RD-07 `Table` | **(a) single-column base; multi-column → RD-07** | ✅ Resolved (user) |
| AR-104 | RD-11 · List search/sort | Does `TSortedListBox`'s sorted order + type-ahead search ship in RD-11? | (a) **ship sorted + type-ahead as `ListView` options now** (`sorted?`/`typeAhead?`, off by default; prefix search jumps focus) — small, "an option on `ListView`, not a new class" (component map §116), directly useful for the kitchen-sink navigator; (b) defer to RD-07 — rejected: cheap here and the navigator wants filter-as-you-type | **(a) ship sorted + type-ahead as options** | ✅ Resolved (user) |
| AR-105 | RD-11 · Scroller↔ScrollBar wiring | How does `Scroller` obtain its scrollbars, given TV `TScroller` takes two injected `TScrollBar*`? | (a) **`Scroller` auto-creates & owns** its bar(s) via a `scrollbars:'vertical'\|'horizontal'\|'both'\|'none'` option, wiring `value`↔delta + sizing `max` to the content extent; an escape hatch may inject/hide — modern DX, drawing stays TV-exact (fidelity governs pixels, not construction); (b) TV-style caller-injected `TScrollBar`s — rejected: boilerplate, no fidelity gain (the API isn't drawn) | **(a) auto-create & own; inject/hide escape hatch** | ✅ Resolved (user) |
| AR-106 | RD-11 · List data model | How do the list controls model data + row rendering? | (a) **generic `ListView<T>` base** — `items: Signal<T[]>` + `getText(item)=>string` + `focused: Signal<number>` (+ optional `selected`); `ListBox` = the `ListView<string>` preset with identity `getText`; matches the map ("base + collection variant") and the AR-100 two-way-signal model; (b) string-only `ListBox` — rejected: forces callers to pre-map objects, loses reuse for `ComboBox`/tables | **(a) generic base + `ListBox` string preset** | ✅ Resolved (user) |
| AR-107 | RD-11 · Dialog modality | What modality does `Dialog` support, given TV `TDialog` is both modal and modeless? | (a) **both** — modal via RD-04 `execView`/`endModal` (primary), modeless via `desktop.add`; `Dialog` is-a `Window` so modeless is intrinsic (near-free); matches `TDialog`; (b) modal-only now — rejected: deferring modeless saves almost nothing given the `Window` base | **(a) both modal + modeless** | ✅ Resolved (user) |
| AR-108 | RD-11 · Dialog result + DEF-16 close-gate | How does a `Dialog` return its result and enforce validity on close? | (a) **terminating command + bound signals** — `execView` resolves to the closing `Commands.*`; data lives in the controls' bound signals (AR-100), no separate result object; a **positive** close (OK/Yes) first runs a child `valid()` sweep (TV `TDialog::valid`→`TGroup::valid`) that vetoes on any blocking-invalid field and refocuses it, **realizing DEF-16**; negative close (Cancel/Esc) skips the sweep; (b) a bespoke `{command,data}` result via `getData`/`setData` — rejected: duplicates the bound signals, diverges from the reactive model | **(a) terminating command + `valid()` sweep (realizes DEF-16)** | ✅ Resolved (user) |
| AR-109 | RD-11 · Standard-button helpers | Does RD-11 ship standard dialog-button helpers, and which set? | (a) **`ok`/`cancel`/`yes`/`no` helpers** (`okButton()`/`cancelButton()`/`yesButton()`/`noButton()` + `yesNoButtons()`/`okCancelButtons()`), each an RD-06 `Button` preset emitting the matching terminating command; adds `ok`/`cancel`/`yes`/`no` to the RD-05 `Commands` set (additive) — the TV standard set, ergonomic + DRY; (b) no helpers, raw `Button` — rejected: every dialog re-implements the same OK/Cancel wiring | **(a) OK/Cancel/Yes/No helpers + `Commands` constants** | ✅ Resolved (user) |
| AR-110 | RD-11 · Kitchen-sink Navigator upgrade | Is the dogfooding navigator-sidebar upgrade part of RD-11? | (a) **yes — an RD-11 AC**: rebuild the showcase navigator from a menu into a persistent `ListView`-in-`Scroller` sidebar (the showcase demoing the component it is built from — the kitchen-sink dogfooding rule; "only `shell.ts` changes"), plus a story per new component; the strongest live proof of RD-11; (b) components + stories only, navigator later — rejected: defers the marquee dogfooding demo the CLAUDE.md Navigator seam was designed for | **(a) include the Navigator sidebar upgrade** | ✅ Resolved (user) |
| AR-111 | RD-11 · ScrollBar binding model | How does `ScrollBar` bind + configure? | (a) **two-way `value: Signal<number>` + `{min,max,pageStep,arrowStep}`** (TV `value`/`minVal`/`maxVal`/`pgStep`/`arStep`), vertical + horizontal; arrow-click steps `arrowStep`, page-click pages `pageStep`, thumb-drag sets proportionally via the RD-05 capture seam — the AR-100 two-way-signal model applied to position; (b) callback getter/setter — rejected: diverges from the reactive model (dominant) | **(a) two-way `value` signal + range/step config** | ✅ Resolved (dominant) |
| AR-112 | RD-11 · Theme roles | How are the ScrollBar/ListViewer colors modeled? | (a) **additive `cpGrayDialog` roles on core `Theme`** — `scrollBarPage`/`scrollBarControls` (`cpScrollBar` slots 4–5) + `listNormal`/`listFocused`/`listSelected`/`listDivider` (ListViewer slots 26–29), decoded from `cpAppColor`; the same additive non-breaking pattern as `windowInactive`/`statusSelected`/the RD-06 control roles (AR-97); the RD-11 stub already reserved these slots; (b) reuse existing roles — rejected: violates the TV-fidelity directive for control colors (dominant) | **(a) additive `cpScrollBar` + ListViewer core roles** | ✅ Resolved (dominant) |
| AR-113 | RD-11 · Package layout | Where do the container/list/dialog components live + how re-exported? | (a) **`packages/ui/src/{scroll,list,dialog}/`** — three new subsystem dirs (one per concern, the established RD-05 dir-per-concern pattern), one barrel `index.ts` each, per-file ≤ 500 lines, **explicit named re-exports** from `src/index.ts` (the layout-convention rule, AR-81/AR-102); (b) one combined dir / scatter into existing dirs — rejected: breaks the one-subsystem-per-concern pattern (dominant) | **(a) `src/{scroll,list,dialog}/` subsystems, explicit named re-exports** | ✅ Resolved (dominant) |
| AR-114 | RD-11 · Demo vehicle | What proves RD-11? | (a) **a headless `demo:containers` walkthrough** (dispatch-driven, an ASCII frame per step, matching `demo:controls`): a keyboard-navigated scrollable list, a `Scroller` revealing oversized content, a modal `Dialog` with an invalid field vetoing OK then passing — **plus** a kitchen-sink story per new visual component (`ScrollBar`/`Scroller`/`ListView`/`Dialog`); reuses the established demo + showcase pattern (AR-98 + the kitchen-sink gate); (b) defer to a `tvforms`/`palette` clone — rejected: no immediate headless proof (dominant) | **(a) headless `demo:containers` + per-component stories** | ✅ Resolved (dominant) |
| AR-115 | RD-07 · Scope split | Does RD-07 hold the whole roadmapped "high-value controls" bucket, or a focused slice? | (a) **thin control-completions slice** — RD-07 = the RD-06 carry-overs + caret (DEF-01 Input selection+clipboard · DEF-02 `picture(mask)` · DEF-03 `MultiCheckGroup` · DEF-19 visible caret); the compound/heavy controls (`History`/`ComboBox`/`Tree`/`Tabs`/`Table`/`Progress`/`Surface`) split out to future sibling RDs (RD-12+), mirroring the RD-06→RD-11 split (AR-93) — tight cohesion (all finish the leaf-control tier), lowest risk (no new overlay/virtual-scroll/multi-column infra); (b) one big RD-07 over the full Tier-2 bucket — rejected: ~10 components S→L across four mechanisms, unplannable, breaks the AR-93 cohesion precedent; (c) completions + Progress/Spinner/Surface grab-bag — considered, dropped (bundles three unrelated themes, weaker cohesion) | **(a) thin control-completions slice; heavy controls → RD-12+** | ✅ Resolved (user) |
| AR-116 | RD-07 · Input selection scope | How much of `TInputLine` selection ships? | (a) **full-faithful** — Shift+Left/Right (grapheme) + Ctrl+Shift+Left/Right (word, `prevWord`/`nextWord`) + Shift+Home/End + mouse-drag + select-all (Ctrl+A); a plain motion collapses the range; Backspace/Delete/typing over a non-empty selection deletes it first (`deleteSelect`), per `tinputli.cpp:203-235,325-372`; (b) lean — Shift+arrows + Shift+Home/End + mouse-drag only, no word-select/select-all (rejected: diverges from the original); (c) mouse-only (rejected: too thin for form editing) | **(a) full-faithful selection** | ✅ Resolved (user) |
| AR-117 | RD-07 · Clipboard model | How do cut/copy/paste work, and on which keys? | (a) **system clipboard** — copy/cut → core `setClipboard(text,caps)` OSC-52 write (caps-gated no-op, matches the modern fork's `TClipboard::setText`, `tinputli.cpp:478`); paste → consume the existing bracketed `PasteEvent` (already routed to the focused view, dispatch.ts:12), replacing selection + filtered by `validator.isValidInput`/`maxLength`; keys Ctrl+Ins/Shift+Ins/Shift+Del (TV-faithful, SIGINT-safe) + additive `Commands.cut`/`copy`/`paste`; app-initiated OSC-52 **read** deferred (terminals block it); (b) internal app-clipboard only — rejected: an island, ignores the OSC-52/paste infra core already ships; (c) same model on Ctrl+C/X/V — considered, dropped (Ctrl+C collides with interrupt/quit in a TUI) | **(a) system clipboard (OSC-52 write) + bracketed paste; Ins/Del chords + commands** | ✅ Resolved (user) |
| AR-118 | RD-07 · Insert/overwrite mode | Include TV's `Ins`/`sfCursorIns` overwrite mode? | (a) **defer (insert-only)** — ship insert-only editing; track overwrite + `Ins` toggle as `DEF-20`; overwrite is rarely used in forms and couples to caret-shape, keeping the completion slice lean; (b) include overwrite mode now (`tinputli.cpp` `sfCursorIns`) — faithful + small but adds caret-shape coupling + edge cases to this RD | **(a) defer overwrite (insert-only); `DEF-20`** | ✅ Resolved (user) |
| AR-119 | RD-07 · picture(mask) scope | How much of `TPXPictureValidator` does `picture()` implement? | (a) **full DSL + autoFill** — all specials `# ? & ! @ * { } [ ] , ;` via the recursive `process`/`scan`/`group`/`iteration`/`checkComplete` machine + optional `autoFill`, faithfully ported from `tvalidat.cpp` (state machine `:264-517`, `autoFill` `:572-585`, `syntaxCheck` `:519`), bounds-safe (`syntaxCheck` rejects malformed masks; bounded recursion/indexing); phone/date/currency masks need groups+repeat+autoFill; (b) subset (literals + `#`/`?`/`@`, fixed-length, no groups/repeat/autoFill) — rejected: a materially weaker, non-faithful validator | **(a) full TPXPictureValidator DSL + autoFill** | ✅ Resolved (user) |
| AR-120 | RD-07 · MultiCheckGroup binding | How are the per-item multi-state values bound? | (a) **idiomatic `Signal<number[]>`** — one state index per item (`0..states.length-1`), a `states` marker string, Space/click cycles an item `(state+1)%len` (`tmulchkb.cpp:86-101`); the **visual stays TV-faithful** (`" [ ] "` box + `states[state]` marker via `drawMultiBox`, `tcluster.cpp:87`) using the RD-06 `cluster*` roles; matches the AR-100 two-way-signal model; (b) faithful packed `uint32` bitfield + `flags` (bits/mask) per `tmulchkb.cpp` — rejected: a packed-int API is un-idiomatic + error-prone in TS (behavior may modernize where the visual stays faithful, per the directive) | **(a) idiomatic `Signal<number[]>` + faithful visual** | ✅ Resolved (user) |
| AR-121 | RD-07 · Visible caret depth | How deep does the caret (DEF-19) go? | (a) **logical + real hardware caret** — mark the logical caret cell at `curPos−firstPos+1` (`tinputli.cpp:160`) AND add a minimal additive `View`→host caret seam (`View` reports a view-local desired caret → `RenderRoot` translates to an absolute cell → the `EventLoop` carries it beside `onFrame` → `run()`/host positions the real cursor via core `cursor.show()`/`to()`), hidden when no focused view requests one; mirrors the additive RD-05 `onFrame`/`setCapture` seams; (b) logical caret only — no real terminal cursor (rejected: the OS caret still doesn't blink where you type); (c) both deferred (rejected: a field with no visible caret is the DEF-19 defect) | **(a) logical + hardware caret via additive seam** | ✅ Resolved (user) |
| AR-122 | RD-07 · Selection theme role | How is the selection highlight colored? | (a) **an additive faithful `Input` selection role** on core `Theme`+`defaultTheme`, decoded from `TInputLine::draw`'s `getColor` chain at plan GATE-1 (`tinputli.cpp:145-156`) — the same additive non-breaking pattern as the RD-06 control roles (AR-97); (b) reuse an existing role — rejected: violates the TV-fidelity directive for control colors (source-determined) | **(a) additive faithful selection role (decoded at GATE-1)** | ✅ Resolved (source-determined) |
| AR-123 | RD-07 · Package layout | Where do the completions live + how re-exported? | (a) **extend the existing `packages/ui/src/controls/` subsystem** (no new dir — the AR-102 convention): edit `input.ts`, add `controls/validators/picture.ts` + `controls/multi-check-group.ts`, explicit named re-exports for `picture`/`MultiCheckGroup` from `src/index.ts`; the caret seam is additive across `view/`/`event/`/host; (b) a new subsystem dir — rejected: these are completions of existing `controls/`, not a new concern (dominant, AR-102) | **(a) extend `src/controls/`; additive caret seam** | ✅ Resolved (dominant) |
| AR-124 | RD-07 · Demo vehicle | What proves RD-07? | (a) **a `MultiCheckGroup` kitchen-sink story** (the one new visual component; headless smoke test) + an **extended `Input` story / `demo:controls`** walkthrough showing selection→copy→paste, a `picture` field, and the visible caret — reuses the AR-98 demo pattern + the NON-NEGOTIABLE kitchen-sink gate; (b) a new dedicated demo only — rejected: the completions belong on the existing controls demo/story (dominant) | **(a) `MultiCheckGroup` story + extended `Input` story/`demo:controls`** | ✅ Resolved (dominant) |
| AR-125 | RD-12+ · Set scope | Does the high-value-controls bucket get drafted as one focused slice or the full sibling set now? | (a) **full set of sibling RDs now** — draft all ~6 high-value-control RDs up front (dependency-aware, coherent architecture) with MVP-vs-later phasing marked inside each; mirrors the RD-06→RD-11 cohesive-family drafting; avoids inter-sibling drift; (b) MVP slice first (only the tvdemo-clone controls), rest later — rejected: risks re-slicing when deferred siblings land; (c) one RD to start — rejected: weakest cross-control coherence | **(a) full sibling set now, MVP-phased inside** | ✅ Resolved (user) |
| AR-126 | RD-12+ · Slicing | How is the bucket grouped into RDs? | (a) **by mechanism (~6 RDs)** — Input-dropdowns (`History`+`ComboBox`) · `Tree` · `Table`/`DataGrid` (+RD-11's deferred multi-column `numCols`) · `Tabs` · Feedback (`ProgressBar`+`Spinner`) · `Surface`(+`SurfaceView`); each RD = one shared mechanism, mirroring RD-06/07/11; (b) one RD per control (~7) — rejected: `History`+`ComboBox` duplicate the shared dropdown spec; (c) fewer broader RDs (~3) — rejected: mixes unrelated mechanisms in one RD | **(a) by mechanism (~6 sibling RDs)** | ✅ Resolved (user) |
| AR-127 | RD-12+ · tvdemo north-star | Is cloning `tvdemo` a binding acceptance target or aspirational? | (a) **aspirational north-star** — the demos guide design + prove fidelity, but each RD's acceptance criteria stay per-control (TV draw/geometry/behavior parity + kitchen-sink story); a tvdemo clone is a stretch goal, not a gate; (b) binding acceptance target — rejected: forces cross-control integration + a full clone into the set's DoD, materially larger scope | **(a) aspirational north-star; per-control TV-parity + story acceptance** | ✅ Resolved (user) |
| AR-128 | RD-12+ · Numbering | What RD numbers do the 6 sibling RDs take? | (a) **contiguous RD-14…RD-19** — a clean sequential block after RD-13; simplest to read (RD-08/09 stay reserved for Editor/Files, RD-12 becomes an unused gap); (b) RD-12 then RD-14…RD-18 — rejected: honors the roadmap's reserved RD-12 slot but reads non-contiguously (12→14 around runtime-hardening's RD-13) | **(a) contiguous RD-14…RD-19** | ✅ Resolved (user) |
| AR-129 | RD-12+ · MVP phasing | Which controls are the MVP (implement-first) phase inside the set? | (a) **interactive stars first** — MVP = `Input`-dropdowns (RD-14) + `Tree` (RD-15) + `Table`/`DataGrid` (RD-16); Later = `Tabs` (RD-17) + Feedback (RD-18) + `Surface` (RD-19); front-loads the highest-value, tvdemo-relevant, hardest work; (b) cheap wins first — rejected: defers the marquee widgets; (c) no phasing — rejected: loses prioritization guidance for the plan step | **(a) interactive stars first (RD-14/15/16 MVP)** | ✅ Resolved (user) |
| AR-130 | RD-14 · History store | How does `History` model its stored entries (TV = global bounded ring buffer keyed by `historyId`, `histlist.cpp`)? | (a) **faithful global by-id store + signal escape hatch** — a bounded module-singleton MRU string store keyed by `historyId` (`historyAdd`/`historyStr`/`historyCount`; skip-empty, dedup, evict-oldest-when-full) so inputs sharing an id share history exactly like TV, PLUS an optional injectable `history: Signal<string[]>` override (the AR-105 auto-own + escape-hatch pattern); (b) per-instance `Signal<string[]>` only — rejected: drops TV by-id cross-field sharing + the zero-config default; (c) both, no default — rejected: loses the zero-config TV behaviour | **(a) faithful global by-id store + signal escape hatch** | ✅ Resolved (user) |
| AR-131 | RD-14 · ComboBox mode | ComboBox is new (no TV counterpart) — which mode(s)? | (a) **both via `editable?: boolean` (default `true`)** — editable = `Input` + dropdown (free text + filter, the component-map "Input + ListView"); select-only = a field/button that only picks from the list; covers the full modern combobox surface; (b) editable-only — rejected: drops the very common pure-picker; (c) select-only — rejected: contradicts the component map's "Input + ListView" intent | **(a) both modes, `editable` default true** | ✅ Resolved (user) |
| AR-132 | RD-14 · Popup mechanism | What hosts the dropdown popup (TV `THistoryWindow` = modal `execView`, dismiss on outside-click)? | (a) **non-modal anchored overlay** — extract a shared internal anchored-popup primitive from the RD-05 menu overlay+outside-click-catcher (`menu/controller.ts`): the list mounts top-z, dismisses on outside-click/Esc/focus-loss, non-blocking; **drawing stays TV-faithful** (directive governs pixels not construction, AR-105); shared by History+ComboBox; (b) faithful `execView` modal — rejected: a modal dropdown blocks the rest of the UI (un-combobox-like), though most faithful to construction | **(a) non-modal anchored overlay (generalize the menu popup)** | ✅ Resolved (user) |
| AR-133 | RD-14 · Package layout | Where do these controls live + how re-exported? | (a) **new `packages/ui/src/dropdown/` subsystem** — `History` + `ComboBox` + the shared anchored-popup primitive; one barrel `index.ts`, per-file ≤500 lines, explicit named re-exports from `src/index.ts` (the AR-102/113 dir-per-concern convention); a cohesive new concern with its own popup mechanism, sibling to `menu/`; (b) extend `src/controls/` — rejected: dropdowns are a compound concern (popup mechanism), not a leaf widget like Button/Input | **(a) new `src/dropdown/` subsystem, explicit named re-exports** | ✅ Resolved (user) |
| AR-134 | RD-14 · Combo filtering | In editable mode, how does typing interact with the dropdown? | (a) **filter-as-you-type (autocomplete), default on** — typing narrows the dropdown to matching items via an overridable predicate (default case-insensitive substring); select-only mode instead uses TV's type-ahead position-jump (AR-104); reuses the RD-11 list infra; (b) type-ahead position-jump only — rejected: weaker for an editable combobox though more TV-ListBox-faithful; (c) no filtering — rejected: an editable combobox without filtering is barely a combobox | **(a) filter-as-you-type (default on); select-only uses type-ahead jump** | ✅ Resolved (user) |
| AR-135 | RD-14 · Open keys | Which keys open the dropdown? | (a) **faithful + Alt+Down** — TV's Down-arrow-while-focused + click on the `▐↓▌` button (`thistory.cpp`), PLUS Alt+Down (the ubiquitous modern open-combobox chord — the extra-behaviour the directive permits since it doesn't alter TV drawing); (b) faithful only — rejected: omits the modern Alt+Down affordance | **(a) faithful (Down / button-click) + Alt+Down** | ✅ Resolved (user) |
| AR-136 | RD-14 · Data model | How does ComboBox model data + binding? | (a) **generic `ComboBox<T>`** — `items: Signal<T[]>` + `getText: (item)=>string` + a two-way bound `value` (the selected `T`, or the text in editable mode), mirroring RD-11 `ListView<T>` (AR-106) + the AR-100 two-way-signal model; `History` is string-only (TV historyStr); (b) string-only ComboBox — rejected: forces callers to pre-map objects, loses reuse | **(a) generic `ComboBox<T>` (AR-106 model)** | ✅ Resolved (dominant) |
| AR-137 | RD-14 · Shared primitive | Do History + ComboBox share one popup implementation? | (a) **yes — one internal shared anchored-popup primitive** (DRY, coding-standards) built on the AR-132 overlay + RD-11 `ListView`: anchor rect → clamped placement, dismissal, Enter/Esc/click routing; both controls compose it; (b) each control implements its own popup — rejected: duplicates the popup + dismissal logic | **(a) one shared anchored-popup primitive** | ✅ Resolved (dominant) |
| AR-138 | RD-14 · Geometry + selection | Popup geometry + on-pick behaviour? | (a) **faithful** — popup rect = anchor grown ±1 wide, default **7 rows** tall (`thistory.cpp`), clamped to the host extent, scrolls when items overflow (configurable `maxRows`); on pick, History **replaces the input text + `selectAll`** (`thistory.cpp` cmOK path), ComboBox **sets the bound `value`**; Esc/outside-click cancels (no change); (b) adaptive/other geometry — rejected: diverges from the decoded TV rect | **(a) faithful geometry (7-row default) + TV pick behaviour** | ✅ Resolved (source-determined) |
| AR-139 | RD-14 · Theme roles | How are the dropdown colours modelled? | (a) **additive faithful roles on core `Theme`** for the History button (`cpHistory`), the popup window (`cpHistoryWindow`), and the viewer list (`cpHistoryViewer`) — decoded through the `getColor` chain at **plan GATE-1**; the same additive non-breaking pattern as the RD-06/07/11 control roles (AR-97/112/122); ComboBox reuses the `input*`/`list*` roles + the button; (b) reuse existing roles — rejected: violates the TV-fidelity directive for control colours | **(a) additive faithful roles, decoded at plan GATE-1** | ✅ Resolved (source-determined) |
| AR-140 | RD-14 · Demo vehicle | What proves RD-14? | (a) **kitchen-sink stories** for `History` (an Input + `▐↓▌` history button) and `ComboBox` (editable + select-only) + a headless **`demo:dropdowns`** walkthrough (open → filter/type-ahead → pick, ASCII frame per step, matching `demo:controls`/`demo:containers`) — the NON-NEGOTIABLE kitchen-sink gate + the AR-98/114 demo pattern; (b) a dedicated live demo only — rejected: the controls belong in the showcase + a headless walkthrough for CI proof | **(a) kitchen-sink stories + headless `demo:dropdowns`** | ✅ Resolved (dominant) |
| AR-141 | RD-15 · Node model | How does `Tree` model nodes (TV `TNode{text,childList,next,expanded}` behind data-model-agnostic accessors)? | (a) **concrete reactive `TreeNode<T>` (plain data)** — `TreeNode<T> = { value: T, children: TreeNode<T>[], … }` + a `getText`, mirroring `ListView<T>`'s items model (AR-106); the view **owns expand state reactively** (a Set keyed by node identity) so user data stays plain; an abstract `TreeModel` escape hatch may come later; (b) abstract `TreeModel` accessor interface (TV-faithful getRoot/getChild/getNext/getText/hasChildren/isExpanded over any backing store) — rejected for v1: heavier DX + unusual for TS, though more powerful/reusable | **(a) concrete reactive `TreeNode<T>`; view-owned expand state** | ✅ Resolved (user) |
| AR-142 | RD-15 · Arrow keys | How do ←→ behave (TV maps ←→ to up/down focus, not expand; only `+`/`-`/`*` expand)? | (a) **modern override — ←=collapse (or to-parent) / →=expand (or descend)**, while keeping TV's faithful `+`/`-`/`*` + ↑↓ nav; a behavioural extension the directive permits (glyphs stay 100% faithful, cf. RD-14 Alt+Down AR-135); (b) faithful ←→ = up/down nav — rejected: Left/Right doing nothing tree-specific is a real usability gap | **(a) modern ←collapse/→expand + faithful `+`/`-`/`*`** | ✅ Resolved (user) |
| AR-143 | RD-15 · Children loading | When are child nodes loaded? | (a) **eager (whole tree in memory) now; lazy deferred** — the full node tree is provided up front and flattened reactively on expand/collapse (faithful to TV in-memory + our reactive model); lazy-load-on-expand (async provider) tracked as a deferred enhancement; (b) lazy-on-expand now — rejected: adds async state + loading UI to the first Tree (more scope) | **(a) eager now; lazy deferred** | ✅ Resolved (user) |
| AR-144 | RD-15 · Selection model | What selection does the Tree support? | (a) **single-select (faithful) now; checkbox/multi-select deferred** — one focused + one selected node (Enter/double-click emits a select command, mirroring `ListView`/`cmOutlineItemSelected`), faithful to TV's single-select outline; a checkbox/multi-select tree tracked as a deferred enhancement; (b) add checkbox/multi-select now — rejected: a whole state model + rendering on the first Tree (more scope) | **(a) single-select faithful; checkbox/multi-select deferred** | ✅ Resolved (user) |
| AR-145 | RD-15 · Build strategy | Does the Tree reuse the RD-11 `ListView` renderer or its own? | (a) **reuse the RD-11 virtual-scroll helpers (`virtual.ts` `clampIndex`/`keepVisible`) + own a `ScrollBar` (like `ListView`), with a Tree-specific row renderer** — `ListRows` draws one role per row at col 1 with ←→ ignored (`list/list-rows.ts`), so it can't express the `│├└─+` graph prefix, the collapsed-text two-tone colour, or the graph-click expand zone; TV made `TOutlineViewer` its own `TScroller` subclass for exactly this; (b) force it through `ListView<TreeNode>` with `getText`=graph+text — rejected: loses the two-tone collapsed colour + the graph-click expand zone (fidelity) | **(a) reuse scroll helpers; Tree-specific row renderer** | ✅ Resolved (dominant) |
| AR-146 | RD-15 · Graph glyphs | What tree-line glyphs + expand markers? | (a) **faithful** `graphChars = "\x20\xB3\xC3\xC0\xC4\xC4+\xC4"` (`toutline.cpp:367`) = space · `│` · `├` · `└` · `─` · `─` · **`+`** (collapsed-with-children) · **`─`** (expanded) — **no `[+]`/`[-]` brackets**; collapsed-node **text** drawn in the *not-expanded* colour (`color >> 8`, `drawTree`); indent (`levelWidth`) + end (`endWidth`) widths + exact `getColor` chain decoded at **plan GATE-1**; (b) redesigned glyphs — rejected: violates the TV-fidelity directive | **(a) faithful `│├└─`+`+`/`─` glyphs + two-tone collapsed text** | ✅ Resolved (source-determined) |
| AR-147 | RD-15 · Selection binding | How is focus/selection bound + emitted? | (a) **mirror `ListView`** — a `focused: Signal<number>` (flattened-visible index) + a `selected`/`onSelect`/`command` seam; Enter/double-click emits the select command (TV `cmOutlineItemSelected`=301); the AR-100 two-way-signal + AR-106 model; (b) bespoke binding — rejected: diverges from the established list model | **(a) mirror `ListView` focused/selected/command** | ✅ Resolved (dominant) |
| AR-148 | RD-15 · Package layout | Where does the Tree live + how re-exported? | (a) **new `packages/ui/src/tree/` subsystem** (dir-per-concern, AR-133/113) — `tree.ts` (`Tree<T>` + `TreeNode<T>`), `graph.ts` (the faithful `createGraph` line-prefix builder), one barrel `index.ts`, per-file ≤500 lines, explicit named re-exports from `src/index.ts`; (b) put it under `list/` — rejected: it is its own `TScroller`-style concern, not a `ListView` | **(a) new `src/tree/` subsystem, explicit named re-exports** | ✅ Resolved (dominant) |
| AR-149 | RD-15 · Theme roles | How are the outline colours modelled? | (a) **additive faithful `cpOutlineViewer` roles** on core `Theme`+`defaultTheme` — outline normal/focus/select/notExpanded (`cpOutlineViewer "\x6\x7\x3\x8"`, `toutline.cpp:15`), decoded through the `getColor` chain at **plan GATE-1**; the same additive non-breaking pattern as the RD-06/07/11 control roles (AR-97/112/122/139); (b) reuse `list*` roles — rejected: violates the fidelity directive for control colours (the outline has its own palette) | **(a) additive faithful `cpOutlineViewer` roles at plan GATE-1** | ✅ Resolved (source-determined) |
| AR-150 | RD-15 · Demo vehicle | What proves RD-15? | (a) **a kitchen-sink `Tree` story** (an expandable outline with a visible focused/selected echo; ← collapse / → expand / +−* / click-to-toggle) + a headless **`demo:tree`** walkthrough (ASCII frame per step: expand → navigate → collapse → select), matching `demo:controls`/`demo:containers` + the NON-NEGOTIABLE kitchen-sink gate; (b) a live demo only — rejected: the Tree belongs in the showcase + a headless walkthrough for CI proof | **(a) kitchen-sink `Tree` story + headless `demo:tree`** | ✅ Resolved (dominant) |
| AR-151 | RD-16 · Grid shape | GATE-1 revealed TV has **no** table/grid/spreadsheet class — its only multi-column mechanism is `TListViewer::numCols`, a **newspaper-column flow of a single-field list** (`tlstview.cpp` `item = j*size.y+i+topItem`), not a heterogeneous-column table. What is RD-16? | (a) **a real `DataGrid<T>` as a documented TV-extension on the `TListViewer` virtual-scroll spine** — heterogeneous columns (different fields) + a header + per-column width; the drawing grounded in `TListViewer`'s decode (`│` divider `getColor(5)`, `cpListViewer` row colours, `showMarkers`), with the *header row + per-column accessors* the flagged extension (the "may extend behaviour; visuals must match" rule, cf. reactive/truecolor/async modality); this is what RD-11 AR-104 meant by "a real table"; (b) faithful `numCols` newspaper-flow only — rejected: 100% faithful but is **not** a table (no header, no heterogeneous columns) and isn't what AR-104 deferred; (c) both in one RD — rejected: two very different layouts (column-major single-field vs. row-spanning multi-field) bloats one RD | **(a) real `DataGrid<T>`, TV-extension on the `TListViewer` spine** | ✅ Resolved (user) |
| AR-152 | RD-16 · Column API | How are a `DataGrid`'s columns defined? | (a) **typed `Column<T>[]` descriptors** — `DataGrid<T>` generic over a row type; each column `{ title, accessor: (row: T) => string, width, align? }`, mirroring `ListView<T>`'s `getText` model (AR-106); type-safe, composes with a reactive `Signal<T[]>`; cell text is sanitized at the injection boundary; (b) string-matrix `rows: string[][]` + `headers: string[]` — rejected: loses type safety + reactive per-field binding, caller pre-formats every cell | **(a) typed `Column<T>[]` descriptors over `DataGrid<T>`** | ✅ Resolved (user) |
| AR-153 | RD-16 · Column sizing | How are column widths sized? | (a) **per-column `fixed \| fr \| auto`**, apportioned with the RD-02 layout engine's integer `solveTrack` (already built), with **horizontal scroll** (the `HScrollBar` `TListViewer` already wires) when the total exceeds the width; most flexible, reuses proven infra; (b) equal-width TV-style (`size.x/numCols`) — rejected: rigid, a `Size` column gets as much room as `Name`; (c) fixed cell widths only — rejected: no auto/fr, caller must pick every width | **(a) per-column fixed/fr/auto via RD-02 `solveTrack` + HScroll on overflow** | ✅ Resolved (user) |
| AR-154 | RD-16 · Header + sort | Header row and sorting? | (a) **a sticky (non-scrolling) header row of column titles + click-to-sort with a `▲`/`▼` indicator** (reuses RD-11's `sorted` computed); header colours grounded in a decoded role, sort a documented extension; (b) sticky header, no sort — rejected: click-to-sort is the defining table affordance and RD-11 already has the `sorted` machinery; (c) no header row — rejected: least table-like, loses the column labels | **(a) sticky header + click-to-sort `▲`/`▼`** | ✅ Resolved (user) |
| AR-155 | RD-16 · Selection granularity | Row- or cell-granular focus/selection? | (a) **row-granular** focus + selection — faithful to `TListViewer`'s item-focus (`focused`/`isSelected` are per-item, `tlstview.cpp`); one focused row, Enter/double-click selects (mirrors `ListView`); cell-granular selection deferred; (b) cell-granular — rejected: `TListViewer` has no cell cursor; a per-cell model is a separate mechanism and un-TV | **(a) row-granular (faithful); cell-select deferred** | ✅ Resolved (recommended-and-noted; user did not object) |
| AR-156 | RD-16 · Horizontal scroll | How do overflowing columns scroll? | (a) **the owned `HScrollBar`** — reuse the RD-11 `ScrollBar` + `TListViewer`'s already-wired `hScrollBar` (`indent = hScrollBar->value`, `tlstview.cpp:draw`) so columns wider than the viewport scroll horizontally; the vertical `ScrollBar` is owned exactly as `ListView` does; (b) clip/no horizontal scroll — rejected: real tables overflow width, TV itself wires the H-bar | **(a) owned `HScrollBar` (faithful) + owned `VScrollBar`** | ✅ Resolved (recommended-and-noted; user did not object) |
| AR-157 | RD-16 · Data binding | How is the row data bound? | (a) **`Signal<T[]>`** (reactive rows) mirroring `ListView<T>`'s `items` model (AR-106) — updates re-render the visible window; the AR-100 two-way-signal convention for `focused`/`selected`/sort state; (b) a static array — rejected: diverges from the established reactive list model | **(a) reactive `Signal<T[]>`, mirroring `ListView<T>`** | ✅ Resolved (dominant) |
| AR-158 | RD-16 · Sort mechanics | How does click-to-sort work? | (a) **reuse RD-11's `sorted` computed** — clicking a header sets a `sort: Signal<{col,dir}>`; the displayed order is a `computed` sort by that column, default **accessor-string compare** (locale-aware) with an **optional per-column `compare?: (a,b)=>number`** for typed/numeric columns; the `▲`/`▼` indicator reflects `dir`; a documented extension (TV has no header sort); (b) no comparator hook (string-only) — rejected: numeric columns (`Size`) would sort lexicographically; the optional hook is cheap and correct | **(a) reuse `sorted` computed; optional per-column comparator, default string compare; `▲`/`▼`** | ✅ Resolved (source-determined + dominant) |
| AR-159 | RD-16 · Row/divider colours | How are the grid's row + column-divider colours modelled? | (a) **reuse the already-decoded `cpListViewer` roles** — rows use the existing `listNormal`/`listFocused`/`listSelected` and the **column divider `│` reuses the existing `listDivider`** role (`getColor(5)`, already decoded + shipped by RD-11, `theme.ts:222-225`); **no new row/divider roles**; only the **header row** needs a new additive role decoded at **plan GATE-1** (a TV heading colour, e.g. via the frame/label chain); (b) invent new grid roles — rejected: the rows *are* `TListViewer` rows; reusing the decoded roles is the faithful choice | **(a) reuse decoded `list*`/`listDivider` roles; one additive header role at plan GATE-1** | ✅ Resolved (source-determined) |
| AR-160 | RD-16 · Package layout | Where does the DataGrid live + how re-exported? | (a) **a new `packages/ui/src/table/` subsystem** (dir-per-concern, AR-133/113/148) — `data-grid.ts` (`DataGrid<T>` + the `Column<T>` type), `columns.ts` (the width apportion + cell-extract + sort helpers), one barrel `index.ts`, per-file ≤ 500 lines, **explicit named re-exports** from `src/index.ts`; (b) put it under `list/` — rejected: it is its own multi-column `TListViewer`-derived concern, not a single-column `ListView` | **(a) new `src/table/` subsystem, explicit named re-exports** | ✅ Resolved (dominant) |
| AR-161 | RD-16 · Demo vehicle | What proves RD-16? | (a) **a kitchen-sink `DataGrid` story** (a typed multi-column table with a header, sortable columns, a visible focused-row echo, mixed fixed/fr/auto widths + horizontal scroll) passing the headless smoke test + a headless **`demo:table`** walkthrough (ASCII frame per step: render → navigate rows → sort a column → horizontal-scroll), matching `demo:containers`/`demo:tree`; (b) a live demo only — rejected: the DataGrid belongs in the showcase + a headless walkthrough for CI proof | **(a) kitchen-sink `DataGrid` story + headless `demo:table`** | ✅ Resolved (dominant) |
| AR-162 | RD-14 · Input linkage (preflight PF-001) | `History`/`ComboBox` need an `Input`'s text + `selectAll` + `maxLength`, but our `Input` keeps `value`/`maxLength`/`selectAll` `protected` (TV's are public members, `thistory.cpp:106-107`). How? | (a) **a minimal additive PUBLIC Input linkage seam** — promote `selectAll()` to public + a public accessor for the bound `value` signal + a `maxLength` read, so `History` can faithfully replace-text-and-`selectAll` a linked field; additive, existing callers unaffected; corrects the RD's "no Input changes" framing; (b) caller passes the `value` signal + drops faithful `selectAll` — rejected: loses AC-4 fidelity; (c) cast through `protected` — rejected: violates the type-safety standard | **(a) additive public Input linkage seam** | ✅ Resolved (user, preflight) |
| AR-163 | RD-14 · Shared overlay (preflight PF-002) | The app overlay is one `Group` with one `state.visible` flag toggled by the menu controller; a dropdown sharing it would stomp/hide the menu (and vice versa). How do two clients share it? | (a) **derived overlay visibility** — a small additive app-shell seam makes `visible` derived from having any mounted popup child (ref-counted), so `MenuBar` + dropdown coexist without stomping; listed in the additive surface; (b) a separate dropdown overlay layer — viable but adds a layer to the app root; (c) document mutual-exclusion (never simultaneously open) — rejected: a real behavioral limitation | **(a) derived (any-child) overlay visibility seam** | ✅ Resolved (user, preflight) |
| AR-164 | RD-14 · ComboBox value (preflight PF-003) | `value` = "the selected `T` or the text in editable mode" is incoherent for `T ≠ string` (free text matching no item can't be a `T`). How to model it? | (a) **two signals** — `value: Signal<T \| null>` (selection; `null` when free text matches nothing) + the composed `Input`'s own `text: Signal<string>`; type-safe, testable, AC-5/6/7 reworded; (b) restrict editable mode to `ComboBox<string>` — rejected: narrower feature; (c) `value: Signal<T \| string>` union — rejected: pushes ambiguity onto consumers | **(a) two signals (value: T\|null + text: string)** | ✅ Resolved (user, preflight) |
| AR-165 | RD-14 · Theme provenance (preflight PF-004) | The RD claimed the History roles occupy "slots 22–25 RD-11 reserved (AR-112)" — but no such reservation exists (AR-112 reserved ListViewer 26-29 + scrollBar 4-5). | (a) **drop the phantom claim** — no slots pre-reserved in core; decode the exact `cpHistory`/`cpHistoryWindow`/`cpHistoryViewer` attribute bytes at plan GATE-1 (per AR-139); roles stay additive | **(a) drop phantom claim; decode bytes at GATE-1** | ✅ Resolved (user, preflight) |
| AR-166 | RD-14 · Popup dismissal (preflight PF-005/PF-006) | "non-modal / not blocked" + "focus-loss" dismissal were undefined against the full-viewport catcher the popup reuses. | (a) **concrete dismissal** — the popup list takes focus on open; dismiss on Esc / outside mouse-down (**consumed**, dismiss-only, no pass-through — matching the menu catcher) / the list losing focus (Tab-away, via the PF-009 per-view focus-change signal); non-modal = the rest of the UI keeps updating + is interactable after a dismissing click | **(a) concrete dismiss-only + focus-loss via PF-009** | ✅ Resolved (user, preflight) |
