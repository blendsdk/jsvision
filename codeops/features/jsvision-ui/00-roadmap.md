# Roadmap: jsvision UI

> **Feature-Set**: jsvision UI
> **Status**: In Progress
> **Created**: 2026-06-29
> **Last Updated**: 2026-07-03
> **Progress**: 16 / 19 (84%)
> **CodeOps Skills Version**: 2.0.0

The `@jsvision/ui` layer — a reimagined Turbo Vision widget framework on
`@jsvision/core`, using the **disciplined hybrid** model (retained widget tree +
fine-grained signals + `Show`/`For`). Scope and triage: the component map at
[`tui-ui/01-component-map.md`](tui-ui/01-component-map.md). This roadmap is the
successor to the completed foundation feature-set (RD-01…RD-10), which is finished
and archived at [`_archive/foundation/`](_archive/foundation/00-roadmap.md).

RD numbering restarts for this feature-set; these RDs are **not** the archived
foundation RDs of the same number.

## Legend

⬜ Backlog · ✏️ RD Drafted · 🔎 RD Preflighted · 📋 Plan Created · 🔬 Plan Preflighted · 🔄 Executing · ✅ Done · ⛔ Blocked · ⏸️ Deferred

## Tracker

| ID | Title | RD | Plan | Stage | Status | Last Updated | Notes / Blocker |
|----|-------|----|------|-------|--------|--------------|-----------------|
| RD-01 | Reactive core — `signal`/`computed`/`effect` + `Show`/`For` | [RD-01](../requirements/RD-01-reactive-core.md) | [reactive-core](reactive-core/00-index.md) | Done | ✅ | 2026-06-29 | Phase 0 pillar (XL). UI-independent; every widget property binds to it. **Shipped** in `packages/ui/src/reactive/` — 20 ST (ST-01…ST-20) + impl tests green (55 ui tests), `yarn verify`/`check:deps`/`lint` clean, all public symbols importable from `@jsvision/ui`, every file ≤ 500 lines w/ JSDoc. exec_plan complete (4 phases / 4 commits). |
| RD-02 | Layout engine — cell-native flex `row`/`col` | [RD-02](../requirements/RD-02-layout-engine.md) | [layout-engine](layout-engine/00-index.md) | Done | ✅ | 2026-06-29 | Phase 0 pillar (XL). ADR-008 Accepted; built on the golden-tested `apportion`/`solveTrack` spike. **Complete**: all 4 phases / 18 spec oracles (ST-01…ST-18 ↔ AC-1…AC-18) + impl tests green. `layout(root, viewport) → parent-relative integer rects`: `row`/`col` via one axis abstraction, `fixed`/`fr`/`auto` sizing (`auto` pre-resolved via `naturalSize`), `justify`/`align`/`gap`/`padding`, overflow (extend past edge, `fr`→0), degenerate→zero rects, recursion in each box's local frame. Pure/no-mutation; `check:deps` clean; files ≤ 217 lines. Symbols re-exported from `@jsvision/ui`. |
| RD-03 | View/Group spine + `DrawContext` + theming | [RD-03](../requirements/RD-03-view-group-spine.md) | [view-group-spine](view-group-spine/00-index.md) | Done | ✅ | 2026-06-29 | Phase 0 keystone (XL). Retained `View`/`Group` tree, stateless clipped `DrawContext`, theme-role resolution. Closes the reactive seam (per-view owner scope + `bind` + injectable coalescing scheduler, AR-09/AR-02) and owns the RD-02 reflow pass. Ships the **complete** `View` shape (onEvent stub + visible/disabled/focused); dispatch/focus **logic** deferred → RD-04. 20 AC; AR-30…AR-46. **Planned** ([plan](view-group-spine/00-index.md)): 7 phases / 21 sessions / ~29–41 h; PA-1…PA-8 + 2 additive primitives (RD-01 `runWithOwner`, core `ScreenBuffer.clone()`). **Plan preflighted** ([report](view-group-spine/00-preflight-report.md)): 1 MAJOR + 2 MINOR resolved, 1 OBSERVATION recorded. **Complete** ✅ — executed all 7 phases spec-first (RED→GREEN→impl) across 8 commits. Lands `packages/ui/src/view/` (geometry, types, theme-style, draw-context, view, group, reflow, render-root) + 2 additive primitives (`runWithOwner` on reactive, `ScreenBuffer.clone()` on core) + `demo:view`. 22 spec oracles (ST-01…ST-20 + ST-21/22) + impl tests green (ui 142 unit, +3 e2e); `yarn verify`/`check:deps`/`lint` clean; files ≤ 231 lines. Runtime decisions RT-1…RT-5 recorded. |
| RD-04 | Event loop + focus + modality + commands | [RD-04](../requirements/RD-04-event-loop.md) | [event-loop](event-loop/00-index.md) | Done | ✅ | 2026-06-30 | Phase 0. Host-agnostic dispatch **mechanism** (`EventLoop`/`createEventLoop`): pure `dispatch(event)`, faithful 3-phase (pre/focus/post), per-group `current` focus chain (Tab/click), top-most-first mouse hit-test, typed command layer (registry + key→command keymap), async `execView`/`endModal` modality; drives RD-03's `RenderRoot` one frame per tick. Concrete `Application`/`run()`/shell → RD-05. 20 AC; AR-47…AR-59 (8 user choices + 5 dominant). **RD preflighted** ([report](../requirements/00-preflight-report-RD-04.md)): 3 MAJOR + 4 MINOR + 1 OBSERVATION resolved (all Option A); recorded as AR-60…AR-66. **Planned** ([plan](event-loop/00-index.md)): 5 phases / 15 sessions / ~23–33 h; PA-1…PA-9 (4 user choices) + no cross-package primitive. 20 spec oracles (ST-01…ST-20 ↔ AC-1…AC-20). New `packages/ui/src/event/` (types/dispatch/commands/focus/hit-test/modal/event-loop) + additive `view.ts`/`group.ts` + `demo:events`. **Plan preflighted** ([report](event-loop/00-preflight-report.md)): 2 MAJOR + 3 MINOR + 2 OBSERVATION resolved — single `runTick` shared by every public mutator (PF-001/PA-11), Phase-2 focus-bubble clamped to the modal scope root (PF-002/PA-12), built-in `tab`/`shift+tab`→focus traversal (PF-003/PA-10), + ST-02-wiring/flush-counter/envelope-copy notes; 33 tasks. All `file:line` code claims verified against live source. **Complete** ✅ — executed all 5 phases spec-first (RED→GREEN→impl). Lands `packages/ui/src/event/` (types · dispatch · commands · focus · hit-test · modal · event-loop · index) + additive `view.ts`/`group.ts`/`types.ts` (`focusable`/`preProcess`/`postProcess`, `onEvent(DispatchEvent)`, `Group.current`, contract types) + `demo:events`. 20 spec oracles (ST-01…ST-20) + impl tests green; full gate clean — `yarn verify` (8/8), `test:e2e` (event-demo + core), `check:deps`, `lint`; every `event/` file ≤ 227 lines. The single `runTick` (PA-11), Phase-2 bubble clamp (PA-12), and built-in Tab/Shift-Tab (PA-10) all realized; one runtime fix recorded (a pathological re-entrant impl-test handler caused an infinite cascade — corrected to emit only on key events; the faithful drain-loop has no runaway guard, per design). |
| RD-05 | App shell — Application/Desktop/Window/Frame/MenuBar/StatusLine | [RD-05](requirements/RD-05-app-shell.md) | [app-shell](plans/app-shell/00-index.md) | Done | ✅ | 2026-06-30 | Phase 0 integration keystone (XL). The first RD to touch a live TTY: `Application`/`run()` wires `createHost` ↔ RD-04's `dispatch` (quit→exit code, guaranteed restore-on-every-path, suspend/resume); a **full interactive** `Desktop` window manager (z-order **raise**-on-click — the piece RD-04 deferred — + drag-move + free drag-resize + zoom + cascade/tile + Alt-N + close); `Window`/`Frame` chrome with **active/inactive** theming (the one additive `@jsvision/core` `Theme` change); **full nested** `MenuBar`/`MenuPopup` (F10/Alt-hotkey/click activation, ↑↓/Enter/←→/Esc nav, tilde accelerators, enable/disable); a **static** `StatusLine` (+ enable/disable greying). **Drafted** ([RD](requirements/RD-05-app-shell.md)): 8 user choices (AR-67…AR-74) + 7 dominant (AR-75…AR-81); 22 AC; demos = headless `demo:shell` + a real-TTY interactive app. **Scope refined:** `ScrollBar`/`Scroller` deferred to RD-06 (pair with `ListView` virtual scroll — AR-69); rich `Dialog` reuses RD-04 `execView`, also RD-06 (AR-79). **RD preflighted** ([report](requirements/00-preflight-report-RD-05.md)): codebase-grounded audit (14 files, ~20 refs verified; independent challenger on the 3 MAJORs) — 3 MAJOR + 3 MINOR resolved (all Option A), recorded as AR-82…AR-87. Two **additive intra-package `EventLoop` seams** added (pointer `setCapture`/`releaseCapture` for drag/resize tracking — PF-001; an `onFrame` hook so `run()` delivers async frames to `host.render` — PF-003); suspend/resume reworded (core's host owns mode-reassert + repaint, `onResume` notify-only — PF-002); `resize`/`move` dropped from `Commands` (PF-004); exit-code source + cascade/tile edge cases pinned (PF-005/PF-006). **Planned** ([plan](plans/app-shell/00-index.md)): 6 phases (foundation seams+theme role → Application/`run()` → Desktop+Window/Frame → Menus → StatusLine+demos+gate) / 18 sessions / ~34–48 h (incl. a spec-first **Phase 0** for RD-02 absolute placement + RD-03 `DrawContext.role`); PA-1…PA-22 (4 user choices: `windowInactive` role, menu **overlay layer**, optional `viewport?` default, compact WM preset; 10 dominant; + PA-15…PA-22 from the two plan-preflight passes). 22 spec oracles (ST-01…ST-22 ↔ AC-1…AC-22) + FX-01…FX-05 (Phase 0). New `packages/ui/src/{app,desktop,window,menu,status}/` + additive core `windowInactive` role + additive `event/` capture+`onFrame` seams + RD-02 `position:'absolute'` + RD-03 `DrawContext.role` + `examples/shell-demo/`. **Plan preflighted ×2** ([report](plans/app-shell/00-preflight-report.md)): iter-1 PF-01…PF-09 (1 CRITICAL — free-floating/overlapping windows aren't expressible on the flex reflow → RD-02 absolute placement as Phase 0; PA-15…PA-19) → re-baselined to 6 phases / 18 sessions / 48 tasks; iter-2 PF-10…PF-14 (1 CRITICAL — an empty full-viewport overlay would swallow all mouse input; + ST-04/AR-66 restore-on-throw contradiction; + a Phase-2-needs-Phase-3-`Desktop` ordering gap) resolved (all Option A) → PA-20…PA-22. Independent challenger confirmed the critical/major findings against live source. **Complete** ✅ — executed all 6 phases spec-first (RED→GREEN→impl). Lands `packages/ui/src/{app,desktop,window,menu,status}/` + Phase-0 additive primitives (RD-02 `position:'absolute'`+`rect`, RD-03 `DrawContext.role`, RD-04 `EventLoop` `setCapture`/`releaseCapture`/`onFrame`, the sole cross-package edit = core `Theme.windowInactive`) + `examples/shell-demo/` (`demo:shell`). 22 spec oracles (ST-01…ST-22) + impl tests green; full gate clean — `yarn verify` (273 ui + core), `test:e2e` (8 core + examples shell-demo), `check:deps`, `lint`; largest new file `menu/controller.ts` 332 lines. Two runtime notes recorded: the menu `→`/`←` semantics (open-sub-else-switch-top / close-level-else-switch-top), and the `StatusLine` private `layout()` helper renamed to `itemBoxes()` to avoid shadowing the inherited `View.layout` property. |
| RD-06 | Essential controls + validators | [RD-06](requirements/RD-06-essential-controls.md) | [essential-controls](plans/essential-controls/00-index.md) | Done | ✅ | 2026-07-01 | Phase 1. **Complete** ✅ — executed all 7 phases spec-first (RED→GREEN→impl): lands `packages/ui/src/controls/` (`Text`/`Label`/`Button`/`Input`/`CheckGroup`/`RadioGroup` + internal `Cluster` base + `filter`/`range`/`lookup` validators) + the additive `cpGrayDialog` core theme roles + two additive intra-ui primitives (`ev.emit`/`ev.focusView` on the dispatch envelope + the PF-009 per-view focus-change signal) + `examples/controls-demo/` (`demo:controls`). ST-01…ST-16 green; final gate clean (`yarn verify` 8/8, `check:deps`, `lint`, e2e 14/14). One runtime decision **PA-15** — `range` `validChars` made TV-faithful per `tvtext2.cpp:144-145` (unsigned `"+0123456789"` / signed `"+-0123456789"`), correcting the ST-07 oracle cell. **Planned** ([plan](plans/essential-controls/00-index.md)) — the focused **leaf controls + validators** (split per AR-93): `Text`/`Label`/`Button`/`Input`/`CheckGroup`/`RadioGroup` (+ internal `Cluster` base) + `filter`/`range`/`lookup`. Adds the faithful **`cpGrayDialog` control theme roles** to core `Theme` (the one cross-package edit; buttons reuse `button`/`buttonFocused`) + two additive intra-ui primitives (`ev.emit`/`ev.focusView` on the dispatch envelope + a per-view focus-change signal, PF-009). New `packages/ui/src/controls/` subsystem; headless `demo:controls`. **Plan:** 7 phases / 20 sessions / 24 tasks / ~21–35 h, spec-first (ST-01…ST-16); PA-1…PA-14 (3 user + 11 dominant/source); grounded in 3 recon passes (UI seams · TV `t*.cpp` geometry · `tvalidat.cpp` + `app.h` palette, hand-verified). 13 AC; AR-93…AR-102. **Plan preflighted** ([report](plans/essential-controls/00-preflight-report.md)): codebase-grounded audit (~14 files; TV-source fidelity independently verified TRUE — palette slots/glyphs/shadow/arrows; 3 independent challengers) — 2 iterations, 9 findings (3 MAJOR + 4 MINOR + 2 OBS), all resolved (all Option A; PF-009 added a 2nd additive primitive = the focus-change signal). **Deferred (tracked):** Input selection+clipboard/`picture`/`MultiCheckGroup` → RD-07; modal focus-trap → RD-11; multi-column cluster + Text-align + hardware caret (DEF-16…19). `ScrollBar`/`Scroller`/`ListView`/`Dialog` → **RD-11**. Next: `exec_plan essential-controls`. |
| RD-11 | Containers, scrolling & lists | [RD-11](requirements/RD-11-containers-scrolling-lists.md) | [containers-scrolling-lists](plans/containers-scrolling-lists/00-index.md) | Done | ✅ | 2026-07-01 | **Complete** ✅ — executed all 6 phases spec-first (RED→GREEN→impl), lands `packages/ui/src/{scroll,list,dialog}/` (`ScrollBar`·`Scroller`·`ListView<T>`/`ListBox`·`Dialog` + OK/Cancel/Yes/No helpers) + 4 Containers kitchen-sink stories + the dogfooding `ListBox` navigator sidebar + `demo:containers`. ST-01…16 green (fidelity ST-14 + packaging ST-15); final gate clean (`yarn verify` 8/8, `test:e2e`, `check:deps`, `lint`, `gate` PASSED). **DEF-16 Shipped** (Dialog `valid()` close-gate). Runtime decisions **PA-16** (capture seam on `DispatchEvent`), **PA-17** (Scroller offsets content in `draw()`), **PA-18** (wheel-on-bar), **PA-19** (dialog frame corrected to TV-faithful white/white/brightGreen). Phase 5 (kitchen-sink) complete: four Containers stories (`scrollbar`/`scroller`/`listview`/`dialog`, 17 smoke tests), the **dogfooding navigator** — a persistent `ListBox` sidebar `[sidebar | canvas]` in `shell.ts` (select→`showStory` via a PF-003 row map, type-ahead, `execView` seam wired for the Dialog story), and `demo:containers` + its e2e (ScrollBar→13 · Scroller reveal · type-ahead→Grape · Dialog veto→resolve). Phase 4 (Dialog + buttons) complete: `dialog/{dialog,buttons}.ts` (`TDialog`, GATE-1+2) — `Dialog extends Window` in the `dialog` frame role (close box, no zoom, PF-001), `ModalHostAware` (PA-1) resolving `execView` to the terminating command, the `valid()` child-sweep close-gate (**DEF-16**), frame-close/Esc→cancel (PF-002), + `okButton`/`cancelButton`/`yesButton`/`noButton` helpers. Frame chrome generalized (`frame.ts` dialog-aware + `dialog` role `icon`); **PA-19** corrected the dialog role to TV-faithful white/white/brightGreen. Phase 3 (ListView/ListBox), Phase 2 (Scroller, PA-17/18), Phase 1 (ScrollBar, PA-16), Phase 0 (foundations). ST-01…12 + 54 component tests green; `yarn verify` (414 ui) + `check:deps` clean. **Plan preflighted** 🔬 ([report](plans/containers-scrolling-lists/00-preflight-report.md)) — codebase-grounded audit (~16 files; TV citations + UI seams independently re-verified; 1 independent challenger on the blockers): 8 findings (1🔴 modal close/Esc bypasses `endModal`→stuck modality · 1🟠 Dialog frame reuse not additive → `frame.ts` generalized · 4🟡 + 2🔵), **all resolved** (all Option A / single-fix), fixes applied; +task P4.3a (frame generalization), 34 tasks. Phase 1 sibling of RD-06 (split per AR-93). **Planned** 📋 ([plan](plans/containers-scrolling-lists/00-index.md)) — `ScrollBar` + `Scroller` (auto-owned bars) · generic single-column virtual-scroll `ListView<T>` (+ sorted/type-ahead · `ListBox` preset) · rich modal/modeless **`Dialog`** (terminating-command result + a child `valid()` close-gate = **DEF-16** · OK/Cancel/Yes/No helpers). **Plan:** 6 phases (0 foundations → ScrollBar → Scroller → ListView → Dialog → kitchen-sink) / 33 tasks, spec-first (ST-01…ST-16 ↔ AC-1…AC-15); PA-1…PA-15 (3 user + 12 decoded/dominant, ✅ GATE PASSED). Grounded in 2 recon passes: UI seams (all additive) + a hand-verified TV GATE-1 decode (`tscrlbar`/`tscrolle`/`tlstview`/`tlistbox`/`tdialog.cpp` + the `cpGrayDialog` palette chain → byte-accurate theme colours). Additive only: 6 theme roles + `Commands` ok/cancel/yes/no + the `attachModalHost` loop seam (PA-1) + new `src/{scroll,list,dialog}/`. Deferred → RD-07: multi-column `ListViewer`/`Table`, `ComboBox`. Next: `exec_plan containers-scrolling-lists`. |
| RD-07 | Essential-control completions | [RD-07](requirements/RD-07-essential-control-completions.md) | [essential-control-completions](plans/essential-control-completions/00-index.md) | Done | ✅ | 2026-07-02 | **Complete** ✅ — executed all 7 phases spec-first (RED→GREEN→impl). Lands `Input` **text selection + logical caret** + **clipboard** (OSC-52 write via core `setClipboard`; Ctrl+Ins/Shift+Ins/Shift+Del; bracketed paste), the **`picture(mask)`** validator (full `TPXPictureValidator` DSL + autoFill via the additive `Validator.fill`, bounds-safe PA-2), **`MultiCheckGroup`** (idiomatic `Signal<number[]>`, faithful `drawMultiBox`), and the **visible caret** — logical (reversed-style in-buffer cell) + **hardware** via the additive `EventLoop.onCaret`/`refreshCaret` + `View.desiredCaret()` + `RenderRoot.originOf()` seams, positioned by `run()` (cursor + OSC-52 clipboard output, re-applied on resume). Additive-only core edit: the `inputSelection` theme role + `Commands.cut/copy/paste`. Cluster base generalized to TV's marker-string model (PF-001); caret derived post-`flush()` so it survives partial recompose (PF-002). Runtime decisions **PA-14…PA-19** (5 user + impl refinements: fill-before-validate `insertFilled`, `refreshCaret` seam). **DEF-01/02/03/19 → Shipped**; new deferrals **DEF-21** (grapheme caret stepping), **DEF-22** (cluster caret), **DEF-25** (OSC-52 clipboard read). Kitchen-sink: `MultiCheckGroup` story + enriched `Input` story (picture field + selection/clipboard/caret) + `demo:controls` extended. Spec ST-01…ST-16 green; final gate clean (`yarn verify`, `test:e2e`, `check:deps`, `lint`, `gate`). **Drafted** ✏️ (`add_requirement`, AR-115…AR-124) — the **thin control-completions slice** the roadmapped "high-value controls" bucket was cut down to (AR-115): `Input` **selection + system clipboard** (DEF-01 · OSC-52 write via core `setClipboard` + bracketed paste; keys Ctrl/Shift+Ins/Del; `Commands.cut`/`copy`/`paste`), the **`picture(mask)`** validator (DEF-02 · full `TPXPictureValidator` DSL + autoFill, bounds-safe), **`MultiCheckGroup`** (DEF-03 · idiomatic `Signal<number[]>`, faithful `" [ ] "`+marker visual), and the **visible caret** (DEF-19 · logical + hardware via an additive `View`→host caret seam). Additive core edits: a faithful `Input` **selection** theme role + `Commands.cut/copy/paste`. 15 AC (AC-1…AC-15). **Deferred here:** overwrite/`Ins` mode (DEF-20), OSC-52 clipboard *read*. **Preflighted** 🔎 ([report](requirements/00-preflight-report-RD-07.md)) — codebase-grounded audit (6 recon agents + 1 adversarial challenger; the decoder-chord **feasibility** + the TV `file:line` citations independently verified against source): **PASSED WITH NOTES** — 0🔴 / 0🟠 + 3🟡 + 2🔵. All infra-existence claims exact; the caret host-seam confirmed additive with **no core change** (challenger REFUTED the initial MAJOR framing). 3 MINOR **fixed** (caret suspend/resume clause added · 2 drifted TV cites corrected: `autoFill`→`:572-585`, state-machine→`:264-517` · "Depends On" reworded); 2 OBS (selection-role naming · grapheme index unit) carried to `make_plan` GATE-1. **Planned** 📋 ([plan](plans/essential-control-completions/00-index.md)) — 9 docs; 7 phases (0 foundations → Input selection+logical caret → clipboard → picture → MultiCheckGroup+story → hardware caret+demos → gate) / 34 tasks, spec-first (ST-01…ST-16 ↔ AC-1…AC-15); **PA-1…PA-13 (4 user + 9 dominant/source)**. Grounded in **3 GATE-1 TV decodes** (`TInputLine` selection/clipboard/`setCursor`, `TPXPictureValidator` full DSL, `TMultiCheckBoxes`/`drawMultiBox` — citation-rich, in the `03-*` docs). 4 user gate-choices: **code-point** caret unit (PA-1; grapheme→DEF-21), **reject-at-syntaxCheck** mask DoS bound (PA-2), **autoFill default ON** (PA-3), role name **`inputSelection`** (PA-4). Additive-only: core `inputSelection` role + `Commands.cut/copy/paste` + the `View.desiredCaret`/`RenderRoot.caret`/`EventLoop.onCaret`/`writeClipboard` seams (no core host change — challenger-confirmed). New deferrals DEF-21 (grapheme stepping), DEF-22 (cluster caret). **Plan preflighted** 🔬 ([report](plans/essential-control-completions/00-preflight-report.md)) — codebase-grounded audit (~14 ui+core files read; ~40 `file:line` refs verified exact; 3 TV C++ decodes re-checked against source; 1 independent challenger on the blockers): 8 findings (**2🟠** + 4🟡 + 2🔵), **all resolved** (all Option A). The two MAJORs were genuine plan-level catches the RD-07 requirements preflight missed: **PF-001** — `MultiCheckGroup` cannot inherit the boolean `Cluster.draw()` seam → base generalized to TV's marker-string model (`markers`+`markIndex`, CheckGroup/RadioGroup migrated, +P4.7 RD-06 regression); **PF-002** — hardware-caret collection coupled to the compose walker breaks on partial recompose → caret now derived by the EventLoop from `RenderRoot.originOf()` post-`flush()` (+partial-recompose oracle). Minors: task total reconciled **34→32**, P0.1 GATE-1 broadened to settle the `inputSelected` fidelity question, "1:1" mapping reworded, additive ui-seam inventory added; 2 OBS (clipboard prose trimmed, logical-caret style+edge specced). Next: `exec_plan essential-control-completions`. |
| RD-13 | Runtime hardening & defect remediation | [RD-13](requirements/RD-13-runtime-hardening.md) | [runtime-hardening](plans/runtime-hardening/00-index.md) | Done | ✅ | 2026-07-02 | **DONE** ✅ — all 10 phases / 120 tasks complete; final full gate PASSED (`yarn verify`/`test:e2e`/`check:deps`/`lint`/`gate`). Remediated the five-agent audit backlog (3 critical + 12 major + ~25 minor) spec-first per HR-NN, with TV GATE-1/GATE-2 decodes for every fidelity item. **Phase 10** ✅ governance: CHANGELOG backfill + RD-13 entries, `BLENDTUI_*`→`JSVISION_*` docs sweep, HR-55 wording, kitchen-sink smoke (18 stories) + demo goldens green. **Phase 9/10 COMPLETE** ✅ (TV GATE) — containers/lists minors: HR-49 (ScrollBar track click jumps to position + follows the drag, `tscrlbar.cpp:193-207` — corrects the ST-02 page-step oracle), HR-50 (list keeps the focused-row `listSelected` highlight when focus leaves, `tlstview.cpp:86-130`), HR-51 (`<empty>` at column 1, `:147-148`), HR-53 (owned bar pgStep = size.y−1, `:48-52`), HR-61 (Scroller reserves the SE corner cell in both-bars mode — bar bg, never content), HR-62 (click below the last row clamps to the last item, `focusItemNum` `:185-195`). ST-8.n–s + impl tests green; containers-demo updated for the jump-to-position semantics; full gate PASSED. 117/120 tasks. **Phase 8/10 COMPLETE** ✅ (TV GATE) — controls/input-editor minors: HR-43 (paste maps `\t\r\n`→space, drops C0), HR-45 (caret advances past the typed char, not trailing autoFill), HR-46 (Input drag guard — only a drag it started mutates), HR-47 (deletions/cut re-validate + revert, TV transient-revert), HR-48 (Ctrl+Backspace/Ctrl+Del word-wise via `prevWord`/`nextWord`), HR-52 (disabled Button/Cluster hot run in the disabled color, `tbutton.cpp:107-108`), HR-54 (double-click window disarmed by any edit), HR-55 (JSDoc: autoFill is trailing-only), HR-56 (Button click rect excludes col 0, `clickRect.a.x++`), HR-57 (`Text` preserves internal whitespace + indentation verbatim), HR-58 (maxLength clamp-and-accept, not reject), HR-59 (external write clamps the full selection tuple), HR-60 (floored modulo in `MultiCheckGroup.press`). Extracted `input-editing.ts` (word/delete helpers) to keep `input.ts` ≤500. ST-8.a–m + impl tests green; full gate PASSED. 105/120 tasks. **Phase 7/10 COMPLETE** ✅ (TV GATE) — event/shell minors: HR-35 (bare top-level menu item — Enter emits+closes, Esc always closes, per `tmnuview.cpp:390` GATE-1/GATE-2 decode, PA-17), HR-36 (outside-click catcher tracks viewport resize via a new `EventLoop.onResize` seam), HR-37 (`Dialog.modalHost` cleared on both resolution paths → a retained dialog stops swallowing global Esc), HR-38 (TV-faithful top-down `endModal(quit)` cascade with per-modal `valid()` veto, per `tprogram.cpp:205`/`tgroup.cpp:173` GATE decode, PA-2), HR-39 (disabling the focused view evicts focus; `advance()` recovers from a non-candidate anchor by tree order), HR-40 (one-click menu-title switch — the catcher hit-tests the bar row), HR-41 (zoomed windows re-maximize + `restoredRect` clamps on desktop resize), HR-42 (dispatch sweeps skip unmounted views). ST-7.a–h + impl tests green; full gate PASSED. 82/120 tasks. **Phase 6/10 COMPLETE** ✅ — reactive/view minors: HR-27 (throwing computed stays re-evaluable — never a silent `undefined`), HR-28 (computed-cycle detection → `ReactiveCycleError`, scoped to the compute stack so effect self-writes stay the runaway guard's job), HR-29 (batch body error wins; flush error reported, PA-15), HR-30 (draw-context width-aware title centering + combining-mark composition), HR-31 (`invalidate()` on a visibility flip escalates to relayout, PA-8), HR-32 (`View.onCleanup` binds to the view scope via `untrack`, fires once), HR-33 (`naturalSize` excludes absolute children), HR-34 (partial-recompose occlusion expanded by the `{2,1}` shadow margin, PA-16). ST-6.a–h + impl tests green; full gate PASSED. 66/120 tasks. **Phase 5/10** ✅ — core-engine minors: HR-15 (host restart resets the render baseline + decoder carry → full first repaint), HR-16 (`ESC ESC`→Alt+Escape, PA-3), HR-17 (combining marks compose onto the base cell), HR-18 (wide-glyph fallback `'? '` two-cell, PA-11), HR-19 (comprehensive EAW W/F `WIDE` table + dev-only `gen-eaw-table.mjs`, PA-18), HR-20 (continuation-damage re-emits its lead glyph, PA-14), HR-21 (byte-exact OSC-52 clipboard, PA-7 — updates ST-8), HR-22 (async-detection passthrough surfaced for re-injection), HR-23 (`KEY_NAMES`/`PasteState` exports), HR-24 (flush timer for any ESC-prefixed carry), HR-25 (width-aware box-title centering), HR-26 (`JSVISION_DEBUG`/`JSVISION_LOG` rename, PA-4). ST-5.a–l + impl tests green; full gate PASSED. 53/120 tasks. **Phase 4/10** ✅ (TV GATE) — shell majors HR-08 (`Desktop.handleCommand` handles `Commands.close` → closes the active window; F3/File→Close now work) + HR-09 (frame close/zoom/resize-grip affordances gated on **active** per `tframe.cpp:150-193` GATE-1/GATE-2 decode: an inactive window's first click only raises+activates, the second acts). ST-4.a–b + impl tests green; full gate PASSED. 39/120 tasks. **Phase 3/10** ✅ — lifecycle majors HR-11 (`isFocusable` requires mounted), HR-10 (removing the focused child re-homes focus via the new `ViewHost.healFocus` seam, PA-10), HR-12 (`flush()` snapshots+clears pending-work flags first, PA-12), HR-13 (`addDynamic` takes a combinator **factory** built under the group scope → disposed on unmount; 5 call sites updated), HR-14 (additive `ev.hasCapture` seam + Desktop/StatusLine stale-gesture guards, PA-13). ST-3.a–e + impl tests green; full gate PASSED. 30/120 tasks. **Phase 2/10** ✅ — core majors HR-04 (DCS/CSI `'incomplete'` carry — no reply leaks as keys), HR-05 (C0→space at the grid boundary, PA-5), HR-06 (logger `{dev,ino}` device-identity guard: `'auto'`→ring / explicit stderr→throw, PA-6), HR-07 (UTF-8 locale ⇒ box/half-block glyphs, PA-9; all 3 demos dropped their glyph overrides + demo-golden). ST-2.1–2.4 + demo golden green; 4 pre-existing impl tests updated to the changed contracts. 20/120 tasks. **Phase 1/10** ✅ — critical trio HR-01 (post-assembly UTF-8 validation → non-scalar/overlong/surrogate drop, no `RangeError`), HR-02 (modal hit-test through `absoluteOrigin(scopeRoot)`), HR-03 (final disposal — `Computation.disposed` skips queued-but-disposed effects). Spec-first RED→GREEN→impl; full gate PASSED (`yarn verify`/`test:e2e`/`check:deps`/`lint`/`gate`). **Hardening RD** (no new features) capturing a 5-agent deep-audit backlog: **3 critical** (HR-01 hostile-UTF-8 stdin crash — `RangeError` DoS, reproduced; HR-02 modal mouse-coords offset by ancestor position — breaks the kitchen-sink dialogs; HR-03 dispose doesn't neutralize queued effects → use-after-dispose + permanent resurrection) + **12 major** (DCS reply leaks as keystrokes, `\t`/`\n` into the cell grid, logger writes onto the UI in the common config, capability detection never enables glyphs, `Commands.close` handled by nothing, inactive-window close-zones live, focus-chain dangles on child removal, `addDynamic(Show/For)` leak, stale-gesture teleport, `flush()` drops mid-flush invalidations, detached-view `isFocusable`) + **~20 minor** (TV-fidelity + `Input` editor correctness, each behind a GATE-1/GATE-2 decode). Additive-only surface; spec-first per HR-NN with fuzz/regression oracles for the critical trio. Next: `make_plan RD-13`. |
| RD-08 | Editor family | — | — | Backlog | ⬜ | 2026-06-29 | Phase 3 (XL gap-buffer). Editor/Memo/EditWindow/Indicator/Terminal. Demo: `tvedit`. |
| RD-09 | Files package `@jsvision/files` | — | — | Backlog | ⬜ | 2026-06-29 | Phase R. Relocated fs-bound dialogs: FileDialog/FileList/DirList/ChDir. Demo: `tvdir`. |
| RD-14 | Input dropdowns — History · ComboBox | [RD-14](requirements/RD-14-input-dropdowns.md) | [input-dropdowns](plans/input-dropdowns/00-index.md) | Done | ✅ | 2026-07-02 | **Done** ✅ (`exec_plan input-dropdowns` complete 2026-07-02) — all 5 phases / 47 tasks landed spec-first: Phase 0 additive seams + 5 core `history*` theme roles · Phase 1 shared `openAnchoredPopup` (TV-faithful `±1`/`intersect`/`-1` geometry) · Phase 2 `History` + per-id MRU store (TV-derived, GATE-1/2; `cpHistoryViewer` row palette via an additive `ListView` `roles` override, PA-17) · Phase 3 `ComboBox<T>` (editable filter / select-only type-ahead, two-signal value⟂text bind; shared `▐↓▌` icon + `absoluteRect` DRY'd into `popup.ts`) · Phase 4 kitchen-sink `History`+`ComboBox` stories (smoke green) + headless `demo:dropdowns` (+ e2e) + `dropdown.packaging` ST-34/ST-35 (packaging + sanitize/injection + maxLength-clamp). Runtime decisions PA-16 (pick-on-`selected`-change) + PA-17 (viewer `roles` override). Full `yarn verify` + `yarn test:e2e` + `yarn check:deps` green (ui 660 unit tests; every dropdown file ≤ 260 lines). **Plan preflighted** 🔬 ([report](plans/input-dropdowns/00-preflight-report.md)) — fresh-session, codebase-grounded audit (9 source files read; ~20 `file:line` refs verified — TV decode + Input-seam/ListView/overlay/theme cites all exact). 6 findings (**3🟠 + 2🟡 + 1🔵**), **all resolved (all Option A), fixes applied**: 🟠 PF-001 the PA-5 "derived overlay visibility" was specified as a reactive `effect` over **non-reactive** `overlay.children` (a plain array, `group.ts:33`) + `state.visible` (a plain boolean, `view.ts:47`) → replaced with an **imperative** `syncOverlayVisible` helper (`children.length>0`+`invalidate()`; menu **replaces** its `:229/:247` toggles) · 🟠 PF-002 no path for a **leaf** control to reach the overlay/focus (`DispatchEvent` has no `getFocused`/overlay; only `MenuBar` is `attach()`ed) → added a **3rd additive intra-`ui` seam**: `DispatchEvent` `getFocused()`+`PopupHost` accessor sourced in `routeContext` · 🟠 PF-003 popup-height contradiction — `03-02` content-shrank (`min(maxRows,n)+2`) vs. the fixed TV `+7` everywhere else → **fixed `maxRows+2`**, host-intersect the only row reducer; 🟡 PF-004 focus-loss dismissal guarded on `state.focused===false` (no self-dismiss on open) · 🟡 PF-005 `00-index` seam miscount reworded; 🔵 PF-006 ST-2/ST-31 AC cross-refs swapped. Task total **34→35** (added Phase-0 envelope-seam task). Next: `exec_plan input-dropdowns`. **Planned** 📋 ([plan](plans/input-dropdowns/00-index.md)) — 8 docs; **5 phases / 35 tasks** spec-first (ST-1…ST-36 ↔ AC-1…AC-14): Phase 0 seams+theme → 1 anchored popup → 2 History (TV-derived) → 3 ComboBox → 4 stories+`demo:dropdowns`+gate. Foundation on existing primitives only; additive surface = 3 small seams (public `Input` linkage `getValueSignal`/`getMaxLength`/`selectAll` · **derived** overlay visibility, menu migrates · 5 core History theme roles). **Full TV GATE-1 decode captured** in `03-01` (icon `▐↓▌` U+2590/**U+2193**/U+258C, `getColor(0x0102)`→arrow `0x20`/sides `0x72`, popup rect `±1`/`+7`, blue-window frame `0x1F`/accent `0x1A`, viewer `0x1F`/focused `0x2F`, store `histlist.cpp`) with GATE-2 diff tasks. **PA-1…PA-15 ✅ GATE PASSED** — 4 user choices (2026-07-02): store **per-id entry-count cap** (default 16, not the byte-block; PA-2/PF-008) · **U+2193 ↓ narrow** arrow (PA-3) · `maxRows` = **visible rows, default 6** (window = maxRows+2; PA-4/PF-007) · **derive** overlay visibility from children (PA-5). **2 fidelity corrections** (C++ oracle): AC-2 list order → **oldest→newest top→bottom** (`historyStr(id,0)`=oldest, PA-6) · AC-8 geometry → **8-row window / 6 visible** (was 7/~5, PA-7). Next: `exec_plan input-dropdowns` (or `preflight` first). **Preflighted** 🔎 ([report](requirements/00-preflight-report-RD-14.md)) — fresh-session, codebase-grounded audit (2 recon agents: UI-code claims vs. live source + TV C++ fidelity claims vs. `magiblot/tvision`). **TV decode verified flawless** (icon `tvtext1.cpp:86`, `getColor(0x0102)`, `cpHistory "\x16\x17"`, open chord, rect `r.b.y+=7`, pick→`selectAll`+`maxLen` clamp, cancel paths, framing, all 5 `histlist.cpp` semantics — 0 mis-decodes). 8 findings (**3🟠 + 3🟡 + 2🔵**), all resolved (all Option A, AR-162…AR-166): 🟠 PF-001 `History`↔`Input` needs a **public Input linkage seam** (`value`/`maxLength`/`selectAll` were `protected`; "no Input changes" was false) · 🟠 PF-002 the **single shared overlay** `state.visible` needs a **derived (any-child) visibility seam** so `MenuBar`+dropdown don't stomp · 🟠 PF-003 `ComboBox` **value** was T-vs-string incoherent → **two signals** (`value: Signal<T\|null>` + `text: Signal<string>`); 🟡 PF-004 dropped a phantom "slots 22–25 reserved" claim · 🟡 PF-005 dismiss-only outside-click (consumed) · 🟡 PF-006 concrete focus-loss via the PF-009 signal; 2🔵 (popup-height-vs-visible-rows, byte-vs-count store) carried to GATE-1. RD + register + ACs updated. **Drafted** ✏️ (`make_requirements`, RD-12+ set sibling 1/6, MVP phase AR-129). `History` — faithful TV `▐↓▌` dropdown of a field's past values over a **global by-`historyId` MRU store** (dedup + evict-oldest, `histlist.cpp`) + a `Signal<string[]>` escape hatch (AR-130); GATE-1 decode of `THistory`/`THistoryWindow`/`THistoryViewer` done (icon `tvtext1.cpp:86`, 7-row popup rect `thistory.cpp`). New `ComboBox<T>` (no TV counterpart, AR-131) — Input + drop-down `ListView`, **editable** (filter-as-you-type, AR-134) or **select-only** (type-ahead jump); generic `items`+`getText`+two-way `value` (AR-136). One shared **non-modal anchored-popup** primitive generalizing the RD-05 menu overlay (AR-132/137); open keys faithful + Alt+Down (AR-135). New `src/dropdown/` subsystem (AR-133); additive faithful History theme roles at plan GATE-1 (AR-139); kitchen-sink `History`+`ComboBox` stories + headless `demo:dropdowns` (AR-140). 14 AC (AC-1…AC-14). Next: `make_plan RD-14` (or `preflight` first). |
| RD-15 | Tree — expandable outline | [RD-15](requirements/RD-15-tree.md) | [tree](plans/tree/00-index.md) | Done | ✅ | 2026-07-03 | **DONE** ✅ (`exec_plan tree` 2026-07-03) — all 4 phases / 35 tasks complete spec-first (ST-1…ST-24 ↔ AC-1…AC-13), TV GATE-1 decode + GATE-2 cell-by-cell diff. Lands `packages/ui/src/tree/` (`graph.ts` faithful `createGraph`+`flattenVisible`, `tree-rows.ts` renderer, `tree.ts` `Tree`/`TreeNode`/`TreeOptions` + expand-state model) + 4 additive core `cpOutlineViewer` roles + kitchen-sink `Tree` story + `demo:tree`. Full `yarn verify` (8/8) + `test:e2e` (5/5) + `check:deps` + `lint` green; files ≤ 380 lines. **Two exec-time fidelity decisions:** **PA-16** (supersedes PA-9) — the gray-dialog outline decode is degenerate (`Normal == Focus == 0x70`, focus invisible), user chose the **blue-window** host (`outlineNormal` 0x1E yellow-on-blue / `outlineFocused` 0x71 blue-on-lightGray / `outlineSelected` 0x1A brightGreen-on-blue / `outlineNotExpanded` 0x1F white-on-blue; `cpBlueWindow` `views.h:955`→`cpAppColor` `app.h:143`); **PA-17** — the expand marker is TV's literal `expanded ? '─' : '+'` (`toutline.cpp:200`), not plan-03-02's `children`-based form (`ovChildren` is expanded-only, so a collapsed node must still show `+`) — corrected `graph.ts` + the ST-4 oracle. **Plan preflighted** 🔬 ([report](plans/tree/00-preflight-report.md)) — codebase-grounded audit (TV `toutline.cpp`/`outline.h` decode + RD-11 reuse surfaces independently re-verified `file:line`): **PASSED** — 0🔴 / 0🟠 + 3🟡 (all **fixed**) + 1🔵. Every TV citation accurate to the line; the §4 two-tone decode (`getColor(0x0202/0x0303/0x0401)` + `color>>8`) transcribed exactly; the mouse graph-zone-toggle vs double-click-select model confirmed against `:433-481` (PA-14 adaptation legitimate); additive-only surface confirmed (no pre-existing `outline` role); all 13 AC ↔ ST-1…ST-24 mapped. Fixes applied: **PF-001** task count 34→35 (checklists were 35); **PF-002** story `category: 'Containers'` pinned (was vague "Data"; siblings use it); **PF-003** empty-tree `<empty>` to match `ListRows`. PF-004 (ST-20 colour bytes pinned at exec GATE-1) accepted — precedented. Next: `exec_plan tree`. **Planned** 📋 ([plan](plans/tree/00-index.md), `make_plan RD-15` 2026-07-03) — 8 docs; **4 phases / 35 tasks** spec-first (ST-1…ST-24 ↔ AC-1…AC-13): Phase 0 theme roles + pure `graph.ts` builder/flatten → 1 `TreeRows` renderer + `Tree` composition → 2 nav/mouse/select → 3 story+`demo:tree`+GATE-2+gate. Built **entirely on existing primitives** (RD-11 `virtual.ts`/`ScrollBar` owned-bar pattern, RD-03 `View`/`DrawContext`); **no new engine seams** — the only additive surface is **4 core `cpOutlineViewer` theme roles**. **Full TV GATE-1 decode captured** in [03-01](plans/tree/03-01-tree.md) (`graphChars` §1 `toutline.cpp:367`, `getGraph` widths §2 `:364-370`, `createGraph` §3 `:165-205`, `drawTree` two-tone §4 `:54-102`, palette+`getColor` chain §5 `:15,351-355`/`mapcolor.cpp`/`palette.cpp:23-28`, event/mouse §6 `:419-541`) with GATE-1 BEFORE/GATE-2 AFTER diff tasks. **PA-1…PA-15 ✅ GATE PASSED** — 4 user choices (2026-07-03): source shape = **forest** `roots: Signal<TreeNode<T>[]>` (PA-2) · initial expand = **collapsed default + `expandedByDefault?`**, node data plain (PA-3) · **vertical-only** scroll, H deferred (PA-5) · **both Should-Haves** (`guides?` toggle + `expandAll`/`collapseAll`, PA-6); dominant decodes for the rest (expand-Set = object-identity Set in a Signal PA-4 · 4-file split PA-7 · roles `outline*` PA-8 · colour bytes pinned at exec GATE-1 vs gray-dialog owner PA-9 · mouse select mirrors RD-14 PA-16 no-double-click PA-14). Next: `exec_plan tree` (or `preflight` first). **Drafted** ✏️ (`make_requirements`, RD-12+ set sibling 2/6, MVP phase AR-129). Concrete reactive `TreeNode<T>` (AR-141); ←collapse/→expand + faithful `+`/`-`/`*` (AR-142); eager (AR-143); single-select (AR-144); reuses RD-11 virtual-scroll + own `ScrollBar`, Tree-specific renderer (AR-145); new `src/tree/` (AR-148); additive `cpOutlineViewer` roles (AR-149); story + `demo:tree` (AR-150). 13 AC. |
| RD-16 | Table / DataGrid — multi-column grid | [RD-16](requirements/RD-16-table.md) | [table](plans/table/00-index.md) | Done | ✅ | 2026-07-03 | **DONE** ✅ (`exec_plan table` 2026-07-03) — 4 phases / 28 tasks spec-first (ST-1…ST-24 + impl), TV GATE-1 decode + GATE-2 diff against `tlstview.cpp`. `DataGrid<T>` in `packages/ui/src/table/` (`columns.ts` pure width/sort math, `grid-rows.ts` GridRows+GridHeader, `data-grid.ts` Group) + **1 additive core role** `tableHeader` (`0x3F` white-on-cyan) + kitchen-sink `data-grid` story + `demo:table` + `table-demo.e2e`. Layout per the PF-101 fix — inner `col` container so the parent's `layout` stays free, PF-101 width-matched bands `topRow[header fr\|corner 1] / body[rows fr\|vbar 1] / botRow[hbar fr\|corner 1]`. One impl fix surfaced by ST-9: `GridRows` repaint bound to the display **array** (not its length) so a pure reorder repaints. Files ≤ 500 lines (grid-rows 443); full `yarn verify`/`test:e2e`/`check:deps`/`lint` green. Prior: **Plan preflighted** 🔬 ([report](plans/table/00-preflight-report.md), `preflight table` 2026-07-03) — ⚠️ same-session audit, every code claim re-verified `file:line`. **PASSED WITH NOTES — 0🔴 / 1🟠 / 3🟡 / 2🔵** (8 of 13 dims clean). **PF-101 (MAJOR, fixed):** the `03-01` layout diagram misaligned header vs data columns by 1 cell — a bare full-width header child stretches to `W` while `rows` (fr beside the 1-cell vbar) is `W−1` (`layout.ts:209` `align:'stretch'`); restructured to `topRow[header fr\|corner 1] / body[rows fr\|vbar 1] / botRow[hbar fr\|corner 1]` so all three fr-bands share `W−1` (matches the approved AR-174 preview). PF-102 (auto-measure split O(rows) `measureAutoWidths` computed + O(cols) `apportionColumns` per-draw), PF-103 (active sort col clips title to `width−1`, arrow always shown), PF-104 (`alignCell` width-aware `glyphWidth` clip vs `ListRows.slice`) all fixed; PF-105/PF-106 (🔵) accepted. Next: `exec_plan table`. **Planned** 📋 ([plan](plans/table/00-index.md), `make_plan RD-16` 2026-07-03) — 9-doc set, 4 phases / 22 tasks spec-first (ST-1…ST-24), ✅ GATE PASSED (AR-172…AR-182): header role `tableHeader` white-on-cyan `0x3F` (TV has no table class → documented extension colour, not a decode); `auto` pre-measured to `fixed` = widest over all rows, `maxWidth`-or-uncapped, recompute-on-change; layout `[header 1][body fr][hbar 1]`, body `[rows fr\|vbar 1]`; all 3 Should-Haves in (`sortBy`, min/maxWidth, zebra); zebra reuses `staticText 0x70` (no 2nd new role); mouse-select mirrors `ListRows` (click focus+select, Enter/Space emit — **double-click deferred**, `MouseEvent` has no click-count, `events.ts:30`). TV GATE-1 BEFORE-decode + GATE-2 AFTER-diff tasks in Phase 3. Next: `preflight table` (optional) → `exec_plan table`. **Preflighted** 🔎 ([report](requirements/00-preflight-report-RD-16.md), `preflight requirements RD-16` 2026-07-03) — fresh-session, codebase-grounded audit + independent TV C++ re-verification (`/home/gevik/workdir/github/tvision`). **TV GATE-1 fidelity confirmed flawless:** no table class exists, newspaper flow `item=j*size.y+i+topItem` exact (`tlstview.cpp:110`), `│`=`\xB3` divider via `getColor(5)`=`cpListViewer[5]`=`0x1D` (`:30,:150`), row colours `getColor(1)/(2)/(3)/(4)` (`:86-97`), `hScrollBar` indent (`:99-102`), `showMarkers` mono-only (`tprogram.cpp:253`), Space-select (`:282-286`). **PASSED — 0🔴/0🟠 + 7🟡 (all fixed) + 2🔵 (folded in).** Fixes: PF-001 stale `theme.ts:222-225`→`131-146`/`284-287` cite · PF-002 "reuse `sorted` computed"→**pattern-reuse + new `{col,dir}`+`compare` comparator** (the existing `list-rows.ts:110-116` toggle is fixed-ascending single-field, not parameterizable) · PF-003 `solveTrack` has **no `auto`** (`apportion.ts:18-20`)→`auto` pre-measured to `fixed`, scope/cap at plan GATE-1, `fr`⟂overflow noted · PF-004 `DataGrid` reframed **`View`→`Group`** (container + focusable rows-renderer(View) + header + V/H bars, per `ListView`/`Tree`), AR-160 file split gains the renderer file · PF-005 `showMarkers` dropped from "preserved faithful" (mono-only) · PF-006 **Space** added to select (AC-8) · PF-007 new **AC-14** `<empty>` state; 🔵 PF-008 `focused` positional-across-sort documented · PF-009 plan-GATE `size.y*numCols`↔`±viewport` reconciliation. Refinements = register AR-167…AR-171. Next: `make_plan RD-16`. **Drafted** ✏️ (`make_requirements`, RD-12+ set sibling 3/6, the **last MVP** RD AR-129); realizes RD-11's deferred multi-column surface (AR-104). **GATE-1 finding:** TV has **no** table class — `TListViewer::numCols` is a **newspaper-flow single-field list** (`item=j*size.y+i+topItem`, `tlstview.cpp`), not a grid. So RD-16 = a real **`DataGrid<T>`** as a **documented TV-extension** on the `TListViewer` spine (AR-151): rows / `│`-divider (`getColor(5)`=`listDivider`) / `cpListViewer` row colours / virtual-scroll **faithful**; the header + heterogeneous `Column<T>[]` + click-sort the flagged extension. Typed `Column<T>` `{title,accessor,width,align?,compare?}` (AR-152); `fixed`/`fr`/`auto` sizing via RD-02 `solveTrack` + horizontal scroll (AR-153); sticky header + click-to-sort `▲`/`▼` reusing RD-11 `sorted` (AR-154/158); row-granular single-select (AR-155); owned V+H `ScrollBar` (AR-156); reactive `Signal<T[]>` (AR-157). New `src/table/` (AR-160); reuses the existing `list*`/`listDivider` roles + **one** additive header role at plan GATE-1 (AR-159); kitchen-sink `DataGrid` story + `demo:table` (AR-161). 13 AC. Next: `make_plan RD-16`. |
| RD-17 | Tabs — tabbed layout container | [RD-17](requirements/RD-17-tabs.md) | [plan](plans/tabs/00-index.md) | Done | ✅ | 2026-07-03 | **Done** ✅ — `exec_plan tabs` complete: 4 phases / **21 tasks** spec-first (ST-1…ST-38 ↔ AC-1…AC-15 + impl). RD-17 is a **documented new component** (TV has no tab class, AR-172); GATE-1 pinned the 3 `tab*` bytes by gray-dialog analogy (active `0x7F`/inactive `0x70`/disabled `0x78`) + decoded the 4 tees (`┬`=U+252C, `┴`=U+2534, `├`=U+251C, `┤`=U+2524), GATE-2 AFTER-diff ✅ matches. `packages/ui/src/tabs/` (`tab-view.ts` + `tab-strip.ts` + `index.ts`): `TabView extends Group` (eager pages via a keyed `For`, one visible via a **`state.visible` flip — no mount/dispose**, PF-001; read/render-time self-correcting clamp + snap, PF-003; Ctrl+PageUp/Down + Alt-hotkey in `preProcess` scoped by `isWithin(getFocused, this)`, PF-002; `select`/`next`/`prev`/`closeTab` + `onChange`), `TabStrip extends View` (folder-tab chrome, `stripGeometry` overflow auto-scroll, `hitStrip`, `←→`). An inner `col` container keeps `TabView.layout` free (DataGrid PF-101 idiom — fixed a layout-clobber the specs caught). Only additive surface = **3 core `tab*` roles**. Kitchen-sink `containers/tabs` story + `demo:tabs` + `tabs-demo.e2e`. Full `yarn verify`/`test:e2e`/`check:deps` green. jsvision-ui now 14/18. Prior — **Plan Preflighted** 🔬 ([report](plans/tabs/00-preflight-report.md)) — `preflight tabs` **PASSED** (0🔴/**2🟠**/1🟡/1🔵, all resolved Option A; codebase-grounded recon — direct reads of `dispatch.ts`/`keys.ts`/`theme.ts`/`show.ts`). **PF-001 (MAJOR):** the page-switch was specified both "via `Show`" **and** "no mount/dispose" — but `Show` **disposes** the inactive branch (`show.ts:33-37`), losing page state (a tabbed form's fields) and breaking ST-2 → switched to a reactive **`state.visible` binding** (all pages stay mounted, state preserved; `reflow.ts:68-70`). **PF-002 (MAJOR):** "global from anywhere inside the `TabView`" was unspecified for multiple/nested tab views — `preProcess` fires on **every** tab view in scope (`dispatch.ts:154`), switching the wrong panel → chord + Alt-hotkey now scoped by **`isWithin(ev.getFocused(), this)`**; new **ST-37/ST-38** two-`TabView` oracles. **PF-003 (MINOR):** clamp made **read/render-time** (caller owns `active`). **PF-004:** AC label fixed. Edits applied across 8 plan docs. Next: `exec_plan tabs`. Prior — **Plan Created** 📋 ([plan](plans/tabs/00-index.md)) — `make_plan RD-17`: `plans/tabs/` (9 docs, **21 tasks / 4 phases**, spec-first ST-1…ST-36 ↔ AC-1…AC-15). **Zero-Ambiguity Gate PASSED** (PA-1…PA-7; 4 user choices all took the recommendation): PA-1 all three Should-Haves (`select`/`next`/`prev` + snap-to-first-enabled + `onChange`) in scope · PA-2 **local `src/tabs/` glyph set** (frame line/corner code points + the added tab-junction tees `┬ ┴ ├ ┤`; no `frame.ts` edit) · PA-3 **3 roles** `tabActive`/`tabInactive`/`tabDisabled` (no strip-bg role) · PA-4 renderer split `tab-strip.ts`. Phase 1 = GATE-1 **BEFORE-decode** (pin the 3 `tab*` bytes via `cpAppColor` + decode the 4 tees) → theme roles spec-first; Phase 2 = `TabView`+`TabStrip` spec-first (**AC-4 feeds real decoder bytes** `CSI 6;5~`/`CSI 5;5~`; Ctrl+Tab asserted inert without the keyboard protocol); Phase 3 = GATE-1 **AFTER-diff** + impl tests; Phase 4 = packaging + kitchen-sink `containers/tabs` story + `demo:tabs` e2e. Next: `exec_plan tabs`. Prior — **Preflighted** 🔎 ([report](requirements/00-preflight-report-RD-17.md)) — `preflight RD-17` **PASSED** (0🔴/1🟠/1🟡/1🔵, all resolved Option A; fresh codebase-grounded recon via 3 Explore agents + 1 independent challenger). **PF-001 (MAJOR):** the RD's headline **Ctrl+Tab/Ctrl+Shift+Tab** "switch from anywhere" silently never fires on real terminals — the decoder maps byte `0x09`→`{tab, ctrl:false}` (`keys.ts:32,251-253`), plain Tab and Ctrl+Tab are byte-identical, and the disambiguating CSI-u/`modifyOtherKeys` path is unimplemented + host-disabled (`keys.ts:97`, `modes.ts:11-15` DEF-2); Ctrl+Shift+Tab collapses to reverse-focus (`dispatch.ts:124-128`), and a synthesized spec event passes GREEN while the feature is dead (challenger CONFIRMED all legs). → **Ctrl+PageUp/PageDown** made the reliable global switch (decoder-produced from `CSI 5;5~`/`CSI 6;5~`, `keys.ts:57-69,187-192`), Ctrl+Tab demoted to a best-effort keyboard-protocol-gated accelerator, AC-4 now feeds real bytes (**AR-183**). **PF-002 (MINOR):** the "reuse the shipped frame glyph set, no new glyph decode" claim is inaccurate — `frame.ts:77-94` glyph consts are module-private and contain **no `┬`/tee** → reuse the line/corner glyphs + **add the tab-junction tees `┬ ┴ ├ ┤`** at plan GATE-1, AC-2 reworded (**AR-184**). **PF-003:** story id pinned `containers/tabs` (**AR-185**). 8 of 10 reuse claims verified exact. Next: `make_plan RD-17`. Prior — **Drafted** ✏️ (`make_requirements --continue`, RD-12+ set sibling 4/6, first **Later**-phase RD, AR-129). **GATE-1 finding:** Turbo Vision has **no** tab/notebook/tabstrip class (whole-tree search: only `TRefTable` + the unrelated `TTable` ASCII demo view) — so RD-17 is a **documented new component** (stronger than RD-16, which had the `TListViewer` spine), with the *pieces* grounded in shipped TV facilities: box chrome (RD-05 frame glyphs), `~X~` hotkeys (`parseTilde`/`tildeSegments`), disabled greying (Button/Cluster), active/inactive colour (`cpAppColor` @ plan GATE-1). Self-contained **`TabView` extends `Group`** = focusable folder-tab strip (box-drawing, top, `┬`-notched) + bordered content region, one page visible via `Show`. Decisions (AR-172…182): folder-tab style/top strip (AR-173) · owns its frame (AR-174) · eager pages + `Show` (AR-175) · **MVP = all four** disabled + `~X~` hotkeys + `◄►` overflow + closeable/dynamic (AR-176) · `active: Signal<number>` clamp-on-remove (AR-177) · `tabs: Signal<Tab[]>` caller-owned, built-in `×` mutates + `onClose`, `Tab={title,content,disabled?,closeable?}` (AR-178) · Ctrl+Tab global + `←→` on focused strip + Alt-hotkey, plain Tab = content focus (AR-179) · additive `tab*` roles bytes @ plan GATE-1 (AR-180) · new `src/tabs/` (AR-181) · kitchen-sink `Tabs` story + `demo:tabs` (AR-182). 15 AC. Next: `make_plan RD-17` (or `preflight` first). |
| RD-18 | Feedback — ProgressBar + Spinner | [RD-18](../requirements/RD-18-feedback.md) | [plan](plans/feedback/00-index.md) | Done | ✅ | 2026-07-03 | **Done** ✅ (`exec_plan feedback --auto-commit` complete: 4 phases / **22 tasks** spec-first, 2026-07-03). Lands `packages/ui/src/feedback/` — **`ProgressBar`** (round-first smooth sub-cell fill `█`+`PARTIAL[1..7]`(U+258F…U+2589) over `░`; whole-cell `#`/`-` ASCII branch selected via `ctx.caps`; optional centred `NN%` caption; `value` clamped on read; `set`/`percent`) + **`Spinner`** (caller-driven `frame` with negative-safe mod; frozen `SPINNERS` presets dots/line/blocks; `asciiOnly` preset-swap to `line`; sanitized label) + **`runSpinner`** (self-re-arming one-shot timer over an injectable `TimerSeam`, idempotent leak-free `stop`) + `asciiOnly`/`PARTIAL` exports. Additive surface = **2 core `progress*` theme roles** (`progressFill` 0x1B / `progressTrack` 0x13, cyan-on-blue gauge family) + the **`DrawContext.caps` seam** (PA-1 — required 5th `makeDrawContext` param, threaded through `composeView`; 12 test call sites updated). GATE-1 BEFORE-decode + AFTER-diff recorded in code JSDoc (RD-18 has no TV counterpart, AR-186). Runtime **PA-11**: extended RD-17 `tabs-theme.spec` ST-30 key allowlist for the sanctioned `progress*` roles (byte guards preserved). Kitchen-sink `feedback/progress-bar` + `feedback/spinner` stories (ST-13 smoke) + headless `demo:feedback` + `feedback-demo.e2e`. Full `yarn verify` (8/8, 840 ui unit) + `test:e2e` (5/5) + `check:deps` (4/4) green; ESLint clean on all RD-18 files. 6 commits (`361efc4`…`cba2ab4`) pushed. jsvision-ui now **15/18** RDs. Prior — **Plan Preflighted** 🔬 ([report](plans/feedback/00-preflight-report.md), `preflight feedback` 2026-07-03) — PASSED (0🔴/1🟠/0🟡/0🔵, resolved Option A). **PF-001 (MAJOR):** the `DrawContext.caps` seam (PA-1) undercounted its blast radius — stated "~2/~3" test call sites and scoped task 1.1.2 to the `view.drawcontext*` glob, but `makeDrawContext(` is called at **12 test sites across 5 files** incl. **`view.hardening.spec.test.ts:119`** (outside that glob) → an executor would hit a typecheck miss on the required 5th param. Fixed: inventory corrected to ~11 sites across 5 files + the hardening file added to task 1.1.2 + a "grep `makeDrawContext(` to enumerate" instruction, across `02-current-state.md`/`03-03-theme-packaging.md`/`00-ambiguity-register.md` (PA-1)/`99-execution-plan.md`. All fidelity pins (glyph tables, `0x1B`/`0x13` bytes, one-shot timer, one production `makeDrawContext` site, `Theme`-literal safety) verified TRUE against live code; single code-verified finding, no challenger. Next: `exec_plan feedback`. Prior — **Plan Created** 📋 ([plan](plans/feedback/00-index.md), `make_plan RD-18` 2026-07-03) — `plans/feedback/` (9 docs, **22 tasks / 4 phases**, spec-first ST-1…ST-14 ↔ AC-1…AC-14 + impl). **Zero-Ambiguity Gate PASSED** (PA-1…PA-10). One **user-gated** architectural decision: **PA-1 — additive `DrawContext.caps` seam** (the RD's "the widget reads `caps`" had no draw-time delivery — `DrawContext` exposed no `caps`, the render root held it only for `serialize()`; Option A = surface `caps` on `DrawContext`, populated by `makeDrawContext` from the render root's caps, **1** production call site + ~3 mechanical test call sites). Source-determined GATE-1 pins: **PA-2** unified `asciiOnly=!utf8\|\|!halfBlocks`, bar whole-cell `#`/`-`, spinner non-`line`→`line`; **PA-3** 2 additive extension roles `progressFill 0x1B`/`progressTrack 0x13` (cyan-on-blue scrollbar-gauge family; caption/label reuse `staticText`/`label`); **PA-4** `round`-first fill `PARTIAL[1..7]`=U+258F…U+2589 over `░`; **PA-5** `SPINNERS={dots(⠋⠙⠹⠸⠼⠴⠦⠧⠇⠏),line,blocks(▏▎▍▌▋▊▉█)}`, default `dots`; **PA-6** files `progress-bar.ts`+`spinner.ts`+`run-spinner.ts`+`index.ts`; **PA-7** `runSpinner(frame,{intervalMs=80,timer})→stop()` over `Pick<RuntimeAdapter,'setTimer'\|'clearTimer'>`; **PA-8** `value`/`frame` caller-owned signals + `set`/`percent`; **PA-10** stories `feedback/progress-bar`+`feedback/spinner`, `demo:feedback`. Phase 1 = GATE-1 BEFORE-decode + the `DrawContext.caps` seam + 2 `progress*` roles spec-first; Phase 2 = widgets + `runSpinner` spec-first; Phase 3 = GATE-1 AFTER-diff + impl tests; Phase 4 = packaging + 2 kitchen-sink stories + `demo:feedback` e2e. Only additive surface = 2 core `progress*` roles + the `DrawContext.caps` seam. Next: `exec_plan feedback` (or `preflight feedback` first). Prior — **Preflighted** 🔎 (`preflight RD-18` PASSED — 0🔴/2🟠/1🟡/2🔵, all resolved, independent challenger confirmed both MAJORs; report `requirements/00-preflight-report-RD-18.md`). **PF-001 (MAJOR):** the ASCII fallback was specified as the shipped serialize-time `fallbackGlyph` path "exactly as frame/scrollbar", but `glyphs.ts:61-70` lacks 6 of the 7 eighth-block partials AND collapses both `█` and `░` to `#` (fill≡track, and `░→#` is global) → the bar/spinner now do a **widget-level `caps`-driven ASCII selection** (whole-cell `#`/`-` bar, spinner `line` preset), keeping "no new core primitive/no `glyphs.ts` edit" *true*. **PF-002 (MAJOR):** the fill-math oracle was self-inconsistent (FR `round(v·w·8)` vs AC-2 `floor(v·w·8/8)`, partial unspecified) → **AC-2 is now the single `round`-first oracle** (`e=round(v·w·8)`, `floor(e/8)` full `█` + `PARTIAL[e mod 8]` + `░`). **PF-003:** any non-`line` preset falls back to `line`; `blocks` glyphs pinned @ plan GATE-1. **PF-004/005:** mechanism wording + `host/types.ts` cite (126-129). RD + Ambiguity Register (AR-186/189/191) patched. Next: `make_plan RD-18`. Prior: **Drafted** ✏️ (`make_requirements --continue`, RD-12+ set sibling 5 of 6, second **Later**-phase RD). **GATE-1 finding: TV has NO progress/gauge/spinner/meter/throbber class** (whole-tree search of `source/`+`include/` returns nothing) → RD-18 = two **documented new components** (like RD-17), pieces grounded in Unicode Block Elements + the CP437 shade convention (bar), the shipped `fallbackGlyph` capability path (ASCII fallback, `render/glyphs.ts`), and the shipped `RuntimeAdapter.setTimer`/`clearTimer` seam (optional spinner timer, `host/types.ts:126-128`). 4 user decisions (AR-186…194, 14 AC): **both** a determinate `ProgressBar` (`value: Signal<number>` in `[0,1]`, clamped, optional `%` caption) **and** an indeterminate `Spinner`, no marquee bar (AR-187); **smooth sub-cell fill** `█`+`▏▎▍▌▋▊▉` over `░` with ASCII fallback (AR-189); **caller-driven pure Spinner** rendering `frames[frame() mod n]` + an optional `runSpinner` helper over the injectable timer seam (AR-190); spinner presets `dots`(braille)/`line`/`blocks`, default `dots` with ASCII fallback→`line` (AR-191). Additive feedback theme roles, bytes @ plan GATE-1 (AR-192). New `src/feedback/` (AR-193); kitchen-sink `ProgressBar`+`Spinner` stories + `demo:feedback` (AR-194). Next: `preflight RD-18` (optional) → `make_plan RD-18`. |
| RD-19 | Surface / SurfaceView | — | — | Backlog | ⬜ | 2026-07-03 | The last RD-12+ sibling (6 of 6; the **Later** phase, AR-126/129): **RD-19** Surface/SurfaceView (`TDrawSurface`/`TSurfaceView`). Unlike RD-17/18, this one **has a TV counterpart** at `include/tvision/surface.h`, so its RD requires a full GATE-1 **decode** (offscreen cell buffer + viewport). To be drafted via `make_requirements --continue`. Demo north-star (aspirational, AR-127): **clone `tvdemo`**. |
| RD-10 | TV behavioral fidelity — status press/release · cascade · tile · left-grow resize | [RD-10](requirements/RD-10-tv-behavioral-fidelity.md) | [tv-behavioral-fidelity](plans/tv-behavioral-fidelity/00-index.md) | Done | ✅ | 2026-06-30 | **Shipped** — completed RD-05's TV fidelity for the 4 behaviors the drawing pass (`1caa188`) deferred. **Status** press feedback + **emit-on-release** (green held-highlight, command of the item under the release point — PA-10 corrected AR-88; additive core `statusSelected` role); **TV-exact cascade** (`doCascade` `(i,i)`/corner-pinned, supersedes AR-87) + **tile** (`iSqr`/`mostEqualDivisors`/`dividerLoc`/`calcTileRect` verbatim, n=2 stacks, supersedes AR-87) with `tileError` no-op; functional **left-grow resize** (`dmDragGrowLeft`, SW grip, PA-11 width-floor clamp). 14/14 tasks, spec-first; ST-01…ST-09 + rewritten desktop ST-05/06 + status emit oracles all green. Commits `d326604`(P1) `6874d3f`(P2) `0ab8eda`(P3) `2aa8877`(P4); `yarn gate` PASSED. |
| DEF-23 | Glyph auto-swap fallback — probe-driven ASCII-safe chrome | [DEF-23](requirements/DEFERRED.md) | [glyph-auto-swap](plans/glyph-auto-swap/00-index.md) | Done | ✅ | 2026-07-02 | **Shipped ✅ (2026-07-02)** — all 4 phases / 20 tasks green (ST-01…ST-16); full verify + core e2e + gate pass. The `217f9ea` width-probe follow-up, now tracked (DEF-23; sibling DEF-24 = in-app warning visibility stays deferred). **Planned** 📋 ([plan](plans/glyph-auto-swap/00-index.md)) — closes the probe→render loop: new `glyphs.ambiguousWide` cap (default false) gating an 8-entry ncurses-style ASCII map in `fallbackGlyph` (`▲▼◄►•↑↕×` → `^v<>*^vx`); the probe amended in place (AR-16) to **two groups** (arrows→`ambiguousWide` · box/shade→`boxDrawing`/`halfBlocks` off, downgrade-only); host-side **effective serialize caps** (AR-4) + additive `adaptAmbiguousWidth` (core off / ui on, mirrors `warnAmbiguousWidth`) + the host-level `JSVISION_ASCII` force switch (AR-15 — env layer proved ineffective: apps override glyph caps) + two warning variants; probe skipped when caps already ASCII-safe (AR-13). 4 phases / 20 tasks, spec-first (ST-01…ST-16); AR-1…AR-17 ✅ GATE PASSED (3 surfaced during authoring). Zero app-API churn; ui touched only for option threading (AR-17). **Preflighted** 🔬 ([report](plans/glyph-auto-swap/00-preflight-report.md), 2026-07-02): 7 findings, all resolved & applied — **1🟠 PF-001** the SIGCONT resume repaint (`signals.ts:118`) serialized with the *original* caps, silently undoing adaptation after one Ctrl-Z/`fg` (fix: `getSerializeCaps` seam into `installSignals`; +ST-15; challenger-confirmed); **4🟡** (api-stability asserts docs not exports; `isAsciiSafe` now counts `utf8:false` as ASCII-safe +ST-16; `WidthProbeOptions.glyphs`→`arrowGlyphs?`/`boxGlyphs?`; ST-14 given a concrete probe-bytes observable) + **2🔵** (warn-text "monospaced" restored; EAW label softened). Next: `exec_plan glyph-auto-swap`. |

## Notes

- **2026-06-29** — Roadmap created for the jsvision UI feature-set, seeded from the
  component map ([`tui-ui/01-component-map.md`](tui-ui/01-component-map.md)) and
  ADR-008. No `requirements/RD-*.md` or execution plans exist yet — all rows start in
  Backlog. The foundation feature-set (RD-01…RD-10) is complete and archived at
  `_archive/foundation/`; this is its successor.
- **Pre-work already landed (before formal RDs):** the model decision — **disciplined
  hybrid** (retained tree + signals + `Show`/`For`); **ADR-008 Accepted** (build a
  cell-native layout engine, flex first / grid Tier 2, as a module in `@jsvision/ui`);
  and the **integer apportionment spike** (`apportion`/`solveTrack` + golden test) that
  de-risks RD-02's central premise. `@jsvision/ui` is scaffolded and CI-green.
- **2026-06-29** — **RD-01 (Reactive core) drafted** → stage `RD Drafted`. Requirements
  set re-initialized for this feature-set: the stale foundation `requirements/` scaffolding
  (README, ambiguity-register, _draft) was moved into `_archive/foundation/requirements/`,
  and a fresh set authored at `requirements/` (README + `00-ambiguity-register.md` AR-01…AR-12
  + `RD-01-reactive-core.md`). Four design decisions locked with the user: callable+methods
  signal API, synchronous effects + `batch()`, owner-scope tree + `onCleanup`, key-function `For`.
- **2026-06-29** — **RD-01 preflighted** → stage `RD Preflighted`. Fresh-session audit
  (`requirements/00-preflight-report.md`, iteration 2) surfaced 9 findings (3 MAJOR, 6 MINOR, 0
  CRITICAL) — all under-specification at the edges, none a design flaw. User accepted every
  recommended resolution; applied to RD-01 + new register entries AR-13…AR-18 (error base class
  = `TuiError`, no-owner dev-warn, exception propagation, nested-`batch` outermost flush, `For`
  duplicate-key policy, fixed 1000-iteration runaway limit). Re-scan clean.
- **2026-06-29** — **RD-01 plan created** → stage `Plan Created`. `plans/reactive-core/` written
  (8 docs): ambiguity register (plan decisions PA-1…PA-5 over inherited AR-01…AR-18), index,
  requirements, current-state + target file layout, 3 component specs (reactive-graph, ownership,
  combinators), testing strategy (ST-01…ST-20 ↔ AC-1…AC-20), execution plan (4 phases / 11 sessions,
  spec-first). Three plan-level decisions locked with the user: dev warnings = `console.warn` gated
  `NODE_ENV!=='production'` (PA-1), multi-throw cascade = first rethrown + rest `console.error`
  (PA-2), granular file split (PA-3).
- **2026-06-29** — **reactive-core plan preflighted** → stage `Plan Preflighted`. Fresh-session,
  codebase-grounded audit (`plans/reactive-core/00-preflight-report.md`, iteration 1): all 9 plan
  docs across 13 dimensions; every structural claim verified against the real code (`TuiError` ctor,
  barrel/entry pattern, two-project vitest, `check:deps`, no-`console.*`-in-`src`); scheduler
  propagation walked for glitch-freedom — no correctness defect. 6 findings (0 critical/major; 4
  MINOR + 2 observations), all resolved: ST-15 leak check made behavioral (PF-001), `For` duplicate-key
  output pinned via new **PA-6** (PF-002), `Show` flip driver specified (PF-003), `EqualsOption` export
  reconciled (PF-004), 2 clarity fixes (PF-005/006).
- **2026-06-29** — **RD-01 executed & shipped** → stage `Done` ✅. `exec_plan reactive-core` ran all
  4 phases spec-first (RED→GREEN→impl) across 4 commits: P1 graph foundation (signal/effect/scheduler/
  owner), P2 lazy memoized computeds + glitch-free diamond, P3 Show/For combinators, P4 packaging +
  gate. Lands `packages/ui/src/reactive/` (12 files, ≤ 500 lines each, full JSDoc, zero native deps).
  All 20 specification tests ST-01…ST-20 green + impl tests (55 ui tests total); `yarn verify` /
  `check:deps` / `lint` clean; every public symbol + type importable from `@jsvision/ui`. Two
  scheduler subtleties resolved during exec (recorded in the impl): resolve **all** of a CHECK node's
  sources before running it (glitch-free diamond), and don't mark observers on a computed's first
  compute; the runaway guard relies on a self-writing effect's mid-run re-mark surviving.
- **2026-06-29** — **RD-02 (Layout engine) drafted** → stage `RD Drafted` ✏️. `add_requirement`
  authored `requirements/RD-02-layout-engine.md` on top of ADR-008 + the landed apportionment
  spike. Scope: a pure `layout(LayoutBox tree, viewport) → parent-relative integer rects` pass —
  `row`/`col` flex containers, `fixed`/`fr`/`auto` sizing (`auto` via a `measure()` seam),
  `justify` (start/center/end/space-between), `align` (start/center/end/stretch, default stretch),
  `gap`/`padding`, overflow extends-past-edge (no shrink). 11 decisions locked AR-19…AR-29; grid
  (Tier 2), stack/overlay, and min/max constraints deferred. 18 acceptance criteria.
- **2026-06-29** — **layout-engine plan preflighted** → stage `Plan Preflighted` 🔬. Codebase-grounded
  audit (`plans/layout-engine/00-preflight-report.md`): 1 MAJOR + 4 MINOR + 1 OBSERVATION, all
  resolved by applying recommended fixes — corrected the false "exports already flow to
  `src/index.ts`" claim (layout uses explicit named re-exports, not `export *`), documented the
  acyclic/distinct-instance input precondition RD-02 deferred to planning, specified the `measure`
  `available` value, and clamped justify `free` to ≥ 0 so overflow extends past the far edge for
  any `justify`. Ready for `exec_plan`.
- **2026-06-29** — **RD-02 plan created** → stage `Plan Created` 📋. `plans/layout-engine/` written
  (7 docs): ambiguity register (PA-1…PA-5 over inherited AR-19…AR-29), index, requirements,
  current-state + target layout, 2 component specs (node-model-and-sizing, layout-pass), testing
  strategy (ST-01…ST-18 ↔ AC-1…AC-18), execution plan (4 phases / 11 sessions, spec-first). Builds
  on the `apportion`/`solveTrack` spike unchanged; geometry types defined locally (core exports
  none); CSS-parity defaults confirmed.
- **2026-06-29** — **RD-02 (Layout engine) complete** → stage `Done` ✅. Executed
  `plans/layout-engine/` across all 4 phases (spec-first, RED→GREEN per phase). Added
  `packages/ui/src/layout/{types,measure,layout}.ts` on the unchanged `apportion`/`solveTrack`
  spike: `layout(root, viewport) → Map<LayoutBox, Rect>` of parent-relative integer rects —
  `row`/`col` via a single axis abstraction, `fixed`/`fr`/`auto` sizing (`auto` pre-resolved via
  `naturalSize`), `justify`/`align`/`gap`/`padding`, overflow (extend past edge, `fr`→0),
  degenerate→zero rects, recursion in each box's local frame. 18 spec oracles (ST-01…ST-18 ↔
  AC-1…AC-18) + impl tests green; `yarn verify`/`check:deps`/`lint` clean; all files ≤ 217 lines;
  symbols re-exported from `@jsvision/ui`. Commits `f9a8cae`→ (Phases 1–4).
- **2026-06-29** — **RD-03 (View/Group spine) drafted** → stage `RD Drafted` ✏️.
  `add_requirement` authored `requirements/RD-03-view-group-spine.md` — the Phase-0 keystone
  binding RD-01 + RD-02 into a retained widget tree. 13 decisions locked AR-30…AR-42 (7 user
  choices, 6 dominant): RD-03 ships the **complete** `View`/`Group` shape with an overridable
  `onEvent` stub + `visible`/`disabled`/`focused` state but defers dispatch/focus **logic** to
  RD-04 (AR-30); each `View` owns an RD-01 owner scope + a `bind()` helper that closes the AR-09
  reactive seam (AR-31); a coalescing, **injectable** scheduler closes AR-02's redraw-frame seam
  (AR-32); RD-03 owns the **reflow pass** (view tree → `LayoutBox` → `layout()` → `bounds`), with
  relayout/repaint as distinct dirty-phases (AR-33); bounds-clip + back-to-front paint, occlusion
  deferred (AR-34); one app theme via `ctx.color(role)`, per-subtree override deferred behind a
  seam (AR-35); dynamic children reuse `Show`/`For` with `N=View`, nested scopes ⇒ leak-free
  unmount (AR-36); geometry reuses RD-02's public `Rect`/`Size2D` (AR-37); single shared
  `ScreenBuffer` composition (AR-38); stateless clipped `DrawContext` (AR-39); `View` abstract /
  `Group` concrete, no leaf widgets (AR-40); `visible:false` ⇒ `display:none` (AR-41); `draw()`
  throw ⇒ isolate + log, finish the frame (AR-42). 20 acceptance criteria. README index +
  dependency graph + glossary synced.
- **2026-06-29** — **RD-03 preflighted** → stage `RD Preflighted` 🔎. Codebase-grounded audit
  against the live `@jsvision/core` (render/color/safety) + RD-01/RD-02 surfaces. 6 findings
  (2 MAJOR, 3 MINOR, 1 OBSERVATION); all resolved into the RD + register (new entries AR-43…AR-46),
  report at [`requirements/00-preflight-report.md`](../requirements/00-preflight-report.md).
  Key catches: **PF-001** — RD-01's `createRoot` nests under the *ambient* owner, so
  imperatively-added child scopes don't nest under their parent; resolved by adding a small
  **additive RD-01 `runWithOwner(owner, fn)`** primitive (AR-43), to be the RD-03 plan's first task.
  **PF-002** — `RenderRoot` lacked the required `caps: CapabilityProfile` + previous-buffer that
  core `serialize()` needs (AR-44). Plus `ThemeRoleName = keyof Theme` + role→Style adapter (AR-45),
  relative intra-package geometry import (PF-004), and `bind` repaint/relayout opt-in (AR-46).
- **2026-06-29** — **RD-03 plan created** → stage `Plan Created` 📋. `make_plan` produced
  [`plans/view-group-spine/`](view-group-spine/00-index.md) from RD-03: 9 docs, 7 spec-first
  phases / 21 sessions / ~29–41 h, gate PASSED (PA-1…PA-8 + inherited AR-30…AR-46). Three new
  plan-level decisions beyond the RD/preflight: **PA-1** `runWithOwner` API = Solid-parity
  (`runWithOwner`+`getOwner`+opaque `Owner`, additive on RD-01, doesn't touch `createRoot`);
  **PA-2** `bind` happens in `onMount` (scope exists at add-time; pre-mount bind throws
  `TuiError`); **PA-8 / clone** — partial recompose (AC-7) needs a faithful previous-frame
  snapshot, so the plan adds a second additive primitive **`ScreenBuffer.clone()`** on core
  `render`. Phase 1 builds both primitives first; the spine (geometry → tree → DrawContext →
  reflow → render-root → scheduler → dynamic children) follows; Phase 7 ships `demo:view`.
- **2026-06-29** — **view-group-spine plan preflighted** → stage `Plan Preflighted` 🔬.
  Codebase-grounded audit ([`plans/view-group-spine/00-preflight-report.md`](view-group-spine/00-preflight-report.md)):
  all 10 plan docs across 13 dimensions; every `file:line` claim re-verified against the live code
  (`reactive/owner.ts`/`scheduler.ts`/`for.ts`/`show.ts`, core `buffer.ts`/`serialize.ts`/
  `theme.ts`/`logger.ts`, both barrels, the RD-03 preflight report) — all accurate. 4 findings
  (1 MAJOR, 2 MINOR, 1 OBSERVATION; 0 CRITICAL), all resolved by applying recommended fixes:
  **PF-001** — `Group` had no API to accept a `Show`/`For` producer (`add` takes a `View`, a
  producer is an accessor); added `Group.addDynamic(producer)` wired through the existing
  `runWithOwner`/`mountView` reconcile seam. **PF-002** — corrected the draw-error log call to the
  real `Logger.error(component, msg, fields?)` signature. **PF-003** — disambiguated bare "RD-04"
  (foundation render engine, not jsvision-ui's event loop) in DoD #6. **PF-004** — recorded that
  partial recompose assumes non-overlapping siblings (a constraint RD-05 windows inherit). Plan is
  feasible, spec-first, accurately grounded; ready for `exec_plan`.
- **2026-06-29** — **RD-03 (View/Group spine) complete** → stage `Done` ✅. Executed
  `plans/view-group-spine/` across all 7 phases spec-first (RED→GREEN→impl per phase), 8 commits.
  Phase 1 added the two enabling primitives (`runWithOwner` on RD-01 reactive, `ScreenBuffer.clone()`
  on core render); Phases 2–7 built the spine under `packages/ui/src/view/`: the `View`/`Group`
  retained tree + owner-scope lifecycle, the stateless clipped `DrawContext` + theme-role adapter,
  the RD-02 reflow pass, the render root + compose walker (clip, back-to-front, bg fill, draw-error
  isolation), the coalescing scheduler + partial recompose (repaint/relayout phases), and dynamic
  children (`Group.addDynamic` over `Show`/`For`) + packaging + the runnable `demo:view`. 20 AC
  realized as ST-01…ST-20 (+ ST-21/22 primitives); ui 142 unit tests + 3 e2e green;
  `verify`/`check:deps`/`lint` clean. One latent bug fixed during exec: `View.mount` now `untrack`s
  its scope setup so the wiring-reset `onCleanup` binds to the view's own scope, not an ambient
  reconcile effect (would have leaked dynamic children). Runtime decisions RT-1…RT-5 recorded in the
  plan's `00-ambiguity-register.md`.
- **2026-06-29** — **RD-04 (Event loop + focus + modality + commands) drafted** → stage
  `RD Drafted` ✏️. `add_requirement` authored `requirements/RD-04-event-loop.md` — the
  host-agnostic dispatch **mechanism** that makes the RD-03 spine interactive. 8 design
  decisions locked with the user (AR-47…AR-54): ships the dispatch mechanism (`EventLoop`),
  deferring concrete `Application`/`run()`/shell to RD-05 (AR-47, mirrors the RD-03 boundary);
  per-group `current` focus chain (AR-48); pure injectable `dispatch(event)`, host wiring
  deferred (AR-49); top-most-first mouse hit-testing in RD-04 (AR-50); faithful 3-phase
  pre/focus/post dispatch (AR-51); typed command layer — `CommandEvent` + registry +
  key→command keymap (AR-52); modal stack + `endModal(result)` → `Promise` (AR-53); the loop
  drives `RenderRoot` frames via the injected scheduler (AR-54). 5 dominant decisions recorded
  (AR-55…AR-59): `EventLoop`/`createEventLoop` central object, additive `focusable` predicate,
  Tab/Shift-Tab traversal, `onIdle`-only (broadcast/timers → RD-05), headless `demo:events`
  vehicle. 20 acceptance criteria. README index + dependency graph synced.
- **2026-06-29** — **RD-04 preflighted** → stage `RD Preflighted` 🔎. Codebase-grounded audit
  ([report](../requirements/00-preflight-report-RD-04.md)) against the live `@jsvision/core`
  input/host surfaces + RD-03 `View`/`RenderRoot` seams: 8 findings (3 MAJOR, 4 MINOR, 1 OBS),
  all resolved Option A and applied to RD-04 (recorded AR-60…AR-66). Key corrections: the 3-phase
  `handled` flag can't live on core's **readonly** `InputEvent` → a `DispatchEvent` envelope
  (PF-401); "inject scheduler into the render root" was infeasible (construct-time/private seam) →
  the loop now **builds** the `RenderRoot` (PF-402); the bespoke `'Ctrl-Q'` keymap duplicated/
  conflicted with core's existing `createKeymap` (`'ctrl+q'`) → reuse it (PF-403); 1-based mouse
  coords vs 0-based bounds (PF-404); dispatch-tick batch model (PF-405); `focusable` default-false +
  subtree semantics (PF-406); injectable loop `logger` for AC-19 (PF-407).
- **2026-06-30** — **RD-04 plan created** → stage `Plan Created` 📋. `make_plan` produced
  [`plans/event-loop/`](event-loop/00-index.md): 5 phases / 15 sessions / ~23–33 h, spec-first
  (ST-01…ST-20 ↔ AC-1…AC-20). Zero-Ambiguity Gate passed — 4 user choices (PA-1 keymap **consume**;
  PA-2 additive `preProcess`/`postProcess` booleans; PA-3 unknown command **enabled by default**;
  PA-4 **explicit-only** `endModal`, defaults → RD-05) + 5 dominant (PA-5…PA-9). Builds entirely on
  the existing core + RD-03 surface — **no** cross-package primitive. New `packages/ui/src/event/`
  (types · dispatch · commands · focus · hit-test · modal · event-loop) + additive `view.ts`/
  `group.ts`/`view/types.ts` + `demo:events`.
- **2026-06-30** — **RD-04 plan preflighted** → stage `Plan Preflighted` 🔬. Codebase-grounded audit
  ([report](event-loop/00-preflight-report.md)) verified every `file:line` claim against live source
  (View/Group/RenderRoot/geometry + core events/keymap) and re-derived the deferring-scheduler
  frame-ownership mechanism as sound. **2 MAJOR + 3 MINOR + 2 OBSERVATION**, all resolved: **PF-001**
  — a single internal `runTick` now drives every public mutator (not just `dispatch`/`resize`), so a
  standalone `focusNext`/`emitCommand` paints and `emitCommand` actually drains (**PA-11**);
  **PF-002** — the Phase-2 focused-chain bubble is clamped to the modal scope root so capture can't
  leak to the outer tree (**PA-12**); **PF-003** — built-in `tab`/`shift+tab`→`focusNext`/`focusPrev`
  (consumed; keymap-bound `tab` overrides), user-confirmed (**PA-10**); + ST-02 focus-wiring,
  flush-counter, and `{...ev}` envelope-copy notes (PF-004/006/007); task denominator fixed 30→33
  (PF-005). No CRITICAL; AC↔ST coverage complete 1:1.
- **2026-06-30** — **RD-04 executed** → stage `Done` ✅. All 5 phases run spec-first (RED → GREEN →
  impl) the same way RD-01…RD-03 went. Lands `packages/ui/src/event/` (types · dispatch · commands ·
  focus · hit-test · modal · event-loop · index) + additive `view.ts`/`group.ts`/`view/types.ts`
  (`focusable`/`preProcess`/`postProcess`, `onEvent(DispatchEvent)`, `Group.current`, the
  `CommandEvent`/`AppEvent`/`DispatchEvent` contract types) + `event-demo` (`demo:events`). 20 spec
  oracles (ST-01…ST-20 ↔ AC-1…AC-20) + impl tests green; full gate clean (`yarn verify` 8/8,
  `test:e2e` event-demo + core, `check:deps`, `lint`); every `event/` file ≤ 227 lines, no dead code.
  PA-10/PA-11/PA-12 all realized (built-in Tab, single `runTick`, modal scope clamp); PF-008 onEvent
  spec-oracle adaptation applied. One runtime note: a pathological re-entrant impl-test handler
  caused an infinite command cascade — fixed in the test (emit only on key events); the faithful
  drain-loop has no runaway guard, per the plan design.
- **2026-06-30** — **RD-05 (App shell) drafted** → stage `RD Drafted` ✏️. `add_requirement` authored
  `requirements/RD-05-app-shell.md` — the Phase-0 **integration keystone** composing RD-04's `EventLoop`
  into a runnable windowed application: `Application`/`run()` (the first live-TTY `createHost` ↔ `dispatch`
  wiring, quit→exit code, guaranteed restore-on-every-path, suspend/resume), a full interactive `Desktop`
  window manager (z-order raise-on-click — the piece RD-04 deferred — drag-move, free drag-resize, zoom,
  cascade/tile, Alt-N, close), `Window`/`Frame` with active/inactive theming, full nested `MenuBar`/
  `MenuPopup`, and a static `StatusLine`. 15 decisions locked: **AR-67…AR-74** (8 user choices — WM
  ambition, menu depth, the Scroller/ScrollBar boundary, demos, `run()`/quit, status-line dynamics,
  active/inactive theming location, window resize) + **AR-75…AR-81** (7 dominant — composition-over-
  inheritance, `Commands` constants, tilde hotkeys, raise semantics, `execView` reuse, desktop background,
  packaging). 22 AC. Scope **refined**: `ScrollBar`/`Scroller` and the rich `Dialog` moved to RD-06
  (AR-69/AR-79); the **only** cross-package change is one additive active/inactive window role on
  `@jsvision/core`'s `Theme` (AR-73). Demos = headless `demo:shell` + a real-TTY interactive app (AR-70).
- **2026-06-30** — **RD-05 preflighted** → stage `RD Preflighted` 🔎. Codebase-grounded audit
  ([report](requirements/00-preflight-report-RD-05.md)) of the draft against the live RD-03/RD-04/core
  surfaces (14 files examined, ~20 references verified; an independent challenger re-verified the 3
  MAJOR findings against the same `file:line` evidence). **3 MAJOR + 3 MINOR resolved (all Option A),
  recorded as AR-82…AR-87.** The two MAJORs that matter most: RD-04 routes each mouse event only to the
  top-most hit view (no capture, no bubble), so drag-move / single-cell resize need an **additive
  pointer-capture seam** (`setCapture`/`releaseCapture` — AR-82/PF-001); and the loop exposes no
  frame-flushed hook, so async `endModal`/command frames would never paint — fixed by an **additive
  `onFrame` hook** the `run()` wiring pushes to `host.render` (AR-84/PF-003). Both are intra-package
  additive seams (the loop is composed, not re-shaped). The third MAJOR corrected a stale claim: core's
  host already re-asserts modes + full-repaints on SIGCONT before firing `onResume` (notify-only —
  AR-83/PF-002). MINORs: `resize`/`move` dropped from `Commands` until a keyboard mode exists (AR-85),
  `run()` exit code defined (`0` / `quit` arg — AR-86), cascade/tile edge cases pinned (AR-87). Most
  RD-05 references verified accurate as-is (host options, core exports, the genuinely-missing
  active/inactive Theme role, View/Group surfaces, full EventLoop API, Alt+hotkey decoding).
- **2026-06-30** — **RD-05 (App shell) planned** → stage `Plan Created` 📋. `make_plan` authored
  [`plans/app-shell/`](plans/app-shell/00-index.md) (8 docs): register (PA-1…PA-14 + inherited AR-67…AR-87,
  ✅ GATE PASSED), index, requirements (Source: RD-05), current-state, five component specs
  (Application/run · Desktop WM · Window/Frame · Menus · StatusLine/Commands/Theme/seams), testing
  strategy (ST-01…ST-22), and a 6-phase / 16-session / ~30–42 h execution plan. **4 user choices**
  (PA-1 `windowInactive` core role · PA-2 dedicated menu **overlay layer**, non-modal · PA-3 optional
  `viewport?` default stdout→80×24 · PA-4 compact WM preset 10×3/title-row-clamp/+1+2) + **10 dominant**
  (PA-5 capture semantics · PA-6 `onFrame` timing · PA-7 Desktop↔loop seam · PA-8 Frame-as-helper · PA-9
  MenuBar-owned nav controller · PA-10 gesture state on Desktop · PA-11 file layout · PA-12 command wiring ·
  PA-13 demos · PA-14 fake-runtime lifecycle tests). One cross-package edit (`windowInactive`); two additive
  intra-package loop seams (capture + `onFrame`); the loop is composed, not re-shaped. Cascaded into the
  **jsvision-ui** portfolio row.
- **2026-06-30** — **RD-05 (App shell) plan preflighted ×2** → stage `Plan Preflighted` 🔬.
  Codebase-grounded audit of the plan against live RD-02/RD-03/RD-04/core source
  ([report](plans/app-shell/00-preflight-report.md)). **Iteration 1** — 1 CRITICAL + 4 MAJOR + 2 MINOR
  + 2 OBS (PF-01…PF-09): the WM's direct-`bounds` mutation can't express free-floating/overlapping
  windows on the pure-flex reflow (PF-01), and a flex overlay can't host menu popups (PF-02) — both
  resolved by an additive RD-02 `position:'absolute'` placement mode landed as a spec-first **Phase 0**
  (+ RD-03 `DrawContext.role` for the desktop pattern/frame colors, PF-03); `onFrame` made a settable
  `EventLoop` member (PF-04), `Window.focusable` (PF-05), a menu click-catcher (PF-06), + 3 corrections
  (PF-07/08/09). Recorded as PA-15…PA-19; re-baselined to **6 phases / 18 sessions / 48 tasks**.
  **Iteration 2** — 1 CRITICAL + 2 MAJOR + 2 MINOR (PF-10…PF-14): the always-present full-viewport
  overlay would itself win the top-z hit-test and **swallow all mouse input when empty** (PF-10 → gate
  `overlay.state.visible` on menu state); ST-04/AC-4's "restore on a *handler* throw" contradicts the
  shipped AR-66 handler-isolation (PF-11 → re-spec ST-04 to the escaping-throw path); and Phase 2's
  `createApplication`/ST-01 needs `Desktop`/chrome classes only built in Phases 3–5 (PF-12 → seed minimal
  Phase-1 skeletons). All Option A, recorded as PA-20…PA-22; two text fixes (PF-13/14). An independent
  challenger confirmed every critical/major finding against `file:line` evidence.
- **2026-06-30** — **RD-05 (App shell) complete** → stage `Done` ✅. `exec_plan app-shell` executed all
  6 phases spec-first (48/48 tasks): Phase 0 additive primitives (RD-02 `position:'absolute'`+`rect`,
  RD-03 `DrawContext.role`), Phase 1 seams+theme (`EventLoop` `setCapture`/`releaseCapture`/`onFrame`,
  core `Theme.windowInactive` — the sole cross-package edit), Phase 2 `createApplication`/`run()`
  lifecycle, Phase 3 `Desktop` WM + `Window`/Frame chrome, Phase 4 overlay-hosted nested `MenuBar`
  menus, Phase 5 `StatusLine` + `demo:shell` + final gate. 22 spec oracles (ST-01…ST-22) + impl tests
  green; full gate clean (`verify` 273 ui + core · `test:e2e` 8 core + examples · `check:deps` · `lint`);
  largest new file `menu/controller.ts` 332 lines. Runtime notes: menu `→`/`←` semantics + the
  `StatusLine.itemBoxes()` rename (avoided shadowing the inherited `View.layout` property).
- **2026-06-30** — **TV drawing-fidelity pass shipped** (commit `1caa188`). Audited every existing
  chrome component (desktop · window/frame · menu · status) against the original `magiblot/tvision`
  source per the NON-NEGOTIABLE fidelity directive and corrected colors/glyphs/geometry/hotkeys to
  match: the blue `cpBlueWindow` (white active / lightGray inactive frame on blue + brightGreen icon
  accent), the steel `0x71` desktop (blue ░ on lightGray), red menu-bar/popup/status hotkeys (incl.
  multi-char `~…~` runs via a new `tildeSegments`), the 2-column drop shadow (`shadowSize {2,1}`),
  active-gated frame icons, off-by-one icon columns, both resize grips, and TV-faithful title
  truncation. `yarn verify` (823 tests) + `lint` clean; 3 spec/impl oracles updated to the faithful
  values. **Four behavioral items deferred** (status emit-on-release, cascade/tile geometry, left-grow
  resize) → captured as **RD-10**.
- **2026-06-30** — **RD-10 (TV behavioral fidelity) SHIPPED** → stage `Done` ✅.
  `exec_plan tv-behavioral-fidelity --auto-commit` ran all 4 phases spec-first (RED→GREEN→impl): **P1**
  status press-feedback + emit-on-release with pointer capture (additive core `statusSelected` role;
  PA-10 corrected AR-88 to TV's *item-under-release* target) `d326604`; **P2** TV-exact cascade + tile
  — `iSqr`/`mostEqualDivisors`/`dividerLoc`/`calcTileRect` ported verbatim from `tdesktop.cpp`,
  `tileError` no-op, ST-11 rewritten to ST-05/06 `6874d3f`; **P3** functional left-grow resize
  (`dmDragGrowLeft`, SW grip → `resize-left` gesture/zone; PA-11 refined PA-7's clamp to a width-floor
  mirroring the SE corner) `0ab8eda`; **P4** TV-accurate demo narration + final gate `2aa8877`. Final:
  `yarn verify` (core 483 · ui 301 · examples 49), `check:deps`, e2e 5/5, `lint` clean, `yarn gate`
  PASSED. One additive cross-package edit (`statusSelected`); the loop composed, not re-shaped.
  Cascaded to the portfolio row.
- **2026-06-30** — **RD-10 (TV behavioral fidelity) planned** → stage `Plan Created` 📋.
  `make_plan` authored [`plans/tv-behavioral-fidelity/`](plans/tv-behavioral-fidelity/00-index.md) (9
  docs): register (PA-1…PA-9 over inherited AR-88…AR-92, ✅ GATE PASSED), index, requirements
  (Source: RD-10), current-state (each behavior's `file:line`), three component specs (status
  press/release · cascade+tile geometry · left-grow resize), testing strategy (ST-01…ST-09 + the
  rewritten desktop ST-11 + status emit oracles), and a 4-phase / 10-session / 14-task / ~12–19 h
  spec-first execution plan. One user plan-decision (PA-6: too-small desktop ⇒ TV `tileError` no-op,
  superseding AR-87's clamp-overflow); the rest dominant/source-determined (TV `tdesktop.cpp`/
  `tstatusl.cpp`/`tframe.cpp` algorithms ported verbatim). One additive cross-package edit
  (`statusSelected` core role); the loop is composed, not re-shaped. Cascaded to the portfolio row.
- **2026-06-30** — **RD-10 (TV behavioral fidelity) drafted** → stage `RD Drafted` ✏️.
  `add_requirement` authored `requirements/RD-10-tv-behavioral-fidelity.md` + register **AR-88…AR-92**
  (all user choices). Captures the four behaviors the drawing pass deferred: (1) status-line
  press-feedback + **emit-on-release** (TV `drawSelect`, held item black-on-green, command on mouse-up
  if still over the enabled item) — supersedes emit-on-press, re-adds the additive `statusSelected`
  core role; (2) **TV-exact cascade** (+1col/+1row stagger, extend to the desktop corner); (3)
  **TV-exact tile** (`mostEqualDivisors`/`dividerLoc`/`leftOver`, n=2 stacks) — (2)+(3) **supersede
  AR-87**'s compact preset and rewrite the ST-11 oracle; (4) **functional left-grow resize**
  (`dmDragGrowLeft`, the already-drawn bottom-left grip). Placed as **RD-10** (RD-06…09 reserved for
  the widget tiers — AR-92); 11 AC; M complexity. README index + dependency note synced; cascaded to
  the portfolio roadmap.
- **2026-06-30** — **RD-06 (Essential controls + validators) drafted** → stage `RD Drafted` ✏️, and
  **RD-11 (Containers, scrolling & lists) stubbed** as its sibling. `add_requirement` **split** Phase-1's
  controls (AR-93): RD-06 = the focused **leaf controls + validators** (`Text`/`Label`/`Button`/`Input`/
  `CheckGroup`/`RadioGroup` + internal `Cluster` base + `filter`/`range`/`lookup` validators), demoable in
  a plain `Window` via a headless `demo:controls`; `ScrollBar`/`Scroller`/`ListView`/`Dialog` reserved in
  the **RD-11 stub** (authored later — its `Dialog` hosts RD-06's controls). New register **AR-93…AR-102**
  (7 user + 3 dominant/source). One additive cross-package edit — the faithful **`cpGrayDialog` control
  theme roles** on core `Theme` (slots 6–21/31; ScrollBar/ListViewer roles → RD-11). **Deferred (tracked)
  → RD-07:** `Input` selection+clipboard (AR-94), `picture`/mask validator (AR-95), `MultiCheckGroup`
  (AR-96) — recorded in RD-06's "Deferred (tracked)" table + the RD-07 row. 13 AC; new
  `packages/ui/src/controls/` subsystem. README index + dependency graph synced; cascaded to the
  portfolio roadmap.
- **2026-06-30** — **Consolidated deferred-items register added** ([`requirements/DEFERRED.md`](requirements/DEFERRED.md)).
  A single feature-wide index (`DEF-01…DEF-15` + whole-RD scope + non-goals + a foundation cross-ref) so
  no intentionally-deferred capability is lost between RDs — swept from every per-RD Won't-Have/Deferred
  table (RD-01…RD-06, RD-10). Amends AR-99 (per-RD tables stay authoritative; this aggregates them). Each
  future `add_requirement` appends its deferrals here and flips absorbed items to `→ <RD>`.
- **2026-07-01** — **RD-06 (Essential controls + validators) planned** → stage `Plan Created` 📋.
  `make_plan` produced [`plans/essential-controls/`](plans/essential-controls/00-index.md) (12 docs):
  register (PA-1…PA-14, ✅ GATE PASSED — 3 user choices: the `ev.emit` command primitive, the
  expose-`valid()`-no-trap invalid behavior, the `boolean[]`/`number` cluster value shapes; 11
  dominant/source), index, requirements (Source: RD-06), current-state, seven component specs
  (foundation · Text+Label · Button · Validators · Input · Clusters · demo), testing strategy
  (ST-01…ST-16), and a 7-phase / 20-session / 24-task / ~21–35 h spec-first execution plan. Grounded in
  three current-state recon passes (UI control seams · TV `t*.cpp` geometry · `tvalidat.cpp` + the
  `app.h`/`cpGrayDialog` palette, hand-verified — an agent decode was caught wrong, so role bytes are
  pinned from source in the spec oracle). One additive cross-package edit (control theme roles; buttons
  reuse `button`/`buttonFocused`) + one additive intra-ui primitive (`ev.emit`/`ev.focusView`). Planning
  surfaced DEF-16…18 (modal focus-trap → RD-11, multi-column cluster, Text-align), added to
  `DEFERRED.md`. Cascaded to the portfolio row.
- **2026-07-01** — **RD-06 (Essential controls + validators) SHIPPED** → stage `Done` ✅ (24/24 tasks,
  spec-first, final gate green) and the kitchen-sink showcase landed (Storybook-for-TUI; the older
  `demo:*` walkthroughs folded in as Foundations stories). See the portfolio roadmap for the full note.
- **2026-07-01** — **RD-11 (Containers, scrolling & lists) drafted** → stage `RD Drafted` ✏️.
  `add_requirement` ran a condensed discovery + the Zero-Ambiguity Gate for the sibling reserved by
  AR-93 (now unblocked — RD-06 settled): **`ScrollBar`** (two-way `value` signal + range/step, V+H) ·
  **`Scroller`** (auto-creates & owns its bars) · a generic single-column virtual-scroll **`ListView<T>`**
  (`getText` + items/focused signals; optional sorted + type-ahead; `ListBox` string preset) · the rich
  modal **+ modeless `Dialog`** (terminating-command result + a child `valid()` close-gate that **realizes
  DEF-16**; OK/Cancel/Yes/No standard-button helpers). Additive `cpScrollBar` (4–5) + ListViewer (26–29)
  core theme roles (the AR-97 pattern) + `ok`/`cancel`/`yes`/`no` `Commands`; new `src/{scroll,list,dialog}/`
  subsystems. Dogfooding: the kitchen-sink navigator upgrades to a `ListView`-in-`Scroller` sidebar (an AC).
  **15 AC; AR-103…AR-114** (AR-103…110 user, AR-111…114 dominant). Deferred → RD-07: multi-column
  `ListViewer`/`Table`, `ComboBox`. Register + README + `DEFERRED.md` (DEF-16 → RD-11) synced; cascaded to
  the portfolio row.
- **2026-07-01** — **RD-11 (Containers, scrolling & lists) planned** → stage `Plan Created` 📋
  ([`plans/containers-scrolling-lists/`](plans/containers-scrolling-lists/00-index.md)). 12 docs; 6 phases
  (0 foundations → ScrollBar → Scroller → ListView/ListBox → Dialog → kitchen-sink) / 33 tasks, spec-first
  (ST-01…ST-16 ↔ AC-1…AC-15); **PA-1…PA-15 (3 user + 12 decoded/dominant, ✅ GATE PASSED)**. Two parallel
  recon passes: (a) UI seams — **all RD-11 changes are additive** (Window/Desktop/loop/controls reused
  as-is; `execView` already returns the modal result); (b) a **hand-verified TV GATE-1 decode**
  (`tscrlbar`/`tscrolle`/`tlstview`/`tlistbox`/`stddlg`/`tdialog.cpp` + the `cpGrayDialog`→`cpAppColor`
  chain, byte-checked → the six theme roles get real TV colours, not guesses). 3 user plan-choices: the
  additive `attachModalHost` loop seam (PA-1), `ListView = Group[rows + owned ScrollBar]` (PA-2), linear
  type-ahead (PA-3). Additive-only edits: 6 core theme roles + `Commands` ok/cancel/yes/no + the modal-host
  seam + new `src/{scroll,list,dialog}/`. DEF-16 realized by the Dialog `valid()` gate. Cascaded to the
  portfolio row. Next: `exec_plan containers-scrolling-lists` (optionally `preflight` first).
- **2026-07-01** — **RD-07 (Essential-control completions) drafted → preflighted** → stage `RD Preflighted`
  🔎 ([report](requirements/00-preflight-report-RD-07.md)). Drafted via `add_requirement` (the thin
  control-completions slice: `Input` selection+system-clipboard · `picture(mask)` · `MultiCheckGroup` ·
  visible caret; AR-115…AR-124), then a **same-session** codebase-grounded preflight (6 parallel recon
  agents + 1 adversarial challenger). **PASSED WITH NOTES** — 0🔴 / 0🟠 + 3🟡 + 2🔵. Key results: every
  infra-existence claim verified **exact** (`setClipboard`/`PasteEvent`+1 MiB cap/`cursor.*`/`Validator`/
  `Cluster`/DispatchEvent seams/`keyof Theme` auto-flow/missing `cut/copy/paste`); the pivotal
  **feasibility** question — can the core decoder emit Shift/Ctrl-Shift arrows + Ctrl+A + Ctrl/Shift-Ins/Del?
  — is **YES** (test-backed, `input/keys.ts`); the hardware-caret host seam confirmed **additive, no core
  change** (challenger REFUTED an initial MAJOR framing — `run()` co-owns the output stream). 3 MINOR fixed
  in the RD (caret suspend/resume clause · 2 drifted TV `file:line` cites corrected · "Depends On"
  reworded); 2 OBS (selection-role naming, grapheme index unit) carried to `make_plan` GATE-1. Cascaded to
  the portfolio row.
- **2026-07-01** — **RD-07 (Essential-control completions) planned** → stage `Plan Created` 📋
  ([`plans/essential-control-completions/`](plans/essential-control-completions/00-index.md)). 9 docs; 7
  phases (0 foundations → Input selection+logical caret → clipboard → `picture` → `MultiCheckGroup`+story →
  hardware caret+demos → gate) / 34 tasks, spec-first (ST-01…ST-16 ↔ AC-1…AC-15); **PA-1…PA-13 (4 user + 9
  dominant/source, ✅ GATE PASSED)**. Grounded in **3 citation-rich GATE-1 TV decodes** performed up front
  (`TInputLine` selection/clipboard/`setCursor`; `TPXPictureValidator` full DSL + autoFill + syntaxCheck;
  `TMultiCheckBoxes`/`TCluster::drawMultiBox`) — recorded in the `03-*` specs, re-verified per component at
  BEFORE-decode/AFTER-diff gate tasks. 4 user gate-choices: **code-point** caret unit (PA-1; full grapheme →
  new DEF-21), **reject-at-syntaxCheck** mask DoS bound (PA-2: `MAX_REPEAT`+step budget), **autoFill default
  ON** (PA-3), selection role **`inputSelection`** (PA-4). Additive-only edits: core `inputSelection` Theme
  role + `Commands.cut/copy/paste` + the `View.desiredCaret`/`RenderRoot.caret`/`EventLoop.onCaret`/
  `writeClipboard` seams (**no core host change** — the caret host-application was challenger-confirmed
  additive at preflight). New deferrals: DEF-21 (grapheme stepping), DEF-22 (cluster caret). Cascaded to the
  portfolio row. Next: `exec_plan essential-control-completions`.
- **2026-07-02** — **RD-07 (Essential-control completions) plan preflighted** → stage `Plan Preflighted` 🔬
  ([report](plans/essential-control-completions/00-preflight-report.md)). Codebase-grounded 13-dimension audit
  (~14 ui+core files read; ~40 `file:line` refs verified exact against source; the 3 TV C++ decodes re-checked
  against `tinputli.cpp`/`tmulchkb.cpp`/`tcluster.cpp`; 1 independent adversarial challenger on the two
  blockers). **8 findings — all resolved (all Option A):** **2🟠** genuine plan-level catches the RD-07
  requirements preflight missed — **PF-001** `MultiCheckGroup` cannot inherit the boolean `Cluster.draw()` seam
  (base generalized to TV's marker-string `markers`+`markIndex` model; CheckGroup/RadioGroup migrated; +P4.7
  RD-06 regression task) and **PF-002** hardware-caret collection coupled to the compose walker breaks on
  partial recompose (caret now derived by the EventLoop from a new pure `RenderRoot.originOf()` post-`flush()`;
  +partial-recompose persistence oracle); **4🟡** (task total reconciled 34→32; P0.1 GATE-1 broadened to settle
  the RD-06 `inputSelected` fidelity question the `cpInputLine` decode exposed; "1:1" AC↔ST wording; additive
  ui-seam inventory) + **2🔵** (clipboard prose trimmed; logical-caret reversed `Style` + right-edge overlap
  specced). Cascaded to the portfolio row. Next: `exec_plan essential-control-completions`.
- **2026-07-02** — **DEF-23 glyph-auto-swap planned** 📋 ([plan](plans/glyph-auto-swap/00-index.md)). The
  `217f9ea` width-probe follow-ups formally registered (DEF-23 auto-swap → this plan · DEF-24 in-app warning
  visibility → stays deferred; DEF-21/22 backfilled from the RD-07 plan while at it). Compact full plan
  (AR-1), 17-item register ✅ GATE PASSED — 3 items surfaced during authoring: **AR-15** moved `JSVISION_ASCII`
  from the env layer to **host-level** (evidence: glyph caps have no positive detection layer, every real app
  passes `override.glyphs`, and override outranks env per PL-7 — an env switch would never win); **AR-16**
  approved amending the day-old `probeAmbiguousWidth`/`WidthProbeResult` public API + its spec oracles in
  place (two-group probe: arrows→`ambiguousWide`, box/shade→`boxDrawing`/`halfBlocks`, downgrade-only);
  **AR-17** amended the no-ui-edits fence to allow the additive `adaptAmbiguousWidth` threading through
  `app/run.ts`/`application.ts` (core off / ui on, the `warnAmbiguousWidth` pattern). 4 phases / 20 tasks,
  spec-first (ST-01…ST-14, incl. the AR-16-cited width-probe oracle rewrite). Next: `exec_plan glyph-auto-swap`.
- **2026-07-02** — **DEF-23 glyph-auto-swap preflighted** 🔬 ([report](plans/glyph-auto-swap/00-preflight-report.md)).
  Codebase-grounded audit: 31 plan references mapped, 29 verified accurate; **7 findings, all resolved &
  applied** (iteration 2 clean). Headline **PF-001 🟠**: the plan's "signals keep the original caps" claim was
  false — the SIGCONT resume full-repaint (`signals.ts:118`) would have re-emitted un-swapped wide glyphs and,
  since `render()`'s `prev` holds the same logical chars, the shear would persist; fixed via a
  `getSerializeCaps: () => effectiveCaps` seam into `installSignals` + new oracle ST-15 (independent challenger
  agent confirmed, incl. that it is the only missed serialize site). Also: `isAsciiSafe` now treats
  `utf8: false` as ASCII-safe (+ST-16), `WidthProbeOptions` split into `arrowGlyphs?`/`boxGlyphs?`, ST-14 got a
  concrete probe-bytes observable, api-stability governance claim corrected (asserts docs, not exports), plus
  two cosmetic text fixes. Oracles now ST-01…ST-16. Next: `exec_plan glyph-auto-swap`.
- **2026-07-02** — **DEF-23 glyph-auto-swap SHIPPED** ✅. All 4 phases / 20 tasks green, spec-first
  (ST-01…ST-16 all pass): Phase 1 `glyphs.ambiguousWide` flag + 8-entry `AMBIGUOUS_FALLBACK` map (behavior-neutral
  default); Phase 2 two-group probe (`probeAmbiguousWidth`/`WidthProbeResult` amended per AR-16 + `degradeCapsForWidth`/
  `degradeCapsFully`/`isAsciiSafe`); Phase 3 host effective-caps wiring + `JSVISION_ASCII` + PF-001 `getSerializeCaps`
  resume-repaint seam + ui `adaptAmbiguousWidth` threading (default on); Phase 4 exports + CHANGELOG/README + tracking.
  Full `yarn verify` + core `test:e2e` + `yarn gate` pass. Two runtime notes (AR-R1 combined-write probe read; AR-R2
  green-keeping host-width-warn feed edit in Phase 2). DEF-24 (in-app warning visibility) stays deferred.
- **2026-07-02** — **RD-13 (Runtime hardening) planned** → stage `Plan Created` 📋. `make_plan` produced
  [`plans/runtime-hardening/`](plans/runtime-hardening/00-index.md) (14 docs): Zero-Ambiguity Gate
  **PASSED** — 19 decisions (**PA-1…PA-11** user choices resolved interactively: single plan · TV-faithful
  `endModal(quit)` cascade with `valid()` veto (HR-38) · same-chunk `ESC ESC`→Alt+Escape (HR-16) ·
  `JSVISION_DEBUG`/`JSVISION_LOG` rename, no alias (HR-26) · C0→one-space at the grid (HR-05) · logger
  `'auto'`→ring fallback / explicit-stderr throws (HR-06) · byte-exact clipboard (HR-21) · `invalidate()`
  honors visibility flips (HR-31) · env-layer glyphs + all 3 demos drop overrides (HR-07) · focus re-home
  to next focusable (HR-10) · `'? '` wide fallback (HR-18); **PA-12…PA-19** dominant mechanisms
  batch-confirmed: snapshot-first flush, `ev.hasCapture` guard, lead-in-damage-run, reactive multi-throw
  batch policy, `shadowSize {2,1}` occlusion margin, TV-faithful bare-menu-item emit+close, generated EAW
  WIDE table, slug+phase order). 9 component specs (03-01…03-09, GATE-1 citation indexes for the fidelity
  batches), testing strategy with per-HR ST oracles (ST-\<AC\>.\<letter\>, critical trio + fuzz/property),
  and a 10-phase / 29-session / **120-task** execution plan (critical trio → core majors → lifecycle
  majors → shell majors → subsystem minor batches with BEFORE-decode/AFTER-diff pairs → governance/final
  gate). Decision-relevant `file:line` sites re-verified against live source during planning.
  Next: preflight the plan, then `exec_plan runtime-hardening`.
- **2026-07-02** — **RD-12+ high-value-controls set opened + RD-14 (Input dropdowns) drafted** → stage
  `RD Drafted` ✏️ ([RD-14](requirements/RD-14-input-dropdowns.md)). `make_requirements` sliced the roadmapped
  high-value bucket into **6 sibling RDs by mechanism** (AR-125/126): RD-14 Input dropdowns · RD-15 Tree ·
  RD-16 Table/DataGrid · RD-17 Tabs · RD-18 ProgressBar/Spinner · RD-19 Surface — full set drafted now,
  MVP = RD-14/15/16 (AR-129), contiguous numbering (AR-128), tvdemo aspirational (AR-127). **RD-14** authored
  with its GATE-1 History decode (`THistory`/`THistoryWindow`/`THistoryViewer`/`histlist.cpp`, icon
  `tvtext1.cpp:86`): `History` over a global by-`historyId` MRU store + a `Signal<string[]>` escape hatch
  (AR-130); the new `ComboBox<T>` (editable/filter or select-only/type-ahead, AR-131/134/136); one shared
  **non-modal anchored-popup** primitive generalizing the RD-05 menu overlay (AR-132/137); open keys faithful
  + Alt+Down (AR-135); new `src/dropdown/` (AR-133); additive History theme roles at plan GATE-1 (AR-139);
  kitchen-sink stories + headless `demo:dropdowns` (AR-140). AR-130…AR-140 recorded; 14 AC. Cascaded to the
  portfolio row. Next: `make_plan RD-14` (or `preflight` first); RD-15 next in the drafting queue.
- **2026-07-02** — **RD-15 (Tree) drafted** → stage `RD Drafted` ✏️ ([RD-15](requirements/RD-15-tree.md)).
  RD-12+ set sibling 2/6 (MVP, AR-129). A virtual-scroll `Tree<T>` from TV `TOutlineViewer`/`TOutline` with
  the GATE-1 decode recorded (`toutline.cpp` — `graphChars "\x20\xB3\xC3\xC0\xC4\xC4+\xC4"` = `│├└─`+`+`/`─`
  glyphs, **no `[+]`/`[-]` brackets**, two-tone collapsed text `color>>8`; palette `cpOutlineViewer`; keys
  ↑↓/PgUp-Dn/Home-End + `+`/`-`/`*` + Enter→`cmOutlineItemSelected`; graph-click toggle). Decisions:
  concrete reactive `TreeNode<T>` + view-owned expand Set (AR-141), **←collapse/→expand** modern override
  keeping faithful `+`/`-`/`*` (AR-142), eager children (lazy deferred, AR-143), single-select
  (checkbox/multi deferred, AR-144), reuse the RD-11 virtual-scroll helpers + own `ScrollBar` with a
  Tree-specific renderer (AR-145), new `src/tree/` (AR-148), additive `cpOutlineViewer` roles at plan GATE-1
  (AR-149), `Tree` story + `demo:tree` (AR-150). AR-141…AR-150 recorded; 13 AC. Cascaded to the portfolio
  row. Next: `make_plan RD-15`; RD-16 (Table/DataGrid) next in the drafting queue.
- **2026-07-02** — **RD-16 (Table/DataGrid) drafted** → stage `RD Drafted` ✏️ ([RD-16](requirements/RD-16-table.md)).
  RD-12+ set sibling 3/6, the **last MVP** RD (AR-129) — the **MVP set (RD-14/15/16) is now fully drafted**.
  The **GATE-1 decode** surfaced the key fidelity finding: **Turbo Vision has no table/grid class** — its
  only multi-column mechanism is `TListViewer::numCols`, and the decode (`tlstview.cpp`) shows it is a
  **newspaper-column flow of a single-field list** (`item = j*size.y + i + topItem`), *not* a spreadsheet.
  So a real heterogeneous-column grid has no TV counterpart → RD-16 = a **`DataGrid<T>`** as a **documented
  TV-extension** (AR-151, the same class as reactive/truecolor/async modality): the virtual-scroll spine,
  the `│` (`\xB3`) divider in `getColor(5)`, and the `cpListViewer` row colours are **faithful**; the header
  row + heterogeneous `Column<T>[]` accessors + click-sort are the flagged extension. This is exactly what
  RD-11's AR-104 meant by deferring "a real table". Decisions: typed `Column<T>` `{title,accessor,width,
  align?,compare?}` over a `DataGrid<T>` generic (AR-152), `fixed`/`fr`/`auto` widths via RD-02 `solveTrack`
  + horizontal scroll (AR-153), sticky header + click-to-sort `▲`/`▼` reusing RD-11's `sorted` computed
  (AR-154/158), row-granular single-select (cell-select deferred, AR-155), owned V+H `ScrollBar` (AR-156),
  reactive `Signal<T[]>` (AR-157), new `src/table/` (AR-160), **reuse** the already-decoded `list*`/
  `listDivider` roles + **one** additive header role at plan GATE-1 (AR-159), `DataGrid` story + `demo:table`
  (AR-161). AR-151…AR-161 recorded; 13 AC. Cascaded to the portfolio row. Next: `make_plan RD-16`; RD-17
  (Tabs) next in the drafting queue (the Later phase begins).
- **2026-07-02** — **RD-14 (Input dropdowns) planned** → stage `Plan Created` 📋
  ([plan](plans/input-dropdowns/00-index.md)). `make_plan` produced `plans/input-dropdowns/` — 8 docs;
  **5 phases / 34 tasks** spec-first (ST-1…ST-36 ↔ AC-1…AC-14): Phase 0 additive seams + core theme →
  1 shared anchored-popup → 2 History (TV-derived) → 3 ComboBox → 4 stories + `demo:dropdowns` + gate.
  Built entirely on existing primitives (RD-11 `ListView`/ScrollBar, RD-05 overlay/catcher, RD-06/07
  `Input`); additive surface = 3 small non-breaking seams (public `Input` linkage `getValueSignal`/
  `getMaxLength`/`selectAll` · **derived** overlay visibility with the menu controller migrating off its
  explicit toggle · 5 additive core History theme roles). Two parallel recon agents drove it: a **full TV
  GATE-1 decode** (captured verbatim in `03-01` — icon `▐↓▌` = U+2590/**U+2193**/U+258C, `getColor(0x0102)`
  → arrow `0x20` black-on-green / sides `0x72` green-on-lightGray, popup rect `±1`/`+7`, **blue-window**
  frame `0x1F` + accent `0x1A`, viewer `0x1F` / focused `0x2F`, store `histlist.cpp`) + a current-state map
  of the exact APIs the seams extend. **PA-1…PA-15 ✅ GATE PASSED** — 4 user choices: History store =
  **per-id entry-count cap** (default 16, not the shared byte-block; PA-2 resolves PF-008) · **U+2193 ↓
  narrow** down-arrow (PA-3) · `maxRows` = **visible rows, default 6** (window = maxRows+2; PA-4 resolves
  PF-007) · **derive overlay visibility from children** (PA-5). Plus **2 fidelity corrections** (C++
  outranks the RD): AC-2 list order → **oldest→newest, top→bottom** (`historyStr(id,0)` = oldest, verified
  `histlist.cpp:176-185`; PA-6) · AC-8 geometry → **8-row window / 6 visible** (was "7/~5"; PA-7). GATE-2
  cell-by-cell diff tasks scheduled for History (Phase 2) + ComboBox (Phase 3). Next: `exec_plan
  input-dropdowns` (or `preflight input-dropdowns` first); RD-15/RD-16 remain `make_plan`-ready.
- **2026-07-02** — **RD-14 (Input dropdowns) plan preflighted** → stage `Plan Preflighted` 🔬
  ([report](plans/input-dropdowns/00-preflight-report.md)). Fresh-session, codebase-grounded audit (9
  source files read; ~20 `file:line` refs verified — the TV GATE-1 decode + the Input-seam
  (`input.ts:67/69/433`), ListView, overlay/menu, `focusSignal`, and theme-role cites all confirmed
  exact against live source). 6 findings (**3🟠 + 2🟡 + 1🔵**), **all resolved (all Option A) and applied**.
  The three MAJORs were genuine catches: **PF-001** — the PA-5 "derived overlay visibility" was specified
  as a reactive `effect` over `overlay.children().some(...)`, but `Group.children` is a plain array
  (`group.ts:33`) and `ViewState.visible` a plain mutated boolean (`view.ts:47`) — neither reactive, so
  the effect would never re-fire → replaced with an **imperative `syncOverlayVisible(overlay)`** helper
  (`children.length>0`+`invalidate()`), the menu controller **replacing** (not deleting) its `:229/:247`
  toggles; **PF-002** — a leaf `History`/`ComboBox` has **no path** to the overlay or `getFocused()`
  (`DispatchEvent` exposes `emit`/`focusView`/`setCapture` only; the overlay is `attach()`ed to the
  `MenuBar` alone) → added a **3rd additive intra-`ui` seam**, a `DispatchEvent` `getFocused()` +
  `PopupHost` accessor sourced in `routeContext`; **PF-003** — `03-02` content-shrank the popup
  (`min(maxRows,n)+2`) contradicting the fixed TV `+7` (ST-2/decode §3/PA-4/PA-7) → **fixed `maxRows+2`**,
  host-intersect the only row reducer. Minors/obs: **PF-004** focus-loss dismissal guarded on
  `state.focused===false` (no self-dismiss on open) · **PF-005** `00-index` seam miscount reworded (now 3
  genuine intra-`ui` seams + 5 core roles) · **PF-006** ST-2/ST-31 AC cross-refs corrected. Task total
  **34→35** (added Phase-0 envelope-seam task 0.2.5). Next: `exec_plan input-dropdowns`.
- **Recommended next:** **`exec_plan input-dropdowns`** to implement RD-14 (spec-first, GATE-2 per
  TV-derived component; the plan is now preflight-clean). The other MVP siblings **`make_plan RD-15`/`RD-16`**
  remain ready, as do the RD-07/RD-11/glyph-auto-swap `exec_plan`s.
