# Roadmap: DataGrid

> **Feature-Set**: DataGrid (`@jsvision/datagrid`)
> **Status**: In Progress
> **Created**: 2026-07-12
> **Last Updated**: 2026-07-17
> **Progress**: 10 / 15 (67%) · RD-07 done (columns & layout) · RD-15 done (showcase app) · RD-08 done (rows & selection) · RD-09 done (footer, aggregation & master-detail — 47 tasks / 6 phases)
> **CodeOps Skills Version**: 3.4.1

Enterprise-class editable data grid for the jsvision TUI SDK (SAP ALV / MS-Access / Paradox-class),
built on `@jsvision/ui`. A new zero-dependency package `@jsvision/datagrid` (PostgreSQL / Data Studio
is a separate downstream app). Grounded in the Data Studio feasibility spike (Probes 0–7,
GO-WITH-CAVEATS). v1 = "the enterprise datasheet": per-cell immediate editing, row-oriented selection,
virtual scroll, and the enterprise column/sort/filter/footer surface. Zero-Ambiguity Gate PASSED
(AR-01…AR-32). Preflighted 2026-07-12 — codebase-grounded audit PASSED, all 10 findings resolved
(see requirements/00-preflight-report.md).

## Legend

⬜ Backlog · ✏️ RD Drafted · 🔎 RD Preflighted · 📋 Plan Created · 🔬 Plan Preflighted · 🔄 Executing · ✅ Done · ⛔ Blocked · ⏸️ Deferred

## Tracker

| ID | Title | RD | Plan | Stage | Status | Last Updated | Notes / Blocker |
|----|-------|----|------|-------|--------|--------------|-----------------|
| RD-01 | Foundation & grid-engine exposure | [RD-01](requirements/RD-01-foundation.md) | [foundation](plans/foundation/00-index.md) | Done | ✅ | 2026-07-13 | Package scaffold · ui grid-engine promotion · value/format/parse · read-only `EditableDataGrid` · 6 phases / 39 tasks green · unblocks RD-02…RD-14 |
| RD-02 | Editing engine & commit model | [RD-02](requirements/RD-02-editing-engine.md) | [editing-engine](plans/editing-engine/00-index.md) | Done | ✅ | 2026-07-13 | Cell cursor · in-cell overlay lifecycle · per-cell commit · dirty tracking · editable story · 6 phases / 43 tasks green · gate PASSED · preflight PASSED (Tab/Shift-Tab deferred to RD-10) · **exec_plan complete** · depends RD-01 |
| RD-03 | Cell editors & value help | [RD-03](requirements/RD-03-cell-editors.md) | [cell-editors](plans/cell-editors/00-index.md) | Done | ✅ | 2026-07-13 | Typed editors (`text`/`integer`/`decimal`/`boolean`/`date`/`enum`/`lookup`/`readonly`/`custom`) + F4 value help · 6-phase / **45-task** plan, spec-first · gate PASSED (3 user + 10 plan AR) · preflight PASSED (6 findings resolved) · **exec_plan complete (`--auto-commit`) — 116 datagrid tests, full `yarn verify` green; AR #14 runtime (DatePicker/ComboBox focus `.input`)** · additive over RD-02 `createCellEditor` seam (no ui promotion) · depends RD-01, RD-02 |
| RD-04 | Formatting & cell rendering | [RD-04](requirements/RD-04-formatting-rendering.md) | [formatting-rendering](plans/formatting-rendering/00-index.md) | Done | ✅ | 2026-07-14 | `Intl` `fmt` registry + matched inverse parsers · custom `render` (cell-clipped, draw-error isolated) · `cellStyle` conditional colour · 3-phase / 24-task plan, spec-first · gate PASSED (4 forks + 9 confirmations, incl. AR-13) · **preflight PASSED (5 findings resolved — PF-001 `danger` is a `ThemeColors` alias, not a role → explicit `Style` + `gridDirty` fg; PF-002 `GridColumn.parse` widened to `V\|PARSE_FAILED` + commit rejects sentinel; see 00-preflight-report.md)** · self-contained (no ui change, AR #1) · Should-Have rules engine deferred (Phase B, AR #10) · **exec_plan complete (`--auto-commit`) — 3 phases / 24 tasks, spec-first; 153 datagrid tests, full `yarn verify` green; `fmt` registry + widened `parse`/commit-reject + self-contained `draw` override + formatting story** · Depends RD-01. |
| RD-05 | Sorting | [RD-05](requirements/RD-05-sorting.md) | [sorting](plans/sorting/00-index.md) | Done | ✅ | 2026-07-15 | Single/multi/value-aware/push-down. Depends RD-01 (RD-04 demo-only). **Plan created 2026-07-15 — gate PASSED (16 AR; user picked own-the-header + case-insensitive collator + row-key re-anchor + all Should-Haves). Surfaced a foundation blocker: core `MouseEvent` carries no modifiers, so Ctrl+click needs an additive optional `ctrl?/alt?/shift?` on core (Phase 1, user-authorised — first datagrid→core touch). 30 tasks / 4 phases, spec-first.** · **Preflight PASSED 2026-07-15 — codebase-grounded audit, 0 critical/major, 5 findings (3 minor + 2 obs) all resolved: display/source/columnMap → instance fields, `alignCell` 4th `measure` arg, AC-1 grep scoped, mouse-test seams documented, `redact.ts` reviewed/unchanged (see plans/sorting/00-preflight-report.md).** · **exec_plan COMPLETE 2026-07-15 (per-phase commits, pushed) — 31 tasks / 4 phases, spec-first; full `yarn verify` green (192 datagrid tests). Phase 1: additive optional `ctrl?/alt?/shift?` on core `MouseEvent` (+ CHANGELOG). Two runtime ARs resolved: #17 (`input-corpus` `mouse.json` golden fixtures updated to the wheel convention), #18 (empty editor `overlay` was swallowing header clicks → hit-transparent `EditorOverlay`). Owns the header (`SortHeader`) + `Signal<SortKey[]>` model + client `sortRowsMulti`/guarded push-down + row-key re-anchor + tri-state API; `sorting` kitchen-sink story.** |
| RD-06 | Filtering | [RD-06](requirements/RD-06-filtering.md) | [filtering](plans/filtering/00-index.md) | Done | ✅ | 2026-07-15 | Quick row + condition + value-list + funnel/"N of M" + distinct + push-down. **Done 2026-07-15 — all 45 tasks / 6 phases green (234 datagrid tests; full `yarn verify` passing), committed per-phase (`--auto-commit`). New: `filter.ts` model, `EditableDataGrid` filter API + `setFilter` push-down + N-of-M count, `QuickFilterRow`, `SortHeader` funnel, `FilterPopup` (condition + embedded `ValueList`), `distinctFor`, filtering kitchen-sink story. 7 runtime AR amendments (#23–#29).** **Plan created 2026-07-15 — gate PASSED (17 AR; user picked full-scope-phased + reactive count/story echo (no footer band) + quick-filter opt-in default-off + text match on formatted display + widened `distinct`→`{values,truncated?}`). 45 tasks / 6 phases, spec-first, data-plane-first; mirrors the RD-05 sort architecture (`filter.ts` twin of `sort.ts`, `setFilter` push-down twin of `setSort`, funnel extends `SortHeader`). RD-07/RD-09 deps resolved: single-panel funnel now (header already owned), "N of M" as reactive `filteredCount()`/`totalCount()` + story echo (RD-09 owns the footer band).** **Preflight PASSED 2026-07-15 — codebase-grounded audit (same-session; the one MAJOR verified by an independent challenger), 1 MAJOR + 3 minor + 1 obs, all resolved: PF-001 funnel→popup now forwards the live `DispatchEvent` focus/popup seam (mirrors `editing.ts`; seam lands in Phase 3), PF-002/003/005 doc reconciliations, PF-004 reword; recorded as AR #18–#22 (see plans/filtering/00-preflight-report.md).** **Follow-up 2026-07-16 — [filter-entry-point](plans/filter-entry-point/00-index.md) plan created (GitHub #92): the condition popup + value-list were unreachable on an unfiltered column (funnel drawn/hit-tested only when already filtered). Plan adds an always-visible funnel (muted `listDivider` / emphasized `tableHeader`, same ▽) + an `Alt+Down` keyboard opener + a `GridColumn.filterable` opt-out, and folds in the RD-06 revision (now five spots: §Feature Overview/§Condition/§Funnel/Technical §Funnel+count/AC#4 + new non-filterable AC) with an ST-19 re-spec. Zero-Ambiguity Gate PASSED (12 AR across 3 decision rounds). 30 tasks / 4 phases, spec-first, gate-first. **Preflight PASSED 2026-07-16 (🔬 Plan Preflighted) — fresh-session, codebase-grounded audit; 3 MAJOR + 5 minor + 2 obs, all resolved (all recommendations accepted, fixes applied). The 3 majors were verified by an independent challenger: PF-001 Alt+Down is *repurposed* from the base's modifier-agnostic row-down (not "unbound") and must intercept before `super.onEvent`; PF-002 grid.ts must retain `parts.headers` + refresh in `rebuildBody` to reach the owning header; PF-003 two more RD-06 spots needed the funnel-rule edit; RD-06 funnel revision moved to Phase 2. **exec_plan COMPLETE 2026-07-16 (✅ 30/30 tasks, 4 phases, spec-first; per-phase commits via /gitcm, no push) — #93 quick-filter width fix committed first (separable, PF-010). Shipped: `GridColumn.filterable` opt-out (nullable-slot quick-filter), an always-visible funnel on every filterable column (muted `listDivider` / emphasized `tableHeader`, hit-test gated on filterability), and an `Alt+Down` keyboard opener from the non-editing body (repurposed before `super.onEvent` per PF-001; `grid.ts` retains + refreshes `parts.headers` per PF-002; mouse & keyboard share one `funnelAnchor`). RD-06 revised (5 spots + non-filterable AC) with the ST-19 re-spec. Showcase + kitchen-sink hints reworded. Full `yarn verify` green (turbo 26/26). #92 ready to close.** See plans/filter-entry-point/00-preflight-report.md.** Depends RD-01, RD-04. |
| RD-07 | Columns & layout | [RD-07](requirements/RD-07-columns-layout.md) | [columns-layout](plans/columns-layout/00-index.md) | Done | ✅ | 2026-07-16 | Resize/reorder/hide + frozen pinned-panel columns + sticky header. Depends RD-01. **exec_plan COMPLETE 2026-07-16 (`--auto-commit`, per-phase commits pushed) — 58 tasks / 7 phases, spec-first, all green (full `yarn verify` 26/26). Ships: pure `column-model.ts` (visible-order / freeze-partition / width-clamp), 3 sliced `EditableGridRows` panels built only when frozen (no-freeze single-body path byte-identical → zero RD-01…06 regression); `SortHeader` grip-resize + double-click auto-fit + title drag-reorder (drop indicator, panel-clamped, reverts the on-down sort via `onReorderStart`); `grid-panels.ts` unified segment assembly (header / pinned-band / body rows); frozen rows (`freezeRows` pinned band via `rowFloor`/`rowCeil` window clamp, over-freeze clamp+devWarn) + compact density (the one `@jsvision/ui` touch: additive `dividers` param on `apportionColumns`); public layout API (`columnOrder`/`setColumnOrder`/`setColumnVisible`/`setColumnWidth`/`autoFitColumn`/`frozen`); kitchen-sink `columns-layout` story + a live `Columns & layout` showcase cluster (5 demos, replacing the RD-07 placeholder → roadmap oracles re-based to RD-08…14); ST-27 sanitize-survives-layout security oracle. 2 runtime ARs (#19 rebuild folded into Phase 4; #20 revert-sort-on-drag).** **Plan created 2026-07-15 — gate PASSED (4 user forks: full scope incl. frozen-rows + density pulled forward from RD Phase B/C; linear cross-panel cursor overriding the RD's literal boundary wording; kitchen-sink story + datagrid-showcase upgrade; per-column min/max width. Plus 12 grounded design ARs + 3 inherited RD ARs). 57 tasks / 7 phases, spec-first, data-plane-first. 3 sliced `EditableGridRows` panels built only when frozen (no-freeze keeps the single-body path → zero RD-01…06 regression); new pure `column-model.ts` twin of `sort.ts`/`filter.ts`.** **Preflight PASSED 2026-07-15 — codebase-grounded, same-session with an independent challenger verifying the 7 load-bearing claims; all feasibility CONFIRMED (capture on the non-focusable header works, geometry pure over subsets, per-panel overlay origin, `ev.clickCount` live). 3 MAJOR + 4 minor + 3 obs, all resolved: PF-001 density needs an additive optional `apportionColumns` divider param (the "forward `solveTrack` gap" path was refuted — the plan's ONE ui touch, AR-17); PF-002 `onFunnelClick` must carry the clicked header for per-panel popup anchoring; PF-003 order contract = `setColumnOrder` permutes visible ids, hidden keep anchor slots (AR-18); see plans/columns-layout/00-preflight-report.md.** |
| RD-08 | Rows, records & selection | [RD-08](requirements/RD-08-rows-selection.md) | [rows-selection](plans/rows-selection/00-index.md) | Done | ✅ | 2026-07-16 | Row selection, checkbox column, gutter, insert/delete/duplicate, null policy. Depends RD-01, RD-02, RD-07. **exec_plan COMPLETE 2026-07-16 (per-phase commits, NO push — awaiting user review before pushing to PR #95) — 50 tasks / 6 phases, spec-first, data-plane-first; full `yarn verify` green (372 datagrid + 259 examples tests). Ships: pure `selection.ts` model (`toggleKey`/`selectRange`/`selectAll`/`triState`) + `GridSelection` controller (keys-set + anchor, keyed by `rowKey` so selection survives re-sort/re-filter with no reconcile); multi-selection paint reuses the body's own `draw()`/`paintDirtyMarkers()` (`item===selected` → `selectedKeys.has(rowKey)`), the base single `selected` kept as the click sink under a cursor-only `select()` override; `synthetic-columns.ts` opt-in checkbox column (tri-state header select-all over the filtered display) + row-number gutter as separate leading band views (`SyntheticBodyBand extends GridRows` → free virtual-scroll sync, byte-identical no-prefix path → zero RD-01…07 regression); `RowMutations` controller over a `GridDataSource.insert?/remove?` seam (`fromRows` splices a fresh array) + `insertRow`/`deleteRows` (prunes selection)/`duplicateRow` (`structuredClone` guarded + `assignKey` fresh key); per-column `nullable?`/`nullDisplay?` (null renders distinct from `''`, empty edit commits null); `rows-selection` kitchen-sink story + a live `Rows & selection` showcase cluster (5 demos, replacing the RD-08 placeholder → showcase oracle re-based to RD-09…14, +1 category); ST-21 mutation + sanitize security oracles. 6 runtime notes (RT-1…6; RT-6 extracted `RowMutations` to keep grid.ts under the line guard).** **Plan created 2026-07-16 — gate PASSED (AR-1…15: 8 user forks across 2 AskUserQuestion rounds + 6 grounded design + 1 authoring; every recommendation accepted). 50 tasks / 6 phases, spec-first, data-plane-first. Zero core/ui change — multi-selection paint reuses the datagrid body's own `draw()` override (`editable-grid-rows.ts:443`, swap `item===selected` for `selectedKeys.has(rowKey)` at both paint sites); supersedes the base-owned single `selected` index (kept as the base's required click sink). New pure `selection.ts` (model twin) + `synthetic-columns.ts` (checkbox/gutter prefix); `RowMutations` seam + `fromRows` splice + `assignKey` duplicate-key hook; flat `nullable?`/`nullDisplay?` (avoids the `nulls?` collision, AR-15); `selectionMode` default `'multi'`, checkbox/gutter opt-in; select-all over the filtered display.** **Preflight PASSED 2026-07-16 — codebase-grounded, same-session with an independent challenger confirming the 4 load-bearing claims. 1 CRITICAL + 3 MAJOR + 2 minor + 2 obs, all resolved & applied (AR-16…21): PF-001 the base `selected` is a required, base-written `GridRowsConfig` field — kept (not removed) + a cursor-only `select()` override; PF-002 the "vestigial / never set" premise was false (base sets it on every click) → plain-click = cursor-only; PF-003 paint swaps at TWO sites (`draw()` + `paintDirtyMarkers()`); PF-004 `Space`=begin-edit on editable cells, toggle-select on read-only cells; PF-005 commit hook → `editing.ts:274`. See plans/rows-selection/00-preflight-report.md.** |
| RD-09 | Footer, aggregation & master-detail | [RD-09](requirements/RD-09-footer-aggregation-master-detail.md) | [footer-aggregation-master-detail](plans/footer-aggregation-master-detail/00-index.md) | Done | ✅ | 2026-07-17 | Footer band + aggregates + master-detail. Depends RD-01, RD-08. **exec_plan COMPLETE 2026-07-17 (`--auto-commit`, per-phase commits pushed) — 47 tasks / 6 phases, spec-first, data-plane-first; full `yarn verify` green (turbo 30/30). Ships: pure `aggregate.ts` fold model (sum/avg/min/max/count, edge-safe); a sticky column-aligned reactive footer band (`FooterBand` + `FooterController`, lazy fold, honesty `(loaded)` labelling via `source.complete?()`) assembled in `buildGridBody`; free-form footer widget row (`Button`→`ev.emit`, N-of-M/selection `Text` read-outs); reactive readouts `displayedRows()`/`focusedRow()`/`focusedKey()`; editable write-through master-detail (`fromReactiveRows` + `masterDetail`, disposed with the surrounding scope); footer-master-detail kitchen-sink story + a 5-demo datagrid-showcase cluster (replacing the RD-09 placeholder → oracles re-based); ST-28 sanitize oracle. 3 runtime ARs (AR-R1 `GridFooter` non-generic under `noUnusedParameters`; AR-R2 `grid.ts` line guard re-based 1200→1250 per the preflighted AC#11 after the Step-2.0 extractions, never re-inlining; AR-R3 user-approved backward-compat `measure()` added to `@jsvision/ui` `Text`+`Button` so raw footer widgets self-size in the flow row).** **Plan created 2026-07-16 — gate PASSED (AR-1…14). Preflight PASSED 2026-07-17 (🔬 fresh-context; 2 MAJOR + 5 minor/obs resolved).** **Plan created 2026-07-16 — gate PASSED (AR-1…14: 6 user forks across 2 AskUserQuestion rounds + 8 grounded design; every recommendation accepted). 44 tasks / 6 phases, spec-first, data-plane-first. User steered master-detail to the no-shortcut path: an EDITABLE reactive write-through source (`fromReactiveRows`, twin of `fromRows`) so detail cell-edits + insert/delete persist into the master's owned collection — not the read-only computed or the synced-signal hack. Full Must-Have v1 (footer band · reactive column aggregates over the displayed set · flow-row widget slots with `Button`→`ev.emit` · sticky-for-free fixed band · aggregate honesty via an optional `source.complete?()` predicate + test-double · master-detail `masterDetail` helper + `focusedRow()`/`focusedKey()` · N-of-M/selection widgets); Phase B stays deferred (server `source.aggregate()` · navigator/pager · toolbar · drill-in) per RD AR #17. All logic in new modules (`aggregate.ts`/`footer-band.ts`/`grid-footer.ts`/`master-detail.ts` + `GridFooter` controller) because `grid.ts` is at the hard `<1200` guard. 3 code-recon passes grounded the band/geometry, reactive-fold, and widget/command/sanitize seams.** **Preflight PASSED 2026-07-17 (🔬 Plan Preflighted) — fresh-context, codebase-grounded audit; 0 critical + 2 MAJOR + 3 minor + 2 obs, all resolved (every recommendation accepted, fixes applied). The 2 majors were run past an independent challenger: PF-001 the `<1200` guard was unreachable as written (grid.ts at 1198) → Phase 2 now first extracts the self-contained `EditorOverlay`/`PopupCatcher` (→ `overlay.ts`) + `devWarn` (→ `dev.ts`) for ~40 lines of headroom, and AC#11 is reconciled with AR-10 (guard re-based with rationale if the irreducible public surface still crosses it — never by re-inlining); PF-002 the `FooterController` reads lazily (NO owned `computed`, matching `GridSelection`/`RowMutations`) with `FooterBand` binding the fold to its data deps — the datagrid uses zero bare `computed`s package-wide. Plan grew 44→47 tasks. See plans/footer-aggregation-master-detail/00-preflight-report.md.** |
| RD-10 | Navigation & interaction | [RD-10](requirements/RD-10-navigation-interaction.md) | [navigation-interaction](plans/navigation-interaction/00-index.md) | Executing | 🔄 | 2026-07-17 | Consolidated keymap, mouse, synthesized double-click · **+ `Tab`/`Shift-Tab` cell traversal + Tab commit-advance received from RD-02** (dispatch swallows unbound Tab; needs keymap→command — PF-001). Depends RD-02. **Plan created 2026-07-17 — Zero-Ambiguity Gate PASSED (AR-1…16: 4 user forks via AskUserQuestion + 12 grounded defaults). Key recon finding: the framework ALREADY ships the machinery the RD assumed we'd build — `@jsvision/ui` `editor/keymap.ts` (the keymap→action pattern to mirror), a live `ev.clickCount` double-click stamp (500 ms, injectable clock), and the `route()` Tab-swallow whose only escape hatch is the loop keymap. So the plan wires the grid into existing seams: full per-grid `GridAction` dispatch (new `keymap.ts`) replacing 4 hardcoded handlers; app-opt-in Tab via a `gridKeymap` fragment + `installGridNavigation` helper (NO core/ui change); double-click reuses `ev.clickCount`; full nav coverage delegating to base `protected` helpers. 42 tasks / 5 phases, spec-first, model-first; all new logic in `keymap.ts`+`navigation.ts`.** **Preflight PASSED 2026-07-17 (🔬 Plan Preflighted) — same-session, codebase-grounded audit; 0 critical + 4 MAJOR + 2 minor + 1 obs, all resolved (every recommendation accepted, fixes applied). The 4 majors were run past an independent challenger (all confirmed REAL against the code): PF-001 AC-4's column half was unmet — a single click in a single-body grid set only the row cursor (`mouseColumns: frozen`), so click→F2 edited the wrong column → single-click now sets `focusedCol`; PF-002 a malformed caller chord would throw (core's `createKeymap` throws; `parseChord` private), violating AC-8 → `mergeKeymap` now guards chords via per-entry try/catch + compiles once; PF-003 Tab-commit lost keyboard focus (the command path has no envelope and the loop heals into the emptied editor overlay) → `installGridNavigation` re-focuses `grid.rows` on `'moved'`; PF-004 the `< 1250` guard (a gated impl test) would breach with 3 new `@example` delegators at 1206 → re-based `< 1250` → `< 1300` in both guard tests. Plus PF-005 the `Keymap` type renamed `GridKeymap` (core/ui already export `Keymap`), PF-006 the `runAction` router preserves the per-panel `localCol() < 0` guard, PF-007 `nextCell()`'s vetoed-commit return documented. Plan grew 42→45 tasks. See plans/navigation-interaction/00-preflight-report.md.** **exec_plan STARTED 2026-07-17 (`--auto-commit`, per-phase commits) — Phases 1–2/5 ✅ (16/45 tasks): Phase 1 pure `keymap.ts` model (`GridAction` vocabulary, frozen `DEFAULT_KEYMAP`, `resolveGridAction` compile-once/memoized, `mergeKeymap` two-axis validate + malformed-chord-safe, `GridKeymap` rename); Phase 2 body-dispatch refactor — `onEvent` resolves chord→`GridAction`→`runAction` (nav delegates to base helpers) + `tryPrintableEdit` fallback, replacing the four hardcoded handlers; the merged keymap threads through `grid.ts`→`grid-panels.ts` into every panel; per-panel `localCol()<0`/`rowFloor` guards preserved (PF-006). Zero RD-02…09 regression (all 74 datagrid test files / 451 tests green); full `yarn verify` green.** Depends RD-02. |
| RD-11 | Data at scale | [RD-11](requirements/RD-11-data-at-scale.md) | — | RD Preflighted | 🔎 | 2026-07-12 | Virtual scroll, server paging, opt-in pager (backend deferred AR-28). Depends RD-01. |
| RD-12 | Validation & lifecycle | [RD-12](requirements/RD-12-validation-lifecycle.md) | — | RD Preflighted | 🔎 | 2026-07-12 | Per-cell + per-row + BeforeSave gates, error surfacing, loading/empty/error. Depends RD-02, RD-03. |
| RD-13 | Export, import & personalization | [RD-13](requirements/RD-13-export-import-personalization.md) | — | RD Preflighted | 🔎 | 2026-07-12 | CSV/HTML/JSON export (+ formula-injection escaping) · import · layout variants · Phase B · depends RD-05, RD-06, RD-07 |
| RD-14 | Non-functional requirements | [RD-14](requirements/RD-14-non-functional.md) | — | RD Preflighted | 🔎 | 2026-07-12 | Perf, security posture, a11y, theme roles, test tiers, API governance. Cross-cutting. |
| RD-15 | DataGrid showcase app | [RD-15](requirements/RD-15-showcase-app.md) | [datagrid-showcase](plans/datagrid-showcase/00-index.md) | Done | ✅ | 2026-07-15 | Standalone datagrid showcase in `packages/examples/datagrid-showcase/` (`demo:datagrid`): granular one-per-capability demos for the shipped RD-01…06 surface + per-RD "coming soon" panels for RD-07…14; dedicated shell seeded from `kitchen-sink/shell.ts`; smoke + headless-walkthrough gated. **Drafted + Preflighted 2026-07-15 — RD Zero-Ambiguity Gate PASSED (AR #33–#41). Preflight PASSED WITH NOTES (PF-020…PF-023 applied). Plan created 2026-07-15 — 2-phase big-bang (22 tasks), spec-first; plan Zero-Ambiguity Gate PASSED (9 items: big-bang phasing, typecheck-included, one-file-per-demo, bespoke push-down source).** **Plan preflighted 2026-07-15 — codebase-grounded audit PASSED, 1 MAJOR + 2 minor + 2 obs all resolved (challenger-converged): walkthrough tier driven via the real `app.loop.emitCommand` command path + a read-only `disposedCount()` shell seam (PF-001); count/roadmap fixes (PF-002/003). See plans/datagrid-showcase/00-preflight-report.md.** **exec_plan COMPLETE 2026-07-15 (`--auto-commit`, per-phase commits pushed) — 22 tasks / 2 phases, spec-first. Standalone `packages/examples/datagrid-showcase/` (`demo:datagrid`): 46 registry entries (38 shipped demos across Foundation/Editing/Cell-editors/Formatting/Sorting/Filtering + 8 RD-07…14 placeholders), a focused sidebar-navigator shell with a `disposedCount()` test seam, a shared demo-data lib + bespoke push-down spy source. Two headless tiers (smoke per-demo render + walkthrough shell-navigation via `app.loop.emitCommand`) — 57 tests green; full `yarn verify` green (turbo 26/26). Every demo is tsc-checked (examples tsconfig include). kitchen-sink-gate reconciled.** The living acceptance surface each future RD extends. Depends RD-01…RD-06. |
