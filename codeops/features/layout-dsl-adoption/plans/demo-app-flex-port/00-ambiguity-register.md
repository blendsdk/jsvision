# Ambiguity Register: demo-app-flex-port

> **Document**: 00-ambiguity-register.md
> **Parent**: [Index](00-index.md)
> **Status**: ✅ GATE PASSED
> **Created**: 2026-07-20 12:31
> **Items**: 17 · Resolved: 17 · Deferred: 0 (AR-17 raised at execution time)
> **Amended**: 2026-07-20 after preflight — AR-15 and AR-16 added; AR-4, AR-9, AR-10 and AR-14
> superseded when their phases were dropped.

All items below carry an explicit user decision. No item is deferred. Every scope, design, and
verification decision in the plan documents cites a row here.

> **⚠️ Four rows below are SUPERSEDED.** AR-4, AR-9, AR-10 and AR-14 all decided details of the GH
> #110 / #111 conversions, which AR-15 removed from this plan's scope. They are kept for the record
> and struck through; **do not execute them**. Note in particular that PR #127 — which now owns that
> work — resolved AR-9 the *other* way, deliberately keeping the wholesale field write in
> `sizeWorkspace()` with an explaining comment. That is #127's decision to own, not this plan's.

## Register

| AR # | Category | Ambiguity | Options presented | ✅ Decision | Status |
|------|----------|-----------|-------------------|------------|--------|
| AR-1 | Scope | RD-01 **FR-6** sanctions ~470 Tier-3 canvas conversions, but issue **#110's body** (written before the flex-elimination decision) out-scopes "every deliberate absolute-coordinate demo using the shared `at()` idiom". The two documents disagree about this plan's ceiling. | (a) structural composition writes + shadow retirement; (b) full FR-6 maximal incl. the 411 `at()` call sites; (c) structural only, defer #114 | **(a)** — convert the genuinely flex-shaped composition writes (examples + theme-designer) and retire the shadow helpers. The FR-6 maximal becomes a separate follow-up plan, because each demo canvas is a per-file design decision rather than a mechanical swap, and AC-8 attaches a manual showcase quality pass to every converted story. **Narrowed further by AR-15:** the composition-write half moved to PR #127, leaving the shadow retirement. | ✅ Resolved |
| AR-2 | Scope | Are the `at()`-idiom absolute writes in `containers-demo` (5), `dropdowns-demo` (6), `themes-demo` (2), `color-demo`, `date-demo`, `tabs-demo`, `playground`, `controls-live`, `status-bar.story`, `layout-dsl-playground/playground.ts:147` converted? | in / out | **Out** — they are deliberate absolute canvases. Consequence of AR-1. They may be revisited by the FR-6 follow-up plan. | ✅ Resolved |
| AR-3 | Scope | Are the ~20 `.layout.rect = …` window-placement mutations (`shell-demo`, `amiga-clock` ×4, `tvision-demo` ×3, `matrix-rain`, `web-xterm` ×2, `recipes/live-dashboard`, `chrome-bars-demo:88`, both showcase shells) converted? | in / out | **Out** — RD-01 **FR-4** already lists window/desktop placement, the polar `analog-clock`, and the movable-window desktop apps as keep-absolute. Consequence of AR-1. | ✅ Resolved |
| ~~AR-4~~ | Scope (didactic) | `view-demo/main.ts` (7 sites) and `kitchen-sink/stories/layout.story.ts` (2 sites) port cleanly to flex, but issue #110 flags both as judgment calls: their stated job is teaching the **raw** View/Group spine and the **raw** cell-native flex engine. | port both / keep both raw / port `view-demo` only | **Keep both raw**, and add a short pointer comment in each naming the DSL equivalent. Both already have DSL counterparts (`layout-dsl.story.ts`, `layout-dsl-playground`), so the raw pair is a deliberate contrast. Also avoids `layout.story.ts:30`'s local variable literally named `row`, which would collide with the DSL builder. | ✅ Resolved |
| AR-5 | Scope | RD-01 FR-6 names `@jsvision/docs-site` (44 `at()` calls) in Tier 3, but issue **#112** owns docs modernization. | this plan / #112 | **#112** — docs-site's live examples carry their own build + per-build meta-CSP gate and a separate reviewer lens; bundling them here would mix two verification stories in one PR. | ✅ Resolved |
| AR-6 | Design | The two shadow `at()` helpers (`kitchen-sink/story.ts:69`, `datagrid-showcase/story.ts:68`) do `view.layout = layout` — a **replace**. The real `@jsvision/ui` `at()` **merges** and calls `invalidateLayout()`. How are they retired? | audit + re-export the real `at()` from `story.ts` / delete local and rewrite ~84 files' imports / leave alone | **Audit, then re-export.** Run the same audit-before-migration pass #117 used, then delete the local body and re-export ui's `at()` from `story.ts`, so all 411 call sites keep their import path unchanged. A re-export **of** the DSL export is not a shadow, so #114's acceptance criterion is satisfied with near-zero churn and one audit table as the evidence. | ✅ Resolved |
| AR-7 | Verification | Both #110 and #111 demand "renders identically". What counts as proof? | one-shot before/after buffer diffs (no new test files) / permanent golden tests per demo / existing smoke tests only | **One-shot before/after buffer diffs.** Serialize each touched demo's full screen before and after its conversion, require a zero diff, record the outcome in the plan. Existing `*.e2e.test.ts` / smoke suites stay the standing regression net. Permanent goldens were rejected because they would pin ~20 dev-only demos to byte-exact output forever, which fights the next Tier-3 plan (FR-6 states Tier-3 canvases have no rect oracles and may diverge didactically). Smoke-only was rejected because "paints ≥1 cell" cannot detect the wrong-but-nonempty failure mode a replace→merge change produces. | ✅ Resolved |
| AR-8 | Verification | Which verify command fills every Verify line? | `yarn verify` / `TUI_SKIP_PERF=1 yarn verify` | **`yarn verify`.** The command CLAUDE.md declares. `TUI_SKIP_PERF=1` is available if a loaded machine produces a spurious perf failure, but is not the default — genuine perf regressions must stay visible. Note the build step is load-bearing here: examples tests import the **built** `@jsvision/ui` dist. | ✅ Resolved |
| ~~AR-9~~ | Design | Issue #111 warns that `sizeWorkspace()` (`theme-designer/src/app.ts:308`) re-assigns `workspace.layout` on every resize and "must keep re-applying `direction:'row'` after `row(...)` builds the tree". | keep the caveat / dissolve it via `setLayout` | **Dissolve it.** `workspace.setLayout({ position, rect })` merges, so the `direction:'row'` that `row(...)` set survives untouched. The caveat was written against the replace-semantics field write, before #117 shipped `setLayout`. Derived and presented to the user with the option to veto; not vetoed. | ✅ Resolved |
| ~~AR-10~~ | Design | `router-demo/main.ts:59` and `kitchen-sink/stories/drill-down.story.ts:29` are `this.layout = {…}` inside a `Group` subclass constructor. No `col()` can wrap `this`. | leave as field write / convert to `this.setLayout(…)` / restructure the class | **Convert to `this.setLayout(…)`, children get `fixed()`/`grow()` tags.** This matches issue #110's own reading ("the `DetailScreen extends Group` stays self-config, but its children become `this.add(fixed(title,1))`"). Restructuring the class was rejected as scope creep. Derived and presented with the option to veto; not vetoed. | ✅ Resolved |
| AR-11 | Design | After the shadow body is deleted, `story.ts`'s `import type { … LayoutProps }` becomes unused in both files. | drop the import / keep it | **Drop it** where it becomes unused; keep it where the file still references the type. Zero-semantic-impact cleanup required by the lint gate. | ✅ Resolved |
| AR-12 | Scope | Issue #114 lists `packages/forms/src/form-dialog.ts:58-62` as a shadowing local `place()`. | fix it / note it as already done | **Already done** — the #115 `flex-dialog-bodies` plan deleted it. Verified absent on branch. The issue body is stale and is corrected at close-out, not re-fixed. | ✅ Resolved |
| AR-13 | Scope | `tabs-demo/main.ts:43` defines `placed()`, which does not literally shadow a DSL export name (`placed` ≠ `place`), so #114's acceptance criterion does not strictly reach it. | convert / leave | **Convert** — it is a one-file, one-helper absolute placer semantically identical to the others in this phase, and leaving it behind would make the phase's own rule ("no local absolute placer survives in a touched file") inconsistent. Low stakes; flagged for veto. | ✅ Resolved |
| ~~AR-14~~ | Scope | `chrome-bars-demo` has **no** `*.e2e.test.ts`, unlike every other demo this plan touches. Its one conversion (`main.ts:99`) would land with no standing regression net. | convert with the buffer diff as its only proof / add an e2e test / skip the file | **Convert with the AR-7 buffer diff as its proof.** A single `body.layout = { size: { kind: 'fr', weight: 1 } }` → `grow(body)` on a demo the issue itself calls a "one-line consistency fix" does not justify authoring a new e2e harness; the before/after zero-diff is proportionate evidence for a one-line change. | ✅ Resolved |
| AR-15 | Scope | Preflight found that this plan's Phase 2 (theme-designer, GH #111) and Phase 3 (five example demos + `drill-down.story`, GH #110) are **already implemented in PR #127** (`feat/canvas-flex-adoption`), against the same base branch, under its own preflighted plan set `plans/canvas-flex-adoption/` (merged 2026-07-20, shortly after this ruling). The plan's only mention of #127 rated it a Low-impact roadmap-file conflict, and the feature roadmap has no row for it. | (a) re-scope this plan to Phase 1 only; (b) land #127 first and rewrite 2–3 as a delta; (c) close #127 and absorb its work here | **(a) Re-scope to Phase 1 only.** The shadow `at()` retirement is untouched by #127, is the epic's highest-leverage item, and is independently shippable. Phases 2–3, AC-4/AC-5, and AR-4/AR-9/AR-10/AR-14 are dropped; #110/#111 move to a new `canvas-flex-adoption` roadmap row. Option (c) was rejected because it discards a complete preflighted plan set and contradicts this plan's own "no existing test file edited" contract, which #127 necessarily breaks. | ✅ Resolved |
| AR-16 | Scope | Three local `at`/`row` shadows survive outside this plan's touched set: `theme-designer/src/view/gallery.ts:32` and `inspector-panel.ts:55` (both `at(g, view, x, y, w, h)` — a different signature that also adds to the group) and `examples/keyboard-mouse-playground/main.ts:126` (`row`). The plan originally read as if it left "one `at()` in the repo". | (a) widen Phase 1 to convert them; (b) defer and name them on the follow-up issue; (c) leave them unmentioned | **(b) Defer and name them.** They are per-file conversions rather than a re-export, and the theme-designer pair sits in a package whose only headless proof vehicle does not render the inspector at all — so there is no zero-diff witness for it today. The phase's rule is restated honestly as "no shadow survives in a file this plan touches", and all three go on the follow-up issue (task 2.1.2). | ✅ Resolved |
| AR-17 (runtime) | Verification | The replace→merge audit surfaced exactly one ⛔: `layout.story.ts:31` sets `{direction:'row', gap:1, align:'stretch'}` on a Group that `:35` then places with `at()`. The shadow replaced that object, so the story's declared `gap:1` never reached the engine; the builder merges, so it now renders — a one-space, one-line, one-story diff that breaks the otherwise-clean zero-diff bar. | (i) keep the exact pixels via an explicit field write + comment; (ii) neutralise by deleting `gap:1`; (iii) accept and record as a deliberate fix | **(iii) Accept as a deliberate fix.** This is the `foundations/layout` story, whose stated job is teaching the flex engine and whose own caption advertises `gap` as a supported prop — it was demonstrating the feature while not rendering it. (i) would deliberately preserve a bug in the one story that exists to demo the feature; (ii) would delete the author's stated intent to protect a rendering artifact. Measured, not inferred: 234 before/after renders across both showcases at two viewport sizes produced this single change and nothing else. | ✅ Resolved |

## Gate statement

✅ **GATE PASSED** — 17 items, 17 resolved, 0 deferred. Confirmed with the user 2026-07-20;
re-confirmed 2026-07-20 after the preflight re-scope (AR-15, AR-16).
