# Ambiguity Register — widget-flex-adoption

> **Status**: ✅ GATE PASSED
> **Plan**: widget-flex-adoption · **GitHub**: [#109](https://github.com/blendsdk/jsvision/issues/109) + [#116](https://github.com/blendsdk/jsvision/issues/116)
> **Created**: 2026-07-19
> **Items**: 10 · **Resolved**: 10 · **Deferred**: 0

Every design, scope, and behavior decision below carries the user's explicit ruling. Nothing is
assumed; nothing is deferred.

## Register

| # | Category | Ambiguity | Options considered | ✅ Decision | Source |
|---|----------|-----------|--------------------|------------|--------|
| AR-1 | Behavior | `grow()`/`fixed()` merge (`{...view.layout, size}`) but the code they replace clobbers wholesale. On caller-supplied receivers this silently changes semantics and **no existing test would catch it**. | (a) preserve clobber everywhere; (b) adopt merge everywhere; (c) decide per site | **Per-site, resolved by an externally-observable rule:** preserve the wholesale clobber only where a third party can reach the receiver — `tab-view.ts:254` (`Tab`/`TabViewOptions` are barrel-exported) and `application.ts:330` (`createApplication` is public). Adopt the taggers at the five internal receivers (`button-row.ts:84`, `filter-popup.ts:47/48`, `grid-panels.ts:566/567/658`), each proven unreachable from outside its package. | User, 2026-07-19 |
| AR-2 | Scope | `#109`'s `application.ts` portion after later exclusions. | (a) include the 2 clear sites; (b) defer all of `application.ts` to #117; (c) include all four non-overlay sites | **Include the clear sites only.** `:335`/`:435` are the T-AO1 overlay (closed won't-do, RD-01 FR-4 hidden-host exclusion). `:347`/`:353` are the `{...spread, size}` pattern #117 owns. **Superseded in part by AR-1** — see AR-9. | User, 2026-07-19 |
| AR-3 | Scope | `grid-panels.ts` breadth — the 2026-07-19 sweep recorded "only 3 of 23 convertible", which the current-state audit disproves (all 24 are pure flex; the file contains no `position:'absolute'` at all). | (a) all 24; (b) containers only; (c) defer the file to its own slice | **All 24 sites.** Taggers add no nesting depth and preserve semantics, so `golden-screen.spec` + `a11y-golden.spec` act as the guard: green-and-untouched if the conversion is correct, loudly red the moment it is not. | User, 2026-07-19 |
| AR-4 | Testing | Oracle policy for a behavior-preserving refactor whose oracles include structural locators (e.g. `cell-editor.spec.test.ts:461` reads `editorHost.children[0]` two levels deep). | (a) geometry frozen, locators re-expressible; (b) strict zero spec-test edits; (c) allow re-baselining per RD-01 | **Geometry frozen; locators re-expressible.** Every geometry and golden assertion stays **byte-identical and unedited**. A child-index locator may be re-expressed structurally ONLY where nesting genuinely changes, never weakened, and each such edit is logged as a plan deviation. | User, 2026-07-19 |
| AR-5 | Process | What governs this plan — #109/#116 are behavior-preserving and the roadmap places them **outside** RD-01's divergence set. | (a) issue-driven + RD-02 verification; (b) pure issue-driven; (c) draft a new RD first | **Issue-driven + RD-02.** `Implements: layout-dsl-adoption/GH-109 + GH-116`, adopting RD-02's non-functional/verification bar (bench, zero-regression, verify-green) **without** RD-01. RD-01's divergence licence explicitly does **not** apply. | User, 2026-07-19 |
| AR-6 | Process | The single verify command for every Verify line, given `TUI_SKIP_PERF` does not propagate through turbo and two perf specs flake under parallel load. | (a) `TUI_SKIP_PERF=1 yarn verify`; (b) plain `yarn verify`; (c) fix `turbo.json` `passThroughEnv` first | **`TUI_SKIP_PERF=1 yarn verify`** — reproducible 30/30, and the skipped assertions are informational bench checks that CLAUDE.md states never gate. | User, 2026-07-19 |
| AR-7 | Scope | The four datagrid sites using raw `{position:'absolute', rect}` with dynamically computed coordinates. | (a) convert 3 to `at()`, skip `:272`; (b) leave all four; (c) convert all four | **Convert `quick-filter-row.ts:155`, `personalize-dialog.ts:391`, `overlay.ts:125` to `at()`; leave `filter-popup.ts:272`.** `at()` is merge-preserving and yields the same descriptor shape, so the 15 `.layout.rect?.x` reads in `quick-filter-row.impl` still pass. `:272` is `this.layout` self-config, which the DSL cannot wrap — the #113 S6 deferral, no consumer. | User, 2026-07-19 |
| AR-8 | Scope | The four `{position:'fill'}` sites the sweep listed as "a follow-up" rather than part of #116. | (a) include all four; (b) keep them out per the sweep; (c) only the `editing.ts` cell editor | **Include all four** — `grid.ts:508/511/1417` + `editing.ts:230` → `cover()`. One-for-one merge-preserving replacement with no nesting change, so `editing.spec`'s `overlay.children[0]` and `cell-editor.spec`'s nested locators are untouched. The sweep's deferral predates T-AO1 clarifying which overlays are hidden hosts; all four here mount into a live host. | User, 2026-07-19 |
| AR-9 | Conflict | **AR-2's option text said convert `application.ts:330` to `grow(body)`, but AR-1's later per-site rule classifies `:330` as a public receiver requiring the clobber.** Two user decisions in tension. | (a) AR-1 wins (later + more specific); (b) AR-2 wins (named the site explicitly) | **AR-1 wins.** It was ruled in a later round, with the full per-site audit in evidence, and names `application.ts:330` explicitly as an externally-reachable receiver. Consequence: `application.ts` yields **one** conversion (`:341`) plus one documented, test-pinned non-conversion (`:330`). Surfaced to the user in the plan summary rather than silently reconciled. | Derived + surfaced, 2026-07-19 |
| AR-10 | Scope | JSDoc `@example` blocks containing `.layout =` (`data-grid.ts:89`, `tab-view.ts:199`, `application.ts:314`, `grid.ts:293`, `editable-grid-rows.ts:208`, `button-row.ts:63`). | (a) in scope; (b) out of scope | **Out of scope** — the feature roadmap assigns JSDoc `@example` modernization to **#112**. Resolved by a prior recorded decision, not a new one; listed so the exclusion is explicit and a reader does not read the untouched examples as an oversight. | Roadmap #112 row |

## Gate statement

All 10 items carry an explicit ruling. Zero deferred. AR-9 records a genuine conflict between two
user decisions and the basis on which it was reconciled — it is surfaced in the plan summary rather
than buried here.
