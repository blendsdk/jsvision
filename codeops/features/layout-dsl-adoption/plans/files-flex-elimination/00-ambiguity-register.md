# Ambiguity Register — files-flex-elimination

> **Status**: ✅ GATE PASSED
> **Feature**: layout-dsl-adoption · **Plan**: files-flex-elimination
> **Created**: 2026-07-19
> **CodeOps Skills Version**: 3.10.0

Every gap, ambiguity, and unstated assumption found while planning GH #120, hunted across the 12
gate categories. `RD` = resolved by an already-approved requirement (RD-01/RD-02); `USER` = an
explicit decision taken in this planning session.

## Register

| # | Category | Ambiguity | Options considered | Decision | Source |
|---|----------|-----------|--------------------|----------|--------|
| AR-1 | Scope / design | How far may the rebuilt FileDialog/ChDirDialog layouts move, given RD-01 sanctions divergence? | (a) Preserve the visual arrangement, expressed as flex; (b) re-compose idiomatically (buttons to a bottom band, matching the ui messageBox family) | **(a) Preserve the arrangement.** Divergence is a *means* (machinery deletion), not an end; preserving the composition keeps the two kitchen-sink stories visually stable and minimises oracle churn. | USER |
| AR-2 | Behavior / sizing | Does `errorBox` keep its `min(60, max(24, len+6)) × 7` size formula? | (a) Keep it; (b) let flex size the box from content | **(b) Flex-sized.** Height is derived from content, so a long message is no longer clipped. | USER |
| AR-3 | Sizing detail | `Text.measure()` returns the *unwrapped* line count, so it cannot see wrapping — a message over ~54 chars clips today. How is the errorBox height actually derived? | (a) Wrap-aware fit (count wrapped lines at the content width); (b) `measure()` only (honours explicit `\n` only, clipping bug survives); (c) revert to fixed height 7 | **(a) Wrap-aware fit**: `height = wrapText(message, width - 2).length + 4` (frame 2 + button band 2). Fixes the latent clipping as a side effect. No test asserts height 7 (AR-11), so nothing breaks. | USER |
| AR-4 | Dependencies / scope | A wrap-aware height needs the line count, but `wrapText` is private to `packages/ui/src/controls/text.ts` and `errorBox` lives in `packages/files`. | (a) Export `wrapText` from the ui barrel; (b) reimplement the count inside `files`; (c) make `Text.measure(available)` wrap-aware | **(a) Export from ui**, moving `wrapText` beside `stringWidth` in `controls/measure.ts`. (b) rejected: two wrap algorithms that can drift, and any drift reinstates the very clipping AR-3 fixes. (c) rejected: changes flex sizing for every `Text` in the framework, far outside this issue. **Accepted consequence:** the PR spans ui + files, deviating from #120's "one focused `@jsvision/files` PR" ground rule. | USER |
| AR-5 | Testing | RD-02 NFR-3 says delete `file-dialog-resize.spec.test.ts` (it asserts removed grow-mode). What then covers resize behavior at the spec tier? | (a) Add an invariant spec oracle (containment + the list grew + the min floor held); (b) rely solely on the re-baselined `file-dialog-resize.impl.test.ts` | **(a) New invariant spec oracle.** Keeps the guarantee without re-encoding TV coordinates — asserts properties, not rects. | USER |
| AR-6 | Testing | `history-files.spec.test.ts:42,50` asserts exact `History` bounds on both dialogs but is **not classified** in RD-02's NFR-3 table. | (a) Classify as re-baseline and amend RD-02; (b) choose container paddings so the asserted bounds are preserved and it survives | **(b) Survives unedited.** The geometry derivation (03-01 §3, 03-02 §3) selects the container paddings so `history` lands at `(31,3,3,1)` / `(42,3,3,1)` exactly as today. No RD-02 amendment is needed. **Falsifiable:** if the solver disagrees in the red step, this reverts to (a) and RD-02's table is amended — recorded as a plan deviation. | Derived + USER (AR-1) |
| AR-7 | Layout | Both dialogs currently override the base `Dialog` `padding: 1` to `padding: 0` so absolute rects measure from the outer frame. Keep or restore? | (a) Restore `padding: 1`; (b) keep `padding: 0` | **(a) Restore.** The padded frame interior becomes the content box that `cover()` fills. Required by the `cover(col(...))` idiom; the `padding: 0` override exists only to serve absolute rects. (`file-dialog.ts:116`, `chdir-dialog.ts:93`) | RD-01 FR-5 |
| AR-8 | Behavior | Do the `resizable`/`minWidth`/`minHeight` floors change? | — | **No.** `resizable = true`, `49×19` / `48×18` floors stay exactly as-is; enforced by the untouched `Window` resize path. | RD-01 FR-2 |
| AR-9 | Testing | NFR-2 requires a traversal-order spec test per FR-1 dialog. Does `errorBox` need one, having a single focusable? | (a) Include a minimal one; (b) skip it as trivial | **(a) Include.** NFR-2 is per-dialog with no size exemption; the test is three lines and pins that the OK button stays the sole tab stop. | RD-02 NFR-2 |
| AR-10 | Testing | Traversal oracles assert the *pre*-conversion order, so they pass green when first written rather than red. Is that a spec-first violation? | — | **No.** NFR-2 explicitly specifies green-first witnesses ("written spec-first, from the pre-conversion sequence, and must stay green through the rebuild"). The *geometry* oracles remain red-first per RD-02's test-order discipline. | RD-02 NFR-2/NFR-3 |
| AR-11 | Testing | Does deeper flex nesting break test locators? | — | **No.** `dlg.buttons` is a named `Button[]` field the dialog populates itself (`file-dialog.ts:101`, `chdir-dialog.ts:74`); no test anywhere uses `dlg.children[i]` / `.find` / `.filter`. Verified by a full sweep of `packages/files/test`. The field must keep being populated in construction order regardless of nesting. | Verified |
| AR-12 | Scope | Do the kitchen-sink stories need new work? | — | **No.** `file-dialog.story.ts` and `chdir-dialog.story.ts` already exist and are registered; they construct the dialogs and assert no geometry. They must keep passing the headless smoke test. | Verified |
| AR-13 | Process | Which branch, and what does the PR target? | — | Work on `feat/files-flex-elimination`, cut from `feat/dsl-adoptation` (which carries the #122 focus-traversal prerequisite, unmerged in PR #123). PR retargets to `develop` once #123 lands. | USER |
| AR-14 | Process | What is the verify command? | — | `yarn verify` (lint → typecheck → build → test → check:docs), per the project CLAUDE.md. `yarn lint:fix` before the PR-bound push (repo prime directive). | CLAUDE.md |
| AR-15 | Layout | Most widgets here lack `measure()`, so an `auto` flex child collapses to `{0,0}`. | — | Every non-self-measuring child gets an explicit `fixed(v, n)` or `grow(v)`. Only `Text` and `Button` self-measure (`controls/text.ts:145`, `controls/button.ts:117`); `Label`, `Input`, `History`, `ScrollBar`, `FileList`, `DirList`, `FileInfoPane` and bare `row()`/`col()` Groups do **not**. The issue body's sketched trees omit this and would render four collapsed children. | Verified |

## Gate statement

All 15 items are resolved with an explicit decision; zero deferrals. AR-6 carries a named
falsification path rather than an assumption. The gate is **PASSED** and plan authoring may proceed.
