# Ambiguity Register: docs-example-modernization

> **Document**: 00-ambiguity-register.md
> **Parent**: [Index](00-index.md)
> **Status**: ✅ GATE PASSED
> **Created**: 2026-07-20 15:12
> **Updated**: 2026-07-20 (preflight iteration 1 — AR-4/7/8/9/10/12/13 amended, AR-14…AR-16 added)
> **Items**: 16 · Resolved: 16 · Deferred: 0

Every decision below was put to the maintainer with grounded options and decided by them. Rows
marked *(measured)* were settled by evidence rather than preference — the probe result is cited and
there was no second viable option.

## Register

| # | Category | Ambiguity | Options presented | ✅ Decision | Rationale |
|---|---|---|---|---|---|
| AR-1 | Scope | Does #112 still stop at the flex-shaped `@example`s, or does it expand to the absolute ones? | (a) expand to the 53 `position:'absolute'` lines + `split-view.ts:109` → `cover()`; (b) filed scope only (6 flex lines); (c) flex + split-view only | **(a) expand** | The issue's own "Don't" rationale was *"the DSL has no absolute form"* + *"would only add layout noise"*. #113 shipped `at()`/`cover()`/`center()` and inverted both: `at(v,x,y,w,h)` is **one** line where the raw write is two. The issue pre-authorized exactly this (`"Scope may expand with #113"`). Leaving 53 public examples teaching the raw field write also strands them as wrong docs the moment #117-P4 lands |
| AR-2 | Verification | The `@example` sweep has no oracle — `check-jsdoc.mjs:132` only asserts the *tag exists*; nothing has ever compiled an example | (a) permanent extract-and-typecheck guard; (b) one-shot scratch typecheck; (c) manual review | **(a) permanent guard** | 37 files with no compiler in the loop is how wrong documentation ships. CLAUDE.md already *states* examples are an API contract; this makes it enforced. It is also the only option that keeps paying after this plan |
| AR-3 | Scope | The docs-site slice — 7 local `at()` shadows (38 call sites) and the `list-box.ts` col rewrite the issue calls "optional" | (a) both in; (b) shadows only; (c) defer docs-site entirely | **(a) both in** | *(The first answer selected all three, which is self-contradictory; re-asked and resolved to (a).)* The roadmap already assigns the docs-site sites to #112, and `list-box.ts` becomes the docs-site's one worked flex example |
| AR-4 | Scope boundary | RD-01 FR-6 also sanctions converting docs-site's absolute **canvases** to `col`/`row` composition | (a) out of scope → #129; (b) in scope here | **(a) → #129, with `list-box.ts` carved out by name** | #129 already owns *"the Tier-3 canvas maximal"* with the manual showcase quality pass RD-02 attaches. Per-file design rewrites do not belong inside a documentation pass; keeping them out is what lets this plan stay mechanical and reviewable. **Preflight amendment:** FR-8 converts exactly one such canvas — `examples/containers/list-box.ts` — which AR-3 admitted separately as the docs-site's one worked flex composition. That single file is a **deliberate, named exception** to this row; #129 owns every other docs-site canvas and must not find this one unconverted-but-claimed. Recorded in #112's close-out (task 4.1.2) |
| AR-5 | Design | How is the guard scoped so the plan can finish green, given the true baseline is only knowable once the harness exists? | (a) repo-wide + committed allowlist; (b) layout-carrying blocks only; (c) self-contained blocks only | **(a) repo-wide + allowlist** | Protects all ~377 blocks from day one and keeps the plan green whatever the tail turns out to be. (b) is an odd permanent contract ("only layout examples must compile"); (c) rests on a heuristic a stray `import` can defeat |
| AR-6 | Scope | Four public examples the probe proved are wrong today, unrelated to layout | (a) fix all four here; (b) allowlist them + file a follow-up | **(a) fix all four** | All four are in files this plan already opens. Allowlisting a defect discovered while building the detector for that exact defect is perverse. See [02](02-current-state.md) §Live defects |
| AR-7 | Placement | Where the guard lives — CI runs the full `yarn verify` on Linux only; Windows/macOS run `verify:shipped` (core/ui/web/files), and `ci.yml:47-55` names `ts.createProgram` as a known Windows-timeout risk | (a) `packages/docs-site/test/`; (b) a root script in `verify`; (c) `packages/ui/test/` | **(a) docs-site** | It is **excluded from `verify:shipped`** (`package.json:24` filters to core/ui/web/files), so the compiler cost lands on the Linux cell — exactly where `ci.yml:44-55` already parks the other heavy tooling — and it **already builds a full `ts.createProgram` in this very package** (`test/api-barrel-exports.spec.test.ts` over `src/api/barrel-exports.mjs`, with a 60 s timeout in `vitest.config.ts` explaining that cost). It also lists `typescript` as a direct devDep and depends on built `@jsvision/ui`+`core`. (c) puts a full `ts.createProgram` on the Windows runner, the configuration CI documents as timing out. **Preflight correction:** the original rationale claimed docs-site was *the one* package listing `typescript` directly — it is not (core, ui, files, forms and datagrid list it too). The decision stands on the two load-bearing legs above; the uniqueness claim was wrong and is withdrawn |
| AR-8 | Process | The verify command behind every **phase-closing** Verify line | `yarn verify`; something else | **`yarn verify`** | Per CLAUDE.md: `yarn lint` → `turbo run typecheck build test check:docs` → `check-plugin.mjs`. `yarn lint:fix` runs before the PR-bound push per the repo's prime directive. **Preflight correction:** the row originally said *every* task's Verify line. Steps 1.1 and 1.2 deliberately use the inner-loop `yarn workspace @jsvision/docs-site test` while the harness is still red; `yarn verify` is the gate at every **step boundary** (1.3, 1.4, 2.4, 3.4, 4.1) |
| AR-9 | Contract | What the allowlist promises, given the sweep edits **6** already-allowlisted blocks | (a) may only shrink; (b) edited ⇒ must be fixed; (c) shrink + opportunistic pre-fixes | **(a) may only shrink** | The build fails for any *non*-allowlisted failure and for any *new* error on an allowlisted block; a mechanical edit that changes nothing about why a block fails is permitted. (b) drags six unrelated examples (an ambient `dialog`, an `app`, a datagrid options fix) into a layout-documentation plan. *(Preflight: the row originally said "9" in its question and "six" in its rationale; six is correct — see AC-6 and [02](02-current-state.md) §"The layout surface this plan edits".)* |
| AR-10 | Design | How an allowlist entry is keyed — the sweep shifts line numbers | (a) `file::symbolName`; (b) `file#ordinal`; (c) `file:line`, regenerated | **(a) `file::symbol`, qualified and ordinal-suffixed where the symbol alone is not unique** | The `@example` is attached to a declaration, so the harness already knows the symbol. Stable across line shifts and reorderings, and it reads meaningfully in review. (b) silently re-targets when an example is inserted above it — the one failure mode a ratchet must not have. **Preflight amendment — the bare symbol is not unique.** Three real collisions exist: (i) two `@example` tags on the **same** declaration (`packages/ui/src/controls/input.ts:47` and `:58`, both on `class Input` — a file this plan edits); (ii) member-level blocks keyed by a bare method name, which is not file-unique by construction (39 such blocks: 20 `MethodDeclaration`, 8 `MethodSignature`, 11 `PropertySignature`); (iii) a genuine anonymous block (`packages/files/src/fs/node-fs.ts:9`, an `@example` on the file's leading comment, which binds to the first `ImportDeclaration`). The key is therefore `file::Symbol`, with a `Class.member` qualifier for members and a `#N` suffix (1-based, in source order) **only when a key would otherwise repeat**. `(anonymous)` follows the same rule → `(anonymous)#1`. The `#N` suffix is scoped *within one symbol*, so AR-10's objection to (b) — file-wide ordinals silently re-targeting — does not apply |
| AR-11 | Contract | Enforcement when an allowlisted block starts compiling | (a) fail with a stale-entry error; (b) warn only | **(a) hard failure** | This is what makes AR-9 a ratchet rather than a wish. The message names the entry and instructs removal; the list can never silently carry dead weight |
| AR-12 | Design *(measured)* | Some `@example` bodies are wrapped in a ` ```ts ` fence, others are not | — | **The harness strips a leading/trailing code fence before compiling, unconditionally and package-agnostically** | Leaving fences in makes the backticks parse as template literals — it produced 44 phantom `TS2349` failures in the feasibility probe before being corrected. No second viable option. **Preflight correction:** the original row said "`datagrid` and `theme-designer` fence, `core`/`ui` do not". Re-measured over the 393 distinct blocks the real distribution is **datagrid 85 · ui 8 · forms 2 · theme-designer 0** (the ui blocks are `controls/measure.ts:31,66`, `table/columns.ts:63,115,182,216`, `table/grid-rows.ts:78,359`). Fencing is a **per-block** property, not a per-package one — stripping must never be scoped by package |
| AR-13 | Design *(measured, superseded by AR-16)* | Examples that use a **relative** import (`'./button-row.js'`) cannot resolve from a scratch directory | — | **Each block must be compiled *as if* it sat beside its own source file** | Measured: compiling blocks from a shared temp dir produced ~22 phantom `TS2307 Cannot find module './x.js'` failures. Resolving relative specifiers correctly requires the block to occupy the source's own directory path. A second reason surfaced in preflight: every package is `"type": "module"`, and 37 blocks use top-level `await` — they compile only because the block's path lands inside a `type: module` package. **The *path* is load-bearing; writing an actual file is not** — see AR-16, which keeps this resolution behaviour and drops the filesystem write |
| AR-14 | Design | Does an unused local in an `@example` fail the guard? `tsconfig.base.json:8-9` sets `noUnusedLocals`/`noUnusedParameters`, and 03-01 mandates "the project's own compiler options" | (a) base **minus** `noUnusedLocals`/`noUnusedParameters`; (b) base verbatim; (c) drop the recorded baseline entirely | **(a) base minus the unused-checks** | An unused local in a documentation snippet is not an API defect, and the guard exists to protect the *API contract*. Measured on 300 import-less blocks: base-as-written fails **209**, base-minus fails **160** — ~56 blocks whose sole failure is snippet hygiene. (b) would put all 56 permanently on the allowlist and inflate the drain issue with non-defects; it would also invalidate the AC-6 oracle table, whose `spinner.ts::Spinner` row records `TS2304` where base-as-written reports `TS6133` first. **This was a decision the plan was making implicitly and unknowingly** — the feasibility probe ran with these checks off, which is why its numbers reproduce (a) exactly |
| AR-15 | Scope | Which `src/` trees the guard walks — FR-1's `packages/*/src` glob, 03-01's seven-package enumeration, and docs-site's own four-package boundary all disagreed | (a) the six shipped packages, enumerated; (b) the glob as written; (c) docs-site's four documented packages | **(a) `core, ui, web, files, datagrid, forms` — an explicit enumeration, never a glob** | The glob also matches `packages/docs-site/src` (12 blocks) and `packages/spike-data-studio/src` (2), the latter a package CLAUDE.md marks "delete after the Data Studio decision" — seeding a permanent ratchet from an inert package is wrong. `theme-designer` is dropped too: it is an **app**, not shipped source (CLAUDE.md scopes the JSDoc directive to shipped packages), and it has **no `build` script**, so it has no `dist` for its blocks to resolve against. (c) is too narrow — `datagrid` and `forms` are shipped and carry 104 blocks between them. **Consequence:** `datagrid` and `forms` must be added to `packages/docs-site/package.json` devDependencies, or turbo's `^build` will not order their builds before `docs-site#test` and the allowlist becomes build-order dependent |
| AR-16 | Design *(preflight)* | Where the extracted blocks physically live while they compile. AR-13 wrote real `.ts` files into `packages/<pkg>/src/`, beside real sources | (a) an in-memory `ts.CompilerHost`; (b) on-disk beside the source, hardened; (c) a sibling directory plus a `paths` shim | **(a) in-memory `CompilerHost`** | The on-disk write was the guard's single largest hazard and it failed three ways: it races turbo's `typecheck`/`build` (which have **no ordering** against `test`), a leak is **silent** (verified — `tsc`'s wildcard `include` does not match dot-prefixed filenames, and `.gitignore` has no entry), and a `finally` cannot cover SIGINT/SIGKILL. A custom host serves each block as a virtual `SourceFile` **at the real source's directory path**, so AR-13's relative-specifier and `type: module` resolution are preserved exactly, while `writeFile` is a no-op and no scratch file ever exists. It also removes the intra-vitest race between the spec run, the impl run and AC-9's forced-failure run. (b) is a complete fallback but every one of its controls guards a path nobody exercises; (c) re-opens the ~22 phantom `TS2307`s AR-13 exists to close |

## Runtime decisions (recorded during execution)

| # | Decision point | Outcome |
|---|---|---|
| AR-R1 | `packages/ui/src/app/application.ts:275`'s `syncOverlayVisible` import — which branch of [03-02](03-02-jsdoc-example-modernization.md) §FR-6 applies | **Branch 0 — the root-barrel omission is the defect.** Escalated to the maintainer as 03-02 requires, ruled 2026-07-20: add the symbol to `packages/ui/src/index.ts:87` beside `createApplication`. Evidence for public status was unanimous — full public JSDoc with its own `@example`, exported from `app/index.ts:8`, imported cross-subsystem by `menu/controller.ts:18` and `dropdown/popup.ts:19`, and pinned by both `dropdown.seams.spec.test.ts` and `dropdown.seams.impl.test.ts`. Only the root barrel omitted it. The example additionally declared no `myPopup`, so it now constructs one and merges its two duplicate import lines. Consequence accepted: the public surface grows by one function and the plugin API-ref snapshot drifts, so `yarn plugin:sync --fix` joins this plan's close-out |
| AR-R2 | The 03-03 audit's ⛔ rulings, if any | **None — the audit cleared with 0 ⛔.** Seven sites surfaced across A1–B1, all inert; no argument anywhere carries `padding`. Full evidence in [03-03](03-03-docs-site-shadow-retirement.md) §The two behavioural deltas |
| AR-R3 | **Harness placement rationale corrected (task 1.2.1).** [03-01](03-01-example-compile-guard.md) and task 1.2.1 justify `src/api/` partly on "`tsconfig.json` includes `src/**` but not `test/**`, so only a module in `src/` is covered by `yarn typecheck`". **Verified false**: the include is `["examples/**/*.ts", "src/**/*.ts"]`, which matches only `.ts`, and `allowJs`/`checkJs` are off — `tsc --listFiles` reports **zero** files under `src/api/`, so no `.mjs` there is typechecked (`barrel-exports.mjs` never was either) | **Placement kept, rationale narrowed.** The harness stays at `packages/docs-site/src/api/jsdoc-examples.mjs` on the one rationale that does hold — it mirrors `src/api/barrel-exports.mjs`, the package's established shape for a module shared between plain-`node` scripts and the vitest specs. Widening the include to `src/**/*.mjs` with `checkJs` was rejected: it would newly typecheck five unrelated pre-existing `.mjs` modules, which is scope this plan has no mandate for |
| AR-R4 | **An eighth shadow, outside the plan's census (task 3.4.1).** The plan's file survey covered `packages/docs-site/examples/` and found seven `at()` shadows. `centerInInterior` at `packages/docs-site/src/demo-shell.ts:238` is an eighth instance of the same replace-semantics defect — it placed the built example with `view.layout = {…}`, discarding every prop the example's root carried. Consequence: **no example could return a flex container root at all.** The `list-box` rewrite lost its `direction:'col'` and re-solved as a row, rendering the echo line vertically down a 1-cell column. FR-8's premise that `cover()` makes the example fill the viewport is also wrong — the shell clamps to `intendedSize`, which defaults to 40×10 when no rect is present | **Fix the shell to merge.** Ruled by the maintainer 2026-07-20: `view.setLayout({…})` at `demo-shell.ts:238`. Grounded before the ruling by measurement — under the merged shell the other **six** examples render byte-identical to their pre-change baselines, so the blast radius is zero, and `list-box` solves correctly (list 40×8 · gap 40×1 · echo 40×1). Scope extension beyond `examples/` accepted deliberately: leaving it would arm the same trap for every future flex-rooted example. Post-review follow-up: the shipped comment still taught the (wrong) `cover()` premise, and the 40×10 box was coming from a private fallback constant in the shell rather than from the example. Ruled 2026-07-20: state the size in the example — `at(col(…), 0, 0, 40, 12)`, which the merging shell now honours. **Recorded diff for `list-box`: exactly one row** — the list shows ten rows instead of nine, because `grow()` claims the row the old hand-computed rects left dead at the bottom of the same 40×12 box. Every other example is byte-identical |

## Gate statement

✅ **GATE PASSED** — 16 items, 16 resolved, 0 deferred. Every scope, design, contract and
verification decision in this plan traces to a row above. The maintainer confirmed the original
thirteen before any plan document was written; AR-14, AR-15 and AR-16 were raised by preflight
iteration 1 and decided by the maintainer on 2026-07-20, and AR-4/7/8/9/10/12/13 were amended in the
same pass. See [00-preflight-report.md](00-preflight-report.md) for the evidence behind each.
