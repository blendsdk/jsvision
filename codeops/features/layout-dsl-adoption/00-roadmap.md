# Roadmap: Layout-DSL Adoption

> **Feature-Set**: Layout-DSL Adoption
> **Status**: In Progress
> **Created**: 2026-07-18
> **Last Updated**: 2026-07-21 (#112 ✅ done; #110 ✅ reconciled; #131 + #132 filed; layout-field-lockdown 🔄 executing — **Phases 1 + 2 ✅ done, the field is locked**; #129 canvas adoption next)
> **Progress**: 11 / 12 issues done (#114 ✅ 2026-07-20 via PR #130; #110/#111/#113/#120 closed on GitHub the same day after a tracker audit found them stale-open; **#117 reopened to 🔄** — its P4 read-only field never shipped) (#113 · #122 · #115 · #120 · #109 · #116 · #111) · [canvas-flex-adoption](plans/canvas-flex-adoption/00-index.md) ✅ done 2026-07-20 (exec_plan 32/32, **PR #127 merged**) (#110 + #111 — five example demos + `drill-down.story` + the theme-designer panels/workspace) · [demo-app-flex-port](plans/demo-app-flex-port/00-index.md) 🔬 preflighted 2026-07-20 (20 tasks / 2 phases — **#114 shadow retirement only**; gate passed 16/16; re-scoped from 32/4 after preflight PF-001 found its Phases 2–3 already implemented in #127) · [docs-example-modernization](plans/docs-example-modernization/00-index.md) ✅ done 2026-07-20 (exec_plan 41/41 — **#112**; the `@example` compile guard + the 53-line `at()` sweep + 4 live doc defects fixed + the 7 docs-site shadows retired; allowlist drain → #131) · requirements: RD-01/RD-02 ✏️ drafted · plans: [tier0-parity-safe](plans/tier0-parity-safe/00-index.md) ✅ done (Tier-0 MVP — 12/12) · [focus-traversal-primitive](plans/focus-traversal-primitive/00-index.md) ✅ done (exec_plan 11/11, GH #122) · [flex-dialog-bodies](plans/flex-dialog-bodies/00-index.md) ✅ done 2026-07-19 (exec_plan 14/14) (#115 Tier-2 ui/forms dialog bodies — #122 unblocked) · [files-flex-elimination](plans/files-flex-elimination/00-index.md) ✅ done 2026-07-19 (exec_plan 27/27, PR #125) (#120 Tier-2 files dialogs + grow-mode deletion) · [canvas-flex-adoption](plans/canvas-flex-adoption/00-index.md) ✅ done 2026-07-20 (exec_plan 32/32) (#110 + #111 combined — 32 conversions across the didactic canvases; witness-heavy because the existing harnesses are stdout-smoke only and assert no geometry) · [widget-flex-adoption](plans/widget-flex-adoption/00-index.md) ✅ done 2026-07-19 (exec_plan 35/35) (#109 + #116 combined — all 47 behavior-preserving conversions landed across 10 files + 4 preserved public-receiver sites; first slice outside the RD-01 divergence set; zero existing test edited and both full-screen goldens zero-diff; preflight found 38 items over 2 iterations incl. 1 CRITICAL and narrowed `grid-panels` 23→15 back toward the issue's filed scope, post-phase review found 2 further MAJORs (the second demoted a fourth site to a preserved receiver) — all resolved)
> **CodeOps Skills Version**: 3.9.0

Adopt the layout DSL (`col`/`row`/`grow`/`fixed`/`spacer`/`stack`/`place` + the #113 additions) across
the existing `.layout = {…}` call sites — the deferred **AR-4 follow-up** of the archived `layout-dsl`
plan. **GitHub-issue-driven** (no per-target codeops plans) — this roadmap is the codeops-side register
pointing at the epic. The `dsl-hardening` prerequisite (GH #113) is **done** (its own feature/plan).

**Direction update (2026-07-19) — flex-elimination sanctioned.** After a fresh flex-refactor sweep (now
that #113's `at()`/`cover()`/`center()` shipped), the maintainer decided to **deliberately break Turbo
Vision geometry parity** to *eliminate* absolute placement in favor of flex — including the TV dialogs
and demo canvases. The payoff is machinery deletion (`grow-dialog.ts` + `grow.ts` go) plus ~470 demo/
story/docs sites adopting the idiom, not an `at()`-for-`at()` swap. This is governed by a requirements
set (a departure from the pure issue-driven model, because it overrides the TV-fidelity + immutable-spec
disciplines and needs a recorded decision):

- **[RD-01 — Deliberate TV-divergence flex-elimination policy](requirements/RD-01-deliberate-divergence-policy.md)** ✏️ Drafted
- **[RD-02 — Non-functional & verification](requirements/RD-02-non-functional-and-verification.md)** ✏️ Drafted
- **[Ambiguity Register](requirements/00-ambiguity-register.md)** — ✅ GATE PASSED (13 items)
- **[Plan: tier0-parity-safe](plans/tier0-parity-safe/00-index.md)** — ✅ Done (implements RD-01 Tier-0; verification RD-02) — base `Dialog` center/at · catchers + formDialog body `cover()` · 13 demos · CLAUDE.md carve-out
- **[Plan: focus-traversal-primitive](plans/focus-traversal-primitive/00-index.md)** — ✅ Done (exec_plan 11/11, 2026-07-19; GH **#122**; merged to `feat/dsl-adoptation`; ships to `develop` via PR #123) · 🔎 Preflighted 2026-07-19 ([report](plans/focus-traversal-primitive/00-preflight-report.md) — 7 findings, all resolved + fixed) (Primitive; enables RD-01 FR-2/FR-3) — a Tier-2 blocker surfaced during planning: `col`/`row` nest `Group`s but Tab traversal was group-scoped (emergent behavior of `advance()`, not spec-locked), and an empirical probe showed `FileDialog`/`ChDirDialog` Tab **already dead-ended in the list** today. Fix = scope-ceilinged tree-order `advance()` (with a `descendLast` reverse mirror + exited-group memory reset) so nested-flex dialogs become keyboard-traversable. Shipped: ST-F1…F7 spec oracles, all focus witnesses unedited, verify + bench green. **Prerequisite of #115 + #120.**
- **[Plan: flex-dialog-bodies](plans/flex-dialog-bodies/00-index.md)** — ✅ Done (exec_plan 14/14, 2026-07-19; merged to `feat/dsl-adoptation`; ships to `develop` via PR #123) · 🔎 Preflighted 2026-07-19 ([report](plans/flex-dialog-bodies/00-preflight-report.md) — 6 findings all resolved + fixed; PF-001 confirmed live during execution) · implements RD-01 Tier-2 ui/forms, verification RD-02; GH **#115** — the #122-unblocked dialog-body rebuilds: `messageBox`/`confirm`/`inputBox` + editor `findDialog`/`replaceDialog`/`confirmBox`/`replacePrompt`-inner + `formDialog` buttons → flex (`cover(col)` / centered `row`), deleting the local `at`/`tv`/`place`/`centerX`/`PAIR_WIDTH`/`buttonRects`. Oracle cost (per NFR-3): **message-box survives outright**; only `editor-dialogs.spec:51,89` + `form-dialog.impl:80` re-baselined; per-dialog traversal specs + a message-box render guard added (NFR-2, PF-002). **App-overlay `cover()` split out to a separate follow-up task** (user decision — a ~7-file overlay-locator ripple not in NFR-3, 2-line payoff), tracked as **T-AO1** — since **closed won't-do** (2026-07-19): the attempt proved the overlay is a hidden host whose geometry cannot come from the layout pass, now recorded as RD-01 FR-4's hidden-host exclusion. Splitting it out was the right call — it kept a 14/14 green plan clean of a dead end. 14 tasks.

#115 is re-scoped (ui/forms dialog family, deliberate divergence) and **#120** is new (files dialogs +
grow-dialog deletion). #116 (datagrid) and #117 (setLayout) stay behavior-preserving — out of the
divergence set. First slice = Tier 0 parity-safe — **planned** in `tier0-parity-safe`: base `Dialog`
`center()`/`at()`, menu + dropdown catchers + `formDialog` body `cover()`, the enumerated demo/shell
canvases, and the CLAUDE.md carve-out. **Note (plan gate finding):** the app overlay (`application.ts`)
`cover()` is **deferred to #115** — its `position:'absolute'` descriptor is a spec-test locator, so
it is not zero-spec-oracle-cost like the rest of Tier 0.

## Legend

⬜ Backlog · 📋 Plan Created · 🔎 RD Preflighted · 🔬 Plan Preflighted · 🔄 Executing · ✅ Done · ⛔ Blocked · ⏸️ Deferred · ❌ Won't do

## Tracker (GitHub-issue-driven)

| GH | Title | Kind | Stage | Notes |
|----|-------|------|-------|-------|
| [#108](https://github.com/blendsdk/jsvision/issues/108) | Adopt the layout DSL across the codebase | Epic | ⬜ | Umbrella; re-oriented to flex-elimination |
| [#113](https://github.com/blendsdk/jsvision/issues/113) | DSL hardening (prerequisite) | Prereq | ✅ | Feature [`dsl-hardening`](../dsl-hardening/00-roadmap.md) — done, merged to `develop` via PR #119 |
| [#117](https://github.com/blendsdk/jsvision/issues/117) | Primitive fix — `setLayout(partial)` (read-only field at release) | Primitive | 🔄 | Companion to #113; out of the divergence set. **P4 SHIPPED 2026-07-21 — `packages/ui/src/view/view.ts` now reads `readonly layout: Readonly<LayoutProps> = {}`, and `setLayout` writes it with `Object.assign`.** All 10 subclass `override layout` escape hatches are shut, so neither `view.layout = {…}` nor `view.layout.rect = {…}` compiles anywhere. **Phase 2 measured 843 write sites / 337 files** against the planned ~810 (830 excluding the inert spike): 43 in `ui/src` + `datagrid/src`, 81 across examples/docs/theme-designer and 17 markdown snippets, and 704 in test directories. `turbo run typecheck` went green on the **first** attempt after the flip (15/15) and exactly **2 of 1799** runtime tests failed — both the impl tests that assert the superseded *replace* contract, one inverted and one deleted with a recorded verdict each. Render control: all 6 capturable demo canvases byte-identical to their pre-conversion baselines. **The AR-16 deliberate-erasure inventory was short by two** (EX-6): `tabs/tab-view.ts:255` carries the known site's comment verbatim, and `editor/edit-window.ts:115` is the same erasure on the same kind of public seam with no comment — both now take the explicit-`undefined` reset. **Two further plan defects were caught and corrected at execution:** the prescribed Rule 2b order put `onResized()` *before* the rect write, but `EditWindow.onResized` reads `layout.rect` to re-pin the editor and both scroll bars, so it would have sized them from the pre-drag geometry on every drag-resize (EX-5); and `jsvision-doctor` flagged `win.setLayout({ position:'absolute', … })` — the idiom the flip mandates — while passing the identical old spelling, a fifth thing the flip breaks that is not a type error (EX-8). Phase 1: [layout-field-lockdown](plans/layout-field-lockdown/00-index.md) 🔄 **Phase 1 done 2026-07-20** (14/55 tasks; gate 16/16; preflight 25 findings all applied). **Phase 1 (#132 + the repo-wide gap) is complete and verified**: `packages/examples` typechecks all 255 of its files (153 were invisible), and every package now typechecks its own `test/` — 232 errors cleared across 9 packages, plus 13 hand-written `.d.mts` files for the untyped `.mjs` tooling the tests import. The phase review then found the sharpest defect of the lot: `createTheme`'s `roleOverrides` was typed `Partial<Theme>`, which made each *role* optional but every *field* required — contradicting its own JSDoc and the merge it actually performs — and clearing that type error had quietly turned three theme oracles tautological. Type widened, oracles restored and mutation-tested — covers #132 → #117-P4 → #129. Measured surface is **~810** write sites, not the issue's stale 225 — and the two blockers this row previously recorded both dissolved under measurement: a plain `readonly` **field** (not an accessor) leaves all 10 subclass initializers intact, and `Readonly<LayoutProps>` catches the in-place `rect` mutations once the subclass escape hatches are shut. Preflight then found what the flip breaks that is *not* a type error — the `@example` compile ratchet, the plugin API-ref snapshot, two impl tests asserting the replace contract, and 16 doc snippets — all now carried by the plan as FR-13. The issue's ask is to *replace* the raw `.layout` field; `packages/ui/src/view/view.ts:73` is still `layout: LayoutProps = {}`, a plain writable field, so the headline deliverable is only partly met. Corrected 2026-07-20 after a close-out audit found the roadmap claiming ✅ against code that contradicts it. Plan [setlayout-primitive](plans/setlayout-primitive/00-index.md) ✅ **done 2026-07-20** (exec_plan 22/22) · 🔬 preflighted 2026-07-20 (35 findings over 2 iterations, all resolved) — **P1–P3**: the method + **23 conversions** (14 DSL internals + 7 self-layout + 2 inherited from the closed #109) across 11 files. The replace-semantics audit ran *before* the migration and came out **23 ✅ / 0 ⛔**. Two recorded behaviour deltas: the DSL taggers now reflow on an already-mounted view (observed by nothing today — kept as a correctness default), and `commitPlacement()` gains one benign reflow. **P4 (read-only field) deferred and blocked**: needs the **59** remaining composition writes gone (46 once `spike-data-studio` is deleted — #110 remainder, #112, #114), a solution for **10** subclass `override layout = {…}` field initializers (a class field cannot override an accessor — the issue never mentions them, and this is why P1 ships without the getter), and a solution for the **9 in-place `layout.rect = …` mutations a getter would not stop**. Issue's counts are stale: **82 executable writes / 17 reads** today, not 225/29 — a raw grep says ~144 only because it counts JSDoc `@example` lines |
| [#122](https://github.com/blendsdk/jsvision/issues/122) | **Focus-traversal primitive** — tree-order `Tab` across flex containers | Primitive | ✅ | Companion to #117; **prereq of #115 + #120**; plan [focus-traversal-primitive](plans/focus-traversal-primitive/00-index.md) · ✅ done (exec_plan 11/11, 2026-07-19; merged to `feat/dsl-adoptation`; ships to `develop` via PR #123) · 🔎 preflighted 2026-07-19 |
| [#109](https://github.com/blendsdk/jsvision/issues/109) | ui widgets — data-grid / tab-view / application | Port | ✅ | behavior-preserving; split-view already done. **Done 2026-07-19** via [widget-flex-adoption](plans/widget-flex-adoption/00-index.md) (35/35) — all 12 conversions landed in 3 files; `datagrid.spec`'s 22 golden cases green and unedited. **Scope corrected:** `application.ts` yields **one** conversion, not the ~6 the sweep claimed — `:335`/`:435` are the T-AO1 hidden-host overlay (won't-do), `:347`/`:353` belong to #117, and `:330` is preserved as a public-API clobber contract (AR-1/AR-9) |
| [#110](https://github.com/blendsdk/jsvision/issues/110) | example app-shell demos + maximal canvas flex | Port | ✅ | ✅ **Done 2026-07-20** (closed on GitHub). Tier 3 (RD-01 FR-6); learning material. Plan [canvas-flex-adoption](plans/canvas-flex-adoption/00-index.md) ✅ done 2026-07-20 (exec_plan 32/32, PR #127; combined with #111 — FR-6 names both in one sentence) — **25/25 planned conversions landed in 6 files**; every frame snapshot cell-identical. **Closed with two recorded carve-outs, neither of them backlog:** (i) `view-demo/main.ts` + `layout.story.ts` (9 sites) are **won't-do by policy (AR-2)** — both exist to teach the raw View/Group spine and the raw cell-native flex engine, so converting them changes the lesson; they are not #129's either. (ii) The broader Tier-3 canvas maximal passes to **[#129](https://github.com/blendsdk/jsvision/issues/129)** as successor, which owns the deliberate absolute demo canvases (`containers`, `dropdowns`, `themes`, `color`, `date`, `tabs`, `playground`, `controls-live`, `status-bar.story`) plus five residual `at()`/`row` name shadows |
| [#111](https://github.com/blendsdk/jsvision/issues/111) | theme-designer panels + workspace | Port | ✅ | **Not independent** — RD-01 FR-6 names theme-designer panels alongside `@jsvision/examples`, same governance as #110. Plan [canvas-flex-adoption](plans/canvas-flex-adoption/00-index.md) ✅ done 2026-07-20 (PR #127) — **7/7 conversions landed in 3 files**. Load-bearing ordering: each panel's `direction:'col'` arrives from `app.ts` today, so the builders must convert first (03-02). `inspector`'s direction is vestigial (children all absolute) and is dropped; `sizeWorkspace`'s per-resize re-assignment is preserved |
| [#112](https://github.com/blendsdk/jsvision/issues/112) | JSDoc `@example` + docs modernization | Docs | ✅ | ✅ **Done 2026-07-20** (exec_plan 41/41 — 4 phases). Shipped: a permanent `@example` compile guard (376 blocks extracted and typechecked in memory against a committed shrink-only allowlist, 161 grandfathered → drain tracked by [#131](https://github.com/blendsdk/jsvision/issues/131)); the full 53-line `position:'absolute'` → `at()` sweep across 37 files plus 2 flex blocks and `split-view:109` → `cover()`; **4 live doc defects fixed** (3× `createEventLoop` arity + `syncOverlayVisible`, whose root-barrel omission was ruled the defect, growing the public surface by one function — AR-R1); and the 7 docs-site `at()` shadows retired (38 → 35 call sites) with the `list-box.ts` flex rewrite. **Two discoveries beyond the plan's census:** the audit cleared 0 ⛔ (AR-R2), and `demo-shell.ts` held an *eighth* shadow — it placed every built example with a wholesale layout write, so **no example could return a flex root at all**; fixed to merge, with 6 of 7 examples rendering byte-identical to pre-change baselines as the control (AR-R4). **Tier-3 docs-site composition remains out of scope → #129 (AR-4); the one exception this plan did convert inside that boundary is `examples/containers/list-box.ts`, so #129 need not look for it.** Planning record: 🔬 preflighted 2026-07-20 (41 tasks / 4 phases; gate 16/16; [preflight report](plans/docs-example-modernization/00-preflight-report.md) — 35 findings incl. **3 CRITICAL**, all resolved). |
| [#114](https://github.com/blendsdk/jsvision/issues/114) | local `place()`/`row()` shadow cleanup | Cleanup | ✅ | Plan [demo-app-flex-port](plans/demo-app-flex-port/00-index.md) ✅ **done 2026-07-20** (exec_plan 20/20, on `feat/story-at-retirement`) · 🔬 **preflighted 2026-07-20** (15 findings, all resolved; re-scoped to this issue alone) — now the plan's **sole** scope (Phase 1, 16 tasks of 20), and the epic's highest-leverage item: the two *exported* shadow `at()` helpers (`kitchen-sink/story.ts:69` = 300 calls, `datagrid-showcase/story.ts:68` = 111 calls) carry **411 call sites across 84 files** and retire in two lines via re-export. They are name-identical but semantically different from the blessed builder — the locals **replace** `view.layout`, the real `at()` **merges** and requests a reflow — so the swap gets the same audit-before-migration treatment #117 used (AR-6). Four local placers also retired: `wizard-demo:52,178`, `themes-demo:37`, `tabs-demo:43` (AR-13), `wizard.story:113`. **Executed result:** the audit came out **410 ✅ / 1 ⛔** — the single ⛔ (`layout.story.ts:31`→`:35`) is a `gap:1` the shadow had been silently discarding, accepted as a deliberate fix (AR-17). Delta B was empty repo-wide: every `at()` runs in `build(ctx)` pre-mount, so the added reflow is unobservable. Blast radius **measured, not argued** — 234 before/after renders (all 49 kitchen-sink + 68 datagrid-showcase stories × two viewport sizes) yielded exactly one differing line; the datagrid showcase is byte-identical across all 136. The four demos are zero-diff over 528 rendered lines. A one-shot `tsc` sweep over the normally-excluded paths (the package tsconfig omits `kitchen-sink`, the demos and `test/`) reports **46 pre-existing errors before and after — none introduced**. Post-phase review: perf **no findings** (`markRelayout` self-coalesces, so N calls collapse to one frame); 2 🟡 MINOR from the reviewer, both applied (a retrospective doc block trimmed; a mount-path impl test added beside the immutable oracle). Residual work → **#129**. Three shadows **deferred and named** (AR-16): `theme-designer` `gallery.ts:32` + `inspector-panel.ts:55` (different signature — a conversion, not a re-export) and `keyboard-mouse-playground:126`. **Issue body is stale:** the filed `forms/src/form-dialog.ts:58` was already deleted by [flex-dialog-bodies](plans/flex-dialog-bodies/00-index.md) (AR-12) |
| [#115](https://github.com/blendsdk/jsvision/issues/115) | **flex-eliminate ui/forms dialog family** (deliberate divergence) | Rebuild | ✅ | RD-01/02; Tier-0 done ([tier0-parity-safe](plans/tier0-parity-safe/00-index.md)); **Tier-2 bodies ✅ DONE 2026-07-19** ([flex-dialog-bodies](plans/flex-dialog-bodies/00-index.md), exec_plan 14/14 — ui messageBox family + editor dialogs + forms formDialog buttons) — #122 unblocked them. Last leftover **T-AO1 closed won't-do** (hidden-host exclusion, RD-01 FR-4), so the issue is complete |
| T-AO1 | app-overlay `cover()` (`application.ts:335/435`) | Task | ❌ | **Won't do — attempted 2026-07-19, reverted.** The app overlay is `visible:false` until a popup mounts, and `render-root.ts:174` skips invisible children in the layout pass, so a `'fill'` overlay never gets solved `bounds` — while popup placement reads its extent *at open time*. Broke clamping in `menu/controller.ts:155` and **silently** in `dropdown/popup.ts:209` (no test caught the latter). A fix needs an added fallback-chain helper to buy a 2-line deletion — net machinery up, inverting FR-5. Recorded as the **hidden-host exclusion** in RD-01 FR-4; FR-5 / Tier-0 / AC-6 amended 5 overlays → 4. Do not re-attempt |
| [#116](https://github.com/blendsdk/jsvision/issues/116) | datagrid widgets (button-row / grid-lifecycle / popups) | Port | ✅ | behavior-preserving; out of divergence set. **Done 2026-07-19** via [widget-flex-adoption](plans/widget-flex-adoption/00-index.md) (35/35) — all 35 conversions landed in 6 modules; both full-screen goldens zero-diff. **Scope (post-preflight):** `grid-panels.ts` has **23** sites — the sweep's count was right; only its "all convertible" reading was in question. **15 are in scope, 8 excluded**: four containers accumulate children across branching control flow (with `bodyStack` aliasing `inner`) and four take a runtime-branching `segLayout`, so the issue's "not worth the churn" call was substantially correct (AR-3). The deferred `cover()` cluster is pulled in (AR-8). **Group D is empty** — `quick-filter-row.ts:155` and `personalize-dialog.ts:391` were dropped (issue #116 out-scoped both; `:391` carries a `direction:'col'` that `at()` cannot express and would have flipped the region to horizontal flow unguarded), and `overlay.ts:125` is **preserved**, not converted, as a third public-receiver clobber contract (AR-11). `filter-popup.ts:272` stays excluded as a scope ruling — a reactive self-resize, not composition |
| [#120](https://github.com/blendsdk/jsvision/issues/120) | **flex-eliminate FileDialog/ChDirDialog/errorBox + retire grow-dialog.ts** | Rebuild | ✅ | RD-01/02; Tier 2. Prerequisite #122 ✅ (in PR #123). Plan [files-flex-elimination](plans/files-flex-elimination/00-index.md) ✅ done 2026-07-19 (exec_plan 27/27) — [PR #125](https://github.com/blendsdk/jsvision/pull/125) **merged** to `feat/dsl-adoptation` 2026-07-19; ships to `develop` via PR #123. **Shipped:** both dialogs + `errorBox` on flex; `grow.ts` + `grow-dialog.ts` **deleted** (−208 lines); `wrapText` promoted to the ui barrel (AR-4, spans ui + files). **Fixed en route:** errorBox clipped any message past one line. **Reviews:** 3 phase reviews, 2 MAJOR + 6 MINOR all resolved; astral-wrap gap filed as [#124](https://github.com/blendsdk/jsvision/issues/124) |

**Governed by RD-01/RD-02:** #115, #120, and the Tier-3 parts of #110/#112. **Keep-absolute (excluded, RD-01
FR-4):** window/desktop/gesture, cursor/caret/measure-anchored popups, `theme-designer/gallery.ts` scatter,
movable-window desktop apps, polar `analog-clock`, `keyboard-mouse-playground`. Full line-level map lives in
the sweep artifacts + the GitHub issue bodies.
